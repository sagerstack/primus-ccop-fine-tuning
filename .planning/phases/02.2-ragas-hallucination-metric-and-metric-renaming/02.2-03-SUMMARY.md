---
phase: 02.2
plan: 03
status: complete
duration: ~4 min
---

## Summary

Updated JSON persistence with renamed/new metrics (schema_version 3), and added quality scoring to the query ask command with --no-score opt-out flag.

## Changes

### src/infrastructure/adapters/repositories/json_result_repository.py

**Schema version bumped to 3** in both error and success paths.

**Metric routing updated in _build_grouped_ragas_structure():**
- `faithfulness` → `context_faithfulness` for grounding group
- `hallucination` added to response_quality routing
- Backward compatibility note added for schema_version 2 files

**_enrich_quality_categories_metadata():** No code changes needed — reads from QualityGroup.get_all_groups() which was updated in Plan 01.

### src/rag/presentation/cli/query.py

**--no-score option added** to query_command().

**Quality scoring block added** after response display:
- Only activates in hybrid mode, when --no-score not set, and retrieved contexts exist
- Reuses existing container variable (no duplicate get_container() call)
- Shows context_faithfulness and answer_relevancy only (don't require ground truth)
- Color-coded scores: green >= 0.7, yellow >= 0.4, red < 0.4
- Errors suppressed gracefully — shown only in verbose mode, logged at warning level

## Verification
- schema_version shows 3 in both code paths
- Only "context_faithfulness" references in JSON repo (no bare "faithfulness")
- hallucination routing to response_quality confirmed
- --no-score option and quality scoring block in query.py confirmed
