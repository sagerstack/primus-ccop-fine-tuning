---
phase: 02.2-ragas-hallucination-metric-and-metric-renaming
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/value_objects/quality_group.py
  - src/domain/services/ragas_evaluation_service.py
  - src/application/use_cases/evaluate_model.py
  - src/tests/domain/services/test_ragas_evaluation_service.py
  - src/tests/domain/value_objects/test_quality_group.py
  - src/tests/application/use_cases/test_evaluate_model_metadata.py
autonomous: true

must_haves:
  truths:
    - "Hallucination metric computed via RAGAs faithfulness with [expected_response] as context"
    - "Existing faithfulness metric renamed to context_faithfulness throughout domain layer"
    - "Hallucination metric available in both hybrid and llm-only modes"
    - "context_faithfulness only computed when retrieved_contexts are available (hybrid mode)"
    - "Hallucination metric placed in Model Response Quality group"
    - "context_faithfulness remains in Model-RAG Grounding group"
    - "Overall score normalizes by sum of present category weights"
  artifacts:
    - path: "src/domain/value_objects/quality_group.py"
      provides: "Updated quality groups with hallucination in Model Response Quality, context_faithfulness in Model-RAG Grounding"
      contains: "hallucination"
    - path: "src/domain/services/ragas_evaluation_service.py"
      provides: "Two separate RAGAs faithfulness evaluations: one for hallucination (vs ground truth), one for context_faithfulness (vs retrieved docs)"
      contains: "hallucination"
    - path: "src/application/use_cases/evaluate_model.py"
      provides: "Fixed normalization: weighted_sum / total_weight"
      contains: "total_weight"
  key_links:
    - from: "src/domain/services/ragas_evaluation_service.py"
      to: "src/domain/value_objects/quality_group.py"
      via: "metric names match quality group metric lists"
      pattern: "context_faithfulness|hallucination"
    - from: "src/application/use_cases/evaluate_model.py"
      to: "src/domain/value_objects/quality_group.py"
      via: "_aggregate_quality_categories iterates QualityGroup.get_all_groups()"
      pattern: "QualityGroup.get_all_groups"
---

<objective>
Rename faithfulness to context_faithfulness, add hallucination metric via RAGAs faithfulness with ground-truth context, and fix overall score normalization bug in the domain and application layers.

Purpose: Establishes the core metric changes that all downstream display/persistence plans depend on. Hallucination metric detects claims beyond ground truth (works in both modes). context_faithfulness checks response vs retrieved documents (hybrid only). Normalization fix ensures correct overall scores when running subset of benchmarks.

Output: Updated domain services and aggregation logic with new/renamed metrics, passing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-ragas-hallucination-metric-and-metric-renaming/02.2-RESEARCH.md
@src/domain/value_objects/quality_group.py
@src/domain/services/ragas_evaluation_service.py
@src/application/use_cases/evaluate_model.py
@src/tests/application/use_cases/test_evaluate_model_metadata.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename faithfulness to context_faithfulness and add hallucination metric in domain layer</name>
  <files>
    src/domain/value_objects/quality_group.py
    src/domain/services/ragas_evaluation_service.py
    src/tests/domain/value_objects/test_quality_group.py
    src/tests/domain/services/test_ragas_evaluation_service.py
  </files>
  <action>
    **quality_group.py changes:**
    1. In `get_all_groups()`:
       - Change Model-RAG Grounding metrics from `["faithfulness"]` to `["context_faithfulness"]`
       - Add `"hallucination"` to Model Response Quality metrics list (FIRST position, before llm_judge): `["hallucination", "llm_judge", "answer_correctness", "answer_relevancy"]`
    2. In `get_display_name()`:
       - Remove `"faithfulness": "RAGAs: faithfulness"` entry
       - Add `"context_faithfulness": "RAGAs: context_faithfulness"`
       - Add `"hallucination": "RAGAs: hallucination"`

    **ragas_evaluation_service.py changes:**
    1. In `evaluate_response()`, restructure the evaluation to run TWO separate faithfulness calls:
       a. **Hallucination evaluation** (always runs, both modes):
          - Create a sample with `retrieved_contexts=[reference_text]` (ground truth as context)
          - Run `_Faithfulness()` metric on this sample
          - Map result key `"faithfulness"` to metric name `"hallucination"`
          - Always `applicable=True`
       b. **Context faithfulness evaluation** (only when `is_rag=True`):
          - Create a sample with `retrieved_contexts=retrieved_contexts` (actual retrieved docs)
          - Run `_Faithfulness()`, `_ContextPrecision()`, `_ContextRecall()` on this sample
          - Map result key `"faithfulness"` to metric name `"context_faithfulness"`
          - Mark `applicable=True` only when `is_rag=True`
    2. Update `ragas_metric_names` mapping:
       - Remove `"faithfulness": "faithfulness"`
       - The mapping is now done manually for the two separate evaluations (hallucination and context_faithfulness)
    3. Run base metrics (`_AnswerCorrectness()`, `_AnswerRelevancy()`) as before
    4. When `is_rag=False`, add `context_faithfulness` with `applicable=False` and `score=0.0`
    5. When `is_rag=False`, still add `context_precision` and `context_recall` with `applicable=False`

    **Implementation approach for ragas_evaluation_service.py:**
    - Run THREE separate evaluate() calls (or combine where possible):
      1. Base metrics (answer_correctness, answer_relevancy) on sample WITHOUT retrieved_contexts
      2. Hallucination (faithfulness against ground truth) on hallucination_sample WITH [reference_text] as contexts
      3. Context metrics (faithfulness + precision + recall against retrieved docs) on context_sample WITH retrieved_contexts — ONLY if is_rag
    - Extract scores from each result dict and build the final metric_scores list

    **Tests (TDD - write first):**
    - `test_quality_group.py`:
      - Test `get_all_groups()` returns "context_faithfulness" in Model-RAG Grounding (not "faithfulness")
      - Test `get_all_groups()` returns "hallucination" in Model Response Quality
      - Test `get_display_name("context_faithfulness")` returns "RAGAs: context_faithfulness"
      - Test `get_display_name("hallucination")` returns "RAGAs: hallucination"
      - Test `get_display_name("faithfulness")` falls through to default (returns "faithfulness")
      - Test `get_group_for_metric("hallucination")` returns Model Response Quality group
      - Test `get_group_for_metric("context_faithfulness")` returns Model-RAG Grounding group
    - `test_ragas_evaluation_service.py`:
      - Mock RAGAs evaluate() function to test metric name mapping
      - Test hallucination metric returned with applicable=True even when retrieved_contexts is None
      - Test context_faithfulness returned with applicable=False when retrieved_contexts is None
      - Test context_faithfulness returned with applicable=True when retrieved_contexts provided
      - Test both hallucination and context_faithfulness present in hybrid mode result
  </action>
  <verify>
    Run: `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -m pytest tests/domain/value_objects/test_quality_group.py tests/domain/services/test_ragas_evaluation_service.py -v`
    All tests pass.
  </verify>
  <done>
    quality_group.py has "context_faithfulness" in Model-RAG Grounding and "hallucination" in Model Response Quality.
    ragas_evaluation_service.py produces "hallucination" (applicable in both modes) and "context_faithfulness" (applicable only in hybrid).
    All new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix overall score normalization and update aggregation for new metrics</name>
  <files>
    src/application/use_cases/evaluate_model.py
    src/tests/application/use_cases/test_evaluate_model_metadata.py
  </files>
  <action>
    **evaluate_model.py changes:**

    1. In `_calculate_category_weighted_score()` (lines 359-371):
       - After computing `weighted_score`, calculate `total_weight`:
         ```python
         total_weight = sum(cat["weight"] for cat in category_scores.values())
         ```
       - Normalize: `normalized_score = weighted_score / total_weight if total_weight > 0 else 0.0`
       - Return `normalized_score` instead of `weighted_score`
       - Update log message to show both weighted_sum and total_weight

    2. In `_aggregate_quality_categories()`:
       - The metric name matching already uses `ragas_metric.name == metric_name` which iterates from QualityGroup.get_all_groups()
       - Since quality_group.py now has "context_faithfulness" and "hallucination", the aggregation will automatically pick these up
       - No code changes needed IF the ragas_evaluation metrics use the new names (which they will from Task 1)
       - Verify: the loop `for ragas_metric in result.ragas_evaluation.metrics: if ragas_metric.name == metric_name` will now match "context_faithfulness" and "hallucination"

    **Tests (TDD - write first):**
    - Add to test_evaluate_model_metadata.py:
      - `test_normalization_single_category`: Create results for only B1 benchmarks (weight 0.25). Verify overall score = category_avg (not category_avg * 0.25)
      - `test_normalization_all_categories`: Create results for all 5 categories. Verify overall score = weighted average (same as before, since weights sum to 1.0)
      - `test_normalization_two_categories`: Create results for B1 (0.25) and B21 (0.20). Verify overall score = weighted_sum / 0.45 (not just weighted_sum)

    **Note:** The _aggregate_quality_categories method at lines 533-541 already has normalization for group-level scores (`weighted_sum / total_weight`). Only `_calculate_category_weighted_score` at lines 364-371 is missing it.
  </action>
  <verify>
    Run: `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -m pytest tests/application/use_cases/test_evaluate_model_metadata.py -v`
    All tests pass, including new normalization tests.
  </verify>
  <done>
    Overall score normalization divides by total weight of present categories.
    Single-category evaluation returns category average (not weighted_contribution).
    Existing tests still pass.
  </done>
</task>

</tasks>

<verification>
1. Run all domain and application tests: `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -m pytest tests/domain/ tests/application/ -v`
2. Verify quality_group.py has 7 total metrics across 3 groups (was 6): context_recall, context_precision, context_faithfulness, hallucination, llm_judge, answer_correctness, answer_relevancy
3. Verify ragas_evaluation_service.py produces hallucination metric for both RAG and non-RAG responses
4. Verify _calculate_category_weighted_score normalizes by total weight
</verification>

<success_criteria>
- quality_group.py: "faithfulness" does NOT appear in any metrics list; "context_faithfulness" in Model-RAG Grounding; "hallucination" in Model Response Quality
- ragas_evaluation_service.py: produces 6 metrics for RAG responses (hallucination, context_faithfulness, context_precision, context_recall, answer_correctness, answer_relevancy) and 4 for non-RAG (hallucination, answer_correctness, answer_relevancy + context_faithfulness with applicable=False)
- evaluate_model.py: _calculate_category_weighted_score returns normalized score (weighted_sum / total_weight)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-ragas-hallucination-metric-and-metric-renaming/02.2-01-SUMMARY.md`
</output>
