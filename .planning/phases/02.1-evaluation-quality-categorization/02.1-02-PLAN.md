---
phase: 02.1-evaluation-quality-categorization
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - src/presentation/cli/commands/evaluate.py
autonomous: true

must_haves:
  truths:
    - "Categorized quality summary table REPLACES the old Evaluation Summary table (model, tests, passed, duration incorporated)"
    - "Per-benchmark categorized table REPLACES the old Results by Benchmark table"
    - "Per-benchmark table shows all 6 individual metrics per benchmark with group header rows"
    - "Per-test-case panels reorganized: Model Response Quality section first (LLM Judge + answer_correctness + answer_relevancy), then Model-RAG Grounding (faithfulness), then Retrieval Quality (context_recall + context_precision)"
    - "RAGAs metrics display with 'RAGAs:' prefix, LLM Judge with 'LLM Judge' label"
    - "In llm-only mode, Retrieval Quality and Model-RAG Grounding groups show 'N/A'"
    - "Diagnostic intent: at a glance you can tell if retrieval is weak vs model ignores context"
  artifacts:
    - path: "src/presentation/cli/commands/evaluate.py"
      provides: "Categorized summary tables and reorganized per-test-case panels"
      contains: "quality_categories"
  key_links:
    - from: "src/presentation/cli/commands/evaluate.py"
      to: "src/application/dtos/evaluation_result_dto.py"
      via: "Reads quality_categories from EvaluationSummaryDTO"
      pattern: "summary\\.quality_categories"
    - from: "src/presentation/cli/commands/evaluate.py"
      to: "src/domain/value_objects/quality_group.py"
      via: "Uses QualityGroup.get_display_name for metric labels"
      pattern: "QualityGroup\\.get_display_name|from domain.value_objects.quality_group"
---

<objective>
Replace CLI summary tables with categorized quality displays and reorganize per-test-case panels into 3 diagnostic groups.

Purpose: Transform CLI output from flat metric lists to diagnostic 3-group categorized view. A user should immediately see whether retrieval is weak, the model ignores context, or the model response itself is poor. Replaces both the Evaluation Summary and Results by Benchmark tables.

Output: Fully categorized CLI output with overall summary, per-benchmark breakdown by quality group, and reorganized per-test-case panels.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-evaluation-quality-categorization/02.1-CONTEXT.md
@.planning/phases/02.1-evaluation-quality-categorization/02.1-01-SUMMARY.md

Key files:
@src/presentation/cli/commands/evaluate.py
@src/application/dtos/evaluation_result_dto.py (for quality_categories structure)
@src/domain/value_objects/quality_group.py (for display names and group definitions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace summary tables with categorized quality display</name>
  <files>
    src/presentation/cli/commands/evaluate.py
  </files>
  <action>
    Replace the two existing summary tables (Evaluation Summary + Results by Benchmark) with categorized quality display. The new output goes after the per-test-case panels and "Evaluation Complete!" message.

    1. Add import at top: `from domain.value_objects.quality_group import QualityGroup`

    2. REMOVE the existing "Evaluation Summary" table block (lines creating Table(title="Evaluation Summary") with Model, Total Tests, Passed, Failed, Overall Score, Duration, Evaluation Mode rows).

    3. REMOVE the existing "Results by Benchmark" table block (lines creating bench_table with Benchmark, Total, Passed, Score columns).

    4. ADD new Categorized Quality Summary section that replaces both tables:

       a. **Run info header** (incorporates model/tests/passed/duration from old summary):
          ```python
          # Run info line
          mode_str = summary.results[0].evaluation_mode if summary.results and summary.results[0].evaluation_mode else "N/A"
          console.print(f"\n[bold]Model:[/bold] {summary.model_name}  |  "
                        f"[bold]Tests:[/bold] {summary.total_tests} ({summary.passed_tests} passed, {summary.failed_tests} failed)  |  "
                        f"[bold]Duration:[/bold] {summary.total_duration_seconds:.1f}s  |  "
                        f"[bold]Mode:[/bold] {mode_str}")
          ```

       b. **Overall Quality Summary table** using summary.quality_categories["overall"]:
          - Create Rich Table with title "Overall Quality Summary"
          - Columns: "Quality Group", "Score"
          - For each group in overall data:
            - Add a row with group name and its average score formatted as percentage (or "N/A" if None)
          - Add a final row with bold "Overall Score" showing summary.overall_score as percentage

       c. **Per-Benchmark Quality Breakdown table** using summary.quality_categories["by_benchmark"]:
          - Create Rich Table with title "Quality Breakdown by Benchmark"
          - Columns: "Benchmark", then one column per metric (6 total): "LLM Judge", "RAGAs: answer_correctness", "RAGAs: answer_relevancy", "RAGAs: faithfulness", "RAGAs: context_recall", "RAGAs: context_precision"
          - Also add columns: "Tests", "Passed"
          - Group rows visually using Rich Table section_separator or add blank rows between quality group headers.
          - For each benchmark (sorted by benchmark number):
            - Add a row with benchmark short name, each metric score (formatted as "0.XX" or "N/A"), test count, passed count.
            - Pull test count and passed count from summary.by_benchmark using the full benchmark name (match on short name prefix).
          - Use color coding: green for scores >= 0.7, yellow for 0.4-0.7, red for < 0.4, dim for N/A.

       d. Handle fallback: if summary.quality_categories is None (backward compat with old data), fall back to the old table format (keep old code in an else branch).

    Important rendering details from CONTEXT.md:
    - Single Rich table with group header rows (use Table.add_section() to visually separate groups)
    - RAGAs metrics prefixed with "RAGAs:" in column headers
    - LLM Judge labeled as "LLM Judge" (no prefix)
    - No letter prefixes for group names
  </action>
  <verify>
    Run: `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -c "
from presentation.cli.commands.evaluate import run
import inspect
source = inspect.getsource(run)
print('quality_categories' in source)
print('Overall Quality Summary' in source)
print('Quality Breakdown' in source)
# Old tables should be gone
print('Evaluation Summary' not in source or 'Overall Quality Summary' in source)
"`
    Expected: True, True, True, True
  </verify>
  <done>
    Old Evaluation Summary and Results by Benchmark tables replaced. New output shows: run info header, Overall Quality Summary with 3 group scores, Per-Benchmark Quality Breakdown with all 6 individual metrics per benchmark. Backward compatible fallback for old data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reorganize per-test-case panels into 3 diagnostic groups</name>
  <files>
    src/presentation/cli/commands/evaluate.py
  </files>
  <action>
    Restructure the per-test-case panel content to match the 3-group diagnostic structure. Currently the panel shows: Question, Response, LLM Judge Dimensions, RAGAs Quality Metrics, RAG Context. Reorganize into:

    1. **Question** (unchanged)

    2. **Response** (unchanged)

    3. **Model Response Quality** section (group 3 from QualityGroup):
       - LLM Judge Dimensions (existing judge_metrics display with raw_score/3 format)
       - Judge errors (existing judge_errors display)
       - RAGAs: answer_correctness (from ragas_metrics, if present)
       - RAGAs: answer_relevancy (from ragas_metrics, if present)

    4. **Model-RAG Grounding** section (group 2):
       - RAGAs: faithfulness (from ragas_metrics, if present)
       - If llm-only mode or faithfulness not applicable: show "N/A (llm-only mode)" or "N/A (not applicable)"

    5. **Retrieval Quality** section (group 1):
       - RAGAs: context_recall (from ragas_metrics, if present)
       - RAGAs: context_precision (from ragas_metrics, if present)
       - If llm-only mode or context metrics not applicable: show "N/A (llm-only mode)"

    6. **RAG Context** (unchanged -- only shown in hybrid mode with chunks)

    Implementation:
    - Remove the existing flat "RAGAs Quality Metrics" section
    - Remove the existing flat "LLM Judge Dimensions" section
    - Replace with 3 named sections using bold headers: `[bold]Model Response Quality:[/bold]`, `[bold]Model-RAG Grounding:[/bold]`, `[bold]Retrieval Quality:[/bold]`
    - For RAGAs metric display within sections, use "RAGAs:" prefix: `f"  RAGAs: {metric_name:<24s} {score:.2f}"`
    - Keep judge dimensions using existing format: `f"  {dim_name:<30s} {raw_score}/3  (weight: {weight:.2f})"`
    - Use QualityGroup.get_display_name() for metric label formatting
    - Keep the existing "RAG response detected" dim line and RAG Context chunk display as-is at the bottom
    - Handle case where ragas_metrics is None (no RAGAs evaluation): show "N/A" for all RAGAs metrics in each group
    - Handle ragas_error: show error message in the section where it would apply
  </action>
  <verify>
    Run: `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -c "
from presentation.cli.commands.evaluate import run
import inspect
source = inspect.getsource(run)
# Check new group headers exist
print('Model Response Quality' in source)
print('Model-RAG Grounding' in source)
print('Retrieval Quality' in source)
# Check old flat headers are gone
print('RAGAs Quality Metrics' not in source)
"`
    Expected: True, True, True, True
  </verify>
  <done>
    Per-test-case panels reorganized into 3 diagnostic groups: Model Response Quality (LLM Judge + answer_correctness + answer_relevancy), Model-RAG Grounding (faithfulness), Retrieval Quality (context_recall + context_precision). RAGAs metrics use "RAGAs:" prefix. llm-only mode shows N/A for RAG-dependent groups.
  </done>
</task>

</tasks>

<verification>
1. Old "Evaluation Summary" table is replaced by categorized quality summary
2. Old "Results by Benchmark" table is replaced by per-benchmark quality breakdown with all 6 metrics
3. Per-test-case panels show 3 named diagnostic groups
4. RAGAs metrics display with "RAGAs:" prefix everywhere
5. LLM Judge displays as "LLM Judge" (no prefix)
6. N/A shown appropriately for llm-only mode
7. Backward compat: if quality_categories is None, old format used
8. Existing tests pass: `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -m pytest tests/ -v --ignore=tests/integration`
</verification>

<success_criteria>
CLI output is fully categorized. At a glance, a user can diagnose whether retrieval is weak (low Retrieval Quality scores), the model ignores context (low Model-RAG Grounding), or the model response itself is poor (low Model Response Quality). All 3 display levels (per-test-case panels, per-benchmark table, overall summary) use consistent 3-group structure.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-evaluation-quality-categorization/02.1-02-SUMMARY.md`
</output>
