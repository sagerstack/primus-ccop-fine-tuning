---
phase: 03-ground-truth-v2-overhaul
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - ground-truth/test-suite/b01_ccop_applicability_scope.jsonl
  - ground-truth/test-suite/b02_compliance_classification.jsonl
  - ground-truth/test-suite/b04_it_ot_classification_boundary.jsonl
  - ground-truth/test-suite/b21_hallucination_over_specification.jsonl
autonomous: true

must_haves:
  truths:
    - "B1 has 25 test cases covering CCoP 1.0 evolution, IM8 context, ESCI/FDI awareness"
    - "B2 has 25 test cases with sector-specific compliance classification"
    - "B4 has 25 test cases with IT/OT boundary reasoning across sectors"
    - "B21 has 25 test cases with adversarial hallucination/over-specification traps"
    - "All 100 test cases pass v2 schema validation with 0 errors"
    - "Every test case has expected_label (required for rule-based benchmarks)"
    - "At least 3 sectors represented per benchmark"
    - "Difficulty distribution approximately 25% low, 45% medium, 30% high per benchmark"
  artifacts:
    - path: "ground-truth/test-suite/b01_ccop_applicability_scope.jsonl"
      provides: "25 B1 test cases"
      min_lines: 25
    - path: "ground-truth/test-suite/b02_compliance_classification.jsonl"
      provides: "25 B2 test cases"
      min_lines: 25
    - path: "ground-truth/test-suite/b04_it_ot_classification_boundary.jsonl"
      provides: "25 B4 test cases"
      min_lines: 25
    - path: "ground-truth/test-suite/b21_hallucination_over_specification.jsonl"
      provides: "25 B21 test cases"
      min_lines: 25
  key_links:
    - from: "ground-truth/test-suite/b01_ccop_applicability_scope.jsonl"
      to: "ground-truth/schema/test-case-v2.schema.json"
      via: "All test cases conform to v2 schema"
      pattern: "version.*2.0"
---

<objective>
Generate 100 v2 test cases for the 4 rule-based benchmarks: B1 (25), B2 (25), B4 (25), B21 (25).

Purpose: Rule-based benchmarks are the scoring foundation — they use keyword matching, label comparison, and regex patterns. Every test case must have expected_label. These benchmarks test classification, scope identification, and hallucination detection.
Output: 4 JSONL files with 100 total test cases, all passing schema validation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ground-truth-v2-overhaul/03-01-SUMMARY.md
@.planning/phases/03-ground-truth-v2-overhaul/03-02-SUMMARY.md
@.planning/phases/03-ground-truth-v2-overhaul/03-03-SUMMARY.md
@ground-truth/schema/test-case-v2.schema.json
@ground-truth/archive/phase-2/triage-report.md
@docs/phase-2/benchmark-registry.md
@docs/superpowers/specs/2026-04-01-ground-truth-v2-design.md (Section 3: Test Case Generation Strategy — question design principles, quantity allocation, difficulty distribution)
@docs/superpowers/plans/2026-04-01-ground-truth-v2.md (Task 6 — rule-based benchmark generation instructions)
@artifacts/research/2026-04-01-singapore-ciio-ccop-practices-deep-dive.md (sectors, scenarios, Risk Manager questions)
@ccop-official/CCoP---Second-Edition_Revision-One.pdf (source material for clause references)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate B1 and B2 test cases (50 cases)</name>
  <files>
    ground-truth/test-suite/b01_ccop_applicability_scope.jsonl
    ground-truth/test-suite/b02_compliance_classification.jsonl
  </files>
  <action>
    Generate 25 B1 (CCoP Applicability and Scope) and 25 B2 (Compliance Classification) test cases.

    **B1 — CCoP Applicability and Scope (25 cases):**
    Source from triage report (kept/revised v1 cases) + new questions from:
    - CCoP 1.0 to 2.0 evolution questions (what changed for access control, incident response, etc.)
    - IM8 overlap questions (government agency with both IM8 and CCoP requirements)
    - ESCI/STCC/FDI awareness questions (new entity classifications under Cybersecurity Act amendments)
    - Digital boundary definition questions (cloud, shared services, third-party CII components)
    - Sector coverage: minimum 3 sectors, include energy, banking, government, healthcare
    - Mix of scenario_role: risk_manager (majority) and employee
    - Difficulty: ~6 low, ~11 medium, ~8 high
    - Test categories: mix of positive, negative, edge_case, adversarial
    - EVERY test case must have expected_label (e.g., "applicable", "not-applicable", "partial")

    **B2 — Compliance Classification (25 cases):**
    Source: compliance classification scenarios across sectors with IT/OT nuance
    - Sector-specific classification: Is this practice compliant, non-compliant, or partially compliant?
    - Include IT/OT-specific compliance differences
    - Cover multiple CCoP sections (Protection, Detection, Response, etc.)
    - Difficulty: ~6 low, ~11 medium, ~8 high
    - EVERY test case must have expected_label (e.g., "compliant", "non-compliant", "partially-compliant")

    Each test case must conform to the v2 schema: test_id, version "2.0", benchmark_id, input (question 50+ chars, scenario_sector, scenario_role), ground_truth (expected_label, expected_response 50+ chars, key_facts with fact/source/tier), fail_conditions (forbidden_claims, hallucination_patterns), metadata (section, clause_reference array, domain, difficulty, test_category, created_date "2026-04-01", reviewer null).

    Write each file as JSONL (one JSON object per line, no trailing comma, no array wrapper).

    Validate after generation:
    ```bash
    cd ground-truth/schema && python validate.py --file ../test-suite/b01_ccop_applicability_scope.jsonl
    cd ground-truth/schema && python validate.py --file ../test-suite/b02_compliance_classification.jsonl
    ```
  </action>
  <verify>
    - `cd ground-truth/schema && python validate.py --file ../test-suite/b01_ccop_applicability_scope.jsonl` shows 25 valid, 0 errors
    - `cd ground-truth/schema && python validate.py --file ../test-suite/b02_compliance_classification.jsonl` shows 25 valid, 0 errors
  </verify>
  <done>50 rule-based test cases (B1: 25, B2: 25) pass schema validation</done>
</task>

<task type="auto">
  <name>Task 2: Generate B4 and B21 test cases (50 cases)</name>
  <files>
    ground-truth/test-suite/b04_it_ot_classification_boundary.jsonl
    ground-truth/test-suite/b21_hallucination_over_specification.jsonl
  </files>
  <action>
    Generate 25 B4 (IT/OT Classification and Boundary) and 25 B21 (Hallucination and Over-Specification) test cases.

    **B4 — IT/OT Classification and Boundary (25 cases):**
    Source: IT/OT boundary reasoning scenarios
    - Focus on hybrid sectors: healthcare (medical devices + EHR), aviation (ATC systems + booking), transport (rail signalling + passenger info), energy (smart grid + billing)
    - Include scenarios: SCADA/ICS classification, cloud-connected OT, converged IT/OT networks
    - expected_label: "IT", "OT", or "IT/OT"
    - Difficulty: ~6 low, ~11 medium, ~8 high
    - Cover at least 5 sectors

    **B21 — Hallucination and Over-Specification (25 cases):**
    Source: adversarial questions designed to trap hallucination
    - Non-existent CCoP clause references (e.g., "What does Clause 12.4.3 require?" — no such clause)
    - Requirements from other frameworks framed as CCoP (e.g., "Under CCoP 2.0, implement NIST SP 800-53 controls...")
    - Leading questions suggesting non-existent obligations
    - Over-specification traps (adding requirements beyond CCoP text)
    - Questions about repealed/outdated requirements
    - expected_label: "hallucination", "over-specification", "accurate" (some positive cases needed)
    - Difficulty: ~6 low, ~8 medium, ~11 high (more high because adversarial)
    - Test categories: heavy on adversarial, some positive (correct responses)

    Validate after generation:
    ```bash
    cd ground-truth/schema && python validate.py --file ../test-suite/b04_it_ot_classification_boundary.jsonl
    cd ground-truth/schema && python validate.py --file ../test-suite/b21_hallucination_over_specification.jsonl
    ```

    Run full validation across all files generated so far:
    ```bash
    cd ground-truth/schema && python validate.py
    ```
  </action>
  <verify>
    - `cd ground-truth/schema && python validate.py --file ../test-suite/b04_it_ot_classification_boundary.jsonl` shows 25 valid, 0 errors
    - `cd ground-truth/schema && python validate.py --file ../test-suite/b21_hallucination_over_specification.jsonl` shows 25 valid, 0 errors
    - `cd ground-truth/schema && python validate.py` shows 0 errors (total count varies depending on Wave 3 parallel execution order)
  </verify>
  <done>100 rule-based test cases total (B1: 25, B2: 25, B4: 25, B21: 25) all pass schema validation</done>
</task>

</tasks>

<verification>
- 4 JSONL files with 25 test cases each
- Schema validator reports 0 errors across all files
- Every test case has expected_label
- Sector diversity: at least 3 sectors per benchmark
- Difficulty distribution roughly matches targets
</verification>

<success_criteria>
- 100 rule-based test cases generated and validated
- Questions are scenario-grounded and practitioner-relevant (not abstract/definitional)
- Key facts have source references and tier assignments
- Fail conditions include meaningful forbidden_claims and hallucination_patterns
</success_criteria>

<output>
After completion, create `.planning/phases/03-ground-truth-v2-overhaul/03-05-SUMMARY.md`
</output>
