---
phase: 03-ground-truth-v2-overhaul
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/infrastructure/adapters/repositories/jsonl_test_case_repository.py
  - src/tests/infrastructure/test_jsonl_v2_parsing.py
autonomous: true

must_haves:
  truths:
    - "Repository parser auto-detects v2 format via version field"
    - "V2 nested fields (input.question, ground_truth.key_facts, etc.) are correctly extracted"
    - "key_facts property returns list[str] for backward compatibility with scorers"
    - "V1 flat format parsing is unchanged (no regressions)"
    - "V2 metadata includes scenario_sector, scenario_role, reasoning_chain, acceptable_variations"
    - "All existing tests still pass"
  artifacts:
    - path: "src/infrastructure/adapters/repositories/jsonl_test_case_repository.py"
      provides: "V2-aware JSONL parser with backward compatibility"
      contains: "_parse_v2_test_case"
    - path: "src/tests/infrastructure/test_jsonl_v2_parsing.py"
      provides: "Tests for v2 parsing"
      contains: "test_parses_v2_test_case"
  key_links:
    - from: "src/infrastructure/adapters/repositories/jsonl_test_case_repository.py"
      to: "src/domain/entities/test_case.py"
      via: "Creates TestCase entities from v2 data"
      pattern: "TestCase\\("
    - from: "src/infrastructure/adapters/repositories/jsonl_test_case_repository.py"
      to: "ground-truth/test-suite/"
      via: "Reads JSONL files from configured directory"
      pattern: "glob.*jsonl"
---

<objective>
Update the JSONL repository parser to handle v2 nested format while maintaining backward compatibility with v1 flat format.

Purpose: The evaluation infrastructure must be able to load v2 test cases before they can be used for evaluation. This is infrastructure work that enables Phase 4 (re-baseline).
Output: Updated parser with v1/v2 auto-detection, new tests for v2 parsing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ground-truth-v2-overhaul/03-01-SUMMARY.md
@src/infrastructure/adapters/repositories/jsonl_test_case_repository.py
@src/domain/entities/test_case.py
@docs/superpowers/plans/2026-04-01-ground-truth-v2.md (Task 14 — parser update with full code for _parse_v2_test_case and tests)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for v2 parsing</name>
  <files>
    src/tests/infrastructure/test_jsonl_v2_parsing.py
  </files>
  <action>
    Create src/tests/infrastructure/test_jsonl_v2_parsing.py with the 4 test cases from the reference plan Task 14:

    1. test_parses_v2_test_case — verifies test_id, question, expected_response, expected_label extracted from nested v2 format
    2. test_parses_v2_key_facts_as_strings — verifies key_facts property returns list[str] (not list[dict]) for scorer backward compatibility
    3. test_parses_v2_forbidden_claims — verifies forbidden_claims extracted from fail_conditions
    4. test_parses_v2_metadata — verifies domain, scenario_sector, test_category available in metadata

    Use the V2_TEST_CASE fixture from the reference plan (B3-001 energy sector SCADA scenario).
    Use tmp_path fixture to create temporary JSONL file.

    Run tests to verify they FAIL: `cd src && poetry run pytest tests/infrastructure/test_jsonl_v2_parsing.py -v`
  </action>
  <verify>`cd src && poetry run pytest tests/infrastructure/test_jsonl_v2_parsing.py -v` shows failures (expected — parser doesn't handle v2 yet)</verify>
  <done>4 failing tests written for v2 parsing</done>
</task>

<task type="auto">
  <name>Task 2: Update repository parser for v2 format</name>
  <files>
    src/infrastructure/adapters/repositories/jsonl_test_case_repository.py
  </files>
  <action>
    Update the _parse_test_case method in JSONLTestCaseRepository following the reference plan Task 14:

    1. Replace the single _parse_test_case method with three methods:
       - _parse_test_case(data) — dispatcher that checks data.get("version") == "2.0" and routes to v1 or v2 parser
       - _parse_v1_test_case(data) — existing logic (unchanged, extracted into separate method)
       - _parse_v2_test_case(data) — new method for v2 nested format

    2. _parse_v2_test_case extracts:
       - question from input.question
       - expected_response from ground_truth.expected_response
       - expected_label from ground_truth.expected_label
       - key_facts as list[str] by extracting kf["fact"] from ground_truth.key_facts (scorer compatibility)
       - forbidden_claims from fail_conditions.forbidden_claims
       - benchmark_type from benchmark_id (not benchmark_type — v2 uses benchmark_id)
       - section from metadata.section
       - clause_reference as comma-joined string from metadata.clause_reference array
       - difficulty from metadata.difficulty
       - evaluation_criteria as empty dict (v2 uses universal judge)
       - enriched_metadata merging metadata + scenario_sector, scenario_role, test_category, reasoning_chain, acceptable_variations, key_facts_structured, hallucination_patterns

    3. Also update _discover_benchmark_files to handle v2 format:
       - V2 files have "benchmark_id" not "benchmark_type" in first line
       - Check for both: `data.get("benchmark_type") or data.get("benchmark_id")`

    4. Run all tests: `cd src && poetry run pytest tests/ -v --timeout=30`
  </action>
  <verify>
    - `cd src && poetry run pytest tests/infrastructure/test_jsonl_v2_parsing.py -v` — all 4 pass
    - `cd src && poetry run pytest tests/ -v --timeout=30` — all existing tests pass (no regressions)
  </verify>
  <done>Parser handles both v1 flat and v2 nested formats, all tests pass</done>
</task>

</tasks>

<verification>
- 4 new v2 parsing tests pass
- All existing tests pass (no regressions)
- Parser auto-detects format via version field
- key_facts returned as list[str] for scorer compatibility
</verification>

<success_criteria>
- V2 nested JSONL parsing works correctly
- V1 parsing is completely unchanged
- Enriched metadata includes scenario_sector, reasoning_chain, acceptable_variations
- Benchmark file discovery works for both v1 (benchmark_type) and v2 (benchmark_id) formats
</success_criteria>

<output>
After completion, create `.planning/phases/03-ground-truth-v2-overhaul/03-04-SUMMARY.md`
</output>
