---
phase: 03-ground-truth-v2-overhaul
plan: 11
type: execute
wave: 4
depends_on: ["03-04", "03-05", "03-06", "03-07", "03-08", "03-09", "03-10"]
files_modified:
  - ground-truth/coverage-matrix.md
  - ground-truth/expert-validation/generate_v2_expert_review.py
  - ground-truth/expert-validation/CCoP_V2_Test_Cases_Expert_Review.xlsx
  - docs/phase-2/ground-truth-v2-migration.md
  - src/config/.env.local
  - src/config/.env.example
autonomous: false

must_haves:
  truths:
    - "Coverage matrix shows benchmark x section x sector x difficulty distribution"
    - "All 18 benchmarks have minimum 20 test cases"
    - "Total test cases are approximately 435"
    - "CCoP section coverage is 11/11"
    - "At least 3 sectors represented per benchmark"
    - "Difficulty distribution approximately matches 25/45/30 targets"
    - "Expert validation spreadsheet contains all test cases with review columns"
    - "Migration report documents schema changes, benchmark restructuring, and triage results"
    - "Schema validation passes 100% with 0 errors"
    - "All existing tests pass (no regressions)"
    - "Repository parser loads v2 test cases correctly"
  artifacts:
    - path: "ground-truth/coverage-matrix.md"
      provides: "Coverage analysis across all dimensions"
      contains: "Benchmark.*Section.*Sector"
      min_lines: 50
    - path: "ground-truth/expert-validation/generate_v2_expert_review.py"
      provides: "Excel generator for expert validation"
      contains: "create_workbook"
    - path: "ground-truth/expert-validation/CCoP_V2_Test_Cases_Expert_Review.xlsx"
      provides: "Expert review spreadsheet"
    - path: "docs/phase-2/ground-truth-v2-migration.md"
      provides: "Migration report documenting v1 to v2 changes"
      contains: "Schema Changes"
      min_lines: 30
  key_links:
    - from: "ground-truth/expert-validation/generate_v2_expert_review.py"
      to: "ground-truth/test-suite/"
      via: "Reads all v2 JSONL files to generate spreadsheet"
      pattern: "glob.*jsonl"
    - from: "ground-truth/coverage-matrix.md"
      to: "ground-truth/test-suite/"
      via: "Analyzes all v2 test cases for coverage"
      pattern: "Benchmark.*Section"
---

<objective>
Generate coverage matrix, expert validation spreadsheet, migration report, update test cases directory config, and run final validation across the entire v2 ground truth.

Purpose: These are the deliverables that make the v2 ground truth usable — the coverage matrix identifies gaps, the expert spreadsheet enables domain expert review, the migration report documents changes, and final validation confirms everything works.
Output: Coverage matrix, expert validation Excel, migration report, validated complete v2 ground truth.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ground-truth-v2-overhaul/03-01-SUMMARY.md
@.planning/phases/03-ground-truth-v2-overhaul/03-02-SUMMARY.md
@.planning/phases/03-ground-truth-v2-overhaul/03-03-SUMMARY.md
@.planning/phases/03-ground-truth-v2-overhaul/03-04-SUMMARY.md
@ground-truth/schema/test-case-v2.schema.json
@ground-truth/archive/phase-2/triage-report.md
@docs/phase-2/benchmark-registry.md
@docs/superpowers/specs/2026-04-01-ground-truth-v2-design.md (Section 4: directory structure, deliverables, success criteria)
@docs/superpowers/plans/2026-04-01-ground-truth-v2.md (Tasks 11-16)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate coverage matrix, expert spreadsheet, and migration report</name>
  <files>
    ground-truth/coverage-matrix.md
    ground-truth/expert-validation/generate_v2_expert_review.py
    ground-truth/expert-validation/CCoP_V2_Test_Cases_Expert_Review.xlsx
    docs/phase-2/ground-truth-v2-migration.md
  </files>
  <action>
    **1. Coverage Matrix (ground-truth/coverage-matrix.md):**
    Read all JSONL files in ground-truth/test-suite/ and build a coverage matrix with these dimensions:
    - Benchmark x CCoP Section (11 sections)
    - Benchmark x Sector (12 sectors)
    - Benchmark x Difficulty (low/medium/high with percentages)
    - Benchmark x Test Category (positive/negative/edge_case/adversarial)
    - Benchmark x Domain (IT/OT/IT+OT)
    - Summary: total test cases, benchmarks covered, sections covered, sectors represented
    - Coverage Gaps section: any CCoP sections with < 3 test cases, sectors with < 3 total, benchmarks with < 20 cases
    Follow the template in reference plan Task 11.

    **2. Expert Validation Spreadsheet:**
    Create ground-truth/expert-validation/generate_v2_expert_review.py following reference plan Task 12 exactly. The script:
    - Reads all v2 JSONL files from ground-truth/test-suite/
    - Creates Excel workbook with columns: Test ID, Benchmark, Sector, Domain, Difficulty, Category, CCoP Section, Clause Refs, Question, Expected Label, Expected Response, Key Facts (Critical), Key Facts (Important/Supporting), Reasoning Chain, Forbidden Claims, Approved (Y/N), Accuracy (1-5), Completeness (1-5), Remarks
    - Adds data validation for review columns
    - Sets column widths and freeze panes
    - Saves to ground-truth/expert-validation/CCoP_V2_Test_Cases_Expert_Review.xlsx

    Run the generator: `cd ground-truth/expert-validation && python generate_v2_expert_review.py`

    **3. Migration Report (docs/phase-2/ground-truth-v2-migration.md):**
    Document the v1 to v2 migration following reference plan Task 13:
    - Overview: v1 stats -> v2 stats
    - Schema changes (v1 flat -> v2 nested, field mapping)
    - Benchmark changes (merges, removals, additions from registry)
    - Triage results summary (keep/revise/discard counts from triage report)
    - Quality improvements (tiered key_facts, fail_conditions, reasoning_chain, scenario grounding)
    - Known limitations (expert validation pending, difficulty not empirically calibrated)
    - Next steps (expert validation, baseline evaluation on v2)
  </action>
  <verify>
    - Coverage matrix exists: `ls ground-truth/coverage-matrix.md`
    - Expert spreadsheet generated: `ls ground-truth/expert-validation/CCoP_V2_Test_Cases_Expert_Review.xlsx`
    - Migration report exists: `ls docs/phase-2/ground-truth-v2-migration.md`
  </verify>
  <done>Coverage matrix, expert validation spreadsheet, and migration report all generated</done>
</task>

<task type="auto">
  <name>Task 2: Update test cases directory config and run final validation</name>
  <files>
    src/config/.env.local
    src/config/.env.example
  </files>
  <action>
    **1. Update test cases directory configuration in both env files:**
    Update `src/config/.env.local` and `src/config/.env.example` — change:
    `CCOP_TEST_CASES_DIR=../ground-truth/phase-2/test-suite` to `CCOP_TEST_CASES_DIR=../ground-truth/test-suite`

    **2. Run full schema validation:**
    ```bash
    cd ground-truth/schema && python validate.py --strict
    ```
    Expected: ~435 valid, 0 warnings, 0 errors

    **3. Run all tests (including v2 parsing tests from Plan 04):**
    ```bash
    cd src && poetry run pytest tests/ -v --timeout=30
    ```
    Expected: All tests pass including v2 parsing tests

    **4. Verify all deliverables:**
    ```bash
    echo "=== V2 Test Suite ===" && ls ground-truth/test-suite/*.jsonl | wc -l
    echo "=== Schema ===" && ls ground-truth/schema/
    echo "=== Expert Validation ===" && ls ground-truth/expert-validation/
    echo "=== Archive ===" && ls ground-truth/archive/phase-2/test-suite/*.jsonl | wc -l
    echo "=== Docs ===" && ls docs/phase-2/benchmark-registry.md docs/phase-2/ground-truth-v2-migration.md ground-truth/coverage-matrix.md
    ```
    Expected: 18 v2 JSONL files, schema + validator, Excel + generator, 21 archived files, all docs

    **5. Verify difficulty and sector distribution:**
    Run the distribution check from reference plan Task 16 Step 4.

    **6. Count total test cases:**
    ```bash
    wc -l ground-truth/test-suite/*.jsonl
    ```
    Expected: ~435 total
  </action>
  <verify>
    - Schema validation: 0 errors, 0 warnings in strict mode
    - Test suite: all tests pass
    - 18 JSONL files in ground-truth/test-suite/
    - Total ~435 test cases
    - Difficulty distribution roughly matches 25/45/30
    - All deliverables present
  </verify>
  <done>Test cases directory updated, full validation passes, all deliverables confirmed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete V2 ground truth overhaul:
    - ~435 test cases across 18 benchmarks in v2 nested JSONL format
    - JSON schema + validator
    - Repository parser updated for v2 format (backward-compatible)
    - Coverage matrix, expert validation spreadsheet, migration report
    - Benchmark registry, triage report
  </what-built>
  <how-to-verify>
    1. Review coverage matrix (ground-truth/coverage-matrix.md) — check for coverage gaps
    2. Open expert validation Excel (ground-truth/expert-validation/CCoP_V2_Test_Cases_Expert_Review.xlsx) — spot-check 5-10 test cases across different benchmarks for question quality, ground truth accuracy, and key_facts sourcing
    3. Review difficulty distribution — does it match the ~25/45/30 target?
    4. Review sector diversity — are sectors well-distributed across benchmarks?
    5. Spot-check a few v2 JSONL files for question practitioner-relevance (are they scenario-grounded, not abstract/definitional?)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to address</resume-signal>
</task>

</tasks>

<verification>
- Schema validation: 100% pass rate, 0 errors
- All existing tests pass (no regressions)
- 18 JSONL files with ~435 total test cases
- Coverage matrix shows 11/11 CCoP sections, 3+ sectors per benchmark
- Expert validation spreadsheet generated with all test cases
- Migration report documents complete v1 to v2 transition
</verification>

<success_criteria>
All 13 success criteria from the roadmap Phase 3:
1. V2 JSON schema defined with separated concerns
2. Schema validator passes 100% with 0 errors
3. Benchmark set restructured from 21 to 18
4. Minimum 20 test cases per benchmark, ~435 total
5. Every key_fact has source reference and tier
6. Every reasoning benchmark has >= 2 critical-tier key_facts
7. All questions scenario-grounded and sector-aware (>= 3 sectors/benchmark)
8. CCoP section coverage: 11/11
9. Difficulty distribution: ~25% low, ~45% medium, ~30% high
10. V1 archived to ground-truth/archive/phase-2/
11. Repository parser handles v2 nested format (backward-compatible)
12. Expert validation spreadsheet generated
13. Coverage matrix documents distribution
</success_criteria>

<output>
After completion, create `.planning/phases/03-ground-truth-v2-overhaul/03-11-SUMMARY.md`
</output>
