---
phase: 03-ground-truth-v2-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/phase-2/benchmark-registry.md
autonomous: true

must_haves:
  truths:
    - "All 21 v1 benchmarks have been audited against 5 criteria"
    - "Benchmark merges, removals, and additions are documented with rationale"
    - "Final benchmark set of 18 benchmarks is defined with scoring paths"
    - "Each benchmark has a target test case count and key CCoP sections"
  artifacts:
    - path: "docs/phase-2/benchmark-registry.md"
      provides: "V2 benchmark registry with audit decisions"
      contains: "B22.*Waiver"
      min_lines: 100
  key_links:
    - from: "docs/phase-2/benchmark-registry.md"
      to: "docs/superpowers/specs/2026-04-01-ground-truth-v2-design.md"
      via: "Implements benchmark audit decisions from spec Section 2"
      pattern: "B8.*Risk-Based Prioritization"
---

<objective>
Audit all 21 v1 benchmarks and produce the v2 benchmark registry documenting merges, removals, additions, and the final 18-benchmark set.

Purpose: The benchmark registry is the authoritative reference for all test case generation — it defines what each benchmark tests, which scoring path it uses, and how many test cases to target.
Output: docs/phase-2/benchmark-registry.md with complete benchmark definitions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/superpowers/specs/2026-04-01-ground-truth-v2-design.md (Section 2: Benchmark Audit — audit criteria, keep/refocus/merge/remove/new decisions, final benchmark set)
@artifacts/research/2026-04-01-singapore-ciio-ccop-practices-deep-dive.md (sector-specific scenarios, Risk Manager questions, CIIO practices)
@docs/superpowers/plans/2026-04-01-ground-truth-v2.md (Task 4 — benchmark audit and registry instructions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read all 21 archived v1 test suite files and audit each benchmark</name>
  <files>
    ground-truth/phase-2/test-suite/ (read only — or archive if Plan 01 already ran)
  </files>
  <action>
    Read each of the 21 v1 JSONL files (either from ground-truth/phase-2/test-suite/ or ground-truth/archive/phase-2/test-suite/ if Plan 01 has already archived them). For each benchmark, note:
    - Number of test cases
    - Question quality (scenario-grounded vs abstract/definitional)
    - Key_facts quality (present and atomic, or placeholder/missing)
    - Coverage gaps

    Then apply the 5 audit criteria from the spec:
    1. CIIO Relevance — does this test something a Risk Manager needs?
    2. Distinctiveness — does this measure something unique?
    3. Scorer Alignment — does current scoring infrastructure evaluate this effectively?
    4. Question Feasibility — can 20+ scenario-grounded questions be written?
    5. Evaluation Clarity — clear distinction between good and bad responses?

    Use the CIIO research document for context on what Risk Managers actually need.
  </action>
  <verify>All 21 benchmarks have been reviewed with notes on each criterion</verify>
  <done>Audit complete for all 21 v1 benchmarks with quality notes and criterion scores</done>
</task>

<task type="auto">
  <name>Task 2: Write the v2 benchmark registry</name>
  <files>
    docs/phase-2/benchmark-registry.md
  </files>
  <action>
    Create docs/phase-2/benchmark-registry.md following the template in the reference plan Task 4. Use the audit decisions from the design spec Section 2 as the starting point, adjusted by findings from Task 1.

    The registry must include:

    1. **Overview** — 18 benchmarks, reduced from 21 through audit

    2. **Changes from V1** — Three tables:
       - Merges: B8+B11 -> B8 Risk-Based Prioritization, B14+B15 -> B14 Remediation Quality & Feasibility, B9+B16 -> B9 Risk Identification & Residual Risk
       - Removed/Absorbed: B17 -> B7, B19 -> cross-benchmark quality check, B20 -> B21
       - New: B22 Waiver & Exception Reasoning, B23 Multi-Regulator Coordination, B24 Incident Response Guidance

    3. **Benchmark Definitions** — For each of the 18 v2 benchmarks:
       - Scoring path (rule-based or LLM-judge)
       - Description (what it tests)
       - V2 changes (if refocused or merged)
       - Target count (from spec Section 3 quantity allocation)
       - Key CCoP sections covered
       - Question design guidance (what makes a good question for this benchmark)

    Rule-based benchmarks: B1, B2, B4, B21
    LLM-judge benchmarks: B3, B5, B6, B7, B8, B9, B10, B12, B13, B14, B18, B22, B23, B24

    Ensure directory docs/phase-2/ exists before writing the file.
  </action>
  <verify>
    - File exists: `ls docs/phase-2/benchmark-registry.md`
    - Contains all 18 benchmarks: `grep -c "^### B" docs/phase-2/benchmark-registry.md` returns 18
    - Contains new benchmarks: `grep "B22\|B23\|B24" docs/phase-2/benchmark-registry.md`
    - Contains merge documentation: `grep "Merge" docs/phase-2/benchmark-registry.md`
  </verify>
  <done>Benchmark registry documents all 18 v2 benchmarks with scoring paths, targets, CCoP sections, and audit rationale</done>
</task>

</tasks>

<verification>
- Benchmark registry exists with 18 benchmark definitions
- All merges, removals, and additions documented with rationale
- Each benchmark has scoring path, target count, and key CCoP sections
</verification>

<success_criteria>
- Registry accurately reflects the design spec benchmark audit decisions
- Each benchmark definition is specific enough to guide test case generation
- Scoring path assignments are correct (4 rule-based, 14 LLM-judge)
</success_criteria>

<output>
After completion, create `.planning/phases/03-ground-truth-v2-overhaul/03-02-SUMMARY.md`
</output>
