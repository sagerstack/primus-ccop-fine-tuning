---
phase: 03-ground-truth-v2-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ground-truth/archive/phase-2/test-suite/
  - ground-truth/archive/phase-2/expert-validation/
  - ground-truth/test-suite/
  - ground-truth/schema/test-case-v2.schema.json
  - ground-truth/schema/validate.py
  - ground-truth/expert-validation/
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "V1 ground truth files are archived under ground-truth/archive/phase-2/"
    - "V2 directory structure exists (test-suite/, schema/, expert-validation/)"
    - "V2 JSON schema correctly validates a well-formed v2 test case"
    - "V2 JSON schema rejects test cases with missing required fields"
    - "Schema validator script runs business rule checks beyond JSON Schema"
    - "jsonschema is available as a project dependency"
  artifacts:
    - path: "ground-truth/schema/test-case-v2.schema.json"
      provides: "V2 test case JSON Schema definition"
      contains: "test_id.*version.*benchmark_id.*input.*ground_truth.*fail_conditions.*metadata"
    - path: "ground-truth/schema/validate.py"
      provides: "Schema validator with JSON Schema + business rules"
      contains: "validate_business_rules"
    - path: "ground-truth/test-suite/b03_conditional_compliance_reasoning.jsonl"
      provides: "Sample v2 test case for validation"
      contains: "B3-001"
  key_links:
    - from: "ground-truth/schema/validate.py"
      to: "ground-truth/schema/test-case-v2.schema.json"
      via: "load_schema reads schema file"
      pattern: "test-case-v2.schema.json"
---

<objective>
Archive V1 ground truth, create V2 directory structure, define V2 JSON schema, and build schema validator.

Purpose: Establish the foundation for all v2 test case generation — the schema defines the contract, the validator enforces it, and the directory structure organizes all deliverables.
Output: Archived v1 files, v2 JSON schema, working validator script, sample v2 test case, jsonschema dependency installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-04-01-ground-truth-v2-design.md (Section 1: V2 Test Case Schema — full schema definition, field reference, design principles)
@docs/superpowers/plans/2026-04-01-ground-truth-v2.md (Tasks 1-3 — archive, schema, validator with full code)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive V1 ground truth and create V2 directory structure</name>
  <files>
    ground-truth/archive/phase-2/test-suite/
    ground-truth/archive/phase-2/expert-validation/
    ground-truth/test-suite/
    ground-truth/schema/
    ground-truth/expert-validation/
  </files>
  <action>
    Follow the reference plan Task 1 exactly:

    1. Create v2 directory structure:
       - ground-truth/archive/phase-2/test-suite/
       - ground-truth/archive/phase-2/expert-validation/
       - ground-truth/test-suite/
       - ground-truth/schema/
       - ground-truth/expert-validation/

    2. Move all 21 JSONL files from ground-truth/phase-2/test-suite/ to ground-truth/archive/phase-2/test-suite/

    3. Move expert validation files from ground-truth/phase-2/expert-validation/ to ground-truth/archive/phase-2/expert-validation/:
       - expert_validation_review.md
       - generate_expert_review_excel.py
       - CCoP_Test_Cases_Expert_Review.xlsx
       - Skip any ~$ temp files (Excel lock files)

    4. Remove empty v1 directories (ground-truth/phase-2/test-suite, expert-validation, phase-2) if empty

    5. Verify: All JSONL files under archive, empty test-suite/ and schema/ directories exist
  </action>
  <verify>
    - `ls ground-truth/archive/phase-2/test-suite/*.jsonl | wc -l` returns 21
    - `ls ground-truth/test-suite/` is empty
    - `ls ground-truth/schema/` is empty
    - `ls ground-truth/expert-validation/` is empty or only has non-essential files
  </verify>
  <done>21 v1 JSONL files archived, v2 directory structure created, v1 directories cleaned up</done>
</task>

<task type="auto">
  <name>Task 2: Create V2 JSON schema, validator, and sample test case</name>
  <files>
    ground-truth/schema/test-case-v2.schema.json
    ground-truth/schema/validate.py
    ground-truth/test-suite/b03_conditional_compliance_reasoning.jsonl
    pyproject.toml
  </files>
  <action>
    Follow the reference plan Tasks 2-3 exactly. The full schema JSON and validator Python code are provided in the reference plan.

    1. Install jsonschema dependency: `cd src && poetry add jsonschema`

    2. Create ground-truth/schema/test-case-v2.schema.json with the complete v2 schema from the reference plan Task 2. Key structure:
       - Required top-level: test_id, version ("2.0" const), benchmark_id, input, ground_truth, fail_conditions, metadata
       - input: question (minLength 50), scenario_sector (enum of 12 sectors), scenario_role (enum)
       - ground_truth: expected_response (minLength 50), key_facts (array of {fact, source, tier}), optional reasoning_chain, acceptable_variations, expected_label
       - fail_conditions: forbidden_claims, hallucination_patterns
       - metadata: section, clause_reference (array), domain (IT/OT/IT+OT), difficulty, test_category, created_date, reviewer

    3. Create ground-truth/schema/validate.py with the validator from reference plan Task 3:
       - Loads schema from adjacent test-case-v2.schema.json
       - Validates against JSON Schema (Draft 2020-12)
       - Business rules: test_id prefix matches benchmark_id, reasoning benchmarks need reasoning_chain, rule-based benchmarks need expected_label, minimum 2 critical-tier key_facts for reasoning benchmarks
       - CLI: --file for single file, --strict for warnings-as-errors
       - Reports valid/warning/error counts per file and total

    4. Create ground-truth/test-suite/b03_conditional_compliance_reasoning.jsonl with the B3-001 sample test case from the reference plan Task 3 (the energy sector SCADA shared admin accounts scenario)

    5. Validate: `cd ground-truth/schema && python validate.py --file ../test-suite/b03_conditional_compliance_reasoning.jsonl`
  </action>
  <verify>
    - `cd src && poetry run python -c "import jsonschema; print('OK')"` prints OK
    - `cd ground-truth/schema && python validate.py --file ../test-suite/b03_conditional_compliance_reasoning.jsonl` shows 1 valid, 0 errors
    - Schema file is valid JSON: `python -c "import json; json.load(open('ground-truth/schema/test-case-v2.schema.json')); print('Valid')"`
  </verify>
  <done>V2 JSON schema defined, validator script working, sample B3-001 test case validates successfully, jsonschema dependency installed</done>
</task>

</tasks>

<verification>
- 21 archived JSONL files in ground-truth/archive/phase-2/test-suite/
- V2 directories exist and are properly structured
- Schema validator passes on sample test case with 0 errors
- jsonschema importable in poetry environment
</verification>

<success_criteria>
- V1 files archived, V2 structure ready
- JSON schema enforces all required fields from the design spec
- Validator catches both schema violations and business rule violations
- Sample test case B3-001 passes validation
</success_criteria>

<output>
After completion, create `.planning/phases/03-ground-truth-v2-overhaul/03-01-SUMMARY.md`
</output>
