---
phase: 03.1-eval-run-traceability
plan: 03
type: execute
wave: 3
depends_on: ["03.1-01", "03.1-02"]
files_modified:
  - src/application/use_cases/generate_report.py
  - src/infrastructure/adapters/repositories/json_result_repository.py
  - src/presentation/cli/commands/evaluate.py
  - src/tests/domain/value_objects/test_run_id.py
  - src/tests/infrastructure/test_json_result_repository_v6.py
  - src/tests/application/use_cases/test_evaluate_model_metadata.py
  - src/tests/rag/retrieval/test_graph_state.py
autonomous: true

must_haves:
  truths:
    - "ccop-eval report generate globs src/results/evaluations/**/*-{model}.json and reads schema-v6 files"
    - "Legacy v5 result files (no metadata.run_id) are skipped with a warning; they are not read as v6"
    - "ccop-eval report summary loads per-run files across monthly subdirectories and aggregates correctly"
    - "ccop-eval evaluate run --verbose displays retrieved contexts from the sidecar file per test case"
    - "Per-test-case verbose panel displays system_prompt and user_prompt in a collapsible-friendly format"
    - "test_run_id.py validates RunId format and scope encoding edge cases"
    - "test_json_result_repository_v6.py validates monthly layout, sidecar file, schema_version=6, run_id in metadata, and non-zero token/latency fields"
    - "Existing test_evaluate_model_metadata.py passes against the new repository signature"
    - "GraphState tests confirm new fields exist and defaults are zero/empty"
  artifacts:
    - path: "src/tests/domain/value_objects/test_run_id.py"
      provides: "RunId unit tests"
      contains: "RunId"
    - path: "src/tests/infrastructure/test_json_result_repository_v6.py"
      provides: "v6 repository tests (filename, monthly dir, sidecar, schema marker, per-case field persistence)"
      contains: "schema_version"
    - path: "src/application/use_cases/generate_report.py"
      provides: "Report use case reading per-run glob"
      contains: "evaluations/**"
    - path: "src/presentation/cli/commands/evaluate.py"
      provides: "Verbose CLI reading sidecar for retrieved contexts"
      contains: "-contexts.json"
  key_links:
    - from: "src/application/use_cases/generate_report.py"
      to: "src/results/evaluations/**/*.json"
      via: "pathlib glob"
      pattern: "\\*\\*/\\*-|rglob"
    - from: "src/presentation/cli/commands/evaluate.py"
      to: "{run_id}-contexts.json sidecar"
      via: "lazy read during verbose display"
      pattern: "-contexts\\.json"
---

<objective>
Rewire report tooling to read per-run files via glob across monthly subdirectories, surface the captured prompt + retrieved contexts in the evaluate CLI `--verbose` output by reading the sidecar, and ship tests that lock in the schema v6 contract.

Purpose: Closes SC6 (CLI verbose displays captured data), SC7 (report globber rewired), and SC8 (tests verify schema captures all new fields). Final plan of Phase 3.1.
Output: New glob-based report loader, verbose CLI with sidecar integration, RunId tests, v6 repository tests, updated evaluate-model metadata tests, updated GraphState tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-eval-run-traceability/03.1-CONTEXT.md
@.planning/phases/03.1-eval-run-traceability/03.1-01-SUMMARY.md
@.planning/phases/03.1-eval-run-traceability/03.1-02-SUMMARY.md

# Files being modified
@src/application/use_cases/generate_report.py
@src/presentation/cli/commands/evaluate.py
@src/infrastructure/adapters/repositories/json_result_repository.py
@src/tests/application/use_cases/test_evaluate_model_metadata.py
@src/tests/rag/retrieval/test_graph_state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire report tooling to glob per-run files; add JSONResultRepository.load_by_model via glob</name>
  <files>
    src/infrastructure/adapters/repositories/json_result_repository.py
    src/application/use_cases/generate_report.py
  </files>
  <action>
    (a) `src/infrastructure/adapters/repositories/json_result_repository.py` — implement `load_by_model` via glob:
      - Replace the current stub `async def load_by_model(self, model_name: str) -> List[EvaluationResult]: return []` with a working implementation.
      - Pattern: use `self._results_dir.rglob(f"*-{model_name}.json")` — matches flat directory AND any monthly subdir.
      - For each matched file:
        - Skip files whose name ends with `-contexts.json` (sidecars).
        - Load JSON. Check `metadata.get("schema_version") == 6 and metadata.get("run_id")`:
          - If NOT v6 → log a warning `f"Skipping legacy pre-v6 result file: {path}"` and continue.
          - If v6 → reconstruct minimal EvaluationResult objects from `test_results[]`. For reporting, the repository returns a partial reconstruction focused on what `GenerateReportUseCase._calculate_summary` needs: test_id, benchmark_type, model_name, response, score, passed, threshold, metrics, tokens, latency, evaluated_at.
      - Use `json.loads` + manual construction. It's acceptable to construct lightweight stand-in objects (TestCase + ModelResponse + EvaluationResult via existing constructors with minimal args). Look at existing test_case/model_response/evaluation_result constructors for required fields; fill in defaults where the JSON doesn't have the source data.
      - If full reconstruction proves fragile, fall back to returning the raw dict list and updating `GenerateReportUseCase` to work off dicts. Claude's discretion — stay practical.

    (b) `src/application/use_cases/generate_report.py`:
      - No signature changes. The use case calls `self._result_repository.load_by_model(model_name)`. Once load_by_model works via glob (above), the reporting path Just Works.
      - Add a guard at the top of `get_summary`: if the returned results list is empty AND `self._results_dir` contains legacy `{model}_results.json` files, log a one-line note pointing the user at the migration approach (no automatic migration per CONTEXT.md).
      - In `_calculate_summary`, defensively handle missing fields on reconstructed results (system_prompt, token counts) — default to None/0 when absent.
  </action>
  <verify>
    `cd src && poetry run python -c "import inspect; from infrastructure.adapters.repositories.json_result_repository import JSONResultRepository; src = inspect.getsource(JSONResultRepository.load_by_model); assert 'rglob' in src and 'schema_version' in src and 'contexts.json' in src"`
    Integration smoke (after Plan 02 has produced at least one v6 file):
    `cd src && poetry run ccop-eval report summary --model primus-reasoning` — should aggregate across monthly subdirs and skip any legacy `primus-reasoning_results.json` with a warning.
  </verify>
  <done>
    `load_by_model` uses `rglob` to discover per-run files under any monthly subdir, skips sidecars and legacy v5 files with warnings, and returns reconstructed results that GenerateReportUseCase can summarize. `ccop-eval report generate` and `ccop-eval report summary` work against the new layout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verbose CLI reads sidecar contexts and displays system_prompt / user_prompt / retrieved_contexts per test case</name>
  <files>
    src/presentation/cli/commands/evaluate.py
  </files>
  <action>
    Add a `--verbose-io` flag (or reuse existing verbose at ctx level) so the per-test-case panel surfaces captured prompt and retrieved contexts.

    (a) `src/presentation/cli/commands/evaluate.py` — `run(...)`:
      - Add CLI option: `verbose_io: bool = typer.Option(False, "--verbose-io", help="Show captured system/user prompts and retrieved contexts per test case")`.
      - After `summary = asyncio.run(use_case.execute(request))`, compute the sidecar path:
        ```python
        from pathlib import Path
        results_dir = Path(get_settings().results_dir)
        month_dir = results_dir / datetime.utcnow().strftime("%Y-%m")
        sidecar_path = month_dir / f"{run_id.value}-contexts.json"
        sidecar = {}
        if verbose_io and sidecar_path.exists():
            with open(sidecar_path) as f:
                sidecar = json.load(f)
        ```

      - In the existing `for r in summary.results:` panel loop, inside the detail section (before the diagnostic groups), when `verbose_io` is True:
        - Append `[bold]System Prompt:[/bold]` + `r.system_prompt or "(none)"` (truncated to e.g. 600 chars with an indicator).
        - Append `[bold]User Prompt (with RAG context):[/bold]` + `r.user_prompt or "(none)"` (truncated to e.g. 1200 chars).
        - If `sidecar.get(r.test_id)` non-empty, append `[bold]Retrieved Contexts (detailed):[/bold]` and for each context dict show an indented block with citation_id, section, clause, score, and first ~200 chars of text.

      - Also in the always-visible header info section: display token counts `[bold]Tokens:[/bold] prompt={r.prompt_tokens} completion={r.completion_tokens} total={r.total_tokens}` (replacing/augmenting the existing `r.tokens_used` line).

      - Guard: if `r` is a legacy DTO without the new fields (`getattr(r, 'system_prompt', None) is None`), skip gracefully.
  </action>
  <verify>
    `cd src && poetry run python -c "import inspect; from presentation.cli.commands.evaluate import run; src = inspect.getsource(run); assert 'verbose_io' in src and '-contexts.json' in src and 'System Prompt' in src and 'prompt_tokens' in src"`
    Interactive smoke (requires a completed v6 run): `cd src && poetry run ccop-eval evaluate run --model primus-reasoning --test-ids B3-001 --mode llm-only --verbose-io` — the per-test-case panel should show system_prompt, user_prompt, token counts.
  </verify>
  <done>
    Evaluate CLI accepts `--verbose-io`, reads the sidecar for the current run, and displays captured prompts + retrieved contexts + token counts per test case. No crashes on llm-only mode (sidecar empty or missing is a no-op).
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests — RunId unit tests, v6 repository tests, updated metadata + graph_state tests</name>
  <files>
    src/tests/domain/value_objects/test_run_id.py
    src/tests/infrastructure/test_json_result_repository_v6.py
    src/tests/application/use_cases/test_evaluate_model_metadata.py
    src/tests/rag/retrieval/test_graph_state.py
  </files>
  <action>
    (a) Create `src/tests/domain/value_objects/test_run_id.py`:
      - Test `RunId(mode, scope, timestamp).value` renders `eval-run-{mode}-{scope}-{yyyyMMdd}-{HHmm}` for mode hybrid / llm-only / rag-only.
      - Test `RunId` rejects invalid mode (ValueError).
      - Test `RunId.build_scope` for every branch:
        - suite (nothing provided, benchmarks absent or equal in length to total)
        - benchmark-B3 (single benchmark)
        - benchmarks-B1-B3-B7 (multi, sorted numerically — pass unsorted and verify output is sorted)
        - tier-2
        - test-B3-001
        - test-B3-001-B3-002 (multi-test-id)
      - Test numeric sorting: `build_scope(None, ['B2','B11','B3'], None, 21) == 'benchmarks-B2-B3-B11'` (NOT B11-B2-B3).
      - Test `RunId.for_query` produces scope=query.

    (b) Create `src/tests/infrastructure/test_json_result_repository_v6.py`:
      - Use `tempfile.TemporaryDirectory()` for `results_dir`.
      - Test `save_evaluation_run`:
        - Builds a single EvaluationResult with ModelResponse(prompt_tokens=10, completion_tokens=20, latency_ms=500) and system_prompt/user_prompt set.
        - Metadata includes `run_id` and `evaluated_at`.
        - Assert file is written to `{results_dir}/{yyyy-MM}/{run_id}-{model}.json`.
        - Assert the written JSON has `metadata.run_id`, `metadata.schema_version == 6`, and `test_results[0].prompt_tokens == 10`, `completion_tokens == 20`, `total_tokens == 30`, `latency_ms == 500`, `system_prompt` present.
      - Test sidecar:
        - Pass `contexts_by_test_id={"B3-001": [{"text":"t","citation_id":"c"}]}`.
        - Assert `{run_id}-contexts.json` exists in the monthly dir with the payload intact.
      - Test `save_query_run` produces the same filename shape.
      - Test `load_by_model` via `rglob`:
        - Write two v6 files under two different monthly dirs.
        - Write one legacy v5-style file (no run_id, no schema_version) alongside.
        - Call `load_by_model("primus-reasoning")`. Assert only the two v6 files are loaded; the v5 one triggers a skip.
      - Test `save_batch` logs a deprecation/no-op and writes nothing.
      - Test `_generate_filename_v6` raises when metadata lacks run_id.

    (c) Update `src/tests/application/use_cases/test_evaluate_model_metadata.py`:
      - Read the existing tests. Update mocks/assertions to:
        - Include `run_id` and `schema_version=6` when constructing expected metadata.
        - Expect the new `save_evaluation_run(results, metadata, contexts_by_test_id=...)` signature — update call-site expectations.
      - Keep test intent; only adjust for v6.

    (d) Update `src/tests/rag/retrieval/test_graph_state.py`:
      - Extend existing tests to assert the new GraphState keys are present in `__annotations__`:
        - `system_prompt`, `user_prompt`, `prompt_tokens`, `completion_tokens`, `total_tokens`, `latency_ms`, `retrieved_contexts_detailed`.
      - Add a test that an initial_state produced by `create_rag_pipeline` carries zero/empty defaults for the new fields (without invoking the graph — inspect the `initial_state` dict literal directly, OR test a stub.)
  </action>
  <verify>
    `cd src && poetry run pytest tests/domain/value_objects/test_run_id.py -x --tb=short`
    `cd src && poetry run pytest tests/infrastructure/test_json_result_repository_v6.py -x --tb=short`
    `cd src && poetry run pytest tests/application/use_cases/test_evaluate_model_metadata.py -x --tb=short`
    `cd src && poetry run pytest tests/rag/retrieval/test_graph_state.py -x --tb=short`
    Full sweep to confirm no collateral damage: `cd src && poetry run pytest tests/ -x --tb=short --ignore=tests/rag/ingestion`
  </verify>
  <done>
    Four test files exist/updated and all pass. RunId edge cases are locked in. Repository v6 behavior is regression-proofed (monthly dir, sidecar, schema marker, per-case fields, legacy skip, load_by_model glob). Existing metadata and graph-state tests adapted to the new contract. Broader test suite stays green.
  </done>
</task>

</tasks>

<verification>
Full-suite verification after all three plans:
```bash
# Tests
cd src && poetry run pytest tests/ -x --tb=short --ignore=tests/rag/ingestion

# Live smoke (requires Ollama, optional Qdrant)
cd src && poetry run ccop-eval evaluate run --model primus-reasoning --test-ids B3-001 --mode llm-only --verbose-io
ls src/results/evaluations/$(date -u +%Y-%m)/

# Query persistence
cd src && poetry run ccop-eval query ask "What are the MFA requirements?" --mode llm-only
ls src/results/evaluations/$(date -u +%Y-%m)/eval-run-llm-only-query-*.json

# Report glob
cd src && poetry run ccop-eval report summary --model primus-reasoning
```
</verification>

<success_criteria>
- `ccop-eval report generate` and `ccop-eval report summary` discover v6 result files via `rglob` across monthly subdirectories and skip legacy v5 files with a warning.
- `ccop-eval evaluate run --verbose-io` prints system_prompt, user_prompt, and retrieved_contexts_detailed (from sidecar) plus non-zero token counts per test case.
- RunId unit tests cover every scope branch including numeric sort for multi-benchmark.
- Repository v6 tests lock in monthly directory layout, sidecar file shape, schema_version=6 marker, run_id-in-metadata, non-zero token/latency serialization, save_batch no-op, save_query_run path, load_by_model glob with legacy skip.
- Updated evaluate-model metadata tests pass against new signature.
- GraphState tests assert all 7 new fields exist.
- Full non-ingestion test suite passes (`pytest tests/ --ignore=tests/rag/ingestion`).
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-eval-run-traceability/03.1-03-SUMMARY.md` documenting:
- Report glob pattern and legacy-skip behavior
- CLI --verbose-io behavior and sidecar read pattern
- Test coverage map (which SC each test file covers)
- Any gotchas encountered during reconstruction of EvaluationResult from JSON in load_by_model
</output>
