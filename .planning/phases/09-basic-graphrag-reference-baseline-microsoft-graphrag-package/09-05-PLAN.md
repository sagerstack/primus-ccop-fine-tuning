---
phase: 09-basic-graphrag-baseline-neo4j-emergent-kg
plan: 05
type: execute
wave: 5
depends_on: [09-04]
files_modified:
  - src/presentation/cli/commands/evaluate.py
  - src/rag/presentation/cli/query.py
  - src/rag/retrieval/edges/routing.py
  - src/rag/infrastructure/adapters/langgraph_rag_adapter.py
  - src/rag/graph/retrieval/graph_retrieval_node.py
  - src/domain/value_objects/benchmark_id.py
  - tests/rag/graph/retrieval/test_graphrag_retrieval_only.py
  - tests/domain/test_benchmark_id_casing.py
autonomous: true
requirements: [D-10]

must_haves:
  truths:
    - "D-10: `evaluate run` accepts `--mode graphrag` (graph retrieval → primus generation → scored); NO retrieval-only mode is added to evaluate (unscoreable)."
    - "D-10: `query ask` accepts `--mode graphrag` (graph → primus) AND a graph-retrieval-only inspection mode (the rag-only analog: retrieved subgraph/context, no generation)."
    - "The graph-retrieval-only mode routes graph_retrieval → grade → rag_response (no LLM call), consistent with rag-only."
    - "Blocker resolved: B04/B4 test_id casing is normalized so graphrag eval results align with ground-truth ids."
  artifacts:
    - path: src/presentation/cli/commands/evaluate.py
      provides: "graphrag in VALID_EVAL_MODES"
      contains: "graphrag"
    - path: src/rag/presentation/cli/query.py
      provides: "graphrag + graph-retrieval-only in VALID_MODES"
      contains: "graphrag"
  key_links:
    - from: src/rag/presentation/cli/query.py
      to: src/rag/retrieval/edges/routing.py
      via: "graphrag-retrieval mode → rag_response (no generation)"
      pattern: "graphrag-retrieval"
    - from: src/domain/value_objects/benchmark_id.py
      to: ground-truth test ids
      via: "casing normalization B4↔B04"
      pattern: "normalize"
---

<objective>
Expose the graphrag retrieval built in Plan 04 on both CLIs exactly as `rag-only` differs today: `evaluate run` gains ONLY `--mode graphrag` (scored: graph → primus), while `query ask` gains `--mode graphrag` AND a graph-retrieval-only inspection mode (retrieved subgraph, no generation) for per-question KG iteration. Also resolve the carried B04/B4 test_id casing blocker so graphrag results align with GT ids.

Purpose: complete the D-10 mode surface and remove the known eval-alignment blocker before scoring.
Output: mode wiring across evaluate/query/routing/adapter + benchmark-id casing normalization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-basic-graphrag-reference-baseline-microsoft-graphrag-package/09-CONTEXT.md
@.planning/phases/09-basic-graphrag-reference-baseline-microsoft-graphrag-package/09-04-SUMMARY.md
@src/presentation/cli/commands/evaluate.py
@src/rag/presentation/cli/query.py
@src/rag/retrieval/edges/routing.py

<interfaces>
From src/presentation/cli/commands/evaluate.py: `VALID_EVAL_MODES = ["hybrid", "llm-only"]`; validated at line ~114.
From src/rag/presentation/cli/query.py: `VALID_MODES = ["hybrid", "llm-only", "rag-only"]`; spinner_label dict keyed by mode; scoring gated on mode.
From src/rag/retrieval/edges/routing.py (Plan 04): route_by_mode returns "graph_retrieval" for graphrag; decide_after_grading returns rag_response for "rag-only".
From src/rag/infrastructure/adapters/langgraph_rag_adapter.py: `query(question, mode)` + `is_available(mode)`.
From STATE.md Blockers: GT files use `B04-001` but benchmark codes are `B4` (casing inconsistency across B04/B4, B05/B5, ...).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add graphrag to evaluate + query mode sets (+ graph-retrieval-only)</name>
  <read_first>
    - src/presentation/cli/commands/evaluate.py (VALID_EVAL_MODES, mode validation + help text)
    - src/rag/presentation/cli/query.py (VALID_MODES, spinner_label, scoring gates)
    - src/rag/retrieval/edges/routing.py (decide_after_grading rag_response branch)
    - src/rag/infrastructure/adapters/langgraph_rag_adapter.py (is_available/query mode handling)
  </read_first>
  <behavior>
    - Test: `graphrag` is a valid evaluate mode; a retrieval-only mode is NOT accepted by `evaluate run`.
    - Test: query accepts `graphrag` and the graph-retrieval-only mode name; graph-retrieval-only routes to rag_response (no generation, generation empty / token counts 0).
    - Test: decide_after_grading returns "rag_response" for the graph-retrieval-only mode.
  </behavior>
  <action>
    In src/presentation/cli/commands/evaluate.py set `VALID_EVAL_MODES = ["hybrid", "llm-only", "graphrag"]` and update the `--mode` help text; do NOT add any retrieval-only value (D-10: unscoreable). In src/rag/presentation/cli/query.py set `VALID_MODES = ["hybrid", "llm-only", "rag-only", "graphrag", "graphrag-retrieval"]` (use `graphrag-retrieval` for the graph-retrieval-only inspection mode), add spinner labels, and gate scoring the same way rag-only is gated (no LLM-judge/RAGAs generation-side scoring for graphrag-retrieval). In src/rag/retrieval/edges/routing.py: `route_by_mode` returns "graph_retrieval" for both `graphrag` and `graphrag-retrieval`; `decide_after_grading` returns "rag_response" for `graphrag-retrieval` (mirrors rag-only) and "generate" for `graphrag`. In graph_retrieval_node.py ensure the node is mode-agnostic (populates filtered_documents for both). In langgraph_rag_adapter.py extend `is_available`/`query` to treat graphrag modes as available when the graph provider is configured.
  </action>
  <acceptance_criteria>
    - `grep -n 'VALID_EVAL_MODES = \["hybrid", "llm-only", "graphrag"\]' src/presentation/cli/commands/evaluate.py` matches; no retrieval-only token present in VALID_EVAL_MODES.
    - `grep -n "graphrag-retrieval" src/rag/presentation/cli/query.py` and `src/rag/retrieval/edges/routing.py` both match.
    - `cd src && poetry run pytest ../tests/rag/graph/retrieval/test_graphrag_retrieval_only.py -q` exits 0.
    - `cd src && poetry run ccop-eval evaluate run --help` shows graphrag; `poetry run ccop-eval query ask --help` shows both graphrag modes.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/graph/retrieval/test_graphrag_retrieval_only.py -q</automated>
  </verify>
  <done>Both CLIs expose the correct graphrag mode sets; graph-retrieval-only returns contexts with no generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize B04/B4 test_id casing for GT alignment</name>
  <read_first>
    - src/domain/value_objects/benchmark_id.py (or wherever benchmark ids are parsed — grep first)
    - .planning/STATE.md (Blockers: B04/B4 casing inconsistency detail)
    - src/infrastructure/adapters/repositories/jsonl_test_case_repository.py (how test_id/benchmark_id are read)
  </read_first>
  <behavior>
    - Test: normalize("B4-001") == normalize("B04-001") (zero-padding canonicalization to a single form).
    - Test: benchmark id comparison treats B4 and B04 as equal; result rows for graphrag map onto the GT ids without dropped/duplicated cases.
  </behavior>
  <action>
    First grep to locate the canonical benchmark-id parsing (`grep -rn "benchmark_id\|test_id\|B0" src/domain src/infrastructure/adapters/repositories`). Add a normalization function (canonical zero-padded form, e.g. `B04`) in src/domain/value_objects/benchmark_id.py (or the existing id VO) and apply it where GT test cases and eval results are keyed/compared so `B4-001` and `B04-001` resolve to one identity. Do not renumber benchmarks — only canonicalize casing/padding for matching. Add tests/domain/test_benchmark_id_casing.py.
  </action>
  <acceptance_criteria>
    - `grep -n "def normalize" src/domain/value_objects/benchmark_id.py` (or the chosen VO) matches.
    - `cd src && poetry run pytest ../tests/domain/test_benchmark_id_casing.py -q` exits 0.
    - A graphrag eval over a B4/B04 case produces exactly one result row keyed to the GT id (asserted in the test).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/domain/test_benchmark_id_casing.py -q</automated>
  </verify>
  <done>B04/B4 casing is canonicalized so graphrag results align 1:1 with ground-truth ids.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg → mode routing | Untrusted `--mode` string selects execution path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | Tampering | Invalid/unscoreable mode on evaluate | mitigate | evaluate validates against VALID_EVAL_MODES; retrieval-only deliberately excluded (D-10) |
| T-09-17 | Repudiation | Mismatched GT ids skew scores | mitigate | B04/B4 normalization ensures 1:1 result↔GT mapping; covered by test |
| T-09-18 | Information Disclosure | graph-retrieval-only leaking creds in output | accept | Output is retrieved contexts only; no credential surface; local CLI |
</threat_model>

<verification>
- Unit tests pass; evaluate/query help reflect new modes.
- graph-retrieval-only performs no generation (token counts 0).
- Benchmark-id casing test proves B4↔B04 alignment.
</verification>

<success_criteria>
`evaluate run --mode graphrag` (scored) and `query ask --mode graphrag` / graph-retrieval-only (inspection) are available and correctly routed, and the B04/B4 casing blocker no longer misaligns graphrag results with ground truth.
</success_criteria>

<output>
Create `.planning/phases/09-basic-graphrag-reference-baseline-microsoft-graphrag-package/09-05-SUMMARY.md` when done.
</output>
