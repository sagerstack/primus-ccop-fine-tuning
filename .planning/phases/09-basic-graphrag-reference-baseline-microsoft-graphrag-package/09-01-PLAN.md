---
phase: 09-basic-graphrag-baseline-neo4j-emergent-kg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - src/pyproject.toml
  - src/poetry.lock
  - src/infrastructure/config/settings.py
  - src/config/.env.example
  - tests/infrastructure/config/test_neo4j_settings.py
autonomous: false   # blocking-human package-legitimacy checkpoint (no RESEARCH.md audit)
requirements: [D-01, D-06a, D-07, D-12]
user_setup:
  - service: neo4j
    why: "Local knowledge-graph store for the GraphRAG baseline (Cypher + Browser)."
    env_vars:
      - name: CCOP_NEO4J_PASSWORD
        source: "Chosen locally; set in src/config/.env.local (NOT committed). Must match docker-compose NEO4J_AUTH."

must_haves:
  truths:
    - "D-01/D-12: Neo4j runs as a local Docker service alongside qdrant with the APOC plugin enabled."
    - "D-01: neo4j-graphrag and neo4j are installed via Poetry and importable."
    - "D-06a: extraction model config (openai/gpt-4o-mini via OpenRouter) is exposed as a dedicated, interceptable setting held constant for Phase 10."
    - "D-07: embedding config resolves to BAAI/bge-large-en-v1.5 (1024-dim), exact parity with hybrid's CCOP_QDRANT_EMBEDDING_MODEL."
    - "No Neo4j credentials are committed; the container binds to localhost only."
  artifacts:
    - path: docker-compose.yml
      provides: "neo4j service (Browser 7474 + Bolt 7687, APOC plugin, persistent volume)"
      contains: "neo4j:"
    - path: src/infrastructure/config/settings.py
      provides: "Neo4j + graph extraction/embedding settings"
      contains: "neo4j_uri"
    - path: src/config/.env.example
      provides: "documented CCOP_NEO4J_* env vars"
      contains: "CCOP_NEO4J_URI"
  key_links:
    - from: docker-compose.yml
      to: src/config/.env.local
      via: "NEO4J_AUTH reads ${CCOP_NEO4J_PASSWORD}"
      pattern: "CCOP_NEO4J_PASSWORD"
    - from: src/infrastructure/config/settings.py
      to: OpenRouter
      via: "graph_extraction_model + openrouter_api_key/base_url"
      pattern: "graph_extraction_model"
---

<objective>
Stand up the Neo4j foundation for the emergent-KG GraphRAG baseline: a local Neo4j Docker service (APOC-enabled) beside the existing qdrant service, the `neo4j-graphrag` + `neo4j` Poetry dependencies, and Pydantic settings for Neo4j connection + the two GraphRAG infrastructure models (extraction = gpt-4o-mini via OpenRouter, embeddings = bge-large-en-v1.5, 1024-dim). This is pure infrastructure/config — no graph is built here.

Purpose: give every later Phase-9 plan a reachable, credential-safe Neo4j and a settings surface that keeps the extraction/embedding models interceptable so Phase 10 can layer ontology governance additively (D-16).
Output: running `neo4j` service, installed deps, `CCOP_NEO4J_*` + graph-model settings, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-basic-graphrag-reference-baseline-microsoft-graphrag-package/09-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Existing config surface the executor extends (do NOT re-derive). -->
From src/infrastructure/config/settings.py (Pydantic BaseSettings, env_prefix "CCOP_"):
- openrouter_api_key: Optional[str]  (env CCOP_OPENROUTER_API_KEY)
- openrouter_base_url: str = "https://openrouter.ai/api/v1"
- rag_contextualization_model: str = "openai/gpt-4o-mini"   # precedent for D-06a
- qdrant_embedding_model: Optional[str]  # set to "BAAI/bge-large-en-v1.5" in .env
- model_config: env_file=("config/.env.example","config/.env.local"), env_prefix="CCOP_"

From docker-compose.yml: existing `qdrant` service uses `ports`, `volumes`, `restart: unless-stopped`, `healthcheck` — mirror this shape for `neo4j`.

Package APIs (fetch exact usage via Context7 id `/neo4j/neo4j-graphrag-python`):
- pip package name: `neo4j-graphrag` (official Neo4j); driver package: `neo4j`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for neo4j-graphrag + neo4j</name>
  <read_first>
    - .planning/phases/09-basic-graphrag-reference-baseline-microsoft-graphrag-package/09-CONTEXT.md (D-01, canonical_refs naming the packages)
  </read_first>
  <what-built>No RESEARCH.md Package Legitimacy Audit exists for this phase, so both new pip packages are treated as [ASSUMED] and must be verified before install.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/neo4j-graphrag/ — confirm publisher is Neo4j, recent releases, non-trivial download counts, links to github.com/neo4j/neo4j-graphrag-python.
    2. Open https://pypi.org/project/neo4j/ — confirm it is the official Neo4j Python driver (publisher Neo4j).
    3. Confirm neither is a typosquat (exact names `neo4j-graphrag` and `neo4j`).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms both packages are the official Neo4j packages before any `poetry add` runs.
    - This checkpoint is NOT auto-approvable (workflow.auto_advance ignored) per the legitimacy-gate rule.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to install, or name the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add Neo4j Docker service + Poetry dependencies</name>
  <read_first>
    - docker-compose.yml (existing qdrant service — mirror its structure)
    - src/pyproject.toml ([tool.poetry.dependencies] block)
    - CLAUDE.md (Poetry is the only package manager; commands run from src/)
  </read_first>
  <action>
    In docker-compose.yml add a `neo4j` service: image `neo4j:5.26-community` (5.26 LTS); `container_name: neo4j-local`; `restart: unless-stopped`; bind ports to localhost ONLY — `127.0.0.1:7474:7474` (Browser/HTTP) and `127.0.0.1:7687:7687` (Bolt); `environment` set `NEO4J_AUTH=neo4j/${CCOP_NEO4J_PASSWORD}` and `NEO4J_PLUGINS=["apoc"]` and `NEO4J_apoc_export_file_enabled=true` / `NEO4J_apoc_import_file_enabled=true`; persistent `volumes` `./neo4j_storage:/data` and `./neo4j_storage/logs:/logs`; `healthcheck` running `wget -qO- http://localhost:7474 || exit 1` (interval 30s, timeout 10s, retries 5). Add `neo4j_storage/` to the repo `.gitignore` (mirror qdrant_storage/).
    From src/, run `poetry add neo4j-graphrag neo4j` (extras: if the SentenceTransformer embedder or OpenAI LLM need optional extras per the docs, add `neo4j-graphrag[sentence-transformers]`). Do NOT hand-edit poetry.lock — let Poetry resolve. Never pass `--no-verify` anywhere.
  </action>
  <acceptance_criteria>
    - `docker-compose.yml` contains a `neo4j:` service whose `NEO4J_PLUGINS` includes `apoc` and whose ports are bound to `127.0.0.1`.
    - `NEO4J_AUTH` references `${CCOP_NEO4J_PASSWORD}` (no literal password in the file).
    - `poetry run python -c "import neo4j, neo4j_graphrag"` exits 0.
    - `.gitignore` contains `neo4j_storage/`.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run python -c "import neo4j, neo4j_graphrag; print('ok')"</automated>
  </verify>
  <done>Neo4j service defined (localhost-bound, APOC on) and both packages installed + importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Neo4j + graph-model settings and .env.example docs</name>
  <read_first>
    - src/infrastructure/config/settings.py (existing openrouter_* / qdrant_embedding_model fields + model_config)
    - src/config/.env.example (existing CCOP_OPENROUTER_* / CCOP_QDRANT_* documentation blocks)
    - tests/infrastructure/config/ (existing settings-test pattern, if present)
  </read_first>
  <behavior>
    - Test: Settings default `neo4j_uri == "bolt://localhost:7687"`, `neo4j_user == "neo4j"`, `neo4j_database == "neo4j"`.
    - Test: `graph_extraction_model` defaults to `"openai/gpt-4o-mini"` (D-06a).
    - Test: `graph_embedding_model` defaults to `"BAAI/bge-large-en-v1.5"` and `graph_embedding_dimensions == 1024` (D-07).
    - Test: `neo4j_password` defaults to None (must be supplied via env; never a committed literal).
  </behavior>
  <action>
    Add fields to Settings (env_prefix CCOP_ applies): `neo4j_uri` (CCOP_NEO4J_URI, default `bolt://localhost:7687`), `neo4j_user` (default `neo4j`), `neo4j_password: Optional[str]` (default None), `neo4j_database` (default `neo4j`), `graph_vector_index_name` (CCOP_GRAPH_VECTOR_INDEX, default `ccop_chunk_embeddings`), `graph_extraction_model` (default `openai/gpt-4o-mini` — reuses openrouter_api_key/openrouter_base_url), `graph_embedding_model` (default `BAAI/bge-large-en-v1.5`), `graph_embedding_dimensions: int` (default 1024). Keep `graph_extraction_model`/`graph_embedding_model` as explicit standalone fields (interceptable seam for D-16), documented as "held constant across Phase 9 and Phase 10". In src/config/.env.example add a `# --- Neo4j GraphRAG (Phase 9) ---` block documenting CCOP_NEO4J_URI, CCOP_NEO4J_USER, CCOP_NEO4J_PASSWORD (with a placeholder, never a real secret), CCOP_NEO4J_DATABASE, CCOP_GRAPH_VECTOR_INDEX, CCOP_GRAPH_EXTRACTION_MODEL, CCOP_GRAPH_EMBEDDING_MODEL. Create tests/infrastructure/config/test_neo4j_settings.py covering the behaviors above.
  </action>
  <acceptance_criteria>
    - `grep -n "neo4j_uri" src/infrastructure/config/settings.py` returns a match.
    - `grep -n "CCOP_NEO4J_URI" src/config/.env.example` returns a match; `.env.example` contains NO real password (placeholder only).
    - `cd src && poetry run pytest ../tests/infrastructure/config/test_neo4j_settings.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/infrastructure/config/test_neo4j_settings.py -q</automated>
  </verify>
  <done>Settings expose Neo4j connection + interceptable graph extraction/embedding models with defaults matching D-06a/D-07; env documented without secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → neo4j container | Local process connects over Bolt/HTTP; credentials cross here |
| repo → git remote | Config files could leak secrets if committed |
| app → OpenRouter | Extraction model config carries an API key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | Neo4j credentials | mitigate | Password only via `${CCOP_NEO4J_PASSWORD}` from .env.local; `.env.example` carries a placeholder; no literal creds in docker-compose or settings defaults |
| T-09-02 | Elevation of Privilege | Neo4j network exposure | mitigate | Ports bound to `127.0.0.1` only (no 0.0.0.0); local-first, no cloud |
| T-09-03 | Information Disclosure | neo4j_storage/ + .env.local | mitigate | `neo4j_storage/` gitignored; `.env.local` already gitignored (only `.env.example` committed) |
| T-09-04 | Tampering | pip package supply chain | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies neo4j-graphrag + neo4j on pypi.org before install |
| T-09-SC | Tampering | poetry add neo4j-graphrag / neo4j | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] packages (no RESEARCH.md audit table) |
</threat_model>

<verification>
- `docker compose config` parses; `neo4j` service present with APOC + localhost binding.
- `cd src && poetry run python -c "import neo4j, neo4j_graphrag"` exits 0.
- Settings tests pass; no secret literals in committed files.
</verification>

<success_criteria>
Neo4j service is defined and startable locally, both packages are installed and importable, and settings expose Neo4j connection plus the interceptable gpt-4o-mini extraction / bge-large-en-v1.5 (1024-dim) embedding models — all without committing any credential.
</success_criteria>

<output>
Create `.planning/phases/09-basic-graphrag-reference-baseline-microsoft-graphrag-package/09-01-SUMMARY.md` when done.
</output>
