---
phase: 09-basic-graphrag-baseline-neo4j-emergent-kg
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - src/rag/graph/inspect/__init__.py
  - src/rag/graph/inspect/metrics.py
  - src/rag/graph/cli/graph.py
  - docs/phase-2/neo4j-browser-workflow.md
  - tests/rag/graph/test_kg_metrics.py
  - tests/rag/graph/inspect/test_kg_metrics_integration.py
autonomous: true
requirements: [D-18, D-19]

must_haves:
  truths:
    - "D-18: KG quality is measurable — `ccop-eval graph inspect` reports node/edge counts, entity-type distribution, degree + orphan/isolated-node analysis, clause coverage, duplicate entities, and extraction failure rate."
    - "D-18: clause coverage is computed against src/rag/ingestion/fixtures/clause_inventory.json (how many inventory clauses surface among graph nodes)."
    - "D-18: KG quality is visible — a documented Neo4j Browser workflow (http://localhost:7474) with Cypher snippets to eyeball density/clusters/garbage."
    - "D-19: inspection is the iterate-and-improve surface (inspect → adjust → rebuild → re-inspect) that gates scoring; metrics are honest and do not tune toward B01/B03/B04."
  artifacts:
    - path: src/rag/graph/inspect/metrics.py
      provides: "KGInspector with the D-18 metric methods"
      contains: "class KGInspector"
    - path: src/rag/graph/cli/graph.py
      provides: "graph inspect + graph stats commands"
      contains: "clause_coverage"
    - path: docs/phase-2/neo4j-browser-workflow.md
      provides: "Neo4j Browser/Bloom visual-inspection guide"
      contains: "7474"
  key_links:
    - from: src/rag/graph/inspect/metrics.py
      to: src/rag/ingestion/fixtures/clause_inventory.json
      via: "clause_coverage reads inventory entries"
      pattern: "clause_inventory"
    - from: src/rag/graph/cli/graph.py
      to: src/rag/graph/inspect/metrics.py
      via: "inspect/stats commands call KGInspector"
      pattern: "KGInspector"
---

<objective>
Make the emergent KG *seen and measured* before it is ever scored. Add `ccop-eval graph inspect` / `graph stats` (KGInspector querying Neo4j) reporting the D-18 quality metrics — node/edge counts, entity-type + degree distributions, orphan/isolated nodes, clause coverage vs `clause_inventory.json`, duplicate/near-duplicate entities, and extraction failure rate — plus a documented Neo4j Browser visual-inspection workflow.

Purpose: this is the iterate-and-improve surface (D-19). The user validates and, if needed, fixes malformed/garbage extractions and rebuilds (Plan 02 `--drop`) BEFORE the retrieval/eval plans run — so a degenerate graph is never scored. Honesty guardrail: metrics measure functionality, they are not a knob for chasing B01/B03/B04 scores.
Output: KGInspector, `graph inspect`/`graph stats`, Browser workflow doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-basic-graphrag-reference-baseline-microsoft-graphrag-package/09-CONTEXT.md
@.planning/phases/09-basic-graphrag-reference-baseline-microsoft-graphrag-package/09-02-SUMMARY.md

<interfaces>
From src/infrastructure/config/settings.py: neo4j_uri/user/password/database, graph_vector_index_name.
From src/rag/graph/cli/graph.py (Plan 02): existing `graph_app` Typer + `build` command — ADD `inspect` and `stats` commands to the same app.
From src/rag/ingestion/fixtures/clause_inventory.json:
- top-level keys: {"generated_at", "source_docs", "entries"}; entries is a list of {"clause_id": str, "source_doc": str, ...}
Neo4j driver usage: neo4j.GraphDatabase.driver(uri, auth=(user, password)); session.run(parameterized_cypher).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KGInspector metric methods</name>
  <read_first>
    - src/rag/ingestion/fixtures/clause_inventory.json (entries schema)
    - src/infrastructure/config/settings.py (neo4j_* settings)
    - src/rag/graph/build/kg_builder.py (labels used, e.g. Chunk/__Entity__ conventions from neo4j-graphrag)
    - tests/rag/test_qdrant_vector_store_adapter.py (integration-test precedent: `@pytest.mark.integration` against a live local service)
  </read_first>
  <behavior>
    - Test: node_count/edge_count return ints from a mocked/embedded session.
    - Test: entity_type_distribution returns {label_or_type: count}; degree_distribution returns a histogram/summary; orphan_nodes returns count of degree-0 nodes.
    - Test: clause_coverage(inventory_path) returns {covered: int, total: int, coverage_ratio: float} where total == number of unique clause_ids in entries and covered == how many appear among graph node text/ids (case-insensitive match).
    - Test: duplicate_entities returns groups of same-normalized-name nodes; extraction_failure_rate reads BuildStats-style failures if available else returns 0.0 with a note.
  </behavior>
  <action>
    Create src/rag/graph/inspect/metrics.py with `class KGInspector` (constructed from a neo4j driver + settings). Methods: `node_count()`, `edge_count()`, `entity_type_distribution()`, `degree_distribution()`, `orphan_nodes()`, `clause_coverage(inventory_path)`, `duplicate_entities()`, `extraction_failure_rate()`, and a `summary() -> dict` aggregating all. ALL Cypher must be parameterized (no f-string interpolation of any value into query text). clause_coverage: load `entries` from clause_inventory.json, dedupe clause_id, and count how many clause identifiers appear in node names/ids/text (case-insensitive) — report covered/total/ratio. Add tests/rag/graph/test_kg_metrics.py using either a mocked neo4j session or `neo4j` test doubles so no live DB is needed. Also add tests/rag/graph/inspect/test_kg_metrics_integration.py: a `@pytest.mark.integration` test mirroring tests/rag/test_qdrant_vector_store_adapter.py that seeds a tiny graph in a live local Neo4j (`docker compose up -d neo4j`), runs KGInspector against it, and asserts node_count/edge_count > 0 and summary() returns the D-18 metric keys (clause_coverage, orphan count, entity-type distribution). Mark it `@pytest.mark.integration` so `poetry run pytest -m "not integration"` stays green without a live service.
  </action>
  <acceptance_criteria>
    - `grep -n "class KGInspector" src/rag/graph/inspect/metrics.py` matches.
    - `grep -n "clause_inventory" src/rag/graph/inspect/metrics.py` matches (coverage reads the fixture).
    - No Cypher uses f-string/`.format` value interpolation: `grep -nE 'session.run\(f"|\.run\(.*\.format\(' src/rag/graph/inspect/metrics.py` returns nothing.
    - `cd src && poetry run pytest ../tests/rag/graph/test_kg_metrics.py -q` exits 0.
    - `grep -q "@pytest.mark.integration" tests/rag/graph/inspect/test_kg_metrics_integration.py` — live-Neo4j metrics test present and gated out of the default `-m "not integration"` run.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/graph/test_kg_metrics.py -q</automated>
  </verify>
  <done>KGInspector computes all D-18 metrics via parameterized Cypher, including clause coverage vs the inventory fixture.</done>
</task>

<task type="auto">
  <name>Task 2: `graph inspect` + `graph stats` CLI commands</name>
  <read_first>
    - src/rag/graph/cli/graph.py (Plan 02 graph_app + build command)
    - src/rag/graph/inspect/metrics.py (Task 1 KGInspector)
    - src/presentation/cli/commands/report.py (Rich table output conventions, for consistency)
  </read_first>
  <action>
    Add two commands to the existing `graph_app`: `inspect` prints a human-readable Rich report of KGInspector.summary() — a `clause_coverage` line (covered/total/ratio), node/edge counts, entity-type distribution table, degree summary, orphan count, duplicate-entity groups, extraction failure rate. `stats` prints the same summary as machine-readable JSON (for the Plan 06 KG-quality report section), optionally to `--output <path>`. Both open a neo4j driver from settings. Follow existing Typer/Rich patterns.
  </action>
  <acceptance_criteria>
    - `cd src && poetry run ccop-eval graph inspect --help` and `graph stats --help` both exit 0.
    - The `inspect` output path emits a line containing `clause_coverage` (verified in a unit/CLI test with a mocked KGInspector).
    - `stats --output` writes valid JSON containing `clause_coverage`, `node_count`, `edge_count`.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run ccop-eval graph inspect --help && poetry run ccop-eval graph stats --help</automated>
  </verify>
  <done>`graph inspect` (human) and `graph stats` (JSON) surface the full D-18 metric set, including clause coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Neo4j Browser visual-inspection workflow doc</name>
  <read_first>
    - docs/phase-2/ (existing docs style/location)
    - .planning/phases/09-basic-graphrag-reference-baseline-microsoft-graphrag-package/09-CONTEXT.md (D-18 interactive layer)
  </read_first>
  <action>
    Create docs/phase-2/neo4j-browser-workflow.md documenting the interactive KG-inspection loop (D-18 visual layer / D-19 iterate loop): how to open Neo4j Browser at http://localhost:7474 (Bolt bolt://localhost:7687, auth from .env.local), and a curated set of Cypher snippets to eyeball quality — sample entities/relationships, densest nodes, orphan/isolated nodes, entity-type counts, and duplicate-name candidates. State the honesty guardrail explicitly: iteration is for making emergent extraction functional (fix malformed/garbage nodes), NOT for tuning chunk/prompt to chase B01/B03/B04 — any change beyond "make it work" is a reported, principled decision. Reference `ccop-eval graph inspect/stats` as the quantitative complement and Plan 02 `graph build --drop` as the rebuild step.
  </action>
  <acceptance_criteria>
    - `grep -n "7474" docs/phase-2/neo4j-browser-workflow.md` matches.
    - Doc contains at least 4 Cypher inspection snippets and an explicit "honesty guardrail" section referencing D-19.
    - Doc references `ccop-eval graph inspect` and the rebuild loop.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "7474" docs/phase-2/neo4j-browser-workflow.md && grep -qi "honesty guardrail" docs/phase-2/neo4j-browser-workflow.md</automated>
  </verify>
  <done>A documented Browser workflow lets the graph be visually inspected and iterated, with the emergent-baseline honesty guardrail spelled out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inspect CLI → Neo4j | Read-only Cypher over Bolt |
| inventory file → coverage metric | Local JSON read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Injection (Tampering) | KGInspector Cypher | mitigate | All queries parameterized; no value interpolated into query text (grep-gated in acceptance) |
| T-09-10 | Information Disclosure | Neo4j creds in inspect CLI | mitigate | Credentials read from settings (.env.local); driver auth tuple, never logged |
| T-09-11 | Repudiation | Metrics used to silently tune the baseline | accept/mitigate | Honesty guardrail documented (D-19); metrics measure functionality, not score-chasing; deviations reported |
</threat_model>

<verification>
- Unit tests pass with mocked Neo4j (no live DB required).
- `graph inspect`/`graph stats` help works; JSON stats include clause_coverage.
- Browser workflow doc present with Cypher snippets + guardrail.
</verification>

<success_criteria>
The emergent KG can be both measured (`ccop-eval graph inspect|stats` → clause coverage + structural quality metrics) and visually explored (documented Neo4j Browser workflow), giving the user an honest iterate-and-improve loop that gates scoring in later plans.
</success_criteria>

<output>
Create `.planning/phases/09-basic-graphrag-reference-baseline-microsoft-graphrag-package/09-03-SUMMARY.md` when done.
</output>
