---
phase: 11-align-graphrag-to-graphcompliance-architecture-scenario-anch
plan: 05
type: execute
wave: 4
depends_on: ["11-04"]
files_modified:
  - src/rag/graph/ontology/refers_to_linker.py
  - src/rag/graph/build/policy_graph_builder.py
  - src/rag/graph/cli/graph.py
  - tests/rag/graph/ontology/test_refers_to_linker.py
  - tests/rag/graph/build/test_policy_graph_builder.py
autonomous: false
requirements: [R11-1, R11-2]
must_haves:
  truths:
    - "A human approval gate ends this wave (D-26): the fully built offline Policy Graph (REFERS_TO edges + CLI stats) is reviewed before the Context Graph wave"
    - "Stage 3 creates REFERS_TO edges via regex (explicit refs) + a small LLM (implicit/relative refs), traversed at query time for exception closure (D-05)"
    - "REFERS_TO edges connect CUs by explicit (regex) and implicit/relative (small-LLM) references (Stage 3)"
    - "REFERS_TO candidate detection reuses the boundary-aware clause-id matcher (5.3 never matches inside 5.3.10) — Finding 3 over-linking avoided"
    - "A PolicyGraphBuilder orchestrates Stage 1->2->3 per document with factory injection, failures collected not swallowed, stats re-queried from Neo4j"
    - "`ccop-eval graph build-compliance` builds the full Policy Graph reproducibly (the offline rules graph reused across all cases)"
  artifacts:
    - path: "src/rag/graph/ontology/refers_to_linker.py"
      provides: "Stage 3: REFERS_TO edge creation (regex explicit + LLM implicit)"
      contains: "REFERS_TO"
    - path: "src/rag/graph/build/policy_graph_builder.py"
      provides: "Orchestrator driving classify->extract->link per doc; BuildStats re-queried"
      contains: "class PolicyGraphBuilder"
    - path: "src/rag/graph/cli/graph.py"
      provides: "graph build-compliance CLI command"
      contains: "build-compliance"
  key_links:
    - from: "src/rag/graph/ontology/refers_to_linker.py"
      to: "Neo4j :ComplianceUnit -[:REFERS_TO]-> :ComplianceUnit"
      via: "static parameterized MERGE over computed reference pairs"
      pattern: "MERGE.*REFERS_TO"
    - from: "src/rag/graph/build/policy_graph_builder.py"
      to: "cu_classifier + cu_extractor + refers_to_linker"
      via: "sequential Stage 1->2->3 orchestration per document"
      pattern: "PolicyGraphBuilder"
---

<objective>
Policy Graph Stage 3 (relational linking) per GraphCompliance §3.1, plus the build orchestrator and CLI that assemble the full offline Policy Graph (the reusable rules graph — GraphCompliance's D-01/D-02 two-graph architecture, Policy half). Create `REFERS_TO` edges (regex for explicit refs like "Clause 5.7.2", small LLM for implicit/relative refs) traversed at query time for exception closure.

Purpose: D-05 (REFERS_TO linking), D-01/D-02 (the Policy Graph is the offline rules graph). The `REFERS_TO` edges are what the Compliance Gate traverses for reference-closure exception handling (11-08).
Output: A fully built, reproducible Policy Graph and a first-class `graph build-compliance` command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-CONTEXT.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-PATTERNS.md
@src/rag/graph/ontology/clause_linker.py
@src/rag/graph/build/ontology_kg_builder.py
@src/rag/graph/cli/graph.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage 3 REFERS_TO linker (regex explicit + LLM implicit)</name>
  <read_first>
    - src/rag/graph/ontology/clause_linker.py (whole file; lines 39-54 static `$pairs` MERGE Cypher, 110-133 `_compute_pairs`, 165-173 `_accumulate_stats` — the deterministic post-hoc MERGE-linking template)
    - src/rag/graph/inspect/metrics.py lines 184-193 `KGInspector._clause_id_appears` (boundary-aware matcher — reuse a THIRD time; prevents 5.3 matching inside 5.3.10)
    - src/rag/retrieval/nodes/function_type_routing.py (settings-gated LLM call shape for the implicit-reference half)
    - 11-CONTEXT.md D-05 (REFERS_TO = regex explicit + small LLM implicit), Finding 3 (short-id over-linking)
  </read_first>
  <behavior>
    - Explicit references ("Clause 5.7.2") produce REFERS_TO edges via boundary-aware regex detection over CU verbatim text.
    - Implicit/relative references resolved by a settings-gated small LLM call (degrade-to-none on failure, never raise).
    - No spurious edge from short-id substring collision (5.3 -> 5.3.10) — Finding 3 guard.
    - LinkStats re-queries Neo4j for authoritative post-link edge counts.
  </behavior>
  <action>Create `src/rag/graph/ontology/refers_to_linker.py` on the `clause_linker.py` template: a module-level static `$pairs` MERGE Cypher creating `(:ComplianceUnit)-[:REFERS_TO]->(:ComplianceUnit)` edges, a `_compute_pairs` that reuses `KGInspector._clause_id_appears` for explicit-reference detection over CU text, and a settings-gated LLM half (copy `function_type_routing` shape) for implicit/relative refs returning candidate target ids (enum/id-validated, degrade to empty). Report a `LinkStats` dataclass with edge counts re-queried from Neo4j. Add `tests/rag/graph/ontology/test_refers_to_linker.py` asserting explicit refs link, short-id collision does NOT link, and LLM failure degrades cleanly.</action>
  <acceptance_criteria>
    - `poetry run pytest ../tests/rag/graph/ontology/test_refers_to_linker.py -x` passes, including an explicit "5.3 must not link to 5.3.10" over-linking guard.
    - After a link run, LinkStats reports a Neo4j-re-queried REFERS_TO edge count (not an in-process counter).
    - `refers_to_linker.py` Cypher is module-level with bound params only (grep: no interpolation into query body).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/graph/ontology/test_refers_to_linker.py -x</automated>
  </verify>
  <done>REFERS_TO edges created for explicit + implicit refs; no short-id over-linking; authoritative stats.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PolicyGraphBuilder orchestrator (Stage 1->2->3)</name>
  <read_first>
    - src/rag/graph/build/ontology_kg_builder.py lines 257-399 (`OntologyKGBuilder` — factory injection, idempotent index/constraint bootstrap, BuildStats, failures-collected-in-list, stats re-queried; the orchestrator template)
    - src/rag/graph/ontology/cu_classifier.py + cu_extractor.py + clause_source_annotator.py (11-02 source layer + 11-04 classify/mint/formalize — the stages this orchestrates)
    - 11-CONTEXT.md D-01/D-02 (Policy Graph = offline rules graph)
  </read_first>
  <action>Create `src/rag/graph/build/policy_graph_builder.py` (`PolicyGraphBuilder`) driving Stage 1 classify + mint CUs -> Stage 2 4-tuple extract -> Stage 3 REFERS_TO link in sequence, per document (on the 11-02 source layer). Use injectable factories (driver/LLM/embedder) for unit-testability without a live driver, collect per-doc failures in a list (never swallow — T-09-08), bootstrap any needed CU vector index / `(cu_id, source_doc)` constraint idempotently (swallow already-exists, raise otherwise), and re-query Neo4j for a `PolicyBuildStats` after the build. Add `tests/rag/graph/build/test_policy_graph_builder.py` with injected fakes asserting the 3 stages run in order and failures are collected, not swallowed.</action>
  <acceptance_criteria>
    - `poetry run pytest ../tests/rag/graph/build/test_policy_graph_builder.py -x` passes with injected fakes (no live Neo4j), asserting stage order and failure-collection.
    - PolicyBuildStats fields are populated from Neo4j re-queries (CU count by type, REFERS_TO edge count), not in-process counters.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/graph/build/test_policy_graph_builder.py -x</automated>
  </verify>
  <done>Orchestrator runs the 4 stages in order with factory injection, collected failures, re-queried stats.</done>
</task>

<task type="auto">
  <name>Task 3: `graph build-compliance` CLI + live end-to-end Policy Graph build</name>
  <read_first>
    - src/rag/graph/cli/graph.py lines 61-130 (`build` command + `_run_build` async orchestration + `_print_summary`), lines 147-217 (`build-ontology` variant with --drop/--sample/--permissive/--link flags — mirror this shape)
    - src/rag/graph/build/policy_graph_builder.py (Task 2 output)
    - 11-CONTEXT.md D-25.4 (rebuild graph via graph build --drop)
  </read_first>
  <action>Add a `graph build-compliance` command to `src/rag/graph/cli/graph.py` mirroring `build-ontology`'s `_run_build`-shaped async orchestration (drop flag, corpus load via `load_ccop_corpus_texts`, `PolicyGraphBuilder` construction, stats summary table). Run it live against the re-ingested corpus (requires 11-01 completeness gate green) with `--drop`, following the e2e-testing rule (smallest real slice = the full build once).</action>
  <acceptance_criteria>
    - `poetry run ccop-eval graph build-compliance --drop` completes and prints a stats table showing N actor-CUs, M meta-CUs, P premises, and a REFERS_TO edge count > 0.
    - Post-build Cypher confirms zero CUs without `cu_type`, zero judged CUs without a 4-tuple, and zero CUs without verbatim text (Policy Graph is complete and text-carrying).
    - The build refuses/errors if the 11-01 completeness gate has not passed (no CU without provision text).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run ccop-eval graph build-compliance --drop</automated>
  </verify>
  <done>Live Policy Graph built end-to-end via CLI; complete, typed, tuple-bearing, text-carrying, linked.</done>
</task>


<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full offline Policy Graph built via `graph build-compliance`: REFERS_TO edges (explicit + implicit), the PolicyGraphBuilder orchestrator, and the CLI — with typed CUs, 4-tuples, and hard-linked verbatim text.</what-built>
  <how-to-verify>1. Confirm the build stats table (A/M/P, REFERS_TO edge count > 0). 2. Confirm zero CUs without cu_type / 4-tuple / source text and the no-over-linking guard holds.</how-to-verify>
  <resume-signal>Type "approved" to advance to Wave 5 (11-06), or list what needs rework.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| linker/builder -> Neo4j | Cypher writes creating REFERS_TO edges and CU properties |
| OpenRouter API key | Secret used for implicit-reference LLM half |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Tampering | REFERS_TO edge Cypher | mitigate | Static parameterized `$pairs` MERGE; boundary-aware matcher prevents spurious edges (Finding 3) |
| T-11-12 | Denial of Service | multi-stage build | mitigate | Per-doc failures collected in list, never abort whole build; idempotent index bootstrap |
| T-11-13 | Information Disclosure | OPENROUTER_API_KEY | mitigate | Settings/env only; never logged |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- REFERS_TO linker green (no over-linking); orchestrator runs 4 stages with fakes; live build produces a complete, typed, tuple-bearing, text-carrying, linked Policy Graph.
</verification>

<success_criteria>
- D-05 satisfied: REFERS_TO edges (explicit + implicit).
- D-01/D-02 satisfied: the offline Policy Graph is built reproducibly via CLI.
</success_criteria>

<output>
Create `.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-05-SUMMARY.md` when done.
</output>
