---
phase: 11-align-graphrag-to-graphcompliance-architecture-scenario-anch
plan: 04
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/rag/graph/ontology/cu_classifier.py
  - src/rag/graph/ontology/cu_extractor.py
  - tests/rag/graph/ontology/test_cu_classifier.py
  - tests/rag/graph/ontology/test_cu_extractor.py
autonomous: true
requirements: [R11-2, R11-4]
must_haves:
  truths:
    - "GraphCompliance is reasoning, not retrieval (D-01): CUs are structured for deterministic reasoning (classification/gating/closure), not as a retrieval index"
    - "The Policy Graph is the offline rules graph (D-02): CU classification and formalization run once, offline, reused across all cases"
    - "Stage 1 classifies each CU three ways — premise / meta-CU / actor-CU (D-03) — warm-started from Phase-10 function-type tags"
    - "Stage 2 formalizes each judged CU into the 4-tuple subject/constraint/context/conditions (D-04) via schema-constrained extraction"
    - "Every :ComplianceUnit is classified exactly one of premise / meta-CU / actor-CU (Stage 1, run once offline)"
    - "Classification warm-starts from Phase-10 function-type tags (DefinitionClause->premise, ScopeClause->meta-CU, ControlClause->actor-CU)"
    - "Every actor-CU / meta-CU carries a 4-tuple <subject, constraint, context, conditions> (Stage 2), schema-constrained, never crashing the batch on bad LLM output"
    - "Premise CUs carry text and are embedded/retrievable (runtime STRONG/WEAK confidence signal, D-09)"
  artifacts:
    - path: "src/rag/graph/ontology/cu_classifier.py"
      provides: "Stage 1: premise/meta-CU/actor-CU classification with locked 3-value enum + warm-start dict"
      contains: "VALID_CU_TYPES"
    - path: "src/rag/graph/ontology/cu_extractor.py"
      provides: "Stage 2: schema-constrained 4-tuple extraction with JSON-repair/degrade"
      contains: "subject"
  key_links:
    - from: "src/rag/graph/ontology/cu_classifier.py"
      to: ":ComplianceUnit.cu_type"
      via: "LLM classify + enum-validate, write cu_type property"
      pattern: "cu_type"
    - from: "src/rag/graph/ontology/cu_extractor.py"
      to: ":ComplianceUnit 4-tuple properties"
      via: "schema-constrained LLM extraction of subject/constraint/context/conditions"
      pattern: "constraint"
---

<objective>
Policy Graph Stage 1 (text classification) and Stage 2 (rule formalization) per GraphCompliance §3.1. Classify each CU three ways (premise / meta-CU / actor-CU) and formalize each judged CU into the 4-tuple ⟨subject, constraint, context, conditions⟩.

Purpose: D-03 (classification, warm-started from P10 function-type tags), D-04 (4-tuple extraction), D-09 (premise is a runtime confidence signal so its text must be embedded/retrievable). The premise/CU label is not just a filter — it drives the STRONG/WEAK hypernym bonus consumed at query time (11-06).
Output: Every CU carries a `cu_type` and (for meta/actor CUs) a validated 4-tuple.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-CONTEXT.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-PATTERNS.md
@research/graphcompliance/2510.26309v1.pdf
@src/rag/retrieval/nodes/function_type_routing.py
@src/rag/graph/build/ontology_kg_builder.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage 1 CU classifier — premise / meta-CU / actor-CU</name>
  <read_first>
    - src/rag/retrieval/nodes/function_type_routing.py lines 53-93 (`_classify_function_type` — settings-gated LLM call, defensive quote-strip, enum-or-default, never-raise shape to copy exactly)
    - src/rag/graph/ontology/clause_seeder.py lines 81-97 (`_CCOP_2_0_SECTION_FUNCTION_TYPE` static verified mapping + `DEFAULT_*` fallback — the shape for the warm-start dict)
    - research/graphcompliance/2510.26309v1.pdf §3.1 Stage 1 (premise = non-deontic definitional; meta-CU = applicability/scope gate; actor-CU = obligation on a role-bearing actor)
    - 11-CONTEXT.md D-03 (classification + warm-start), D-09 (premise runtime signal — text must be embedded), D-18 (pure GraphCompliance)
  </read_first>
  <behavior>
    - Each CU classified into exactly one of `{premise, meta-CU, actor-CU}`; bad LLM output degrades to a safe default (log, never raise).
    - Warm-start static dict maps Phase-10 function-type tags: DefinitionClause->premise, ScopeClause->meta-CU, ControlClause->actor-CU; the LLM only classifies where no warm-start tag exists.
    - Premise CUs retain their verbatim text and remain embedded/retrievable (D-09) — classification does not strip text.
    - Definitional premise ("CII means...") -> premise/STRONG source; the Act §7 designation rule -> meta-CU (not premise).
  </behavior>
  <action>Create `src/rag/graph/ontology/cu_classifier.py` copying the `_classify_function_type` shape: locked `VALID_CU_TYPES = {"premise", "meta-CU", "actor-CU"}`, defensive strip-before-validate, settings-gated OpenRouter call (`settings.ontology_discovery_model`), degrade-to-default-and-log on any failure. Encode the D-03 warm-start as a static dict (mirroring `_CCOP_2_0_SECTION_FUNCTION_TYPE`) with an explicit `DEFAULT_CU_TYPE` constant. Write the result to each CU's `cu_type` property via parameterized Cypher. Add `tests/rag/graph/ontology/test_cu_classifier.py` asserting: enum-validation rejects/degrades bad output, warm-start dict overrides the LLM for tagged clauses, and premise CUs keep their text.</action>
  <acceptance_criteria>
    - After classification, a Cypher count reports N actor-CUs + M meta-CUs + P premises, and every `:ComplianceUnit` has a `cu_type` in `VALID_CU_TYPES` (zero unclassified).
    - `poetry run pytest ../tests/rag/graph/ontology/test_cu_classifier.py -x` passes, including a case where malformed LLM output degrades to `DEFAULT_CU_TYPE` without raising.
    - Warm-start assertion: a known DefinitionClause CU classifies `premise`, a ScopeClause CU `meta-CU`, a ControlClause CU `actor-CU`, without an LLM call.
    - Premise CUs retain non-empty `text` (D-09).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/graph/ontology/test_cu_classifier.py -x</automated>
  </verify>
  <done>Every CU typed premise/meta-CU/actor-CU; warm-start honored; degrade-safe; premise text retained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stage 2 4-tuple extractor — ⟨subject, constraint, context, conditions⟩</name>
  <read_first>
    - src/rag/graph/build/ontology_kg_builder.py lines 92-131 (`ONTOLOGY_EXTRACTION_PROMPT` — locked-vocabulary, do-not-invent, strict-JSON-only template to copy)
    - src/rag/graph/build/gleaning_extractor.py lines 152-192 (`_parse_graph_response` — `fix_invalid_json` reuse + try/except/degrade-to-empty shape; Pydantic-validate)
    - research/graphcompliance/2510.26309v1.pdf §3.1 Stage 2 (rule formalization to the obligation 4-tuple)
    - 11-CONTEXT.md D-04 (schema-constrained 4-tuple extraction), D-13 payload (tuple = reasoning rep, text = citation payload — text stays hard-linked regardless of extraction loss)
  </read_first>
  <behavior>
    - Every meta-CU and actor-CU gets a 4-tuple with all four fields (subject, constraint, context, conditions); missing fields default to explicit empty, never crash the batch.
    - Extraction is schema-constrained (Pydantic-validated), degrading to a safe empty tuple on parse failure (case is not lost).
    - The CU's verbatim text remains hard-linked independent of extraction quality (D-13) — extraction loss cannot remove the real text.
  </behavior>
  <action>Create `src/rag/graph/ontology/cu_extractor.py` with a CU 4-tuple extraction prompt modeled on `ONTOLOGY_EXTRACTION_PROMPT` (swap the node/relationship schema for the ⟨subject, constraint, context, conditions⟩ schema, keep the do-not-invent + strict-JSON contract). Reuse `neo4j_graphrag`'s `fix_invalid_json` + a Pydantic `CUTuple` model, mirroring `_parse_graph_response`'s try/except/degrade shape (never raise on bad JSON — return an empty tuple, log). Write the tuple to CU node properties via parameterized Cypher. Add `tests/rag/graph/ontology/test_cu_extractor.py` asserting: a well-formed provision yields a complete 4-tuple, malformed LLM JSON degrades to an empty tuple without crashing, and the CU's `text` property is untouched by extraction.</action>
  <acceptance_criteria>
    - After extraction, every meta-CU/actor-CU has all four tuple fields present (empty-string allowed, null not allowed); a Cypher count of judged CUs missing any tuple field returns 0.
    - `poetry run pytest ../tests/rag/graph/ontology/test_cu_extractor.py -x` passes, including the malformed-JSON-degrades case.
    - Assertion: CU `text` (from 11-02 alignment) is identical before and after extraction (extraction never mutates the citation payload).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/graph/ontology/test_cu_extractor.py -x</automated>
  </verify>
  <done>Judged CUs carry a complete validated 4-tuple; degrade-safe; verbatim text preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus text -> LLM classifier/extractor | Untrusted-shaped provision text prompts an LLM; malformed output must not crash the batch |
| OpenRouter API key | Secret used for classification/extraction calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Denial of Service | LLM batch classification/extraction | mitigate | Settings-gated, try/except degrade-to-default per CU; one bad CU never aborts the build |
| T-11-09 | Information Disclosure | OPENROUTER_API_KEY | mitigate | Read from settings/env only; never logged; not embedded in prompts |
| T-11-10 | Tampering | Cypher writes of cu_type/tuple | mitigate | Parameterized Cypher only (T-09-12) |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (reuses neo4j-graphrag, openai) |
</threat_model>

<verification>
- Every CU typed; warm-start honored; every judged CU has a complete 4-tuple; degrade-safe on bad LLM output; verbatim text preserved.
</verification>

<success_criteria>
- D-03 satisfied: three-way classification, warm-started.
- D-04 satisfied: schema-constrained 4-tuple on every judged CU.
- D-09 satisfied: premise text embedded/retrievable for the runtime STRONG/WEAK signal.
</success_criteria>

<output>
Create `.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-04-SUMMARY.md` when done.
</output>
