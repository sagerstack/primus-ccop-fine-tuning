---
phase: 11-align-graphrag-to-graphcompliance-architecture-scenario-anch
plan: 10
type: execute
wave: 9
depends_on: ["11-08", "11-09", "11-03"]
files_modified:
  - src/application/use_cases/clause_hit_harness.py
  - src/infrastructure/adapters/ragas_evaluation_service.py
  - src/application/use_cases/ab_report.py
  - src/rag/retrieval/nodes/compliance_judgment.py
  - tests/application/test_graph_compliance_acceptance_gates.py
autonomous: false
requirements: [R11-10, R11-9, R11-17, R11-1]
must_haves:
  truths:
    - "Judgment/extraction LLM calls run at temperature 0 for score stability (F5) — no multi-seed on 435"
    - "RAGAs is demoted to secondary with backoff (F4 rate-limit corruption); the A/B is NOT gated on RAGAs"
    - "Verbatim-text-in-prompt assertion passes for all 18 cases (deterministic substring check, D-15.1)"
    - "clause-hit@3 / recall@pool measured on the VALIDATED 18-case gold (D-22) via the reused harness"
    - "graph-compliance beats the hybrid baseline on clause-hit@3 + citation grounding on bdc4927d"
    - "The 3-way A/B is reported WITH the D-16 fairness caveat (deferred Phase-9 baseline) and D-24 baseline-integrity caveat"
  artifacts:
    - path: "src/application/use_cases/clause_hit_harness.py"
      provides: "clause-hit@3 harness parameterized for the graph-compliance provider"
      contains: "FIXED_18_TEST_IDS"
    - path: "src/application/use_cases/ab_report.py"
      provides: "graph-compliance vs hybrid A/B comparison report with caveats"
      contains: "graph-compliance"
  key_links:
    - from: "src/application/use_cases/clause_hit_harness.py"
      to: "graph_retrieval_provider_compliance"
      via: "inject the new provider into the existing harness (do not fork)"
      pattern: "provider"
    - from: "src/application/use_cases/ab_report.py"
      to: "canonical hybrid baseline eval-run-hybrid-tests-18-bdc4927d-20260430-0232"
      via: "compare graph-compliance vs hybrid on clause-hit@3 + grounding"
      pattern: "bdc4927d"
---

<objective>
Make the ruler reliable (F4/F5), pass the measured acceptance gates (D-15), and run the 18-case A/B (graph-compliance vs hybrid) with honest caveats. Temp-0 for score stability, RAGAs demoted to secondary with backoff, verbatim-text-in-prompt asserted across 18, clause-hit@3/recall@pool on the validated gold, and a comparison report.

Purpose: D-15 (acceptance gates measured, not assumed), D-14 (temp-0, F8 already native), D-22 (validated gold from 11-03), D-16/D-01 (A/B fairness caveat). Primary ruler = clause-hit@3 + citation grounding; RAGAs secondary.
Output: Passing acceptance gates + an A/B comparison report demonstrating graph-compliance beats hybrid on clause-hit@3 + grounding.

Terminal plan — carries a `checkpoint:human-verify` before the A/B conclusions are accepted as the phase result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-CONTEXT.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-PATTERNS.md
@src/application/use_cases/clause_hit_harness.py
@src/application/use_cases/ab_report.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eval-ruler reliability — temp 0 determinism + RAGAs demotion/backoff (F4/F5)</name>
  <read_first>
    - src/infrastructure/adapters/ragas_evaluation_service.py (the RAGAs calls that hit rate-limit corruption — F4)
    - src/rag/retrieval/nodes/compliance_judgment.py (11-08 — the judge LLM call; set temperature 0)
    - .planning/phases/10-.../deferred-items.md Findings F4 (RAGAs rate-limit corruption), F5 (score instability)
    - 11-CONTEXT.md D-14 (temp 0 over multi-seed; cache extractions/anchors), D-15 (RAGAs demoted to secondary — add backoff but don't gate on RAGAs)
  </read_first>
  <action>Set temperature 0 on the graph-compliance judgment/extraction LLM calls (F5 stability — cheaper than multi-seed on 435). Add exponential backoff + retry to the RAGAs evaluation calls (F4) and demote RAGAs to a secondary, non-gating metric for this mode (the A/B pass/fail must NOT depend on RAGAs). Ensure per-query extraction/anchor caching is active (D-14). Add a test asserting temp 0 is passed and RAGAs failures degrade to a recorded-but-non-gating result.</action>
  <acceptance_criteria>
    - The graph-compliance judgment/extraction calls pass `temperature=0` (grep/test assertion).
    - RAGAs calls have backoff/retry; a simulated rate-limit does not corrupt the run and does NOT fail the acceptance gate (recorded as secondary).
    - Re-running the same case twice yields identical clause-hit@3 (deterministic ruler — stability check).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/application/test_graph_compliance_acceptance_gates.py -k "temp or ragas or stability" -x</automated>
  </verify>
  <done>Temp-0 judgment, RAGAs demoted+backed-off (non-gating), deterministic clause-hit@3 across reruns.</done>
</task>

<task type="auto">
  <name>Task 2: Acceptance gates (D-15) — verbatim-text-in-prompt (18) + clause-hit@3/recall@pool</name>
  <read_first>
    - src/application/use_cases/clause_hit_harness.py (whole file; FIXED_18_TEST_IDS, CaseClauseHitResult/ClauseHitHarnessResult, disagreement mechanism — parameterize provider injection, do NOT fork)
    - src/rag/ingestion/scripts/validate_gold_clause_sets.py (11-03 — the VALIDATED gold this scores against)
    - 11-CONTEXT.md D-15 (three gates: verbatim-text-in-prompt per case, clause-hit@3/recall@pool, B01-001 first [done in 11-09]), D-22 (validated gold)
  </read_first>
  <action>Extend `clause_hit_harness.py` to accept the `graph_retrieval_provider_compliance` provider (parameterize injection — do not write a parallel harness). Implement the D-15.1 verbatim-text-in-prompt assertion as a deterministic per-case substring check over all 18 cases (the gold clause text is literally present in the assembled prompt). Compute clause-hit@3 / recall@pool over the 18-case VALIDATED gold (from 11-03), reusing the existing scoring + stable tie-break. Fail the gate loudly if any of the 18 cases lacks its gold verbatim text in the prompt. Add `tests/application/test_graph_compliance_acceptance_gates.py` covering the verbatim assertion and the harness parameterization.</action>
  <acceptance_criteria>
    - `poetry run pytest ../tests/application/test_graph_compliance_acceptance_gates.py -x` passes.
    - The verbatim-text-in-prompt gate reports 18/18 cases with gold clause text present in the prompt (or fails loudly naming the missing cases).
    - clause-hit@3 / recall@pool are computed against the 11-03 VALIDATED gold (asserts it does not score against a flagged/hallucinated gold id).
    - The harness runs the graph-compliance provider (not a forked copy) — grep confirms the injected provider parameter.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/application/test_graph_compliance_acceptance_gates.py -x</automated>
  </verify>
  <done>Verbatim-text gate 18/18; clause-hit@3/recall@pool on validated gold via the reused harness.</done>
</task>

<task type="auto">
  <name>Task 3: 18-case A/B run + comparison report (graph-compliance vs hybrid) with caveats</name>
  <read_first>
    - src/application/use_cases/ab_report.py (existing A/B report generator — extend for graph-compliance vs hybrid)
    - src/results/evaluations/2026-04/eval-run-hybrid-tests-18-bdc4927d-20260430-0232*.json (canonical hybrid leg — B04 test_id restored in 11-03)
    - 11-CONTEXT.md D-15 (primary ruler = clause-hit@3 + citation grounding; RAGAs secondary), D-16/D-01 (3-way A/B only fair once Phase-9 basic-graphrag 18-case baseline exists — flag before conclusions), D-24b (baseline integrity)
  </read_first>
  <action>Run the 18-case `bdc4927d` eval under `--mode graph-compliance` and generate an A/B comparison report vs the canonical hybrid baseline on the PRIMARY ruler (clause-hit@3 + citation grounding), with RAGAs shown as secondary/diagnostic. The report MUST include: (a) the D-16 fairness caveat that the 3-way A/B is not final until the deferred Phase-9 basic-graphrag 18-case baseline exists, and (b) the D-24b note confirming the hybrid baseline B04 test_id was restored. Do not claim a fair 3-way conclusion beyond what the guards allow.</action>
  <acceptance_criteria>
    - The 18-case graph-compliance run completes; a comparison report is written showing clause-hit@3 + grounding for graph-compliance vs hybrid on all 18 cases.
    - The report shows graph-compliance >= hybrid on clause-hit@3 (the phase success target) — or, if not, names the failing cases and the recall/payload diagnosis (per D-13 measurement, not assumption).
    - The report contains the explicit D-16 fairness caveat and the D-24b baseline-integrity confirmation.
    - RAGAs appears only as a secondary/diagnostic section, not as a pass/fail gate.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run ccop-eval evaluate run --model primus-reasoning --mode graph-compliance --tests 18 && poetry run python -m application.use_cases.ab_report --modes graph-compliance,hybrid --fixture bdc4927d</automated>
  </verify>
  <done>18-case A/B report produced on the primary ruler with honest D-16/D-24 caveats; RAGAs secondary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The acceptance-gate results (verbatim-text 18/18, clause-hit@3/recall@pool on validated gold) and the 18-case graph-compliance-vs-hybrid A/B comparison report with the D-16/D-24 caveats.</what-built>
  <how-to-verify>
    1. Open the A/B report; confirm clause-hit@3 + citation grounding for graph-compliance vs the hybrid baseline on the 18 cases.
    2. Confirm the verbatim-text-in-prompt gate is 18/18 (or review the named failing cases + the recall/payload diagnosis).
    3. Confirm the D-16 fairness caveat (deferred Phase-9 basic-graphrag baseline) and the D-24b baseline-integrity note are present and understood before any conclusion is drawn.
    4. Decide: does graph-compliance meet the phase success bar (beats hybrid on clause-hit@3 + grounding, verbatim text provably in the prompt)?
  </how-to-verify>
  <resume-signal>Type "approved" to accept the A/B result as the phase outcome, or list the cases/diagnostics needing rework.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RAGAs -> external judge API | Rate-limit corruption can silently break scores (F4) |
| gold + baseline -> A/B verdict | A broken ruler or corrupted baseline yields a meaningless conclusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-26 | Denial of Service | RAGAs rate limits (F4) | mitigate | Backoff + demote to non-gating secondary; A/B decided on clause-hit@3 |
| T-11-27 | Repudiation | A/B fairness | mitigate | Report the D-16 caveat (deferred Phase-9 baseline) + D-24b baseline integrity; human-verify before conclusion |
| T-11-28 | Tampering | scoring against hallucinated gold | mitigate | Score only against the 11-03 validated gold (D-22) |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- Temp-0 deterministic ruler; RAGAs non-gating; verbatim-text 18/18; clause-hit@3 on validated gold; A/B report with caveats; human-verify sign-off.
</verification>

<success_criteria>
- D-15 satisfied: acceptance gates measured and passing (verbatim-text + clause-hit@3), RAGAs secondary.
- D-14 satisfied: temp-0 stability (F8 native from atomic CUs).
- Phase goal met: graph-compliance beats hybrid on clause-hit@3 + citation grounding on bdc4927d with verbatim text provably in the prompt — reported with honest D-16/D-24 caveats.
</success_criteria>

<output>
Create `.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-10-SUMMARY.md` when done.
</output>
