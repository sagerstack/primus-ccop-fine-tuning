---
phase: 11-align-graphrag-to-graphcompliance-architecture-scenario-anch
plan: 07
type: execute
wave: 6
depends_on: ["11-05", "11-06"]
files_modified:
  - src/rag/graph/retrieval/neo4j_compliance_gate_adapter.py
  - src/rag/retrieval/nodes/cu_plan_reranking.py
  - src/infrastructure/config/container.py
  - src/infrastructure/config/settings.py
  - tests/rag/graph/retrieval/test_neo4j_compliance_gate_adapter.py
  - tests/rag/retrieval/nodes/test_cu_plan_reranking.py
autonomous: true
requirements: [R11-6, R11-8, R11-18, R11-13, R11-9]
must_haves:
  truths:
    - "Anchor->CU retrieval scores each anchor vs each CU subject (bi-encoder + hypernym-overlap bonus) -> top-K1, then cross-encoder reranks q(a)=[predicate;actor_type;object_type] vs d(c)=[subject;constraint;condition] -> the CU Plan (eqs. 3-4)"
    - "Retrieval unit is the atomic CU, never a chunk (F8 fan-out eliminated natively)"
    - "Two-channel recall (D-13): anchor->CU-subject match PLUS hybrid dense+BM25 over verbatim clause text; plus a fallback floor to hybrid text retrieval so it is never worse than today"
    - "Lucene special chars in query text are escaped via the reused _escape_lucene_query_text (D-23) — no TokenMgrError regression"
    - "Every returned Document.metadata['provider'] == 'graph-compliance' (distinct routing marker)"
  artifacts:
    - path: "src/rag/graph/retrieval/neo4j_compliance_gate_adapter.py"
      provides: "IGraphRetrievalProvider impl: two-channel anchor->CU retrieval with verbatim clause text payload"
      contains: "class Neo4jComplianceGateAdapter"
    - path: "src/rag/retrieval/nodes/cu_plan_reranking.py"
      provides: "Cross-encoder rerank of CU candidates into the CU Plan (eq. 4)"
      contains: "rerank"
  key_links:
    - from: "src/rag/graph/retrieval/neo4j_compliance_gate_adapter.py"
      to: "Neo4j :ComplianceUnit (subject + verbatim text)"
      via: "HybridCypherRetriever dense + Lucene fulltext over CU subject and clause text"
      pattern: "ComplianceUnit"
    - from: "src/infrastructure/config/container.py"
      to: "graph_retrieval_provider_compliance singleton"
      via: "new _create_compliance_gate_provider staticmethod + providers.Singleton"
      pattern: "graph_retrieval_provider_compliance"
---

<objective>
Compliance Gate — anchor→CU retrieval producing the CU Plan (GraphCompliance §3.3, eqs. 3-4), with the D-13 two-channel recall guarantee. Retrieve over atomic CUs (never chunks — F8 fan-out gone natively), score anchor vs CU `subject` (+ hypernym-overlap bonus) → top-K1, cross-encoder rerank `q(a)` vs `d(c)` → the CU Plan, and always carry the verbatim clause-text payload.

Purpose: D-11 (CU retrieval → CU Plan), D-13 (two-channel recall + payload + fallback floor), D-23 (Lucene escaping reuse), D-18 (retrieval is plain embedding over CU subjects — entity ontology out of the loop), D-14 (F8 fan-out eliminated). Register the new provider as a third additive DI singleton.
Output: `Neo4jComplianceGateAdapter` + CU-Plan reranker + `graph_retrieval_provider_compliance` singleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-CONTEXT.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-PATTERNS.md
@research/graphcompliance/2510.26309v1.pdf
@src/rag/graph/retrieval/neo4j_ontology_graph_retrieval_adapter.py
@src/rag/retrieval/nodes/reranking.py
@src/infrastructure/config/container.py

<interfaces>
<!-- Reuse verbatim from neo4j_ontology_graph_retrieval_adapter.py — do NOT reimplement -->
- IGraphRetrievalProvider.retrieve(query: str, top_k: int) -> list[Document]  (port contract; concrete class MAY add kwargs)
- _escape_lucene_query_text(text: str) -> str  (lines 65-80; MANDATORY reuse for the BM25/fulltext channel — D-23)
- _LUCENE_SPECIAL_CHARS_RE  (line 62)
- HybridCypherRetriever(driver, vector_index_name, fulltext_index_name, retrieval_query, embedder, result_formatter, neo4j_database)  (dense + Lucene fulltext duality = the two D-13 channels)
- Document.metadata["provider"]  (routing marker; ontology adapter uses "graphrag-ontology" at line 171 — new adapter MUST use a distinct "graph-compliance")
- ORDER BY boosted_score DESC, resolved_citation_id ASC  (deterministic tie-break — carry into CU query)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Neo4jComplianceGateAdapter — two-channel anchor->CU retrieval (D-13)</name>
  <read_first>
    - src/rag/graph/retrieval/neo4j_ontology_graph_retrieval_adapter.py (whole class lines 83-227; RETRIEVAL_QUERY, HybridCypherRetriever wiring, `_escape_lucene_query_text` lines 65-80, `_LUCENE_SPECIAL_CHARS_RE` line 62, provider marker line 171, tie-break line 240) — the strongest, most directly reusable analog
    - research/graphcompliance/2510.26309v1.pdf §3.3 eq. 3 (anchor vs CU subject + hypernym-overlap bonus -> top-K1)
    - 11-CONTEXT.md D-11 (retrieval unit = atomic CU), D-13 (two-channel + fallback floor), D-23 (Lucene escaping), D-18 (plain embedding over CU subjects), D-14 (F8 native), D-24a (verify files git-tracked)
  </read_first>
  <behavior>
    - Channel (i): bi-encoder scores each anchor vs each CU `subject` + hypernym-overlap bonus -> top-K1 candidates.
    - Channel (ii): hybrid dense+BM25 over the CU's verbatim clause text (a clause surfaces by its actual wording even if its extracted subject is weak).
    - Fallback floor: if CU-path recall lags, fall back to the existing hybrid text retrieval so the mode is never worse than today's hybrid.
    - Every returned Document carries the verbatim clause text payload + namespaced citation id + `metadata["provider"] == "graph-compliance"`.
    - Lucene special chars escaped via the reused helper (no TokenMgrError on `/` or `'`).
    - Deterministic tie-break preserved.
  </behavior>
  <action>Create `src/rag/graph/retrieval/neo4j_compliance_gate_adapter.py` (`Neo4jComplianceGateAdapter(IGraphRetrievalProvider)`) on the ontology-adapter template. Extend `RETRIEVAL_QUERY` to `MATCH`/`OPTIONAL MATCH` onto `:ComplianceUnit` nodes (subject + verbatim text) the way the analog expands `:Chunk -[:LINKED_TO]-> :Clause`. Keep the port's abstract `retrieve(query, top_k)` untouched; add `anchors`/`hypernyms` kwargs on the concrete class. Import/reuse `_escape_lucene_query_text` + `_LUCENE_SPECIAL_CHARS_RE` verbatim for the channel-(ii) fulltext query (D-23). Set `metadata["provider"] = "graph-compliance"`. Implement the fallback floor to hybrid text retrieval. Preserve `ORDER BY ... resolved_citation_id ASC` tie-break. Add `tests/rag/graph/retrieval/test_neo4j_compliance_gate_adapter.py` asserting: provider marker, verbatim-text payload present on results, Lucene-escaping applied to a `/`-containing query, deterministic ordering.</action>
  <acceptance_criteria>
    - `poetry run pytest ../tests/rag/graph/retrieval/test_neo4j_compliance_gate_adapter.py -x` passes.
    - A query containing `/` and `'` does not raise `TokenMgrError` (asserts D-23 escaping reuse).
    - Every returned Document has `metadata["provider"] == "graph-compliance"` and non-empty verbatim clause text.
    - `grep -n "_escape_lucene_query_text" src/rag/graph/retrieval/neo4j_compliance_gate_adapter.py` confirms reuse (imported, not reimplemented).
    - `git check-ignore` returns nothing for the new adapter file (D-24a — tracked).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/graph/retrieval/test_neo4j_compliance_gate_adapter.py -x</automated>
  </verify>
  <done>Two-channel CU retrieval with verbatim payload, Lucene-safe, distinct provider marker, fallback floor, deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CU Plan cross-encoder reranking (eq. 4) + DI provider singleton</name>
  <read_first>
    - src/rag/retrieval/nodes/reranking.py (whole file; `model.predict(pairs)` over (query, doc) tuples lines 85-91, RRF combine 105-117, top-N funnel 264-266 — reuse the cross-encoder + combine)
    - src/infrastructure/config/container.py lines 282-361 (`_create_ontology_graph_retrieval_provider` + `graph_retrieval_provider_ontology` sibling-singleton pattern — add a THIRD sibling, never modify the existing two)
    - src/infrastructure/config/settings.py (settings key convention, e.g. `graphrag_ontology_enabled` -> add `graph_compliance_enabled`)
    - research/graphcompliance/2510.26309v1.pdf §3.3 eq. 4 (cross-encoder reranks q(a)=[predicate;actor_type;object_type] vs d(c)=[subject;constraint;condition])
    - 11-CONTEXT.md D-11, D-16 additivity (new singleton, not a modification)
  </read_first>
  <action>Create `src/rag/retrieval/nodes/cu_plan_reranking.py` reranking the top-K1 CU candidates into the CU Plan: build pair strings `q(a)=[predicate; actor_type; object_type]` vs `d(c)=[subject; constraint; condition]`, call the SAME cross-encoder as `reranking.py` (`model.predict`), reuse the RRF combine + top-N funnel. Add a `graph_compliance_enabled` settings key. Add a THIRD sibling DI singleton in `container.py`: a `_create_compliance_gate_provider` staticmethod (copy the ontology sibling verbatim, gated on `neo4j_uri` + `graph_compliance_enabled`, returns `Neo4jComplianceGateAdapter`) + a `graph_retrieval_provider_compliance = providers.Singleton(...)` line — never modify the existing two providers. Add `tests/rag/retrieval/nodes/test_cu_plan_reranking.py` asserting pair-string construction and top-N funnel.</action>
  <acceptance_criteria>
    - `poetry run pytest ../tests/rag/retrieval/nodes/test_cu_plan_reranking.py -x` passes, asserting q(a)/d(c) pair strings are built from the CU 4-tuple fields and the funnel keeps top-N.
    - `grep -n "graph_retrieval_provider_compliance\|_create_compliance_gate_provider" src/infrastructure/config/container.py` shows the new sibling singleton added (and the existing two providers unchanged).
    - The CU Plan output preserves deterministic ordering (stable tie-break) for clause-hit@3 reproducibility.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/retrieval/nodes/test_cu_plan_reranking.py -x</automated>
  </verify>
  <done>CU Plan reranker built on the shared cross-encoder; third DI provider singleton registered additively.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query text -> Neo4j fulltext (Lucene) | Raw query chars can throw parser errors / injection into fulltext |
| adapter -> Neo4j | Cypher retrieval over CU nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-17 | Tampering | Lucene fulltext channel | mitigate | Reuse `_escape_lucene_query_text` verbatim (D-23); no raw query interpolation |
| T-11-18 | Tampering | CU retrieval Cypher | mitigate | Static parameterized RETRIEVAL_QUERY via HybridCypherRetriever query_params |
| T-11-19 | Spoofing | provider routing marker | mitigate | Distinct `metadata["provider"]="graph-compliance"` proves which path ran |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (reuses bge-large + bge-reranker + neo4j-graphrag) |
</threat_model>

<verification>
- Adapter returns verbatim-payload CU results, Lucene-safe, distinct provider marker, fallback floor; CU Plan reranker uses the shared cross-encoder; third DI singleton added additively.
</verification>

<success_criteria>
- D-11 satisfied: anchor->CU retrieval -> cross-encoder rerank -> CU Plan over atomic CUs.
- D-13 satisfied: two-channel recall + verbatim payload + fallback floor.
- D-23 satisfied: Lucene escaping reused. D-18/D-14/D-24a satisfied.
</success_criteria>

<output>
Create `.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-07-SUMMARY.md` when done.
</output>
