---
phase: 11-align-graphrag-to-graphcompliance-architecture-scenario-anch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rag/ingestion/chunkers/clause_aware_chunker.py
  - src/rag/ingestion/run_ingestion.py
  - src/rag/graph/build/ontology_kg_builder.py
  - src/rag/ingestion/scripts/verify_clause_completeness.py
  - tests/rag/ingestion/test_clause_aware_chunker.py
  - tests/rag/ingestion/test_clause_completeness_gate.py
autonomous: false
requirements: [R11-14, R11-15, R11-20]
must_haves:
  truths:
    - "Corpus completeness is guarded (D-19): every clause id in the inventory resolves to retrievable verbatim text; the gate fails loudly at build, not silently at eval — no CU may exist without its provision text"
    - "Provenance integrity is asserted (D-20): the 7 source docs have 7 distinct identities and zero document.txt — no fabricated cross-doc citations"
    - "Wave 0 runs first (D-25): re-ingest from the 7 source PDFs + verify complete clause coverage before ANY Policy Graph work — the hard precondition for step-0 clause-text alignment"
    - "A human approval gate ends Wave 0 (D-26): the re-ingested corpus + fail-loud completeness gate (883/883) are reviewed before any Policy Graph work begins"
    - "Every clause id in clause_inventory.json (883 entries / 7 docs; 415 CCoP) resolves to retrievable verbatim body text"
    - "CCoP 2.0 sections 5.3 and 5.4 (and their lettered sub-items) return non-empty verbatim text after re-ingestion"
    - "The 7 source docs have 7 distinct provenance identities; zero nodes carry path/source 'document.txt'"
    - "The completeness gate exits non-zero (fails loudly) when any inventory clause id has no verbatim body"
  artifacts:
    - path: "src/rag/ingestion/scripts/verify_clause_completeness.py"
      provides: "Fail-loud completeness gate: inventory-clause -> verbatim-text resolution + TOC/section count parity"
      contains: "CompletenessReport"
    - path: "src/rag/ingestion/chunkers/clause_aware_chunker.py"
      provides: "Clause-boundary chunker that splits the 5.2->5.3 boundary and emits lettered sub-items as discrete chunks"
      contains: "CLAUSE_PATTERN"
  key_links:
    - from: "src/rag/ingestion/scripts/verify_clause_completeness.py"
      to: "src/rag/ingestion/fixtures/clause_inventory.json"
      via: "load inventory entries, resolve each clause_id to verbatim text"
      pattern: "clause_inventory"
    - from: "src/rag/ingestion/run_ingestion.py"
      to: "per-doc provenance"
      via: "real file_path/source_doc passed per document (no shared default)"
      pattern: "file_path"
---

<objective>
Wave 0 (BLOCKING precondition, D-25). Redo ingestion from the 7 source PDFs in `ccop-official/`, fix the clause-aware chunker that dropped CCoP sections 5.3/5.4 (bug 2026-04-21), and add a FAIL-LOUD completeness gate proving every clause id in `clause_inventory.json` resolves to retrievable verbatim text with correct per-doc provenance.

Purpose: This is the concrete realization of D-19 (corpus completeness) and D-20 (provenance integrity) and the hard precondition for step-0 clause-text alignment — a Compliance Unit cannot carry text the corpus does not contain. "No retrieval strategy can fix a data problem."
Output: A clause-text-complete corpus (Qdrant index + base Neo4j chunk graph with correct provenance) and a committed completeness-gate script/report that no downstream wave may bypass.

**NO downstream wave (11-02 onward) may proceed until this plan's completeness gate passes.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-CONTEXT.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-PATTERNS.md
@docs/project_notes/bugs.md
@src/rag/ingestion/chunkers/clause_aware_chunker.py
@src/rag/ingestion/fixtures/clause_inventory.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the clause-aware chunker 5.2->5.3 boundary + lettered sub-item emission</name>
  <read_first>
    - src/rag/ingestion/chunkers/clause_aware_chunker.py (the file being fixed; CLAUSE_PATTERN lines 42-45, emission loop 109-135, docstring 22-33 documenting the list-item absorption failure class)
    - src/rag/graph/ontology/shacl_validator.py (validate-report-never-silently-mutate shape reused by Task 3)
    - 11-CONTEXT.md D-19 and D-25.1 (chunker fix scope)
    - docs/project_notes/bugs.md entry 2026-04-21 (5.3/5.4 boundary failure) and 2026-03-21 (citation-ID mismatch)
  </read_first>
  <behavior>
    - Given the Docling markdown for CCoP 2.0, the chunker emits a discrete chunk for 5.3 and 5.4 (previously absorbed into the 5.2 tail).
    - Lettered sub-items (e.g. 5.3.1(a), 5.3.1(b), 5.3.1(c)) each emit as their own chunk carrying their own clause_id, not glued to the parent's tail.
    - The previously-documented cases (6.1.1, 8.2.5) still chunk correctly (no regression).
    - No clause-body merging is (re)introduced as a fix — the no-merge emission discipline holds.
  </behavior>
  <action>Extend `CLAUSE_PATTERN` (or its list-item branch) in `clause_aware_chunker.py` so the 5.2->5.3 section boundary is recognized and lettered sub-item boundaries `(a)(b)(c)` are emitted as discrete chunks. Preserve the no-merge emission loop discipline (each match emits its own chunk; do NOT reintroduce clause-body merging). Add a parametrized regression test in `tests/rag/ingestion/test_clause_aware_chunker.py` with one case per previously-glued clause id (5.3, 5.4, 5.3.1, 5.4.1, and at least one lettered sub-item) asserting each surfaces as its own chunk, plus a guard case for the documented 6.1.1 / 8.2.5 clauses.</action>
  <acceptance_criteria>
    - `poetry run pytest ../tests/rag/ingestion/test_clause_aware_chunker.py -x` passes; the test set includes an explicit assertion that clause ids `5.3`, `5.4`, `5.3.1`, `5.4.1` and at least one lettered sub-item each map to a distinct chunk.
    - `grep -n "merge" src/rag/ingestion/chunkers/clause_aware_chunker.py` shows no reintroduced clause-body merge branch (no `<30-word` merge rule).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/ingestion/test_clause_aware_chunker.py -x</automated>
  </verify>
  <done>Sections 5.3/5.4 and lettered sub-items chunk discretely; regression tests green; no merge branch reintroduced.</done>
</task>

<task type="auto">
  <name>Task 2: Re-ingest all 7 source PDFs with correct per-doc provenance + rebuild base chunk graph</name>
  <read_first>
    - src/rag/ingestion/run_ingestion.py (ingestion orchestrator; EXPECTED_CCOP_2_SECTIONS constant + TOC gate from Phase 3.2)
    - src/rag/graph/build/ontology_kg_builder.py lines 362-374 (the `file_path=doc_name` provenance fix — bugs.md 2026-07-02; copy this call shape verbatim)
    - 11-CONTEXT.md D-20 and D-25.2 (per-doc provenance, no document.txt)
    - 11-PATTERNS.md "Provenance-integrity analog (D-20)" section
  </read_first>
  <action>Drop and re-ingest all 7 `ccop-official/` source PDFs through the fixed chunker into the Qdrant `ccop_clauses_hybrid` collection (D-13 channel-ii index) and rebuild the base Neo4j chunk graph. Pass a real per-doc provenance field (`file_path`/`source_doc`) for every document exactly as `ontology_kg_builder.py:362-374` does — never a shared default that collapses to `document.txt`. Reuse the Phase 3.2 TOC sanity gate in `run_ingestion.py` so the ingestion fails at ingest time if the indexed section count diverges from `EXPECTED_CCOP_2_SECTIONS`. Rebuild the graph via `ccop-eval graph build --drop`.</action>
  <acceptance_criteria>
    - After re-ingestion, a Qdrant scroll filtered on `document_source="CCoP 2.0"` returns discrete points for sections 5.1 through 5.12.
    - A Neo4j query over document/chunk provenance returns exactly 7 distinct source-doc identities and ZERO nodes whose provenance equals `document.txt` (assert both).
    - The re-ingestion run completes with the TOC sanity gate passing (indexed section count == EXPECTED_CCOP_2_SECTIONS).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run ccop-eval graph build --drop && poetry run python -m rag.ingestion.scripts.verify_clause_completeness --provenance-only</automated>
  </verify>
  <done>All 7 PDFs re-ingested; 5.1-5.12 present as discrete Qdrant points; 7 distinct provenance identities, zero document.txt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail-loud clause-completeness gate (D-19) — BLOCKING artifact</name>
  <read_first>
    - src/rag/graph/ontology/shacl_validator.py (ValidationReport/Violation dataclasses, validate-and-report-then-exit-nonzero shape — the pattern to copy for CompletenessReport)
    - src/rag/graph/inspect/metrics.py `KGInspector._clause_id_appears` (boundary-aware clause-id matcher — reuse, do NOT hand-roll `in`)
    - src/rag/ingestion/fixtures/clause_inventory.json (883 entries / 7 docs; `entries` = list of {clause_id, source_doc})
    - 11-CONTEXT.md D-19 and D-25.3 (completeness gate scope)
  </read_first>
  <behavior>
    - For every one of the 883 inventory clause ids, the gate resolves a non-empty verbatim body from the re-ingested corpus, using the boundary-aware matcher (so `5.3` never matches inside `5.3.10`).
    - The gate asserts TOC/inventory section count == extracted-with-text count.
    - The gate exits non-zero and names every unresolved clause id when any clause has no verbatim body (fail loudly, never log-and-continue).
    - Spot-check cases 5.3 and 5.4 return content.
  </behavior>
  <action>Create `src/rag/ingestion/scripts/verify_clause_completeness.py` producing a `CompletenessReport` dataclass (copy the shape of `shacl_validator.py`'s ValidationReport). It loads `clause_inventory.json` entries, resolves each clause_id to verbatim body text from the re-ingested corpus using the reused `KGInspector._clause_id_appears` boundary-aware matcher, asserts section-count parity, writes the report to a committed JSON artifact, and raises/`sys.exit(non-zero)` on any missing clause. Add a `--provenance-only` flag (used by Task 2) that checks the 7-distinct-identity / zero-document.txt invariant. Add `tests/rag/ingestion/test_clause_completeness_gate.py` asserting the gate fails non-zero on a synthetic missing-clause corpus and passes on the real re-ingested corpus (spot-checking 5.3/5.4).</action>
  <acceptance_criteria>
    - `poetry run python -m rag.ingestion.scripts.verify_clause_completeness` exits 0 on the re-ingested corpus and writes a CompletenessReport JSON showing 883/883 clause ids resolved.
    - The report JSON records section-count parity (inventory section count == extracted-with-text count).
    - `poetry run pytest ../tests/rag/ingestion/test_clause_completeness_gate.py -x` passes, including a case proving the gate exits non-zero when a clause body is absent.
    - Spot-check: report lists 5.3 and 5.4 as resolved with non-empty text.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run python -m rag.ingestion.scripts.verify_clause_completeness && poetry run pytest ../tests/rag/ingestion/test_clause_completeness_gate.py -x</automated>
  </verify>
  <done>Gate exits 0 with 883/883 resolved on the real corpus and exits non-zero on any missing clause; committed report artifact present.</done>
</task>


<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Wave 0 outputs: fixed clause-aware chunker, re-ingested 7-PDF corpus with correct per-doc provenance, and the fail-loud clause-completeness gate reporting 883/883 clause ids resolved to verbatim text.</what-built>
  <how-to-verify>1. Confirm the completeness gate exits 0 with 883/883 resolved and 5.3/5.4 return content. 2. Confirm 7 distinct provenance identities and zero document.txt. This is the BLOCKING precondition — no downstream wave proceeds until approved (D-25/D-26).</how-to-verify>
  <resume-signal>Type "approved" to release Wave 2 (11-02 + 11-03), or list what needs rework.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF corpus -> ingestion | Trusted-but-unstructured source docs parsed into chunks; malformed content can silently drop clauses |
| ingestion -> Neo4j/Qdrant | Cypher/index writes derived from parsed text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | clause_aware_chunker boundary regex | mitigate | Regression test per clause id + fail-loud completeness gate catches silent clause drop |
| T-11-02 | Repudiation | per-doc provenance | mitigate | Assert 7 distinct identities, zero document.txt at build (D-20) — prevents fabricated cross-doc citations |
| T-11-03 | Information Disclosure | corpus is public regulation | accept | No PII/secrets in CCoP corpus; local Qdrant/Neo4j |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; if any arise, blocking human checkpoint required |
</threat_model>

<verification>
- Chunker regression tests green (5.3/5.4/lettered sub-items discrete).
- Re-ingestion produces 5.1-5.12 discrete Qdrant points, 7 distinct provenance identities, zero document.txt.
- Completeness gate exits 0 with 883/883 resolved; exits non-zero on any missing clause.
</verification>

<success_criteria>
- D-19 satisfied: every inventory clause resolves to verbatim text; gate fails loudly on gaps.
- D-20 satisfied: correct per-doc provenance, zero document.txt.
- D-25 Wave 0 complete: clause-text-complete corpus feeds both retrieval index and CU/clause nodes.
</success_criteria>

<output>
Create `.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-01-SUMMARY.md` when done.
</output>
