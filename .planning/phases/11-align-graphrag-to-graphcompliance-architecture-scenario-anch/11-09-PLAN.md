---
phase: 11-align-graphrag-to-graphcompliance-architecture-scenario-anch
plan: 09
type: execute
wave: 8
depends_on: ["11-08"]
files_modified:
  - src/domain/value_objects/run_id.py
  - src/presentation/cli/commands/evaluate.py
  - src/application/use_cases/evaluate_model.py
  - src/rag/presentation/cli/query.py
  - src/infrastructure/config/container.py
  - src/rag/retrieval/edges/routing.py
  - src/rag/retrieval/graph.py
  - src/rag/graph/retrieval/graph_retrieval_node.py
  - src/rag/application/ports/i_rag_pipeline.py
  - src/rag/infrastructure/adapters/langgraph_rag_adapter.py
  - src/application/dtos/evaluation_result_dto.py
  - tests/integration/test_graph_compliance_mode_wiring.py
autonomous: true
requirements: [R11-11, R11-12, R11-16, R11-10]
must_haves:
  truths:
    - "`--mode graph-compliance` is wired into ALL 8 allowlist/wiring locations (the RunId._VALID_MODES class of bug is defeated)"
    - "`evaluate run --mode graph-compliance` on one case runs the full pipeline end-to-end (E2E slice, D-15.3)"
    - "The full prompt + retrieved CUs + verbatim clause text + tokens + latency propagate GraphState -> RagResponse -> EvaluationResult/CLI (D-21)"
    - "Under --verbose-io, the per-case trace shows Context Graph -> anchors(+hypernym) -> matched CUs by type -> embedded verbatim clauses (D-17)"
  artifacts:
    - path: "tests/integration/test_graph_compliance_mode_wiring.py"
      provides: "Asserts all 8 allowlist locations accept graph-compliance + the E2E slice runs"
      contains: "graph-compliance"
  key_links:
    - from: "src/rag/retrieval/graph.py"
      to: "context_graph_extraction -> anchor_hypernym_mapping -> compliance retrieval -> cu_plan_reranking -> compliance_judgment -> reference_closure"
      via: "route_by_mode conditional edges for graph-compliance"
      pattern: "graph-compliance|graph_compliance"
    - from: "src/rag/infrastructure/adapters/langgraph_rag_adapter.py"
      to: "src/application/dtos/evaluation_result_dto.py"
      via: "map trace fields + tokens/latency from final_state into RagResponse -> DTO"
      pattern: "verbatim_clause_texts|cu_plan"
---

<objective>
Wire the new `--mode graph-compliance` into ALL 8 allowlist/graph locations (the multi-allowlist lesson — the exact regression class the D-16/D-19/D-20 memory warns about), propagate the D-21 trace data end-to-end (GraphState → RagResponse → EvaluationResult → CLI), emit the D-17 verbose-io reasoning trace, and prove the whole thing with the B01-001 E2E slice FIRST (D-15.3).

Purpose: D-16 (new mode, 8 allowlists), D-21 (verbose-io data-pipeline wiring), D-17 (reasoning trace), D-15.3 (E2E slice first). This is the highest-risk cross-cutting change.
Output: A runnable `--mode graph-compliance` with a full reasoning trace, proven on one real case before scaling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-CONTEXT.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-PATTERNS.md
@src/rag/retrieval/graph.py
@src/rag/retrieval/edges/routing.py
@src/rag/graph/retrieval/graph_retrieval_node.py
@ground-truth/test-suite/b01_ccop_applicability_scope.jsonl

<interfaces>
<!-- The 8 wiring locations (current state verified 2026-07-04). Add graph-compliance ADDITIVELY to each -->
1. src/domain/value_objects/run_id.py:24  _VALID_MODES set (has graphrag-ontology at line 30)
2. src/presentation/cli/commands/evaluate.py:30  VALID_EVAL_MODES list + help string lines 58-63
3. src/application/use_cases/evaluate_model.py:40  _RETRIEVAL_EVAL_MODES set (gates RAG-only quality groups at 632, 710)
4. src/rag/presentation/cli/query.py:38-41,117-119,144  choice list + help dispatch + elif branch
5. src/infrastructure/config/container.py  graph_retrieval_provider_compliance (added in 11-07)
6. src/rag/retrieval/edges/routing.py:14-62  route_by_mode — new branch
7. src/rag/retrieval/graph.py:76-141  build_rag_graph — add nodes + conditional-edge map entry
8. src/rag/graph/retrieval/graph_retrieval_node.py:36-137  graph_retrieve_documents — is_compliance_mode branch
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 8-allowlist mode wiring + StateGraph node wiring (single cross-cutting task)</name>
  <read_first>
    - 11-PATTERNS.md "New `--mode graph-compliance` — the multi-allowlist wiring (D-16)" section (the exact 8 locations with line numbers + the sibling-branch conventions)
    - src/domain/value_objects/run_id.py:24-31; src/presentation/cli/commands/evaluate.py:30,58-63; src/application/use_cases/evaluate_model.py:32-40; src/rag/presentation/cli/query.py:38-41,117-119,144; src/rag/retrieval/edges/routing.py:14-62; src/rag/retrieval/graph.py:76-141; src/rag/graph/retrieval/graph_retrieval_node.py:36-137,67-97
    - 11-CONTEXT.md D-16 (name must NOT be graphrag-*; wire ALL allowlists)
  </read_first>
  <action>Add `"graph-compliance"` additively to all 8 locations (never modify an existing mode's branch): (1) `run_id._VALID_MODES` with a `# Phase 11 (D-16)` comment; (2) `evaluate.VALID_EVAL_MODES` + help string; (3) `evaluate_model._RETRIEVAL_EVAL_MODES`; (4) `query.py` choice list + help dispatch dict + elif branch; (5) confirm `container.graph_retrieval_provider_compliance` exists (11-07); (6) `routing.route_by_mode` new branch (route key targets the first prerequisite node — Context Graph extraction — following the `graphrag-ontology` "route to a prerequisite node" comment style); (7) `graph.build_rag_graph` — add the new nodes (context_graph_extraction → anchor_hypernym_mapping → compliance retrieval → cu_plan_reranking → compliance_judgment → reference_closure) and the conditional-edge map entry; (8) `graph_retrieval_node.graph_retrieve_documents` — add an `is_compliance_mode` branch selecting `container.graph_retrieval_provider_compliance()`. Add `tests/integration/test_graph_compliance_mode_wiring.py` that grep/asserts the mode is accepted at every one of the 8 locations (a parametrized test enumerating each allowlist).</action>
  <acceptance_criteria>
    - `poetry run pytest ../tests/integration/test_graph_compliance_mode_wiring.py -x` passes, with an explicit assertion PER each of the 8 locations that `graph-compliance` is present/accepted.
    - `RunId(mode="graph-compliance", ...)` constructs without raising; `evaluate run --mode graph-compliance --help` and `query ask --mode graph-compliance --help` do not error on the mode value.
    - `grep -rn "graph-compliance" src/domain/value_objects/run_id.py src/presentation/cli/commands/evaluate.py src/application/use_cases/evaluate_model.py src/rag/presentation/cli/query.py src/rag/retrieval/edges/routing.py src/rag/retrieval/graph.py src/rag/graph/retrieval/graph_retrieval_node.py` returns a hit in every listed file (7 code files; the 8th is container.py from 11-07).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/integration/test_graph_compliance_mode_wiring.py -x</automated>
  </verify>
  <done>graph-compliance wired additively in all 8 locations; StateGraph runs the new node chain; wiring test green.</done>
</task>

<task type="auto">
  <name>Task 2: D-21 trace propagation (GraphState -> RagResponse -> EvaluationResult -> CLI) + D-17 verbose-io trace</name>
  <read_first>
    - src/rag/retrieval/state/graph_state.py (the reserved trace fields from 11-06: context_graph_triples, anchors, hypernym_mappings, cu_plan, verbatim_clause_texts)
    - src/rag/infrastructure/adapters/langgraph_rag_adapter.py lines 69-94 (final_state -> RagResponse mapping — extend for the new trace fields + tokens/latency)
    - src/rag/application/ports/i_rag_pipeline.py lines 14-66 (RagResponse DTO — extend)
    - src/application/dtos/evaluation_result_dto.py lines 30-70 (EvaluationResultDTO — extend); src/application/use_cases/evaluate_model.py lines 1040-1061 (tokens/prompt propagation — verify still correct for the new mode)
    - the --verbose-io capture/printer path (extend for the D-17 trace order)
    - 11-CONTEXT.md D-21 (verify whether the hardcoded-zeros bug reproduces on current code; either way the judgment node follows generate_response), D-17 (trace order: Context Graph -> anchors -> matched CUs by type -> embedded clauses)
  </read_first>
  <action>Extend `RagResponse` (`i_rag_pipeline.py`) and `EvaluationResultDTO` (`evaluation_result_dto.py`) with the trace fields (context_graph_triples, anchors+hypernym_mappings, cu_plan by type, verbatim_clause_texts) and confirm tokens/latency flow (drop any hardcoded `tokens_used=0, latency_ms=0` for the new mode). Map them in `langgraph_rag_adapter.py` from `final_state`. Extend the `--verbose-io` printer to emit the D-17 trace in order: (1) Context Graph triples, (2) anchors each with hypernym label+STRONG/WEAK+premise+score, (3) matched CUs by type (premise/meta-CU/actor-CU) with retrieval scores, (4) the embedded verbatim clause text with namespaced ids. First verify against the CURRENT codebase whether the D-21 hardcoded-zero bug still reproduces; record the finding in the summary.</action>
  <acceptance_criteria>
    - A `graph-compliance` run persists non-zero `tokens` and `latency_ms` per case in the result JSON (D-21 fixed for the new mode).
    - `--verbose-io` output for a graph-compliance case prints the 4-part trace in the D-17 order (Context Graph → anchors → matched CUs by type → embedded clauses with namespaced ids) — asserted by a rendering test.
    - The RagResponse/EvaluationResultDTO carry the trace fields end-to-end (a test constructs the chain and asserts the fields survive to the DTO).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/integration/test_graph_compliance_mode_wiring.py -k "trace or verbose or tokens" -x</automated>
  </verify>
  <done>Trace fields + tokens/latency propagate to the DTO/CLI; --verbose-io emits the D-17 trace in order.</done>
</task>

<task type="auto">
  <name>Task 3: B01-001 E2E slice FIRST (D-15.3) — dump the real prompt, confirm verbatim text present</name>
  <read_first>
    - ground-truth/test-suite/b01_ccop_applicability_scope.jsonl (B01-001: scope question, GT not-applicable, cites §1.2.1/§1.4.1/Act §7; §5.6 is the distractor)
    - 11-CONTEXT.md D-15.3 (E2E slice first, dump actual prompt, confirm §1.2.1/§1.4.1 text present) + the specifics section (B01-001 worked example / honesty note: previously hand-simulated, make it real)
    - the e2e-testing rule (smallest real slice through all layers, no mocks at the boundary under test)
  </read_first>
  <action>Run the REAL pipeline end-to-end: `poetry run ccop-eval evaluate run --model primus-reasoning --mode graph-compliance --test-ids B01-001 --verbose-io`. Capture the actual prompt sent to the LLM and the full reasoning trace. Confirm the verbatim §1.2.1 and §1.4.1 clause bodies are literally present in the prompt (deterministic substring check), the Context Graph/anchors/CU-Plan render, and §5.6 does not dominate. This makes the hand-simulated B01-001 walkthrough real (honesty note). Do NOT scale to 18/435 in this plan — that is 11-10.</action>
  <acceptance_criteria>
    - The `evaluate run --mode graph-compliance --test-ids B01-001 --verbose-io` command completes without error and writes a result JSON with `metadata.run_id` prefixed `eval-run-graph-compliance-...`.
    - The dumped prompt for B01-001 contains the verbatim text of §1.2.1 AND §1.4.1 (substring assertion) — the concrete D-15.1 / D-13-payload proof on a real run.
    - The `--verbose-io` trace shows the Context Graph, the hospital-admin-system anchor with a STRONG CII-definition hypernym mapping, meta-CUs §1.2.1/§1.4.1/Act §7 in the CU Plan, and the embedded verbatim clauses.
    - The run's `provider` marker on retrieved docs is `graph-compliance` (proves the new path actually ran, not a hybrid fallback masquerade).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run ccop-eval evaluate run --model primus-reasoning --mode graph-compliance --test-ids B01-001 --verbose-io</automated>
  </verify>
  <done>B01-001 runs for real; verbatim §1.2.1/§1.4.1 provably in the prompt; full trace rendered; provider marker confirms the path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI mode arg -> pipeline | Mode string routes to graph vs hybrid; a missed allowlist silently N/As metrics or falls back |
| verbose-io trace output | Renders prompts/CU text; no secrets in trace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-23 | Tampering | multi-allowlist mode wiring | mitigate | Parametrized test asserts all 8 locations accept the mode (the exact RunId._VALID_MODES regression class) |
| T-11-24 | Repudiation | provider routing marker | mitigate | E2E slice asserts `provider=="graph-compliance"` — no silent hybrid-fallback masquerade |
| T-11-25 | Information Disclosure | verbose-io trace | accept | Corpus is public regulation; no API keys/PII in trace output |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- All 8 allowlists accept graph-compliance (parametrized test); trace + tokens/latency propagate; B01-001 E2E slice proves verbatim §1.2.1/§1.4.1 in the prompt with the full trace and correct provider marker.
</verification>

<success_criteria>
- D-16 satisfied: new mode wired in all 8 locations additively.
- D-21 satisfied: trace data + tokens/latency propagate to DTO/CLI.
- D-17 satisfied: verbose-io reasoning trace in order.
- D-15.3 satisfied: B01-001 E2E slice run first, verbatim text proven in the prompt.
</success_criteria>

<output>
Create `.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-09-SUMMARY.md` when done.
</output>
