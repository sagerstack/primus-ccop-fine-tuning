---
phase: 11-align-graphrag-to-graphcompliance-architecture-scenario-anch
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/rag/graph/ontology/clause_text_aligner.py
  - src/rag/graph/ontology/clause_source_annotator.py
  - tests/rag/graph/ontology/test_clause_text_aligner.py
  - tests/rag/graph/ontology/test_clause_source_annotator.py
autonomous: false
requirements: [R11-3, R11-8]
must_haves:
  truths:
    - "Corpus/source facts size the clause backbone (D-06): clause_inventory.json = 883 entries / 7 docs, CCoP 2.0 = 415 (11 chapter + 51 section headers + 353 leaves) — these are segmentation/text-carrier facts, NOT a CU-count target"
    - "The clause is only the CU's source semantic unit + verbatim-text carrier + citation anchor (D-07); this plan mints NO :ComplianceUnit nodes — CUs (obligations) are minted in 11-04 and their count is emergent (no operative-leaf reconciliation here)"
    - "Clause/source citation ids are namespaced by source doc (D-08): CCoP-5.7.2(b) vs Act-7, so no id collides across the 7 docs; each source node is tagged doc_class (binding/guidance)"
    - "Every clause/source node carries its verbatim provision text (step-0 alignment; fixes Finding 1)"
    - "The verbatim provision text is hard-linked at the source layer so downstream extraction loss cannot remove the citation payload"
    - "A human approval gate ends this wave (D-26): the source layer is reviewed before Wave 3 mints CUs on top of it"
    - "The clause backbone is a precondition this plan OWNS, not an assumption (D-25 follow-through): Wave 0's `graph build --drop` wiped Phase 10's :Clause nodes, so this plan (re-)establishes the 883-node skeleton + HAS_CHILD hierarchy + function_type via `ccop-eval graph seed-clauses` (deterministic, idempotent) BEFORE alignment — the phase must be reproducible from a dropped graph, never dependent on a manual out-of-band seed"
  artifacts:
    - path: "src/rag/graph/ontology/clause_text_aligner.py"
      provides: "Step-0 alignment: attach verbatim clause body text to each source clause node from the re-ingested corpus"
      contains: "class ClauseTextAligner"
    - path: "src/rag/graph/ontology/clause_source_annotator.py"
      provides: "Source layer: annotate clause nodes with source-doc-namespaced citation ids + doc_class (binding/guidance); NO :ComplianceUnit minting"
      contains: "SourceAnnotationStats"
  key_links:
    - from: "src/rag/graph/ontology/clause_source_annotator.py"
      to: "Neo4j source clause nodes"
      via: "MERGE/SET on (clause_id, source_doc) composite key: namespaced citation_id + doc_class"
      pattern: "source_doc"
    - from: "src/rag/graph/ontology/clause_text_aligner.py"
      to: "re-ingested corpus verbatim text"
      via: "boundary-aware clause_id -> text resolution, write text property"
      pattern: "text"
---

<objective>
Build the SOURCE LAYER only. Attach the verbatim provision text to every source clause node (step-0 alignment, fixes Finding 1) and annotate each with a source-doc-namespaced citation id (D-08) + a doc_class (binding/guidance). This layer is the CU's source semantic unit + verbatim-text carrier + citation anchor — it does NOT mint Compliance Units.

Purpose: D-06/D-07/D-08 (corrected). A Compliance Unit is GraphCompliance's atomic *obligation* (4-tuple, typed) — an OUTPUT of the classification+formalization pass in 11-04, not 1:1 with clause ids and not reconciled against the ~220 operative-leaf arithmetic. This plan prepares the source facts; 11-04 mints the CUs from them. The verbatim text hard-linked here is the D-13 citation payload that extraction loss cannot remove.
Output: source clause nodes carrying verbatim text + namespaced citation ids + doc_class, ready for classification/minting (11-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-CONTEXT.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-PATTERNS.md
@src/rag/graph/ontology/clause_seeder.py
@src/rag/ingestion/fixtures/clause_inventory.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Step-0 clause-text alignment — attach verbatim provision text to source clause nodes</name>
  <read_first>
    - src/rag/graph/ontology/clause_seeder.py (`_derive_parent`/`_derive_chapter` dot/paren hierarchy parsing; MERGE-seed + `_accumulate_stats` re-query pattern)
    - src/rag/graph/inspect/metrics.py `KGInspector._clause_id_appears` (boundary-aware matcher — reuse for clause_id -> text resolution)
    - src/rag/ingestion/scripts/verify_clause_completeness.py (11-01 gate — the same resolution logic; alignment MUST agree with the gate)
    - 11-CONTEXT.md D-13 (payload half), D-19 (no CU without text)
  </read_first>
  <behavior>
    - The 883-node :Clause backbone (skeleton + HAS_CHILD hierarchy + function_type) is a PRECONDITION this task establishes first: Wave 0's `graph build --drop` wiped it, so alignment starts by (re-)seeding it via `ccop-eval graph seed-clauses` (deterministic, idempotent — no-op if already present).
    - Every clause_id in the inventory that has a body gets its verbatim text written as a node property (deterministic, from the re-ingested corpus).
    - Resolution reuses the boundary-aware matcher — `5.3` does not absorb `5.3.10` text.
    - Alignment coverage equals the 11-01 completeness-gate coverage (883/883 for docs in scope); any node left textless fails the task.
  </behavior>
  <action>FIRST establish the backbone: run `poetry run ccop-eval graph seed-clauses` (idempotent; (re-)creates the 883 :Clause skeleton + HAS_CHILD hierarchy + function_type tags that Wave 0's `graph build --drop` wiped) — the phase MUST be reproducible from a dropped graph, not dependent on a manual out-of-band seed. Then create `src/rag/graph/ontology/clause_text_aligner.py` (`ClauseTextAligner`) that resolves each inventory clause_id to its verbatim body from the re-ingested corpus (reusing `KGInspector._clause_id_appears`) and writes it to the corresponding source clause node's `text` property via static parameterized Cypher (never string-interpolated). Report an `AlignStats`-shaped dataclass with post-write counts re-queried from Neo4j. Add `tests/rag/graph/ontology/test_clause_text_aligner.py` asserting every source clause node has non-empty `text` and that 5.3/5.4 carry their real bodies.</action>
  <acceptance_criteria>
    - Before alignment, a Cypher count of `:Clause` nodes returns 883 (backbone seeded via `graph seed-clauses`; the step is idempotent).
    - After alignment, a Cypher count of source clause nodes with null/empty `text` property returns 0 for in-scope docs.
    - `poetry run pytest ../tests/rag/graph/ontology/test_clause_text_aligner.py -x` passes, including a 5.3/5.4 verbatim-text assertion.
    - Cypher queries in `clause_text_aligner.py` are module-level strings with bound params (grep shows no f-string/`.format()` splicing into a query body).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/graph/ontology/test_clause_text_aligner.py -x</automated>
  </verify>
  <done>Every in-scope source clause node carries verbatim text; zero textless nodes; parameterized Cypher only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Source-layer annotation — namespaced citation ids + doc_class (D-08), NO CU minting</name>
  <read_first>
    - src/rag/graph/ontology/clause_seeder.py lines 108-137 (`_derive_parent`/`_derive_chapter` — reuse to tag structural-header vs leaf source nodes), lines 155-260 (`SeedStats`, `_ensure_constraint` idempotent composite-key pattern, `_accumulate_stats`)
    - 11-CONTEXT.md D-06 (corpus/source facts, NOT a CU count), D-07 (CORRECTED — the clause is the CU's source unit + text carrier + citation anchor; CUs are minted in 11-04; CU count is emergent), D-08 (namespace ids by source doc; binding vs guidance split)
    - 11-PATTERNS.md "CU node seeding" section (reuse the MERGE + composite-key + idempotent-constraint shape for the SOURCE layer)
  </read_first>
  <behavior>
    - Every source clause node gets a source-doc-namespaced `citation_id` (e.g. `CCoP-5.7.2(b)`, `Act-7`) and a `doc_class` property (`binding` for CCoP 2.0 + Cybersecurity Act; `guidance` for the 4 guides + Response-to-Feedback, per D-08).
    - Structural headers (chapters/sections) remain CONTAIN-hierarchy skeleton — annotated but flagged as non-operative; they are NOT special-cased into any CU count (there is no CU count here).
    - Annotation is idempotent (MERGE/SET on composite `(clause_id, source_doc)`); re-run does not duplicate.
    - NO `:ComplianceUnit` nodes are created in this plan.
  </behavior>
  <action>Create `src/rag/graph/ontology/clause_source_annotator.py` annotating the SOURCE layer only. Reuse `clause_seeder`'s composite `(clause_id, source_doc)` MERGE + idempotent `_ensure_constraint` shape to SET a namespaced `citation_id` and a `doc_class` (`binding`/`guidance` per D-08) on each source clause node, plus a boolean `is_structural_header` flag (reuse `_derive_parent`/`_derive_chapter`). Report a `SourceAnnotationStats` dataclass with counts re-queried from Neo4j. Do NOT mint `:ComplianceUnit` nodes and do NOT assert any operative-leaf/CU count — CU minting and its emergent count belong to 11-04. Add `tests/rag/graph/ontology/test_clause_source_annotator.py`.</action>
  <acceptance_criteria>
    - After annotation, every source clause node has a source-doc-namespaced `citation_id` (grep/Cypher: zero bare `5.7.2` ids without a doc prefix) and a `doc_class` in {binding, guidance}.
    - A Cypher count of `:ComplianceUnit` nodes returns 0 (this plan mints none — asserted explicitly).
    - There is NO acceptance criterion asserting a CU/operative-leaf count anywhere in this plan.
    - Re-running the annotator produces no duplicates (idempotent MERGE/SET).
    - `poetry run pytest ../tests/rag/graph/ontology/test_clause_source_annotator.py -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/graph/ontology/test_clause_source_annotator.py -x</automated>
  </verify>
  <done>Source layer annotated (namespaced ids + doc_class + header flag), idempotent, zero :ComplianceUnit minted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Wave-2 source layer (this plan): source clause nodes carrying verbatim provision text (step-0 alignment) + source-doc-namespaced citation ids + doc_class, with zero :ComplianceUnit nodes minted. (Runs in parallel with 11-03; the wave advances only once BOTH 11-02 and 11-03 are approved — D-26.)</what-built>
  <how-to-verify>
    1. Confirm the alignment stats: zero textless source clause nodes for in-scope docs; spot-check 5.3/5.4 carry real bodies.
    2. Confirm namespaced citation ids (CCoP-... vs Act-...) and doc_class tags look correct.
    3. Confirm zero :ComplianceUnit nodes exist (CU minting is deliberately deferred to 11-04).
  </how-to-verify>
  <resume-signal>Type "approved" to let Wave 3 (11-04) mint CUs on this source layer, or list what needs rework. (Wave 2 also requires 11-03 approval.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aligner/annotator -> Neo4j | Cypher writes constructing source clause text + citation ids |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | Cypher construction in aligner/annotator | mitigate | Static module-level Cypher with bound params only (T-09-12 discipline); grep-asserted |
| T-11-05 | Spoofing | citation id namespacing | mitigate | Composite (clause_id, source_doc) key prevents cross-doc collision (D-08/F2) |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- Every source clause node carries verbatim text; zero textless nodes.
- Namespaced citation ids + doc_class set; idempotent; zero :ComplianceUnit nodes minted.
</verification>

<success_criteria>
- D-13 payload half established: source nodes hard-link verbatim provision text.
- D-06/D-07/D-08 satisfied (corrected): source layer prepared; CUs deferred to 11-04; no operative-leaf reconciliation; namespaced ids + binding/guidance split.
- D-26: wave-boundary human approval gate present.
</success_criteria>

<output>
Create `.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-02-SUMMARY.md` when done.
</output>
