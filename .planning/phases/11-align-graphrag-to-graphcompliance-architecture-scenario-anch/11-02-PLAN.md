---
phase: 11-align-graphrag-to-graphcompliance-architecture-scenario-anch
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/rag/graph/ontology/clause_text_aligner.py
  - src/rag/graph/ontology/cu_seeder.py
  - tests/rag/graph/ontology/test_clause_text_aligner.py
  - tests/rag/graph/ontology/test_cu_seeder.py
autonomous: true
requirements: [R11-3, R11-8]
must_haves:
  truths:
    - "Every :Clause node carries its verbatim provision text (step-0 alignment; fixes Finding 1)"
    - "CU candidate nodes exist for operative provisions only (leaves + lettered sub-items); the 62 chapter/section headers are CONTAIN-hierarchy skeleton, never CUs"
    - "CU/clause citation ids are namespaced by source doc (CCoP-5.7.2(b) vs Act-7); no cross-doc clause_id collision (fixes Finding 2)"
    - "No CU exists without its provision text hard-linked"
  artifacts:
    - path: "src/rag/graph/ontology/clause_text_aligner.py"
      provides: "Step-0 alignment: attach verbatim clause body text to each :Clause / CU node from the re-ingested corpus"
      contains: "class ClauseTextAligner"
    - path: "src/rag/graph/ontology/cu_seeder.py"
      provides: "Deterministic MERGE-seed of :ComplianceUnit nodes with namespaced citation ids + composite uniqueness constraint"
      contains: "CUSeedStats"
  key_links:
    - from: "src/rag/graph/ontology/cu_seeder.py"
      to: "Neo4j :ComplianceUnit"
      via: "MERGE on (cu_id, source_doc) composite key"
      pattern: "MERGE.*ComplianceUnit"
    - from: "src/rag/graph/ontology/clause_text_aligner.py"
      to: "re-ingested corpus verbatim text"
      via: "boundary-aware clause_id -> text resolution, write text property"
      pattern: "text"
---

<objective>
Step-0 clause-text alignment (fixes Finding 1) and CU-node seeding with source-doc-namespaced citation ids (fixes Finding 2). Attach the verbatim provision text to every clause node, and seed the `:ComplianceUnit` layer for operative provisions only (leaves + lettered sub-items), excluding the 62 chapter/section headers.

Purpose: The CU payload guarantee (D-13 payload half) and the D-06/D-07/D-08 corpus-scope + namespacing decisions. A CU tuple is a reasoning representation; the verbatim text hard-linked here is the citation payload that extraction loss cannot remove.
Output: `:Clause` nodes carrying verbatim text + a `:ComplianceUnit` additive layer with namespaced ids, ready for Stage-1 classification (11-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-CONTEXT.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-PATTERNS.md
@src/rag/graph/ontology/clause_seeder.py
@src/rag/ingestion/fixtures/clause_inventory.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Step-0 clause-text alignment — attach verbatim provision text to clause nodes</name>
  <read_first>
    - src/rag/graph/ontology/clause_seeder.py (`_derive_parent`/`_derive_chapter` dot/paren hierarchy parsing; MERGE-seed + `_accumulate_stats` re-query pattern)
    - src/rag/graph/inspect/metrics.py `KGInspector._clause_id_appears` (boundary-aware matcher — reuse for clause_id -> text resolution)
    - src/rag/ingestion/scripts/verify_clause_completeness.py (11-01 gate — the same resolution logic; alignment MUST agree with the gate)
    - 11-CONTEXT.md D-13 (payload half), D-19 (no CU without text)
  </read_first>
  <behavior>
    - Every clause_id in the inventory that has a body gets its verbatim text written as a node property (deterministic, from the re-ingested corpus).
    - Resolution reuses the boundary-aware matcher — `5.3` does not absorb `5.3.10` text.
    - Alignment coverage equals the 11-01 completeness-gate coverage (883/883 for docs in scope); any node left textless fails the task.
  </behavior>
  <action>Create `src/rag/graph/ontology/clause_text_aligner.py` (`ClauseTextAligner`) that resolves each inventory clause_id to its verbatim body from the re-ingested corpus (reusing `KGInspector._clause_id_appears`) and writes it to the corresponding node's `text` property via static parameterized Cypher (never string-interpolated). Report an `AlignStats`-shaped dataclass with post-write counts re-queried from Neo4j. Add `tests/rag/graph/ontology/test_clause_text_aligner.py` asserting every seeded clause node has non-empty `text` and that 5.3/5.4 carry their real bodies.</action>
  <acceptance_criteria>
    - After alignment, a Cypher count of clause nodes with null/empty `text` property returns 0 for in-scope docs.
    - `poetry run pytest ../tests/rag/graph/ontology/test_clause_text_aligner.py -x` passes, including a 5.3/5.4 verbatim-text assertion.
    - Cypher queries in `clause_text_aligner.py` are module-level strings with bound params (grep shows no f-string/`.format()` splicing into a query body).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/graph/ontology/test_clause_text_aligner.py -x</automated>
  </verify>
  <done>Every in-scope clause node carries verbatim text; zero textless nodes; parameterized Cypher only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed :ComplianceUnit layer with source-doc-namespaced ids (D-06/D-07/D-08)</name>
  <read_first>
    - src/rag/graph/ontology/clause_seeder.py lines 108-137 (`_derive_parent`/`_derive_chapter` — reuse to classify leaf vs header), lines 155-260 (`SeedStats`, `_ensure_constraint` idempotent composite-key pattern, `_accumulate_stats`)
    - 11-CONTEXT.md D-06 (883 entries / 415 CCoP; verify what clause_seeder MERGEs), D-07 (CU candidates = operative leaves + lettered sub-items only; 62 headers are CONTAIN skeleton), D-08 (namespace ids by source doc; binding vs guidance split)
    - 11-PATTERNS.md "CU node seeding" section
  </read_first>
  <behavior>
    - CU nodes are created only for operative provisions (leaves + lettered sub-items); chapter/section headers (`1`, `5.7`) become/stay CONTAIN-hierarchy skeleton, never CUs.
    - Every CU citation id is namespaced by source doc (e.g. `CCoP-5.7.2(b)`, `Act-7`).
    - Binding docs (CCoP 2.0 + Cybersecurity Act) yield judged CU candidates; the 4 guides + Response-to-Feedback are marked guidance/premise-context (per D-08).
    - Seeding is idempotent (MERGE on composite `(cu_id, source_doc)`); re-run does not duplicate.
  </behavior>
  <action>Create `src/rag/graph/ontology/cu_seeder.py` seeding an additive `:ComplianceUnit` layer (a clause can spawn multiple CUs — Claude's-discretion new layer, confirmed). Reuse `clause_seeder._derive_parent`/`_derive_chapter` to select CU candidates (leaves + lettered sub-items only, per D-07); skip the ~62 chapter/section headers. MERGE each CU on composite `(cu_id, source_doc)` with an idempotent uniqueness constraint (copy `_ensure_constraint` swallow-already-exists shape); set namespaced `citation_id` and a `doc_class` property (`binding` vs `guidance` per D-08). Link each CU to its source clause node so it inherits the aligned verbatim text (no CU without text). Report `CUSeedStats` with counts re-queried from Neo4j. During planning-verify, confirm what `clause_seeder.py` actually MERGEs so the CU candidacy count reconciles with D-06 (415 CCoP; ~220 operative leaves).</action>
  <acceptance_criteria>
    - After seeding, a Cypher count of `:ComplianceUnit` nodes equals the operative-leaf count (not 883, not the 62-header count); the CUSeedStats report enumerates the candidacy filter result.
    - Zero `:ComplianceUnit` nodes match a chapter/section header pattern (`^\d+$` or `^\d+\.\d+$` with no operative body).
    - Zero `:ComplianceUnit` nodes lack hard-linked verbatim text.
    - Every CU `citation_id` is source-doc-namespaced (grep/Cypher: no bare `5.7.2` id without a doc prefix); re-running the seeder produces no duplicates (idempotent MERGE).
    - `poetry run pytest ../tests/rag/graph/ontology/test_cu_seeder.py -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/graph/ontology/test_cu_seeder.py -x</automated>
  </verify>
  <done>:ComplianceUnit layer seeded for operative provisions only, namespaced ids, text-linked, idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aligner/seeder -> Neo4j | Cypher writes constructing CU nodes and clause text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | Cypher construction in aligner/seeder | mitigate | Static module-level Cypher with bound params only (T-09-12 discipline); grep-asserted |
| T-11-05 | Spoofing | CU citation id namespacing | mitigate | Composite (cu_id, source_doc) MERGE key prevents cross-doc collision (D-08/F2) |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- Every clause node carries verbatim text; zero textless nodes.
- :ComplianceUnit count == operative-leaf count; zero header CUs; namespaced ids; idempotent.
</verification>

<success_criteria>
- D-13 payload half established: CUs hard-link verbatim provision text.
- D-06/D-07/D-08 satisfied: operative-only candidacy, namespaced ids, binding/guidance split.
</success_criteria>

<output>
Create `.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-02-SUMMARY.md` when done.
</output>
