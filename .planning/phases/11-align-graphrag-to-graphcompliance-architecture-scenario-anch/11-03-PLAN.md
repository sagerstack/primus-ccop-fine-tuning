---
phase: 11-align-graphrag-to-graphcompliance-architecture-scenario-anch
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/application/use_cases/clause_hit_harness.py
  - src/rag/ingestion/scripts/validate_gold_clause_sets.py
  - tests/application/test_gold_clause_validation.py
  - .gitignore
autonomous: false
requirements: [R11-17, R11-19]
must_haves:
  truths:
    - "The 18-case gold clause SETs are validated against the actual CCoP PDF / clause inventory before clause-hit@3 trusts them"
    - "Every gold clause id resolves to a real clause; hallucinated ids (e.g. 5.1.5) are flagged/patched, not scored against"
    - "The new mode's adapter/provider files are actually git-tracked (not shadowed by .gitignore models/)"
    - "The canonical hybrid baseline JSON has a valid B04 test_id (corrupted '\\n  ' restored)"
  artifacts:
    - path: "src/rag/ingestion/scripts/validate_gold_clause_sets.py"
      provides: "D-22 broken-ruler guard: validate 18-case gold clause SETs against clause_inventory + CCoP PDF"
      contains: "GoldValidationReport"
  key_links:
    - from: "src/rag/ingestion/scripts/validate_gold_clause_sets.py"
      to: "src/rag/ingestion/fixtures/clause_inventory.json"
      via: "membership check of each gold clause id"
      pattern: "clause_inventory"
    - from: ".gitignore"
      to: "src/infrastructure/adapters/models/ and new mode files"
      via: "negation/un-ignore so mode files are tracked"
      pattern: "models"
---

<objective>
Guard the ruler and the build hygiene before any A/B is scored. Validate the 18-case gold clause SETs against the real CCoP corpus (D-22 — a green clause-hit@3 on a hallucinated gold is meaningless) and fix the two D-24 hygiene traps: the `.gitignore models/` pattern silently un-versioning adapter files, and the corrupted B04 `test_id` in the canonical hybrid baseline JSON.

Purpose: D-22 (clause-hit@3 gold validation) and D-24 (build/eval hygiene). These are independent of the Policy Graph build and run in parallel with 11-02.
Output: A committed gold-validation report + patched/flagged gold, tracked mode files, and a restored canonical baseline test_id.

This plan touches ground-truth gold and a canonical baseline artifact — it carries `checkpoint:human-verify` before overwriting gold or the baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-CONTEXT.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-PATTERNS.md
@src/application/use_cases/clause_hit_harness.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate 18-case gold clause SETs against the corpus (D-22)</name>
  <read_first>
    - src/application/use_cases/clause_hit_harness.py (FIXED_18_TEST_IDS, gold-set loading, `disagreement_test_ids` / `gold_disagreement` mechanism — extend, do not fork)
    - src/rag/ingestion/fixtures/clause_inventory.json (authoritative valid clause ids)
    - 11-CONTEXT.md D-22 (hallucinated clause ids span B2-B20; Finding 7 B02-001 mismatch; leverage /gt-audit)
    - docs/project_notes/bugs.md 2026-03-21 (GT hallucinated clause refs)
  </read_first>
  <action>Create `src/rag/ingestion/scripts/validate_gold_clause_sets.py` producing a `GoldValidationReport`. For each of the `FIXED_18_TEST_IDS`, extract the gold clause SET (from GT `clause_reference` + inline `expected_response` citations) and check every id against `clause_inventory.json` (namespaced, per D-08) and the CCoP PDF. Flag any id with no real clause (e.g. `5.1.5`) and any B02-001-style mismatch (Finding 7). Extend the harness's existing `disagreement_test_ids` mechanism to surface validation failures rather than building a separate validator. Emit the report as a committed JSON artifact enumerating each invalid id + proposed correction (or human-review flag).</action>
  <acceptance_criteria>
    - `poetry run python -m rag.ingestion.scripts.validate_gold_clause_sets` writes a GoldValidationReport listing, per test id, the gold clause SET and a valid/invalid verdict per id against the inventory.
    - Report explicitly flags any gold id absent from `clause_inventory.json` (asserts zero silently-accepted nonexistent ids).
    - `poetry run pytest ../tests/application/test_gold_clause_validation.py -x` passes, including a case where a synthetic hallucinated id is flagged.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run python -m rag.ingestion.scripts.validate_gold_clause_sets && poetry run pytest ../tests/application/test_gold_clause_validation.py -x</automated>
  </verify>
  <done>Gold clause SETs validated; hallucinated ids flagged with proposed corrections in a committed report.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The GoldValidationReport flagging invalid gold clause ids (D-22) and the proposed corrections, plus the two D-24 hygiene fixes staged (below).</what-built>
  <how-to-verify>
    1. Open the committed GoldValidationReport JSON; review each flagged gold clause id and its proposed correction.
    2. Confirm which corrections to APPLY to the 18-case gold vs which to leave as human-review flags (do not silently overwrite GT).
    3. Confirm the corrupted B04 `test_id` restoration value for `eval-run-hybrid-tests-18-bdc4927d-20260430-0232` is correct.
  </how-to-verify>
  <resume-signal>Type "approved" to apply the confirmed gold corrections + baseline restore, or list the ids to leave flagged.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: D-24 hygiene — un-ignore mode files + restore corrupted canonical baseline test_id</name>
  <read_first>
    - .gitignore (the `models/` pattern that shadows `src/infrastructure/adapters/models/`)
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/deferred-items.md 10-01 (both hygiene traps)
    - src/results/evaluations/2026-04/eval-run-hybrid-tests-18-bdc4927d-20260430-0232*.json (the corrupted B04 test_id `"\n  "`)
    - 11-CONTEXT.md D-24
  </read_first>
  <action>Add a `.gitignore` negation so `src/infrastructure/adapters/models/` and any new `--mode graph-compliance` adapter/provider files are tracked (verify with `git check-ignore`). Restore the corrupted B04 `test_id` (`"\n  "`) in the canonical hybrid baseline JSON to the correct `B04-...` id confirmed at the checkpoint. Apply the human-approved gold corrections from Task 1.</action>
  <acceptance_criteria>
    - `git check-ignore -v src/infrastructure/adapters/models/anyfile.py` returns nothing (not ignored); `git status` shows the new mode files as tracked candidates.
    - The canonical baseline JSON B04 entry has a valid non-whitespace `test_id` (grep confirms no `"test_id": "\n  "`).
    - Human-approved gold corrections applied; the ground-truth validator (`poetry run ccop-eval validate-ground-truth`) passes on the corrected files.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run ccop-eval validate-ground-truth && git -C .. check-ignore src/infrastructure/adapters/models/probe.py; test $? -ne 0</automated>
  </verify>
  <done>Mode files tracked; baseline B04 test_id valid; approved gold corrections applied and validator green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gold GT -> scoring | Hallucinated gold ids silently corrupt the clause-hit@3 ruler |
| .gitignore -> VCS | Shadowed adapter files silently un-versioned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | gold clause SETs | mitigate | Validate every gold id against inventory; human-verify before overwriting GT |
| T-11-07 | Repudiation | canonical baseline JSON | mitigate | Restore corrupted B04 test_id; human-confirm value before write |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- Gold validation report flags all invalid ids; validator green after corrections.
- Mode files git-tracked; baseline B04 test_id valid.
</verification>

<success_criteria>
- D-22 satisfied: 18-case gold validated; no scoring against nonexistent clauses.
- D-24 satisfied: mode files tracked; canonical baseline test_id restored.
</success_criteria>

<output>
Create `.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-03-SUMMARY.md` when done.
</output>
