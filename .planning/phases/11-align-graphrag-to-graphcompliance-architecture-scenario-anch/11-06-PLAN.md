---
phase: 11-align-graphrag-to-graphcompliance-architecture-scenario-anch
plan: 06
type: execute
wave: 5
depends_on: ["11-05"]
files_modified:
  - src/rag/retrieval/state/graph_state.py
  - src/rag/retrieval/nodes/context_graph_extraction.py
  - src/rag/retrieval/nodes/anchor_hypernym_mapping.py
  - src/domain/services/hypernym_scoring_service.py
  - tests/rag/retrieval/nodes/test_context_graph_extraction.py
  - tests/rag/retrieval/nodes/test_anchor_hypernym_mapping.py
  - tests/domain/services/test_hypernym_scoring_service.py
autonomous: true
requirements: [R11-5, R11-4]
must_haves:
  truths:
    - "Context Graph = ER/SAO triples -> actor/data/system anchors -> hypernym mapping that normalizes entities (not relations) with top-N=5 and beta=0.3 (D-10)"
    - "Per-query Context Graph = ER/SAO triples extracted from the scenario (GraphCompliance's biggest lever, S2)"
    - "Anchors (actor/data/system) are extracted from the Context Graph and hypernym-mapped to policy vocabulary"
    - "Hypernym proposals are STRONG iff supported by a premise fragment (beta=0.3 bonus), else WEAK (eqs. 1-2)"
    - "New nodes are no-ops unless mode == graph-compliance; state fields reserved for the D-17 trace"
  artifacts:
    - path: "src/rag/retrieval/nodes/context_graph_extraction.py"
      provides: "Per-query ER/SAO triple extraction node (mode-gated)"
      contains: "extract_context_graph"
    - path: "src/rag/retrieval/nodes/anchor_hypernym_mapping.py"
      provides: "Anchor extraction + hypernym mapping node (mode-gated)"
      contains: "map_anchors_to_hypernyms"
    - path: "src/domain/services/hypernym_scoring_service.py"
      provides: "Max-pool + top-N=5 STRONG/WEAK confidence scorer (eqs. 1-2, beta=0.3)"
      contains: "class HypernymScoringService"
  key_links:
    - from: "src/rag/retrieval/nodes/anchor_hypernym_mapping.py"
      to: "src/domain/services/hypernym_scoring_service.py"
      via: "score anchor->policy-fragment mappings with STRONG/WEAK premise bonus"
      pattern: "HypernymScoringService"
    - from: "src/rag/retrieval/nodes/context_graph_extraction.py"
      to: "GraphState.context_graph_triples / anchors / hypernym_mappings"
      via: "write extracted triples/anchors into reserved state fields"
      pattern: "context_graph_triples"
---

<objective>
Build the Context Graph (GraphCompliance §3.2) — the per-query graph of the scenario, which the paper's S2 ablation shows is the single biggest lever (−10.2pp when removed) and exactly the scenario anchoring the current system throws away (Finding 0, query embedded as a bare string). Extract ER/SAO triples from the scenario, derive actor/data/system anchors, and hypernym-map anchors to policy vocabulary with the STRONG/WEAK premise-supported confidence signal.

Purpose: D-10 (Context Graph + anchors + hypernym mapping), D-09 (premise -> STRONG bonus). Hypernym mapping normalizes entities, not relations.
Output: Mode-gated LangGraph nodes populating reserved `GraphState` fields + a pure-domain hypernym scorer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-CONTEXT.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-PATTERNS.md
@research/graphcompliance/2510.26309v1.pdf
@src/rag/retrieval/nodes/query_analysis.py
@src/rag/retrieval/nodes/function_type_routing.py
@src/rag/retrieval/state/graph_state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reserve GraphState trace fields + Context Graph extraction node</name>
  <read_first>
    - src/rag/retrieval/state/graph_state.py lines 39-43 (the `function_type: str` field-reservation precedent — reserve fields now, later task consumes)
    - src/rag/retrieval/nodes/query_analysis.py lines 27-48 (`_generate_hyde` — settings-gated, try/except degrade-to-empty node shape)
    - src/rag/retrieval/nodes/function_type_routing.py lines 96-119 (mode-gated no-op-unless-mode-matches node pattern)
    - research/graphcompliance/2510.26309v1.pdf §3.2 (Context Graph = ER/SAO triples from the scenario)
    - 11-CONTEXT.md D-10, D-17 (trace order), D-21 (fields feed the verbose-io trace)
  </read_first>
  <action>Reserve new `GraphState` fields (`context_graph_triples`, `anchors`, `hypernym_mappings`, `cu_plan`, `verbatim_clause_texts`) mirroring the `function_type` reservation comment convention (each with a short docstring naming the consuming task). Create `src/rag/retrieval/nodes/context_graph_extraction.py` (`extract_context_graph`) — a mode-gated node (`state.get("mode") == "graph-compliance"`, no-op otherwise) that extracts ER/SAO triples from the scenario via a settings-gated OpenRouter call (copy `_generate_hyde` degrade shape), writing `context_graph_triples`. Cache the extraction per query id (Claude's-discretion caching). Add `tests/rag/retrieval/nodes/test_context_graph_extraction.py` asserting no-op for other modes and triple extraction for graph-compliance mode (with a mocked LLM).</action>
  <acceptance_criteria>
    - `poetry run pytest ../tests/rag/retrieval/nodes/test_context_graph_extraction.py -x` passes, including a `mode != graph-compliance` no-op assertion.
    - `grep -n "context_graph_triples\|anchors\|hypernym_mappings\|cu_plan\|verbatim_clause_texts" src/rag/retrieval/state/graph_state.py` shows all five reserved fields.
    - The node degrades to empty triples (logs, returns state) when the LLM call fails or the API key is unset — never raises.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/retrieval/nodes/test_context_graph_extraction.py -x</automated>
  </verify>
  <done>State fields reserved; Context Graph node extracts triples mode-gated, degrade-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hypernym scoring domain service (eqs. 1-2, STRONG/WEAK, beta=0.3)</name>
  <read_first>
    - src/domain/services/ (existing `ScoringService` / `ClauseHitScoringService` — module placement + naming convention; keep business rule out of infrastructure)
    - src/rag/retrieval/nodes/reranking.py lines 105-117 (RRF combine — closest "combine multiple scored signals" analog; different formula)
    - research/graphcompliance/2510.26309v1.pdf §3.2 eqs. 1-2 (max-pool + top-N=5 per entity, STRONG/WEAK premise bonus)
    - 11-CONTEXT.md D-09 (STRONG iff supporting fragment is a premise; beta=0.3), D-10 (top-M fragment retrieval, N=5)
  </read_first>
  <behavior>
    - Given an anchor and a set of scored policy fragments (each tagged premise or not), the scorer max-pools over the top-M fragments, keeps top-N=5, and marks a mapping STRONG (with +beta=0.3 confidence) iff its supporting fragment is a premise, else WEAK.
    - STRONG bonus comes from a definitional premise ("CII means..."), not from a meta-CU designation rule.
    - Pure function, no I/O — unit-testable with synthetic fragment scores.
  </behavior>
  <action>Create `src/domain/services/hypernym_scoring_service.py` (`HypernymScoringService`) implementing eqs. 1-2: max-pool aggregation over top-M premise/CU fragments, top-N=5 per entity, STRONG/WEAK label with a `beta=0.3` premise-support bonus (params configurable via constructor defaults). Place under `domain/services/` per the project convention (no infra deps). Add `tests/domain/services/test_hypernym_scoring_service.py` asserting: premise-supported mapping is STRONG with the bonus, non-premise is WEAK, top-N truncation at 5, deterministic ordering.</action>
  <acceptance_criteria>
    - `poetry run pytest ../tests/domain/services/test_hypernym_scoring_service.py -x` passes, including STRONG-vs-WEAK, the beta=0.3 bonus, and top-N=5 truncation cases.
    - The service imports nothing from `infrastructure`/`rag` (pure domain — grep asserts no such import).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/domain/services/test_hypernym_scoring_service.py -x</automated>
  </verify>
  <done>Pure-domain hypernym scorer with STRONG/WEAK + beta bonus + top-N=5, unit-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Anchor extraction + hypernym mapping node</name>
  <read_first>
    - src/rag/retrieval/nodes/function_type_routing.py lines 96-119 (mode-gated state-writing node; classify->validate->store)
    - src/domain/services/hypernym_scoring_service.py (Task 2 — the scorer this node calls)
    - src/rag/graph/retrieval/neo4j_ontology_graph_retrieval_adapter.py (fragment retrieval over premise/CU nodes — top-M retrieval for hypernym candidates)
    - 11-CONTEXT.md D-10 (anchors actor/data/system; hypernym mapping normalizes entities not relations), D-17.2 (trace: anchors each with label+STRONG/WEAK+premise+score)
  </read_first>
  <action>Create `src/rag/retrieval/nodes/anchor_hypernym_mapping.py` (`map_anchors_to_hypernyms`) — a mode-gated node deriving actor/data/system anchors from `context_graph_triples`, retrieving top-M policy fragments per anchor (reuse the ontology adapter's dense/BM25 retrieval over premise/CU nodes), and calling `HypernymScoringService` to produce STRONG/WEAK mappings. Write `anchors` and `hypernym_mappings` (label + STRONG/WEAK + supporting premise + score) into state for the D-17 trace. Degrade-safe per the shared settings-gated pattern. Add `tests/rag/retrieval/nodes/test_anchor_hypernym_mapping.py` asserting anchors derive from triples and mappings carry STRONG/WEAK + supporting-premise metadata.</action>
  <acceptance_criteria>
    - `poetry run pytest ../tests/rag/retrieval/nodes/test_anchor_hypernym_mapping.py -x` passes.
    - For the B01-001 scenario fixture, the node produces an anchor for the hospital admin system and at least one STRONG mapping supported by a CII-definition premise (asserts the STRONG label + supporting-premise field populated).
    - `hypernym_mappings` entries each carry {label, strong_weak, supporting_premise, score} (the D-17.2 trace payload).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/retrieval/nodes/test_anchor_hypernym_mapping.py -x</automated>
  </verify>
  <done>Anchors + hypernym mappings (STRONG/WEAK + premise + score) populated mode-gated for the trace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user question (scenario) -> LLM extraction | Untrusted question text prompts ER/SAO extraction; prompt-injection risk |
| OpenRouter API key | Secret used per-query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14 | Tampering | prompt-injection via scenario text | mitigate | Extraction output is structured triples validated before use; downstream judgment forbids inference from silence (D-12); citations constrained to retrieved CU set |
| T-11-15 | Denial of Service | per-query LLM calls | mitigate | Settings-gated degrade-to-empty; caching per query id |
| T-11-16 | Information Disclosure | OPENROUTER_API_KEY | mitigate | Settings/env only; never logged |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- Context Graph node extracts triples (mode-gated, degrade-safe); hypernym scorer pure + STRONG/WEAK; anchor node populates trace payload; B01-001 produces a STRONG CII-definition mapping.
</verification>

<success_criteria>
- D-10 satisfied: Context Graph + anchors + hypernym mapping over entities.
- D-09 satisfied: premise-supported mappings are STRONG with beta bonus.
</success_criteria>

<output>
Create `.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-06-SUMMARY.md` when done.
</output>
