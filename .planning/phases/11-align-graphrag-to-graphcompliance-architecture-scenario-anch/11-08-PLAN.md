---
phase: 11-align-graphrag-to-graphcompliance-architecture-scenario-anch
plan: 08
type: execute
wave: 7
depends_on: ["11-07"]
files_modified:
  - src/domain/services/meta_cu_gating_service.py
  - src/domain/services/violation_aggregation_service.py
  - src/rag/retrieval/nodes/compliance_judgment.py
  - src/rag/retrieval/nodes/reference_closure.py
  - tests/domain/services/test_meta_cu_gating_service.py
  - tests/domain/services/test_violation_aggregation_service.py
  - tests/rag/retrieval/nodes/test_compliance_judgment.py
  - tests/rag/retrieval/nodes/test_reference_closure.py
autonomous: false
requirements: [R11-7, R11-8]
must_haves:
  truths:
    - "A human approval gate ends this wave (D-26): the gating + listwise judgment + reference-closure behavior is reviewed before mode wiring"
    - "Structured judgment prompt = evidence window + CU Plan 4-tuples + verbatim clause text; meta-CU gating first; forbid inference from silence; REFERS_TO exception closure; violation-first aggregation (D-12)"
    - "meta-CUs gate applicability FIRST (evaluated before actor-CUs, never a standalone violation)"
    - "The judgment prompt carries the CU Plan as structured 4-tuples PLUS the verbatim clause text (citation payload) — text is provably in the prompt"
    - "Listwise judge emits per-CU {label, confidence, rationale, evidence}; ambiguous/out-of-scope -> INSUFFICIENT/NOT_APPLICABLE (forbid inference from silence — fixes Finding 6)"
    - "For NON_COMPLIANT CUs, REFERS_TO traversal + a 2nd LLM call checks for a valid overriding exception (eq. 6)"
    - "Violation-first aggregation combines per-CU judgments into a final verdict"
    - "The judgment node populates system_prompt/user_prompt/tokens/latency (real LLM call — generate_response pattern, NOT the hardcoded-zeros retrieval-only pattern)"
  artifacts:
    - path: "src/rag/retrieval/nodes/compliance_judgment.py"
      provides: "Listwise structured judgment over CU Plan + verbatim text, meta-CU gating first"
      contains: "judge"
    - path: "src/domain/services/meta_cu_gating_service.py"
      provides: "Applicability precedence (meta-CU gates before actor-CU judgment)"
      contains: "class MetaCuGatingService"
    - path: "src/domain/services/violation_aggregation_service.py"
      provides: "Violation-first final-verdict aggregation"
      contains: "class ViolationAggregationService"
  key_links:
    - from: "src/rag/retrieval/nodes/compliance_judgment.py"
      to: "verbatim clause text in the prompt"
      via: "assemble prompt = CU 4-tuples + verbatim clause text payload"
      pattern: "verbatim"
    - from: "src/rag/retrieval/nodes/reference_closure.py"
      to: "Neo4j :ComplianceUnit -[:REFERS_TO]-> :ComplianceUnit"
      via: "static parameterized traversal + 2nd LLM exception check"
      pattern: "REFERS_TO"
---

<objective>
The reasoning core (GraphCompliance §3.3, eqs. 5-6). meta-CU applicability gating first, listwise structured judgment over the CU Plan (4-tuples PLUS verbatim clause text), REFERS_TO reference-closure exception handling for NON_COMPLIANT CUs, and violation-first aggregation into a final verdict.

Purpose: D-12 (structured judgment + meta-CU gating + reference closure + aggregation), D-13 payload (verbatim text provably in the prompt). "Forbid inference from silence" fixes Finding 6 (hedge-instead-of-decide). The judgment node is the D-21 token/latency-capturing LLM call.
Output: two pure-domain services (gating, aggregation) + two LangGraph nodes (judgment, reference closure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-CONTEXT.md
@.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-PATTERNS.md
@research/graphcompliance/2510.26309v1.pdf
@src/rag/retrieval/nodes/generation.py
@src/rag/graph/ontology/clause_linker.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: meta-CU gating + violation-first aggregation (pure-domain services)</name>
  <read_first>
    - src/domain/services/ (`ScoringService`/`ClauseHitScoringService` — placement/naming convention; keep business rules out of infrastructure)
    - research/graphcompliance/2510.26309v1.pdf §3.3 (meta-CU evaluated first/gates applicability; violation-first aggregation)
    - 11-CONTEXT.md D-12 (meta-CUs gate first, never a standalone violation; violation-first aggregation)
    - 11-PATTERNS.md "No Analog Found" (meta-CU gating + violation-first aggregation are new pure-domain logic)
  </read_first>
  <behavior>
    - MetaCuGatingService: given meta-CU judgments, decides applicability; if a meta-CU gates the scenario out of scope, actor-CU judgments do not produce standalone violations.
    - ViolationAggregationService: given per-CU judgments (+ any exception-closure results), produces a final verdict violation-first (any un-excepted NON_COMPLIANT -> non-compliant; else applicability/compliant verdict).
    - Both are pure functions (no I/O), unit-testable with synthetic per-CU judgments.
  </behavior>
  <action>Create `src/domain/services/meta_cu_gating_service.py` (`MetaCuGatingService`) encoding the applicability-precedence rule (meta-CU gates before actor-CU judgment) and `src/domain/services/violation_aggregation_service.py` (`ViolationAggregationService`) encoding violation-first aggregation. Place under `domain/services/` (no infra deps). Add unit tests asserting: out-of-scope meta-CU suppresses actor-CU violations, a single un-excepted NON_COMPLIANT drives a non-compliant verdict, and an excepted NON_COMPLIANT does not.</action>
  <acceptance_criteria>
    - `poetry run pytest ../tests/domain/services/test_meta_cu_gating_service.py ../tests/domain/services/test_violation_aggregation_service.py -x` passes, including the out-of-scope-gate and excepted-vs-un-excepted cases.
    - Neither service imports from `infrastructure`/`rag` (pure domain — grep asserts).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/domain/services/test_meta_cu_gating_service.py ../tests/domain/services/test_violation_aggregation_service.py -x</automated>
  </verify>
  <done>meta-CU gating + violation-first aggregation as pure, unit-tested domain services.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Listwise compliance judgment node (verbatim text in prompt + token/latency capture)</name>
  <read_first>
    - src/rag/retrieval/nodes/generation.py lines 163-248 (`generate_response` — prompt-assembly + `state["system_prompt"]`/`user_prompt` capture + perf_counter latency + prompt/completion/total token capture; the D-21 fix template to COPY)
    - src/rag/graph/build/ontology_kg_builder.py lines 92-131 (strict-JSON-only, locked-vocabulary prompt discipline for the judge)
    - src/rag/graph/build/gleaning_extractor.py lines 179-191 (Pydantic-validate JSON, degrade to safe default on parse failure)
    - src/rag/retrieval/nodes/rag_response.py lines 44-50 (the ANTI-pattern — hardcoded zeros because NO LLM call; the judgment node must NOT do this — it calls an LLM)
    - research/graphcompliance/2510.26309v1.pdf §3.3 eq. 5 (listwise judgment, per-CU {label, confidence, rationale, evidence}; forbid inference from silence)
    - 11-CONTEXT.md D-12 (structured judgment, meta-CU gate first), D-13 payload (verbatim text in prompt), D-21 (token/latency propagation)
  </read_first>
  <behavior>
    - The judgment prompt carries the CU Plan as structured 4-tuples PLUS the verbatim clause text for each CU (the citation payload).
    - meta-CUs are gated first (call MetaCuGatingService) before actor-CU judgment.
    - The judge emits per-CU {label, confidence, rationale, evidence}; ambiguous/out-of-scope -> INSUFFICIENT/NOT_APPLICABLE (forbid inference from silence); output Pydantic-validated, degrade to INSUFFICIENT on parse failure (never crash the case).
    - `state["system_prompt"]`, `state["user_prompt"]`, `prompt_tokens`, `completion_tokens`, `total_tokens`, `latency_ms` are populated exactly as `generate_response` does — NOT hardcoded to zero.
    - The verbatim clause texts sent are stored in `verbatim_clause_texts` for the D-17 trace and the D-15.1 assertion.
  </behavior>
  <action>Create `src/rag/retrieval/nodes/compliance_judgment.py` (mode-gated on `graph-compliance`). Assemble the judgment prompt from the CU Plan 4-tuples + verbatim clause text; call `MetaCuGatingService` to gate meta-CUs first; issue the listwise judge LLM call following `generate_response`'s exact capture-before-and-after shape (system/user prompt + token + latency capture). Pydantic-validate the per-CU judgment JSON (locked label vocabulary incl. INSUFFICIENT/NOT_APPLICABLE), degrade to INSUFFICIENT on parse failure. Store `verbatim_clause_texts` in state. Add `tests/rag/retrieval/nodes/test_compliance_judgment.py` asserting: verbatim text is present in the assembled prompt, tokens/latency are non-zero on a mocked successful call, and malformed judge JSON degrades to INSUFFICIENT.</action>
  <acceptance_criteria>
    - `poetry run pytest ../tests/rag/retrieval/nodes/test_compliance_judgment.py -x` passes.
    - Assertion in-test: the assembled `user_prompt`/`system_prompt` contains the exact verbatim clause text of each CU in the plan (deterministic substring check — the D-15.1 mechanism).
    - On a mocked successful LLM call, `state["prompt_tokens"]`/`completion_tokens`/`total_tokens`/`latency_ms` are all non-zero (NOT the rag_response.py hardcoded-zero path).
    - Malformed judge JSON degrades the case to INSUFFICIENT without raising.
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/retrieval/nodes/test_compliance_judgment.py -x</automated>
  </verify>
  <done>Listwise judgment carries verbatim text in the prompt, gates meta-CUs first, captures tokens/latency, degrade-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFERS_TO reference-closure exception handling (eq. 6)</name>
  <read_first>
    - src/rag/graph/ontology/clause_linker.py (static-parameterized-Cypher `MATCH ... -[:REFERS_TO]-> ...` traversal pattern — never interpolate a clause id)
    - src/rag/retrieval/nodes/generation.py (the 2nd-LLM-call prompt-assembly + degrade-on-failure shape to reuse)
    - src/domain/services/violation_aggregation_service.py (Task 1 — closure results feed aggregation)
    - research/graphcompliance/2510.26309v1.pdf §3.3 eq. 6 (for NON_COMPLIANT CUs, traverse REFERS_TO, 2nd LLM checks for a valid overriding exception)
    - 11-CONTEXT.md D-12 (reference-closure exception handling), D-05 (REFERS_TO edges built in 11-05)
  </read_first>
  <behavior>
    - For each NON_COMPLIANT CU, traverse `REFERS_TO` (static parameterized Cypher) to gather referenced CUs, then a 2nd settings-gated LLM call checks whether a valid overriding exception applies.
    - An applicable exception flips/annotates the CU judgment before aggregation; no exception leaves it NON_COMPLIANT.
    - Degrade-safe: LLM failure -> no exception applied (conservative), logs, never raises.
  </behavior>
  <action>Create `src/rag/retrieval/nodes/reference_closure.py` (mode-gated) that, for each NON_COMPLIANT CU from the judgment node, traverses `REFERS_TO` via the `clause_linker`-style static parameterized Cypher and issues a 2nd LLM exception-check call (reuse `generate_response`'s assembly + degrade shape). Feed the closure result into `ViolationAggregationService` to produce the final verdict. Add `tests/rag/retrieval/nodes/test_reference_closure.py` asserting: a referenced overriding exception flips the verdict, absence leaves NON_COMPLIANT, and Cypher is parameterized.</action>
  <acceptance_criteria>
    - `poetry run pytest ../tests/rag/retrieval/nodes/test_reference_closure.py -x` passes, including the exception-flips-verdict and no-exception-stays-noncompliant cases.
    - `reference_closure.py` REFERS_TO traversal Cypher is module-level with bound params (grep: no interpolation).
    - For the B01-001 fixture, closure surfaces the §1.4.1 dependency caveat referenced from the scope meta-CU (asserts the REFERS_TO edge is traversed).
  </acceptance_criteria>
  <verify>
    <automated>cd src && poetry run pytest ../tests/rag/retrieval/nodes/test_reference_closure.py -x</automated>
  </verify>
  <done>Reference-closure exception handling wired to aggregation; parameterized traversal; degrade-safe.</done>
</task>


<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The meta-CU gating + violation-first aggregation services, the listwise judgment node (verbatim text in the prompt, tokens/latency captured), and the REFERS_TO exception-closure node.</what-built>
  <how-to-verify>1. Confirm the judgment prompt carries verbatim clause text and meta-CUs gate first. 2. Confirm tokens/latency are non-zero and reference-closure flips a verdict on a valid exception.</how-to-verify>
  <resume-signal>Type "approved" to advance to Wave 8 (11-09), or list what needs rework.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scenario + CU text -> judge LLM | Prompt-injection risk from scenario/corpus text; citations must stay constrained to the retrieved CU set |
| closure -> Neo4j | REFERS_TO traversal Cypher |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-20 | Tampering | prompt-injection via scenario/corpus | mitigate | Judge emits structured labels only; citations constrained to the retrieved CU set; forbid inference from silence (INSUFFICIENT) |
| T-11-21 | Tampering | REFERS_TO traversal Cypher | mitigate | Static parameterized Cypher (T-09-12) |
| T-11-22 | Denial of Service | 2 LLM calls per case | mitigate | Settings-gated degrade; conservative default (no exception) on failure |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- Gating + aggregation pure-domain green; judgment carries verbatim text + captures tokens/latency + degrade-safe; closure flips verdict on valid exception; parameterized Cypher.
</verification>

<success_criteria>
- D-12 satisfied: meta-CU gating first, listwise judgment, reference closure, violation-first aggregation.
- D-13 payload satisfied: verbatim clause text provably in the prompt.
</success_criteria>

<output>
Create `.planning/phases/11-align-graphrag-to-graphcompliance-architecture-scenario-anch/11-08-SUMMARY.md` when done.
</output>
