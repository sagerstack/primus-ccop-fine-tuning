---
phase: 02.3-ragas-metric-split-scoring-formula
plan: 02
type: execute
wave: 2
depends_on: ["02.3-01"]
files_modified:
  - src/presentation/cli/commands/evaluate.py
  - src/infrastructure/adapters/repositories/json_result_repository.py
autonomous: true

must_haves:
  truths:
    - "CLI per-test panels show factual_precision, factual_recall, semantic_similarity instead of answer_correctness, hallucination"
    - "CLI Overall Quality Summary shows updated metric names"
    - "CLI per-benchmark breakdown table has correct column headers for new metrics"
    - "JSON schema bumped to version 4 with new metric names in grouped structure"
    - "JSON ragas_score computed with multiplicative penalty formula"
  artifacts:
    - path: "src/presentation/cli/commands/evaluate.py"
      provides: "CLI display with new metric names"
      contains: "factual_precision"
    - path: "src/infrastructure/adapters/repositories/json_result_repository.py"
      provides: "JSON persistence with schema v4 and new metric structure"
      contains: "schema_version.*4"
  key_links:
    - from: "src/presentation/cli/commands/evaluate.py"
      to: "src/domain/value_objects/quality_group.py"
      via: "QualityGroup.get_display_name() for new metric names"
      pattern: "get_display_name"
    - from: "src/infrastructure/adapters/repositories/json_result_repository.py"
      to: "src/domain/services/ragas_evaluation_service.py"
      via: "reads metric.name from RagasEvaluation.metrics"
      pattern: "metric\\.name"
---

<objective>
Update CLI display and JSON serialization to reflect the new metric names (factual_precision, factual_recall, semantic_similarity replacing answer_correctness and hallucination) and implement the multiplicative penalty formula in JSON ragas_score computation.

Purpose: The domain layer (Plan 01) changed metric names and scoring formula. Presentation and persistence layers must reflect these changes for the user-facing output.

Output: Updated CLI evaluate command (panels, tables, headers) and JSON result repository (schema v4, new metric groups, penalty formula).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-ragas-metric-split-scoring-formula/02.3-01-SUMMARY.md

@src/presentation/cli/commands/evaluate.py
@src/infrastructure/adapters/repositories/json_result_repository.py
@src/domain/value_objects/quality_group.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI evaluate command display</name>
  <files>src/presentation/cli/commands/evaluate.py</files>
  <action>
  **Per-test-case panels (Group 3: Model Response Quality):**

  1. Replace the answer metrics block (~line 220-236):
     Old: Filters for `rm.name in ["answer_correctness", "answer_relevancy"]` and then separately for `rm.name == "hallucination"`
     New: Filter for `rm.name in ["factual_precision", "factual_recall", "answer_relevancy", "semantic_similarity"]`
     Display all four in the Model Response Quality group, each using `QualityGroup.get_display_name()`.
     Remove the separate hallucination_metrics block entirely.

  2. The order within the group should be: factual_precision, factual_recall, answer_relevancy, semantic_similarity (matches QualityGroup.metrics order), followed by LLM Judge (already rendered above these).

  **Per-Benchmark Quality Breakdown table:**

  Replace columns (line ~346-353):
  Old columns: `ctx_recall`, `ctx_precision`, `ctx_faith`, `halluc`, `ans_correct`, `ans_relev`, `LLM Judge`
  New columns: `ctx_recall`, `ctx_precision`, `ctx_faith`, `fct_prec`, `fct_recl`, `ans_relev`, `sem_sim`, `LLM Judge`

  Column style mapping:
  - `ctx_recall`, `ctx_precision` → yellow (Retrieval Quality)
  - `ctx_faith` → magenta (Model-RAG Grounding)
  - `fct_prec`, `fct_recl`, `ans_relev`, `sem_sim`, `LLM Judge` → cyan (Model Response Quality)

  Update the bench_table.add_row() call (~line 376-385) to extract the new display names:
  ```python
  format_metric(metrics_flat.get("RAGAs: context_recall")),
  format_metric(metrics_flat.get("RAGAs: context_precision")),
  format_metric(metrics_flat.get("RAGAs: context_faithfulness")),
  format_metric(metrics_flat.get("RAGAs: factual_precision")),
  format_metric(metrics_flat.get("RAGAs: factual_recall")),
  format_metric(metrics_flat.get("RAGAs: answer_relevancy")),
  format_metric(metrics_flat.get("RAGAs: semantic_similarity")),
  format_metric(metrics_flat.get("LLM Judge")),
  ```

  **Quality group legend (~line 336-339):**
  No changes needed — group names and colors remain the same.

  **Per-test title line (~line 142):**
  Current: `Bench: {score_str} | RAGAs: {ragas_str} | Combined: {combined_str}`
  New: `Bench: {score_str} | RAGAs: {ragas_str}`
  Remove the `Combined:` segment entirely. Combined score is being dropped.

  **Summary table (~line 400-402):**
  Current: 3 rows (Benchmark Score, RAGAs Score, Combined Score)
  New: 2 rows (Benchmark Score, RAGAs Score)
  Delete the `overall_table.add_row("[bold]Combined Score[/bold]", ...)` line.

  Remove all references to `combined_score`, `combined_str`, `combined_overall_score` from evaluate.py.
  </action>
  <verify>
  Run: `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -c "from presentation.cli.commands.evaluate import evaluate_app; print('CLI import OK')"`

  Grep to confirm no remaining references:
  `grep -n 'answer_correctness\|\"halluc' src/presentation/cli/commands/evaluate.py` — should find 0 matches.
  </verify>
  <done>
  CLI per-test panels show factual_precision, factual_recall, answer_relevancy, semantic_similarity (no answer_correctness, no hallucination).
  Per-benchmark table has 8 columns: ctx_recall, ctx_precision, ctx_faith, fct_prec, fct_recl, ans_relev, sem_sim, LLM Judge.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update JSON serialization and schema version</name>
  <files>src/infrastructure/adapters/repositories/json_result_repository.py</files>
  <action>
  **_serialize() method:**

  Replace the ragas_score extraction logic (~line 117-124):
  Old: Iterates over metrics looking for `metric.name == "answer_correctness"` to set `ragas_score`.
  New: Read from the domain entity's `ragas_composite_score` property (single source of truth — formula lives in `EvaluationResult`, not duplicated here):
  ```python
  ragas_score = result.ragas_composite_score
  ```
  That's it. The multiplicative penalty formula is computed in `EvaluationResult.ragas_composite_score` (added in Plan 01). Delete the entire old loop that searched for `answer_correctness`.

  **_build_grouped_ragas_structure() method:**

  1. Bump schema_version from 3 to 4.

  2. Update response_quality metric assignment (~line 236):
     Old: `metric.name in ["answer_correctness", "answer_relevancy", "hallucination"]`
     New: `metric.name in ["factual_precision", "factual_recall", "answer_relevancy", "semantic_similarity"]`

  3. Update the docstring:
     Old: `response_quality: answer_correctness, answer_relevancy, hallucination`
     New: `response_quality: factual_precision, factual_recall, answer_relevancy, semantic_similarity`

  4. Add backward compatibility note in docstring:
     `Note: schema_version 3 files use "answer_correctness" and "hallucination" instead of factual_precision/factual_recall/semantic_similarity`

  **_enrich_quality_categories_metadata() method:**
  No direct changes needed — it reads from QualityGroup.get_all_groups() dynamically, which is already updated in Plan 01.

  **_serialize() — also remove combined_score:**
  Delete the `combined_score` computation and the `"combined_score"` key from the serialized dict. Only `ragas_score` remains (alongside `score` which is the benchmark score).

  **No changes to _generate_filename().**
  </action>
  <verify>
  Run: `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -c "from infrastructure.adapters.repositories.json_result_repository import JSONResultRepository; print('JSON repo import OK')"`

  Grep: `grep -n 'schema_version.*4' src/infrastructure/adapters/repositories/json_result_repository.py` — should find at least 1 match.
  Grep: `grep -n 'answer_correctness' src/infrastructure/adapters/repositories/json_result_repository.py` — should find 0 matches in active code (only in comments about backward compat).
  </verify>
  <done>
  JSON schema version bumped to 4. response_quality group contains factual_precision, factual_recall, answer_relevancy, semantic_similarity. ragas_score reads from domain entity property (no formula duplication). No answer_correctness or hallucination in active metric mappings.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -c "from presentation.cli.commands.evaluate import evaluate_app; print('CLI OK')"`
2. `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -c "from infrastructure.adapters.repositories.json_result_repository import JSONResultRepository; print('JSON OK')"`
3. `grep -rn 'answer_correctness' src/presentation/cli/commands/evaluate.py src/infrastructure/adapters/repositories/json_result_repository.py` — no matches in active code (only backward compat comments allowed)
4. `grep -rn '"hallucination"' src/presentation/cli/commands/evaluate.py src/infrastructure/adapters/repositories/json_result_repository.py` — no matches
</verification>

<success_criteria>
- CLI panels show factual_precision, factual_recall, answer_relevancy, semantic_similarity in Model Response Quality group
- CLI benchmark table has fct_prec, fct_recl, sem_sim columns replacing halluc and ans_correct
- JSON schema_version = 4
- JSON response_quality group contains factual_precision, factual_recall, answer_relevancy, semantic_similarity
- JSON ragas_score reads from domain entity (no formula duplication)
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-ragas-metric-split-scoring-formula/02.3-02-SUMMARY.md`
</output>
