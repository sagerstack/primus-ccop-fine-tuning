---
status: testing
phase: 02.3-ragas-metric-split-scoring-formula
source: [02.3-01-SUMMARY.md, 02.3-02-SUMMARY.md, 02.3-03-SUMMARY.md]
started: 2026-03-21T10:00:00Z
updated: 2026-03-21T11:15:00Z
---

## Current Test

number: 1
name: Per-test panel shows new RAGAs metrics (hybrid mode)
expected: |
  Run: `cd src && poetry run ccop-eval evaluate run --benchmarks B3 --test-ids B3-001 --mode hybrid`
  Per-test panel "Model Response Quality" group shows factual_precision, factual_recall, answer_relevancy, semantic_similarity, LLM Judge. No answer_correctness or hallucination visible.
  Context metrics (Retrieval Quality, Model-RAG Grounding) should show actual scores (not N/A).
awaiting: user response

## Tests

### 1. Per-test panel shows new RAGAs metrics
expected: Run `cd src && poetry run ccop-eval evaluate run --benchmarks B3 --test-ids B3-001 --mode hybrid`. Per-test panel "Model Response Quality" group shows factual_precision, factual_recall, answer_relevancy, semantic_similarity, LLM Judge. No answer_correctness or hallucination visible.
result: retest
note: "LLM-only run confirmed metric names are correct (factual_precision, factual_recall, answer_relevancy, semantic_similarity, LLM Judge). No old metrics visible. Need hybrid run for full verification including context metrics."

### 2. Benchmark table has updated column headers
expected: In the same output, the Per-Benchmark Quality Breakdown table should have columns: ctx_recall, ctx_precision, ctx_faith, fct_prec, fct_recl, ans_relev, sem_sim, LLM Judge. No "halluc" or "ans_correct" columns.
result: passed
verified_by: "LLM-only run output shows exact columns: ctx_recall, ctx_precision, ctx_faith, fct_prec, fct_recl, ans_relev, sem_sim, LLM Judge"

### 3. Two overall scores (no Combined)
expected: The Evaluation Summary section shows exactly two overall scores: "Benchmark Score" and "RAGAs Score". No "Combined Score" row appears anywhere in the output.
result: passed
verified_by: "LLM-only run shows Benchmark Score: 0.33, RAGAs Score: 0.17. No Combined Score row."

### 4. Hallucinating response scores dramatically lower
expected: Run the same test in llm-only mode: `cd src && poetry run ccop-eval evaluate run --benchmarks B3 --test-ids B3-001 --mode llm-only`. The RAGAs Score should be noticeably lower than the hybrid mode RAGAs Score (typically < 0.20 for hallucinating llm-only vs > 0.50 for grounded hybrid). The multiplicative penalty should create a dramatic difference.
result: partial
note: "LLM-only RAGAs Score = 0.17 (< 0.20 threshold confirmed). Need hybrid run to compare."

### 5. LLM-only mode shows N/A for context metrics
expected: In the llm-only output, Retrieval Quality and Model-RAG Grounding groups should show N/A for all their metrics (context_recall, context_precision, context_faithfulness). But Model Response Quality should still show factual_precision, factual_recall, answer_relevancy, semantic_similarity with actual scores.
result: passed
verified_by: "Retrieval Quality: N/A (context_recall N/A, context_precision N/A). Model-RAG Grounding: N/A (context_faithfulness N/A). Model Response Quality: factual_precision 0.29, factual_recall 0.54, answer_relevancy 0.89, semantic_similarity 0.93."

### 6. JSON output contains new metric structure
expected: After a hybrid evaluation run (with results saved), inspect the latest JSON result file in `src/results/`. The JSON should contain: (a) `schema_version: 4`, (b) `response_quality` group with factual_precision, factual_recall, answer_relevancy, semantic_similarity, (c) `ragas_score` per test case (computed value, not null), (d) no `combined_score` field anywhere.
result: skipped
reason: Need hybrid evaluation run with save enabled

### 7. All 56 tests pass
expected: Run `cd src && poetry run python -m pytest tests/domain/services/test_ragas_evaluation_service.py tests/domain/value_objects/test_quality_group.py tests/application/use_cases/test_evaluate_model_metadata.py -v`. All 56 tests should pass with no failures.
result: passed
verified_by: automated (56 passed in 6.63s after bug fix)

## Summary

total: 7
passed: 4
issues: 0
pending: 2 (Tests 1, 4 need hybrid run)
skipped: 1 (Test 6 needs hybrid run with save)

## Gaps

- truth: "Per-test panel shows factual_precision, factual_recall, answer_relevancy, semantic_similarity, LLM Judge with valid scores"
  status: fix_applied
  reason: "Two runtime bugs fixed: (1) SemanticSimilarity embedding wrapping, (2) Score clamping for out-of-range values"
  severity: resolved
  test: 1
  root_cause: "(1) SemanticSimilarity(embeddings=raw_langchain) bypasses RAGAs wrapping; evaluate(embeddings=) does wrap. (2) RAGAs FactualCorrectness can produce out-of-range scores in edge cases."
  fix: "(1) Changed SemanticSimilarity() to use no embeddings= arg; evaluate() handles wrapping. (2) Added _clamp_score() to all 7 metric extractions."
  artifacts: [src/domain/services/ragas_evaluation_service.py]
