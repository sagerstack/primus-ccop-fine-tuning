---
phase: 01-rag-infrastructure
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - src/tests/retrieval/__init__.py
  - src/tests/retrieval/test_citation_resolver.py
  - src/tests/retrieval/test_graph_state.py
  - src/tests/ingestion/__init__.py
  - src/tests/ingestion/test_section_chunker.py
autonomous: false

must_haves:
  truths:
    - "CCoP compliance query returns response grounded in actual CCoP clauses with correct citations"
    - "General/out-of-scope query routes to fallback with model-only label"
    - "Self-correction loop activates when initial retrieval returns irrelevant results"
    - "End-to-end pipeline completes within reasonable time (<60 seconds for single query)"
    - "Citation references point to real CCoP sections that exist in the source documents"
    - "Failed retrievals are logged with diagnostic information"
    - "Existing evaluation framework is not broken by RAG additions"
  artifacts:
    - path: "src/tests/retrieval/test_citation_resolver.py"
      provides: "Unit tests for citation extraction, resolution, formatting"
      min_lines: 40
    - path: "src/tests/retrieval/test_graph_state.py"
      provides: "Unit tests for graph state initialization and validation"
      min_lines: 20
    - path: "src/tests/ingestion/test_section_chunker.py"
      provides: "Unit tests for section-level chunking logic"
      min_lines: 40
  key_links:
    - from: "src/tests/retrieval/test_citation_resolver.py"
      to: "src/retrieval/citations/resolver.py"
      via: "Tests citation extraction and resolution"
      pattern: "extract_citation_ids|resolve_citations"
    - from: "src/tests/ingestion/test_section_chunker.py"
      to: "src/ingestion/chunkers/section_chunker.py"
      via: "Tests chunking logic with sample markdown"
      pattern: "chunk_document|CcopChunk"
---

<objective>
Write unit tests for critical components and perform end-to-end verification of the RAG pipeline with human inspection.

Purpose: This is the quality gate before Phase 1 is complete. Unit tests verify citation resolution and chunking logic. End-to-end verification with human inspection confirms the pipeline returns relevant, correctly-cited CCoP compliance answers. This is the acceptance test for all 6 RAG requirements.

Output: Passing unit tests, end-to-end verification results demonstrating all RAG requirements met, and human approval of output quality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-rag-infrastructure/01-CONTEXT.md
@.planning/phases/01-rag-infrastructure/01-04-SUMMARY.md
@src/retrieval/citations/resolver.py
@src/retrieval/citations/formatter.py
@src/retrieval/state/graph_state.py
@src/ingestion/chunkers/section_chunker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write unit tests for citation resolver and section chunker</name>
  <files>
    src/tests/retrieval/__init__.py
    src/tests/retrieval/test_citation_resolver.py
    src/tests/retrieval/test_graph_state.py
    src/tests/ingestion/__init__.py
    src/tests/ingestion/test_section_chunker.py
  </files>
  <action>
    1. Create `src/tests/retrieval/__init__.py` and `src/tests/ingestion/__init__.py` (empty)

    2. Create `src/tests/retrieval/test_citation_resolver.py`:
       - Test `extract_citation_ids`:
         * Single anchor: `"<c>CCoP 2.0.5.5.2.1</c>"` -> `["CCoP 2.0.5.5.2.1"]`
         * Multiple anchors: text with 3 anchors -> list of 3 IDs
         * No anchors: plain text -> empty list
         * Duplicate anchors: same ID twice -> single ID (deduplicated)
         * Nested/malformed anchors: handles gracefully

       - Test `resolve_citations`:
         * Valid resolution: citation_id matches document metadata -> returns resolved dict
         * Missing citation: ID not in documents -> skipped (no crash)
         * Empty documents list -> empty result
         * Multiple citations resolved correctly with all metadata fields

       - Test `format_response_with_citations`:
         * With citations: produces clean text + "References:" section
         * Anchor removal: `<c>...</c>` tags stripped from response text
         * No citations: returns text as-is, no "References:" section
         * Multiple citations numbered correctly [1], [2], [3]

       - Test `format_model_only_response`:
         * Prepends notice about model-only response
         * No references section

    3. Create `src/tests/retrieval/test_graph_state.py`:
       - Test GraphState initialization with required fields
       - Test default values for optional fields
       - Test state can hold Document objects

    4. Create `src/tests/ingestion/test_section_chunker.py`:
       - Test `chunk_document` with sample markdown:
         * Create a small sample markdown string with ## and ### headers
         * Verify chunks split at header boundaries
         * Verify metadata extraction (section, subsection, clause)
         * Verify citation_id format

       - Test size constraints:
         * Small section (< 200 tokens) merged with preceding chunk
         * Large section (> 1000 tokens) split on paragraph boundaries

       - Test `chunk_qa_pairs`:
         * QAPair converted to CcopChunk correctly
         * document_type="clarification" in metadata
         * Text formatted as "Q: ... A: ..."

       - Test clause extraction:
         * "Section 5.2.1 requires..." -> clause "5.2.1"
         * No clause number -> empty string

    5. Run all tests: `cd src && poetry run pytest tests/ -v`

    AVOID: Do NOT mock Databricks or Ollama for unit tests — these tests cover pure logic (citation parsing, chunking). Integration testing with live services is the human-verify checkpoint.
  </action>
  <verify>
    - `cd src && poetry run pytest tests/retrieval/ -v` all pass
    - `cd src && poetry run pytest tests/ingestion/ -v` all pass
    - `cd src && poetry run pytest tests/ -v` all pass (including existing tests)
    - Coverage report shows >90% for citation resolver and section chunker
  </verify>
  <done>
    Unit tests pass for citation extraction/resolution/formatting and section-level chunking. All existing tests still pass. Coverage >90% for tested modules.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete RAG infrastructure for CCoP 2.0 compliance queries:
    1. PDF parsing of all 8 CCoP documents with structure preservation
    2. Section-level chunking with metadata (document, section, clause, citation_id)
    3. Databricks Vector Search hybrid index with BGE embeddings and reranking
    4. LangGraph adaptive RAG graph (query analysis -> retrieval -> grading -> generation/fallback)
    5. Citation extraction and end-of-response reference formatting
    6. Clean Architecture integration (port/adapter/DI/CLI)
  </what-built>
  <how-to-verify>
    Run these commands and inspect the output:

    1. **Factual compliance query (should use RAG):**
       ```
       cd src && poetry run ccop-eval query "What are the access control requirements for CII under CCoP 2.0?"
       ```
       Expected: Response cites specific CCoP 2.0 sections (Section 5 or similar), shows "RAG-augmented: yes", has numbered references at bottom.

    2. **Specific clause lookup (should use RAG):**
       ```
       cd src && poetry run ccop-eval query "What does CCoP 2.0 say about incident reporting timelines?"
       ```
       Expected: Response references specific clauses about incident reporting, citations point to real CCoP sections.

    3. **Cross-document query (should retrieve from supplementary docs):**
       ```
       cd src && poetry run ccop-eval query "How should a CII owner conduct a cybersecurity risk assessment?"
       ```
       Expected: Response pulls from both main CCoP and Risk Assessment Guide, citations reference both documents.

    4. **General question (should fallback to model-only):**
       ```
       cd src && poetry run ccop-eval query "What is cybersecurity?"
       ```
       Expected: Response shows "RAG-augmented: no", includes notice about model-only response, no citations.

    5. **Verify existing commands still work:**
       ```
       cd src && poetry run ccop-eval --help
       ```
       Expected: Shows evaluate, setup, report, AND query commands.

    6. **Check citation accuracy (manual):**
       Pick 2-3 citations from the responses above. Open the actual CCoP PDF and verify the cited section/clause exists and is relevant to the response content.

    Quality criteria:
    - Responses are coherent and relevant to the query
    - Citations point to real CCoP sections (not hallucinated)
    - RAG-augmented vs model-only correctly indicated
    - Response format is clean (no raw `<c>...</c>` tags visible)
    - Reasonable response time (< 60 seconds per query)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to address.</resume-signal>
</task>

</tasks>

<verification>
- All unit tests pass with >90% coverage on citation resolver and section chunker
- End-to-end RAG pipeline handles compliance queries, general queries, and cross-document queries
- Citations are accurate (verified against actual CCoP documents)
- Response quality acceptable for compliance use case
- Existing evaluation framework unaffected
- All 6 RAG requirements (RAG-01 through RAG-06) verified
</verification>

<success_criteria>
**RAG-01 (Document Ingestion):** All 8 CCoP PDFs ingested with structure preservation
**RAG-02 (Vector Storage):** Chunks in Databricks Vector Search with metadata (section, clause, document source)
**RAG-03 (Retrieval):** Queries return relevant CCoP clauses for compliance queries
**RAG-04 (Re-ranking):** Hybrid search with built-in reranking on Databricks Vector Search
**RAG-05 (Citation Extraction):** End-of-response references link to document + section + clause
**RAG-06 (Clean Architecture):** RAG as IRagPipeline port with LangGraph adapter, wired through DI container
</success_criteria>

<output>
After completion, create `.planning/phases/01-rag-infrastructure/01-05-SUMMARY.md`
</output>
