---
phase: 01.2-local-rag-migration
plan: 05
type: execute
wave: 4
depends_on: ["01.2-04"]
files_modified:
  - tests/rag/__init__.py
  - tests/rag/test_embedding_service.py
  - tests/rag/test_qdrant_vector_store_adapter.py
  - tests/rag/test_qdrant_indexer_adapter.py
  - tests/rag/test_port_adapters.py
  - tests/rag/test_container_vector_store.py
autonomous: false

must_haves:
  truths:
    - "All 8 CCoP documents re-ingested into Qdrant with identical metadata schema"
    - "Sample retrieval queries return relevant CCoP clauses from Qdrant"
    - "Unit tests verify EmbeddingService, adapter contracts, and DI container selection"
    - "E2E verification confirms Qdrant-based pipeline returns results comparable to expectations"
  artifacts:
    - path: "tests/rag/test_embedding_service.py"
      provides: "EmbeddingService unit tests"
      contains: "test_embed_query"
    - path: "tests/rag/test_qdrant_vector_store_adapter.py"
      provides: "QdrantVectorStoreAdapter tests"
      contains: "test_similarity_search"
    - path: "tests/rag/test_qdrant_indexer_adapter.py"
      provides: "QdrantIndexerAdapter tests"
      contains: "test_index_chunks"
    - path: "tests/rag/test_port_adapters.py"
      provides: "Port contract verification tests"
      contains: "test_qdrant_implements_ivectorstore"
    - path: "tests/rag/test_container_vector_store.py"
      provides: "Container integration tests"
      contains: "test_container_selects_qdrant"
  key_links:
    - from: "tests/rag/test_embedding_service.py"
      to: "src/rag/infrastructure/adapters/qdrant/embedding_service.py"
      via: "tests EmbeddingService"
      pattern: "EmbeddingService"
    - from: "tests/rag/test_qdrant_vector_store_adapter.py"
      to: "src/rag/infrastructure/adapters/qdrant/qdrant_vector_store_adapter.py"
      via: "tests QdrantVectorStoreAdapter"
      pattern: "QdrantVectorStoreAdapter"
---

<objective>
Re-ingest all 8 CCoP documents into Qdrant, write unit tests for new components, and verify end-to-end retrieval quality.

Purpose: Validates the entire local RAG migration by populating Qdrant and running quality checks. Tests ensure components meet port contracts and function correctly. Human verification confirms retrieval quality matches expectations.
Output: Populated Qdrant collection, test suite for local RAG components, E2E verification results.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-local-rag-migration/01.2-CONTEXT.md
@.planning/phases/01.2-local-rag-migration/01.2-RESEARCH.md
@.planning/phases/01.2-local-rag-migration/01.2-01-SUMMARY.md
@.planning/phases/01.2-local-rag-migration/01.2-02-SUMMARY.md
@.planning/phases/01.2-local-rag-migration/01.2-03-SUMMARY.md
@.planning/phases/01.2-local-rag-migration/01.2-04-SUMMARY.md

@src/rag/infrastructure/adapters/qdrant/embedding_service.py
@src/rag/infrastructure/adapters/qdrant/qdrant_vector_store_adapter.py
@src/rag/infrastructure/adapters/qdrant/qdrant_indexer_adapter.py
@src/rag/domain/ports/i_vector_store.py
@src/rag/domain/ports/i_indexer.py
@src/infrastructure/config/container.py
@src/rag/ingestion/run_ingestion.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-ingest CCoP documents into Qdrant</name>
  <files>(no new files -- uses existing ingestion orchestrator)</files>
  <action>
    Run the ingestion pipeline to populate Qdrant with all 8 CCoP documents.

    **Pre-requisites:**
    1. Ensure Qdrant is running: `docker compose up -d`
    2. Verify Qdrant health: `curl http://localhost:6333/healthz`
    3. Install new dependencies: `cd src && poetry install` (to get qdrant-client, fastembed)

    **Execute ingestion:**
    ```bash
    cd src
    python -m rag.ingestion.run_ingestion --ccop-dir ../ccop-official
    ```

    This will:
    - Parse all 8 CCoP PDFs (reuses existing parsers)
    - Chunk with section-level semantic splitting (reuses existing chunker)
    - Create Qdrant collection with dense (1024-dim COSINE) + sparse (IDF) vectors
    - Generate embeddings locally via sentence-transformers + FastEmbed
    - Upload all chunks to Qdrant
    - Verify with sample query

    **Expected output:**
    - 8 documents parsed
    - ~800-1200 chunks created (based on Phase 1 numbers)
    - Collection created and populated
    - Sample query returns relevant results

    **Verification queries** (run manually after ingestion):
    ```bash
    cd src
    python -c "
    from infrastructure.config.container import get_container
    container = get_container()
    vs = container.vector_store()
    results = vs.similarity_search_with_scores('What are the access control requirements for CII?', k=5)
    for doc, score in results:
        print(f'Score: {score:.3f} | {doc.metadata.get(\"citation_id\", \"\")} | {doc.metadata.get(\"section\", \"\")}')
        print(f'  {doc.page_content[:100]}...')
        print()
    "
    ```

    Run at least 3 verification queries:
    1. "What are the access control requirements for CII?" (factual, section 5)
    2. "How should incident response plans be tested?" (reasoning, section 7)
    3. "What are the network security requirements?" (factual, section 6)

    Log all verification results for the summary.
  </action>
  <verify>
    - Qdrant collection exists: `curl http://localhost:6333/collections/ccop_clauses_hybrid | python -m json.tool` shows point_count > 0
    - All 8 documents represented in collection
    - Sample queries return relevant results with scores > 0
  </verify>
  <done>
    All 8 CCoP documents ingested into Qdrant. Collection has correct dense + sparse vector config. Verification queries return relevant results.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for local RAG components</name>
  <files>
    tests/rag/__init__.py
    tests/rag/test_embedding_service.py
    tests/rag/test_qdrant_vector_store_adapter.py
    tests/rag/test_qdrant_indexer_adapter.py
    tests/rag/test_port_adapters.py
    tests/rag/test_container_vector_store.py
  </files>
  <action>
    Create `tests/rag/` directory with `__init__.py` and test files.

    **test_embedding_service.py:**
    Tests for EmbeddingService (use real models for integration, mock for unit):
    - `test_embed_query_returns_1024_dim_vector`: Verify output is list of 1024 floats
    - `test_embed_query_prepends_bge_prompt`: Verify query prompt is prepended (mock model, check input)
    - `test_embed_documents_no_prompt_prefix`: Verify documents don't get query prompt
    - `test_embed_documents_batch_returns_list`: Verify batch returns list of lists
    - `test_embed_sparse_returns_indices_and_values`: Verify sparse dict format
    - `test_lazy_initialization`: Verify models not loaded until first embed call
    Mark integration tests (requiring model downloads) with `@pytest.mark.integration`

    **test_qdrant_vector_store_adapter.py:**
    Tests using mocked QdrantClient:
    - `test_similarity_search_calls_query_points`: Mock client, verify query_points called with correct prefetch (dense + sparse) and RRF fusion
    - `test_results_converted_to_documents`: Mock client response, verify Document objects with correct page_content and metadata
    - `test_filter_translated_to_qdrant_format`: Pass dict filter, verify Filter/FieldCondition construction
    - `test_empty_results_returns_empty_list`: Mock empty response, verify empty list returned

    **test_qdrant_indexer_adapter.py:**
    Tests using mocked QdrantClient and EmbeddingService:
    - `test_index_chunks_creates_collection`: Mock client, verify create_collection called with correct vector config (1024 dim, COSINE, IDF modifier)
    - `test_index_chunks_upserts_points`: Mock client + embeddings, verify upsert called with PointStruct containing both dense and sparse vectors
    - `test_index_chunks_preserves_metadata`: Verify payload contains all metadata fields
    - `test_verify_index_returns_stats`: Mock client collection info, verify return dict

    **test_port_adapters.py:**
    Contract tests verifying port compliance:
    - `test_qdrant_implements_ivectorstore`: `assert issubclass(QdrantVectorStoreAdapter, IVectorStore)`
    - `test_qdrant_implements_iindexer`: `assert issubclass(QdrantIndexerAdapter, IIndexer)`
    - `test_databricks_implements_ivectorstore`: `assert issubclass(DatabricksVectorStoreAdapter, IVectorStore)`
    - `test_databricks_implements_iindexer`: `assert issubclass(DatabricksIndexerAdapter, IIndexer)`

    **test_container_vector_store.py:**
    Tests for DI container adapter selection (mock settings):
    - `test_container_selects_qdrant_when_qdrant_url_set`: Patch settings with qdrant_url, verify QdrantVectorStoreAdapter returned
    - `test_container_selects_databricks_when_only_databricks_set`: Patch settings with databricks fields, verify DatabricksVectorStoreAdapter returned
    - `test_container_returns_none_when_nothing_configured`: Patch settings with neither, verify None returned
    - `test_container_prefers_qdrant_over_databricks`: Patch settings with both, verify Qdrant selected

    Use pytest-mock for all mocking. Follow existing test patterns from `tests/` directory.
  </action>
  <verify>
    - `cd src && python -m pytest ../tests/rag/ -v` -- all tests pass
    - `cd src && python -m pytest ../tests/ -v` -- all existing tests still pass (regression check)
  </verify>
  <done>
    Unit tests cover EmbeddingService, both Qdrant adapters, port contract compliance, and DI container selection. All tests pass. Existing tests unaffected.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete local RAG migration:
    1. Qdrant running locally via Docker with persistent storage
    2. All 8 CCoP documents ingested into Qdrant with dense + sparse vectors
    3. Port/adapter abstractions (IVectorStore, IIndexer) with Qdrant and Databricks implementations
    4. DI container selects adapter based on config
    5. Retrieval node and ingestion orchestrator use ports
    6. Unit tests for all new components
  </what-built>
  <how-to-verify>
    1. Run the CLI query to test end-to-end:
       ```bash
       cd src
       ccop-eval query ask "What are the CCoP requirements for access control?" --verbose
       ```
       Expected: Response with relevant CCoP clauses and citations from Qdrant

    2. Verify Qdrant collection stats:
       ```bash
       curl http://localhost:6333/collections/ccop_clauses_hybrid | python -m json.tool
       ```
       Expected: point_count > 800

    3. Run all tests:
       ```bash
       cd src && python -m pytest ../tests/ -v
       ```
       Expected: All tests pass (existing + new RAG tests)

    4. Manual retrieval quality check -- compare results for 2-3 queries:
       - Are the top results relevant to the query?
       - Do citation_ids and sections make sense?
       - Are scores reasonable (not all 0 or all 1)?
  </how-to-verify>
  <resume-signal>Type "approved" if retrieval quality is acceptable, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Qdrant collection populated with all 8 CCoP documents
2. Point count matches expected chunk count from Phase 1
3. Verification queries return relevant results
4. All unit tests pass
5. All existing tests pass (no regressions)
6. CLI query command works end-to-end through Qdrant
7. Human approves retrieval quality
</verification>

<success_criteria>
- All 8 CCoP documents ingested into Qdrant with identical metadata schema
- Unit tests cover EmbeddingService, adapters, ports, and container
- All tests pass (new + existing)
- CLI query returns relevant results via Qdrant pipeline
- Human verifies retrieval quality is acceptable
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-local-rag-migration/01.2-05-SUMMARY.md`
</output>
