---
phase: 01.2-local-rag-migration
plan: 03
type: execute
wave: 2
depends_on: ["01.2-01"]
files_modified:
  - src/rag/infrastructure/adapters/databricks/__init__.py
  - src/rag/infrastructure/adapters/databricks/databricks_vector_store_adapter.py
  - src/rag/infrastructure/adapters/databricks/databricks_indexer_adapter.py
autonomous: true

must_haves:
  truths:
    - "DatabricksVectorStoreAdapter wraps existing DatabricksVectorSearch retrieval behind IVectorStore port"
    - "DatabricksIndexerAdapter wraps existing DatabricksIndexer behind IIndexer port"
    - "Existing Databricks code is NOT modified -- wrappers delegate to existing classes"
    - "Both adapters handle missing Databricks config gracefully (raise clear errors)"
  artifacts:
    - path: "src/rag/infrastructure/adapters/databricks/databricks_vector_store_adapter.py"
      provides: "Databricks IVectorStore wrapper"
      contains: "class DatabricksVectorStoreAdapter"
    - path: "src/rag/infrastructure/adapters/databricks/databricks_indexer_adapter.py"
      provides: "Databricks IIndexer wrapper"
      contains: "class DatabricksIndexerAdapter"
  key_links:
    - from: "src/rag/infrastructure/adapters/databricks/databricks_vector_store_adapter.py"
      to: "src/rag/domain/ports/i_vector_store.py"
      via: "implements IVectorStore"
      pattern: "IVectorStore"
    - from: "src/rag/infrastructure/adapters/databricks/databricks_indexer_adapter.py"
      to: "src/rag/domain/ports/i_indexer.py"
      via: "implements IIndexer"
      pattern: "IIndexer"
    - from: "src/rag/infrastructure/adapters/databricks/databricks_vector_store_adapter.py"
      to: "databricks_langchain.DatabricksVectorSearch"
      via: "wraps existing retriever"
      pattern: "DatabricksVectorSearch"
    - from: "src/rag/infrastructure/adapters/databricks/databricks_indexer_adapter.py"
      to: "src/rag/ingestion/indexers/databricks_indexer.py"
      via: "wraps existing DatabricksIndexer"
      pattern: "DatabricksIndexer"
---

<objective>
Wrap existing Databricks vector search and indexing code behind the IVectorStore and IIndexer port interfaces.

Purpose: Preserves Databricks as an alternate adapter without modifying existing code. The wrappers delegate to the original DatabricksVectorSearch retriever and DatabricksIndexer, adapting their interfaces to match the new ports.
Output: Two thin wrapper adapters in a new databricks/ subdirectory under infrastructure/adapters.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-local-rag-migration/01.2-CONTEXT.md
@.planning/phases/01.2-local-rag-migration/01.2-01-SUMMARY.md

@src/rag/domain/ports/i_vector_store.py
@src/rag/domain/ports/i_indexer.py
@src/rag/retrieval/nodes/retrieval.py
@src/rag/ingestion/indexers/databricks_indexer.py
@src/infrastructure/config/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DatabricksVectorStoreAdapter</name>
  <files>
    src/rag/infrastructure/adapters/databricks/__init__.py
    src/rag/infrastructure/adapters/databricks/databricks_vector_store_adapter.py
  </files>
  <action>
    Create `src/rag/infrastructure/adapters/databricks/` directory with `__init__.py`.

    **DatabricksVectorStoreAdapter** wraps the existing `DatabricksVectorSearch` retriever (from `databricks_langchain`) behind `IVectorStore`.

    **Constructor:**
    ```
    __init__(self, settings: "Settings")
    ```
    - Use `TYPE_CHECKING` pattern for Settings import (same as LangGraphRagAdapter)
    - Lazy initialization: don't create retriever in __init__
    - Store settings, `_retriever = None`, `_initialized = False`

    **_ensure_initialized method:**
    - Validate Databricks config (host, token, vector_search_endpoint) -- raise ValueError if missing
    - Set `DATABRICKS_HOST` and `DATABRICKS_TOKEN` env vars (same pattern as existing retrieval.py lines 52-53)
    - Construct index name: `{catalog}.{schema}.ccop_clauses_hybrid`
    - Create `DatabricksVectorSearch` with same params as existing retrieval.py lines 66-77
    - Set `_initialized = True`

    **similarity_search_with_scores method:**
    - Call `_ensure_initialized()`
    - Call `self._retriever.similarity_search_with_relevance_scores(query=query, k=k)`
    - Return list of (Document, score) tuples (already in correct format from Databricks)
    - Note: `filter` parameter is accepted but logged as warning if provided (Databricks handles filtering differently through the index config). This preserves port compatibility.

    **Important:** This is a THIN wrapper. Extract the retriever initialization logic from `src/rag/retrieval/nodes/retrieval.py` (lines 23-81) into this adapter. The retrieval.py file will be refactored in Plan 04 to use IVectorStore instead.
  </action>
  <verify>
    - `python -c "from rag.infrastructure.adapters.databricks.databricks_vector_store_adapter import DatabricksVectorStoreAdapter; print('imported')"` from src/
    - Verify class has similarity_search_with_scores method
  </verify>
  <done>
    DatabricksVectorStoreAdapter wraps existing DatabricksVectorSearch behind IVectorStore port with lazy initialization and same config validation as existing retrieval node.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DatabricksIndexerAdapter</name>
  <files>src/rag/infrastructure/adapters/databricks/databricks_indexer_adapter.py</files>
  <action>
    **DatabricksIndexerAdapter** wraps the existing `DatabricksIndexer` (from `src/rag/ingestion/indexers/databricks_indexer.py`) behind `IIndexer`.

    **Constructor:**
    ```
    __init__(self, settings: "Settings")
    ```
    - Use `TYPE_CHECKING` pattern for Settings import
    - Lazy initialization: don't create DatabricksIndexer in __init__
    - Store settings, `_indexer = None`, `_initialized = False`

    **_ensure_initialized method:**
    - Create `DatabricksIndexer(self.settings)` (existing class handles its own validation)
    - Set `_initialized = True`

    **index_chunks method:**
    - Call `_ensure_initialized()`
    - Delegate to `self._indexer.index_chunks(chunks)` -- returns index name
    - Return the index name string

    **verify_index method:**
    - Call `_ensure_initialized()`
    - Delegate to `self._indexer.verify_index(index_name, sample_query)`
    - Return the verification dict

    This is a VERY thin wrapper -- almost a direct delegation. The value is having both Databricks and Qdrant behind the same IIndexer interface so the ingestion orchestrator doesn't need to know which backend it's using.
  </action>
  <verify>
    - `python -c "from rag.infrastructure.adapters.databricks.databricks_indexer_adapter import DatabricksIndexerAdapter; print('imported')"` from src/
    - Verify class has index_chunks and verify_index methods
  </verify>
  <done>
    DatabricksIndexerAdapter wraps existing DatabricksIndexer behind IIndexer port with lazy initialization and delegation pattern.
  </done>
</task>

</tasks>

<verification>
1. Both Databricks wrapper adapters importable without errors
2. DatabricksVectorStoreAdapter.similarity_search_with_scores delegates to DatabricksVectorSearch
3. DatabricksIndexerAdapter.index_chunks delegates to DatabricksIndexer.index_chunks
4. Existing DatabricksIndexer code NOT modified (only wrapped)
5. Existing retrieval.py code NOT modified yet (that's Plan 04)
6. Both adapters implement their respective port interfaces
</verification>

<success_criteria>
- DatabricksVectorStoreAdapter wraps existing retriever behind IVectorStore
- DatabricksIndexerAdapter wraps existing indexer behind IIndexer
- Both adapters use lazy initialization and TYPE_CHECKING patterns
- No modifications to existing Databricks code
- Both importable without Databricks credentials (lazy init)
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-local-rag-migration/01.2-03-SUMMARY.md`
</output>
