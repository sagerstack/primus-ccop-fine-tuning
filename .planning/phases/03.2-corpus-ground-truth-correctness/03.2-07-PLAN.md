---
phase: 03.2-corpus-ground-truth-correctness
plan: 07
type: execute
wave: 7
depends_on: ["03.2-06"]
files_modified:
  - ground-truth/schema/validate.py
  - src/presentation/cli/commands/validate_ground_truth.py
  - src/presentation/cli/main.py
  - src/tests/presentation/cli/test_validate_ground_truth.py
  - src/infrastructure/adapters/repositories/jsonl_test_case_repository.py
autonomous: true

must_haves:
  truths:
    - "Validator extended to enforce clause_reference correctness at load/validation time (hard-fail)"
    - "Validator also enforces semantic mismatch gate (hard-fail) — same script, same non-zero exit"
    - "A new CLI command `ccop-eval validate-ground-truth` wraps the validator"
    - "Deprecated test cases (status='deprecated') are skipped by the eval pipeline and not flagged by the validator"
    - "The validator is manual-run only — NOT wired into CI, NOT in pre-commit hook"
  artifacts:
    - path: "ground-truth/schema/validate.py"
      provides: "Extended validator with clause_reference ID-existence + semantic-mismatch hard-fails"
    - path: "src/presentation/cli/commands/validate_ground_truth.py"
      provides: "Typer command exposing the validator via ccop-eval"
    - path: "src/tests/presentation/cli/test_validate_ground_truth.py"
      provides: "Tests: validator passes on clean v2 test suite, fails on synthetic bad-clause fixture"
  key_links:
    - from: "validate.py"
      to: "clause_inventory.json"
      via: "json.load of fixtures path"
      pattern: "clause_inventory\\.json"
    - from: "presentation/cli/main.py"
      to: "validate_ground_truth command"
      via: "app.add_typer"
      pattern: "validate-ground-truth|validate_ground_truth"
    - from: "JSONL repository"
      to: "deprecated-case skip"
      via: "filter on status field"
      pattern: "deprecated"
---

<objective>
Lock in the corrections from Plan 06 by extending the ground-truth validator to hard-fail on invalid clause references AND on semantic mismatches, wire it as a `ccop-eval validate-ground-truth` CLI command, and make the eval pipeline skip deprecated test cases.

Purpose: CONTEXT.md locks "hard-fail after audit complete" — by Plan 07, every surviving test case passes the checks, so the validator is green on clean ground truth and stays red for any future drift. Manual-run only per locked decision (NOT CI, NOT pre-commit).
Output: Extended validator + CLI wrapper + eval pipeline skip logic for deprecated cases + tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03.2-corpus-ground-truth-correctness/03.2-CONTEXT.md
@.planning/phases/03.2-corpus-ground-truth-correctness/03.2-06-SUMMARY.md
@ground-truth/schema/validate.py
@src/presentation/cli/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ground-truth validator with clause_reference + semantic mismatch hard-fails</name>
  <files>ground-truth/schema/validate.py, src/tests/presentation/cli/test_validate_ground_truth.py</files>
  <action>
  Extend `ground-truth/schema/validate.py`:

  1. Add `load_clause_inventory(fixtures_path)` helper — loads `src/rag/ingestion/fixtures/clause_inventory.json` and returns a set of valid `(clause_id, source_doc)` tuples.

  2. Extend `validate_business_rules` (or add a new `validate_clause_references` function called from `validate_file`):
     - For each `metadata.clause_reference` entry, check `(clause, "CCoP 2.0")` in inventory. If not found, append to `errors` (hard-fail) with message `f"{test_id}: clause_reference '{clause}' not found in inventory"`.
     - Extract inline citations from `ground_truth.expected_response` via regex `r"\b(\d+(?:\.\d+)+(?:\([a-z]\))?)\b"`, filter to those looking like clause refs (>=2 dot-segments or item-letter). Check each against inventory; hard-fail if missing.

  3. Add `--semantic-check` flag (opt-in via CLI, but default to TRUE for the Phase 3.2 validator command — per CONTEXT.md "both the clause ID check and the semantic mismatch check are enforced by the same validator"). When enabled:
     - Requires Qdrant availability (get URL from settings). If Qdrant unreachable, print warning and skip (semantic check is best-effort; don't block entire validation on infra). Default threshold same as Plan 06 (`0.35`).
     - For each clause in `clause_reference`, scroll Qdrant for the chunk at that citation_id. Embed `expected_response` + chunk text via the shared EmbeddingService, compute cosine, hard-fail if below threshold.

  4. **Skip deprecated test cases**: at top of `validate_file`, after parsing the JSON object, `if test_case.get("status") == "deprecated": continue`. Log each skipped test_id as INFO.

  5. Preserve existing JSON Schema + business rules unchanged. Keep existing `--strict` flag behavior.

  Write tests in `src/tests/presentation/cli/test_validate_ground_truth.py`:
  - `test_validator_accepts_clean_ground_truth`: run against the corrected `ground-truth/test-suite/` — expect exit 0.
  - `test_validator_rejects_hallucinated_clause`: fixture test case with `clause_reference: ["9.9.9"]` — expect non-zero exit and error message containing "not found in inventory".
  - `test_validator_skips_deprecated_cases`: fixture with `status: "deprecated"` and an invalid clause — expect exit 0 (deprecated bypasses).
  - `test_validator_rejects_inline_citation_hallucination`: fixture with a hallucinated clause inside `expected_response` text — expect non-zero exit.
  </action>
  <verify>
  `cd src && poetry run pytest tests/presentation/cli/test_validate_ground_truth.py -v` — all 4 tests pass.
  Live run: `poetry run python ground-truth/schema/validate.py` exits 0 (post Plan 06 corrections) with the clean ground truth.
  </verify>
  <done>
  Validator hard-fails on any invalid citation. Deprecated cases bypass. SC #17 satisfied for the ID + semantic gates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ccop-eval validate-ground-truth CLI command + eval pipeline deprecated-case skip</name>
  <files>src/presentation/cli/commands/validate_ground_truth.py, src/presentation/cli/main.py, src/infrastructure/adapters/repositories/jsonl_test_case_repository.py</files>
  <action>
  1. Create `src/presentation/cli/commands/validate_ground_truth.py` exposing a Typer subcommand:
     - Option: `--test-suite-dir` (default `ground-truth/test-suite`)
     - Option: `--schema` (default `ground-truth/schema/test-case-v2.schema.json`)
     - Option: `--inventory` (default `src/rag/ingestion/fixtures/clause_inventory.json`)
     - Option: `--no-semantic` (default False; semantic gate ON)
     - Option: `--strict` (treat warnings as errors; preserves existing flag)
     - Delegates to the validator functions from Task 1. Exit code propagates non-zero on any error.

  2. Wire into `src/presentation/cli/main.py`:
     ```python
     from presentation.cli.commands.validate_ground_truth import validate_app
     app.add_typer(validate_app, name="validate-ground-truth", help="Validate v2 ground-truth JSONL files against the clause inventory")
     ```

  3. In `src/infrastructure/adapters/repositories/jsonl_test_case_repository.py` (the JSONL repository loading v2 test cases for eval), add a filter: after parsing, skip test cases where `status == "deprecated"`. Log the skip. This ensures eval runs don't score against deprecated cases.

  4. Explicitly DO NOT modify any `.github/` workflows, pre-commit config, or CI hooks — CONTEXT.md locks this as manual-run only.
  </action>
  <verify>
  `cd src && poetry run ccop-eval validate-ground-truth --help` shows the command.
  `cd src && poetry run ccop-eval validate-ground-truth` exits 0 on clean ground truth.
  Manually introduce a hallucinated clause into a test case file temporarily, run the command, verify non-zero exit and clear error message, then revert the file.
  `grep -c "deprecated" src/infrastructure/adapters/repositories/jsonl_test_case_repository.py` >= 1.
  `grep -r "validate-ground-truth" .github/` returns no results (confirming manual-run only).
  </verify>
  <done>
  CLI command wired. Eval pipeline skips deprecated cases. SC #17 fully satisfied. Manual-only enforcement respected.
  </done>
</task>

</tasks>

<verification>
Running `poetry run ccop-eval validate-ground-truth` on clean ground truth exits 0. Introducing a hallucinated clause causes non-zero exit. Deprecated cases are bypassed by both validator AND eval pipeline. No CI integration added.
</verification>

<success_criteria>
- SC #17: Validator hard-fails on invalid references; enforces both ID existence AND semantic mismatch gates
- CLI command `ccop-eval validate-ground-truth` available and documented in --help
- Eval pipeline respects `status="deprecated"` flag
- Validator is manual-run only (no CI/pre-commit integration)
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-corpus-ground-truth-correctness/03.2-07-SUMMARY.md` per template. This is the final plan of Phase 3.2 — summary should explicitly state sub-goal B complete and the phase ready for gsd-verifier.
</output>
