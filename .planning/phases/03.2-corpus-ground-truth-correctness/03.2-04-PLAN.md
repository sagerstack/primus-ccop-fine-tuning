---
phase: 03.2-corpus-ground-truth-correctness
plan: 04
type: execute
wave: 4
depends_on: ["03.2-03"]
files_modified:
  - .planning/phases/03.2-corpus-ground-truth-correctness/b3-001-rerun-evidence.md
autonomous: false

must_haves:
  truths:
    - "B3-001 hybrid re-evaluation produces RAGAs context_recall > 0"
    - "Retrieved contexts for B3-001 include at least one chunk from section 5.3 (under document_source='CCoP 2.0')"
  artifacts:
    - path: ".planning/phases/03.2-corpus-ground-truth-correctness/b3-001-rerun-evidence.md"
      provides: "Run evidence: run_id, eval JSON path, context_recall value, retrieved citations"
  key_links:
    - from: "ccop-eval evaluate run --test-ids B3-001 --mode hybrid"
      to: "EvaluationResult with context_recall > 0"
      via: "Qdrant hybrid retrieval + RAGAs scoring"
      pattern: "context_recall.*[1-9]"
---

<objective>
Prove sub-goal A end-to-end by re-running the canonical failing case (B3-001) in hybrid mode and demonstrating RAGAs `context_recall` is now non-zero. This is the smoking-gun success gate for the corpus fix.

Purpose: Bug #10's observable defect was exactly `context_recall=0` on B3-001 because sections 5.3/5.4 were missing from the index. If this metric is now > 0, sub-goal A is functionally complete.
Output: Evidence markdown with run_id, JSON path, metric values, and retrieved citations showing a 5.3.* chunk surfaced.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03.2-corpus-ground-truth-correctness/03.2-CONTEXT.md
@.planning/phases/03.2-corpus-ground-truth-correctness/03.2-03-SUMMARY.md
@docs/project_notes/bugs.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run B3-001 in hybrid mode and capture evidence</name>
  <files>.planning/phases/03.2-corpus-ground-truth-correctness/b3-001-rerun-evidence.md</files>
  <action>
  Execute:
  ```
  cd src && poetry run ccop-eval evaluate run --model primus-reasoning --test-ids B3-001 --mode hybrid
  ```
  Phase 3.1 instrumentation produces a per-run JSON under `src/results/evaluations/{yyyy-MM}/{run_id}-{model}.json` and a sidecar `{run_id}-contexts.json`.

  Locate the run_id from stdout (format `eval-run-hybrid-test-B3-001-{date}`). Open the main JSON and the sidecar.

  Record in `b3-001-rerun-evidence.md`:
  1. Run command and run_id
  2. Absolute path to result JSON + sidecar
  3. `ragas` section values for B3-001: context_recall, context_precision, context_faithfulness
  4. List of retrieved citation_ids from the sidecar. Flag whether any citation_id is under section 5.3.
  5. Model response excerpt

  Assert (in the markdown): `context_recall > 0` — PASS or FAIL. If FAIL, diagnose whether retrieval brought back 5.3 content but decomposition failed, vs retrieval still missing 5.3.
  </action>
  <verify>
  JSON file exists. Markdown records `context_recall > 0` with numeric proof. At least one retrieved citation_id is under section 5.3 (e.g. `CCoP 2.0::5.3.1` or `CCoP 2.0::5.3.1(c)`).
  </verify>
  <done>
  SC #9 satisfied with live evidence. Sub-goal A functionally complete.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>B3-001 hybrid re-evaluation. context_recall is now non-zero, proving the corpus fix closed bug #10.</what-built>
  <how-to-verify>
  1. Open `.planning/phases/03.2-corpus-ground-truth-correctness/b3-001-rerun-evidence.md` and confirm numeric context_recall > 0.
  2. Cross-check the sidecar JSON (path recorded in the markdown) — confirm retrieved_contexts list includes a citation_id under section 5.3.
  3. Optional sanity: `cd src && poetry run ccop-eval evaluate run --model primus-reasoning --benchmarks B3 --mode hybrid` and verify no section-5 regressions.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to sub-goal B (Plan 05 onward), or describe discrepancies.</resume-signal>
</task>

</tasks>

<verification>
Sub-goal A closed. Metric-level proof that the ingestion fix cured the observable defect. Sub-goal B can now proceed (it reads clause text from the re-ingested Qdrant index).
</verification>

<success_criteria>
- SC #9: B3-001 hybrid re-evaluation produces context_recall > 0
- SC #10: Bugs #9 and #10 addressed (evidenced by plan 01 regex fix, plan 02 table chunks, plan 03 re-ingestion, plan 04 context_recall proof)
- Evidence markdown committable
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-corpus-ground-truth-correctness/03.2-04-SUMMARY.md` per template. This SUMMARY closes sub-goal A — flag it explicitly so the orchestrator knows sub-goal B plans (05-07) can begin.
</output>
