---
phase: 03.2-corpus-ground-truth-correctness
plan: "03"
subsystem: rag-ingestion
tags: [rag, ingestion, qdrant, corpus, reingest, toc-gate, ccop, docling, bge, bm25]

# Dependency graph
requires:
  - phase: 03.2-01
    provides: "Fixed CLAUSE_PATTERN with ## heading prefix support -- sections 5.3/5.4 recognised as clause boundaries"
  - phase: 03.2-02
    provides: "Table chunks + TOC sanity gate (_verify_toc_coverage) wired into run_ingestion before upload"
provides:
  - "Clean ccop_clauses_hybrid Qdrant collection with 477 points (490 chunks, 13 uuid5-dedup)"
  - "Sections 5.1..5.12 all present as retrievable clause chunks under document_source='CCoP 2.0'"
  - "reingestion-verification.md -- durable evidence log for SC #6/#7, SC #8 N/A resolution"
  - "Live index ready for sub-goal B semantic audit (reads clause text from Qdrant)"
affects:
  - "03.2-04 (clause inventory extraction operates against this populated index)"
  - "03.2-05 (ground truth validator semantic mismatch pass reads clause bodies by citation_id)"
  - "Phase 4 re-baseline (B3-001 re-eval + full 118-case re-baseline consume this corpus)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Drop-before-re-ingest for clean slate: explicit delete_collection + indexer recreate ensures zero stale points"
    - "TOC sanity gate as pre-upload assertion: section completeness verified before embedding work commits"
    - "Deterministic uuid5 dedup as indexing invariant: identical citation_ids across preamble sub-chunks collapse to single points"

key-files:
  created:
    - ".planning/phases/03.2-corpus-ground-truth-correctness/reingestion-verification.md"
    - ".planning/phases/03.2-corpus-ground-truth-correctness/03.2-03-SUMMARY.md"
  modified:
    - ".planning/phases/03.2-corpus-ground-truth-correctness/03.2-03-PLAN.md"

key-decisions:
  - "SC #8 marked N/A (human-approved 2026-04-21): phrases 'individual accountability'/'individual authentication' are not literally present in the CCoP 2.0 source PDF -- their absence from the index is not a chunker defect"
  - "SC #7 is the meaningful gate for this plan: all 12 expected sections (5.1..5.12) present confirms the 03.2-01/02 fix landed in live data"
  - "Phrases preserved in plan must_haves/success_criteria with '# N/A' annotations for traceability rather than deleted"

patterns-established:
  - "Corpus re-ingestion runbook: drop -> run_ingestion -> scroll-verify per section -> phrase scan -> commit evidence markdown"
  - "N/A success criteria policy: keep the line in the plan with an inline N/A annotation pointing to the SUMMARY deviation; never silently delete"

# Metrics
duration: 15min
completed: 2026-04-21
---

# Phase 03.2 Plan 03: Corpus Re-ingestion with Fixed Chunker Summary

**Dropped the stale 303-point ccop_clauses_hybrid collection and re-ingested 7 CCoP documents (490 chunks, 477 unique points) through the fixed chunker + TOC sanity gate; all 12 CCoP 2.0 sections (5.1..5.12) now discrete retrievable chunks, including previously-missing 5.3 (Privileged Access Management) and 5.4 (Trust Relationship Management).**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-21T13:38:09Z
- **Completed:** 2026-04-21T13:53:00Z (approx, includes checkpoint pause + closeout)
- **Tasks:** 2/2 executable tasks complete + 1 human-verify checkpoint approved
- **Files modified:** 3 (1 artifact created, 1 plan annotated, 1 SUMMARY created)
- **Ingestion wall-clock:** 4m 27s (parse + caption + chunk + embed + upload end-to-end)

## Accomplishments

- Started Qdrant (Docker Desktop launch + `docker compose up -d qdrant`) and confirmed healthy at `http://localhost:6333`
- Dropped stale collection (303 points from pre-fix chunker) via direct `QdrantClient.delete_collection()`
- Ran end-to-end ingestion: 7 documents, 490 chunks, 477 unique points post-uuid5-dedup
- TOC sanity gate PASSED inline in the run — all 12 expected sections observed before any upload work
- Verified SC #7: all 12 CCoP 2.0 sections indexed with working `citation_id` filters
- Diagnosed SC #8 as non-actionable: phrases absent from source PDF; approved by human as N/A

## Task Commits

Each task was committed atomically:

1. **Task 1 + 2 (combined artifact): Drop + re-ingest + section/phrase verification** — `9697d66` (feat)
   - Verification markdown contains run log + section table + phrase scan + SC scorecard
2. **SC #8 N/A resolution (plan + verification doc)** — `d401523` (fix)
   - Annotates plan `must_haves.truths` and `<success_criteria>` with `# N/A — phrase not in source PDF`
   - Adds "SC #8 Resolution" subsection to verification markdown with human-approval note

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `/Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/.planning/phases/03.2-corpus-ground-truth-correctness/reingestion-verification.md` — Post-ingest evidence log (run log, per-doc counts, section table, phrase findings, SC #8 resolution, final scorecard)
- `/Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/.planning/phases/03.2-corpus-ground-truth-correctness/03.2-03-PLAN.md` — SC #8 truths + success criterion line annotated `# N/A — phrase not in source PDF; see SUMMARY deviation`
- `/Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/.planning/phases/03.2-corpus-ground-truth-correctness/03.2-03-SUMMARY.md` — This file

## Decisions Made

- **SC #8 marked N/A, not FAIL.** Phrases `"individual accountability"` / `"individual authentication"` return 0 hits because they are literally absent from the parsed CCoP 2.0 markdown (151,269 chars, 0 matches via raw `str.find`). The chunker fix was never about recovering these specific phrases; it was about recovering sections 5.3 and 5.4 clause content, which SC #7 proves is in the index. The human approved N/A on 2026-04-21 so the plan closes cleanly rather than flagging a non-bug as a failed gate.
- **Phrases kept in plan document with inline N/A annotation rather than deleted.** Traceability: the SC #8 assertion was authored based on an assumption about what was "previously missing"; preserving the text + annotation lets a future reader see how the assumption was resolved without archaeology.
- **SC #7 section counts accepted as-is (1–7 points per section).** Sections 5.3/5.4/5.8 have only 1 clause chunk each because those sections themselves contain a single top-level clause in the CCoP 2.0 document — this is structural fidelity, not coverage loss.

## Deviations from Plan

### Rule 4 — Architectural/success-criteria change (human-approved)

**1. [Rule 4 — Architectural] SC #8 re-classified from PASS target to N/A**
- **Found during:** Task 2 (phrase scan)
- **Issue:** Plan asserted `"individual accountability"` and `"individual authentication"` should return >=1 hit each in the re-ingested corpus, implying the phrases existed in the CCoP 2.0 PDF and were previously dropped by the pre-fix chunker. Actual evidence: 0 hits in index AND 0 hits in the raw parsed markdown (151,269 chars). Phrases do not exist in the source document — their absence is not a chunker defect.
- **Decision gate:** Escalated as human-verify checkpoint (plan had `autonomous: false`) because reclassifying a stated success criterion is an architectural/specification change, not an auto-fixable bug.
- **Resolution:** Human approved (2026-04-21) marking SC #8 as **N/A** with these grounds: (a) phrases verifiably absent from source document, (b) real fix (sections 5.3/5.4 as discrete retrievable chunks) proven by SC #7 PASS, (c) related terms (`accountability`, `authentication`) ARE indexed and retrievable.
- **Files modified:** `03.2-03-PLAN.md` (annotated `must_haves.truths` + `<success_criteria>`), `reingestion-verification.md` (added "SC #8 Resolution" subsection)
- **Verification:** Plan YAML still parses; annotations use `#` inline comments that YAML treats as trailing comments on the string entries; SC list preserved verbatim.
- **Committed in:** `d401523`

---

**Total deviations:** 1 (human-approved reclassification under Rule 4)
**Impact on plan:** SC #8 outcome inverted from "must pass" to "N/A with documented rationale". No scope creep. Core fix verified by SC #6 PASS + SC #7 PASS.

## Issues Encountered

- **Qdrant and Docker Desktop were not running at plan start.** Recovered by launching Docker Desktop via `open -a Docker` and then `docker compose up -d qdrant`; waited for `/healthz` before proceeding. Not a plan deviation — `start_local.sh` is the documented runbook; this plan just invoked the underlying primitives directly.
- **ZhipuAI API returned 429 (insufficient balance) during Step 1.5 diagram captioning** for all 21 Security By Design diagrams. Diagrams fell back to placeholder text. This is a pre-existing vendor/quota condition unrelated to the chunker fix and does not affect the CCoP 2.0 clause-level content under test. No action taken; diagram captioning is orthogonal to corpus correctness per plan scope.
- **Ingestion produced 490 chunks but collection ended at 477 points** (difference of 13). Cause: Docling's preamble splitting emits sub-chunks that can re-use a parent citation_id across documents, and deterministic uuid5 dedups them. Documented in verification markdown; no functional impact (unique clause content preserved, all 12 sections present).

## User Setup Required

None — no external service configuration required. Re-ingestion runbook is self-contained (Docker Desktop + Qdrant container + local Docling models; ZhipuAI captioning is optional and its failure is non-blocking for corpus correctness).

## Next Phase Readiness

- **Plan 03.2-04 (clause inventory extraction) unblocked.** The live index has 250 CCoP 2.0 clause chunks with stable `citation_id` values — inventory extraction can read these directly.
- **Plan 03.2-05 (ground truth semantic mismatch audit) unblocked.** Audit reads clause body text from Qdrant by `citation_id`; all expected sections are now retrievable.
- **Phase 4 re-baseline unblocked.** B3-001 and full 118-case re-baseline will operate against this corrected corpus. Expect retrieval/grounding improvements for any test case whose `expected_clause` falls under sections 5.3 or 5.4 (previously un-retrievable, now retrievable).
- **No blockers or concerns** for the next plan. Qdrant collection populated, TOC gate protects future re-ingests from regression, verification evidence committed as durable artifact.

---
*Phase: 03.2-corpus-ground-truth-correctness*
*Completed: 2026-04-21*
