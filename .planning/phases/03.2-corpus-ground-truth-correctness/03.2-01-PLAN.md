---
phase: 03.2-corpus-ground-truth-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rag/ingestion/chunkers/clause_aware_chunker.py
  - src/tests/rag/ingestion/chunkers/test_clause_aware_chunker.py
  - .planning/phases/03.2-corpus-ground-truth-correctness/docling-section-5-audit.md
autonomous: true

must_haves:
  truths:
    - "Docling parser output for CCoP---Second-Edition_Revision-One.pdf contains sections 5.3 and 5.4 text verbatim"
    - "CLAUSE_PATTERN regex matches both plain clause numbers (5.3.1) and item-letter notation (5.3.1(c))"
    - "The <30-word merge rule in chunk_by_clauses is removed — every clause match emits its own chunk regardless of word count"
    - "No chunker output contains text spanning two distinct clause numbers"
  artifacts:
    - path: "src/rag/ingestion/chunkers/clause_aware_chunker.py"
      provides: "Fixed clause-aware chunker"
      contains: "CLAUSE_PATTERN"
    - path: "src/tests/rag/ingestion/chunkers/test_clause_aware_chunker.py"
      provides: "Regression tests covering regex + no-merge + cross-clause contamination"
    - path: ".planning/phases/03.2-corpus-ground-truth-correctness/docling-section-5-audit.md"
      provides: "Pre-chunker audit asserting sections 5.3 + 5.4 present in Docling markdown"
  key_links:
    - from: "clause_aware_chunker.CLAUSE_PATTERN"
      to: "item-letter notation like 5.3.1(c)"
      via: "regex capture group"
      pattern: "\\(\\[a-z\\]\\)"
    - from: "chunk_by_clauses loop"
      to: "one chunk per clause match"
      via: "removed merge_buffer branch"
      pattern: "no merge_buffer"
---

<objective>
Fix the clause-aware chunker so CCoP 2.0 sections 5.3 and 5.4 stop disappearing from the Qdrant index, and prove via Docling that the source PDF already contains those sections.

Purpose: Blocks Phase 4 re-baseline. Bug #10 shows `context_recall=0` for B3-001 because sections 5.3/5.4 were absorbed into the tail of 5.2.2 (the <30-word merge rule) and items like 5.3.1(c) never matched the regex.
Output: Pre-chunker audit document + rewritten chunker + regression tests that lock in "no cross-clause bleed" and "item-letter boundaries captured".
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03.2-corpus-ground-truth-correctness/03.2-CONTEXT.md
@src/rag/ingestion/chunkers/clause_aware_chunker.py
@src/rag/ingestion/parsers/docling_parser.py
@docs/project_notes/bugs.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-chunker Docling audit for sections 5.3 and 5.4</name>
  <files>.planning/phases/03.2-corpus-ground-truth-correctness/docling-section-5-audit.md</files>
  <action>
  Run the existing Docling parser against `ccop-official/CCoP---Second-Edition_Revision-One.pdf` and grep its markdown output for the literal strings "5.3", "5.4", "Privileged Access Management", "individual accountability", and "individual authentication". Record results in the audit markdown.

  Use the parser directly:
  ```
  cd src && poetry run python -c "from rag.ingestion.parsers.docling_parser import parse_all_ccop_documents_with_docling; import json; docs = parse_all_ccop_documents_with_docling('../ccop-official'); md = docs['CCoP 2.0'].markdown; print(md[md.find('5.3'):md.find('5.3')+4000] if '5.3' in md else 'MISSING 5.3')"
  ```

  Document in the audit file: (a) confirmed presence of 5.3.* / 5.4.* headings, (b) first 500 chars of each section, (c) literal text hits for the two target phrases. This is a PASS/FAIL gate — if Docling output is missing 5.3/5.4 then the root cause is upstream of the chunker and the plan pivots to parser fix. Expected: sections present (per bug #10 analysis).

  Do NOT modify parser code in this task — only audit and document.
  </action>
  <verify>
  File `.planning/phases/03.2-corpus-ground-truth-correctness/docling-section-5-audit.md` exists and contains explicit PASS entries for "Section 5.3 heading present", "Section 5.4 heading present", and "'individual accountability' found N times". If any FAIL, task output must state the root cause clearly.
  </verify>
  <done>
  Audit markdown written; PASS gate confirmed OR root cause documented for pivot. SC #1 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend CLAUSE_PATTERN regex and remove the <30-word merge rule</name>
  <files>src/rag/ingestion/chunkers/clause_aware_chunker.py</files>
  <action>
  Make two surgical edits to `clause_aware_chunker.py`:

  1. Extend `CLAUSE_PATTERN` (line 17) to additionally capture item-letter notation like `5.3.1(c)`. Keep the existing capture structure (clause number + heading) so downstream .split() still yields 3-tuples per clause. Per CONTEXT.md locked decision: the regex matches `X.Y.Z(letter)` BUT chunks stop at the `X.Y.Z` level — item-letter sub-items remain embedded in parent clause text. Implementation hint: allow optional `\([a-z]\)` in the number capture group so "5.3.1(c) Implement..." is recognized as a clause boundary, while the chunk for 5.3.1 includes items (a)-(c) as body text.

  2. Remove the `<30-word merge` rule entirely (lines 96-118 — the `if word_count < 30:` branch and the later `if merge_buffer:` flush). Every clause match becomes its own chunk regardless of word count. Also remove the final-flush `if merge_buffer:` block at lines 127-133. Remove `merge_buffer = None` initializer at line 44.

  Keep preamble logic and `_create_clause_chunk` helper unchanged. Do not modify `_filter_boilerplate`, `_extract_section`, or `_build_parent_path`.

  Add a short docstring note near CLAUSE_PATTERN explaining the item-letter support and a comment in `chunk_by_clauses` noting "Every clause emits its own chunk — merging disabled per Phase 3.2 decision (bug #9 root cause)."
  </action>
  <verify>
  `grep -c "merge_buffer" src/rag/ingestion/chunkers/clause_aware_chunker.py` returns 0.
  `poetry run python -c "from rag.ingestion.chunkers.clause_aware_chunker import CLAUSE_PATTERN; import re; assert CLAUSE_PATTERN.search('5.3.1(c) Implement multi-factor authentication') is not None"` exits 0.
  </verify>
  <done>
  Regex matches item-letter notation; no merge-buffer code path remains; chunker produces one chunk per clause match. SC #2, #3, #4 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regression tests for chunker (no cross-clause bleed, item-letter capture)</name>
  <files>src/tests/rag/ingestion/chunkers/test_clause_aware_chunker.py</files>
  <action>
  Create (or extend if exists) a pytest module covering the two fixed behaviors. Write tests against `chunk_by_clauses` using inline markdown fixtures (no Docling dependency).

  Required test cases:
  - `test_item_letter_boundary_recognized`: markdown containing "5.3.1(c) Implement multi-factor authentication\n\nbody text" — assert the resulting chunk's `metadata.clause` starts with "5.3.1" and chunk text contains "multi-factor".
  - `test_no_cross_clause_bleed`: markdown containing a short clause "5.2.2 Account Review\n\nShort body." followed by "5.3 Privileged Access Management\n\nLonger body here with enough words to pass filters." — assert two distinct chunks are emitted, and the chunk with `metadata.clause == "5.2.2"` does NOT contain the string "Privileged Access Management". This directly guards bug #9.
  - `test_short_clause_not_dropped`: markdown with a 10-word clause body — assert a chunk is emitted (previously this would merge forward).
  - `test_section_5_3_produces_discrete_chunk`: markdown snippet containing 5.2.2, 5.3 header, 5.3.1, 5.3.1(a), 5.3.1(b), 5.3.1(c) — assert chunks for 5.3, 5.3.1 exist; 5.3.1 chunk body contains "(a)", "(b)", "(c)" item text (items stay embedded per CONTEXT.md).

  Tests live under `src/tests/rag/ingestion/chunkers/`. Create `__init__.py` files if the directory doesn't exist.
  </action>
  <verify>
  `cd src && poetry run pytest tests/rag/ingestion/chunkers/test_clause_aware_chunker.py -v` passes (4+ tests green).
  </verify>
  <done>
  Regression test suite locks in the fixed chunker behavior. Future edits that re-introduce merging or break item-letter capture fail CI-equivalent local runs.
  </done>
</task>

</tasks>

<verification>
All three tasks complete. Pre-chunker audit confirms Docling preserves sections 5.3/5.4. Chunker regex handles item-letter notation. Merge-buffer logic deleted. Regression tests pass. Nothing ingested yet — that's Plan 03.
</verification>

<success_criteria>
- SC #1: Docling output verified to contain 5.3 + 5.4 (audit markdown)
- SC #2: CLAUSE_PATTERN extended (regex inspection)
- SC #3: Merge-collapse across clause boundaries eliminated (code diff + test)
- SC #4: Leaf-per-chunk granularity at clause level (CONTEXT.md-locked interpretation of SC #4)
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-corpus-ground-truth-correctness/03.2-01-SUMMARY.md` per template.
</output>
