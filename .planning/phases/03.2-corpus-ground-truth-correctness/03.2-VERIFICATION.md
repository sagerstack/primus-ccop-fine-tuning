---
phase: 03.2-corpus-ground-truth-correctness
verified: 2026-04-22T07:57:23Z
status: passed
score: 18/18 must-haves verified
re_verification: null
---

# Phase 03.2: Corpus and Ground Truth Correctness — Verification Report

**Phase Goal:** Guarantee the evaluation substrate is correct before running the main baseline. Two blocking data-integrity defects fixed in sequence: (A) `ccop_clauses_hybrid` Qdrant index missing CCoP 2.0 sections 5.3 and 5.4 due to clause-aware chunker regex failures; (B) v2 ground truth fabricated clause references from bug #8.

**Verified:** 2026-04-22T07:57:23Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (derived from phase goal)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| T1 | Sections 5.3 and 5.4 of CCoP 2.0 are discoverable in the re-ingested Qdrant index | VERIFIED | Live Qdrant scroll (2026-04-22): section 5.3 and 5.4 both PRESENT in `ccop_clauses_hybrid` (495 points total). Confirmed by reingestion-verification.md and b3-001-rerun-evidence.md. |
| T2 | The clause-aware chunker no longer drops or merge-collapses across clause boundaries | VERIFIED | CLAUSE_PATTERN extended to match item-letter (line 42-45); merge rule removed (comment at line 110-111 explicitly confirms removal; code path `i += 3` with no buffering at lines 115-135). |
| T3 | Ingestion fails loudly if any CCoP 2.0 TOC section is absent from the index | VERIFIED | `_verify_toc_coverage()` in `run_ingestion.py:210` raises RuntimeError on missing sections; wired before upload at line 312. 9 passing tests in `test_run_ingestion_sanity.py`. |
| T4 | Every v2 ground-truth clause reference is either validated against the authoritative inventory, marked deprecated, or marked audit_exempt | VERIFIED | `poetry run ccop-eval validate-ground-truth --no-semantic` → `434 valid, 628 warnings, 0 errors` (live run). Validator hard-fails on unverifiable non-deprecated non-exempt clauses (Passes 1+2). |
| T5 | Hallucinated clause references introduced in future edits will fail the validator at commit time | VERIFIED | CLI tests `test_validator_rejects_hallucinated_clause` and `test_validator_rejects_inline_citation_hallucination` both PASS; synthetic `"9.9.9"` returns non-zero exit with "not found in inventory" message. |
| T6 | Deprecated cases survive on disk but are excluded from evaluation scoring | VERIFIED | `jsonl_test_case_repository.py:108-111` filters `status="deprecated"` at load time with skip counter + info log. Ground truth contains 1 deprecated (B05-018) and 11 audit_exempt (B21) cases on disk. |

**Score:** 6/6 truths verified.

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/rag/ingestion/chunkers/clause_aware_chunker.py` | CLAUSE_PATTERN with item-letter support; merge rule removed | VERIFIED | 434 lines; regex at line 42-45 matches `(?:\([a-z]\))?`; merge rule comment at line 110-111 confirms removal; imported by `run_ingestion.py`. |
| `src/rag/ingestion/run_ingestion.py` | `_verify_toc_coverage()` with EXPECTED_CCOP_2_SECTIONS gate | VERIFIED | `EXPECTED_CCOP_2_SECTIONS` defined at line 38; `_verify_toc_coverage()` at line 210 raises RuntimeError; wired at line 312 before upload. |
| `src/tests/rag/ingestion/test_run_ingestion_sanity.py` | 9 tests covering TOC gate pass/fail | VERIFIED | 138 lines, 9 tests (passes with all sections, fails when 5.3/5.4 missing, table chunks don't satisfy gate, etc.). |
| `src/rag/ingestion/fixtures/clause_inventory.json` | Authoritative clause inventory from 7 source PDFs | VERIFIED | 691 entries across 7 source docs (Auditing Guidelines, CCoP 2.0 with 415 entries, CCoP Response to Feedback, Cybersecurity Act 2018, Risk Assessment Guide, Security By Design, Threat Modelling Guide). All sections 5.1-5.12 present for CCoP 2.0. |
| `src/rag/ingestion/scripts/build_clause_inventory.py` | Reproducible inventory extraction script | VERIFIED | 384 lines; produces `clause_inventory.json` from Docling-parsed PDFs. |
| `src/rag/ingestion/scripts/audit_ground_truth_citations.py` | 3-pass audit (ID existence, in-text citation, semantic mismatch) | VERIFIED | 1377 lines; exports `SOURCE_CCOP`, `load_inventory`, `parse_clause_reference`, `extract_intext_citations` with context-anchored regex (`_CCOP_ATTRIBUTED_RE`, `_CLAUSE_PHRASE_RE`). Reads Excel as authoritative source. |
| `.planning/phases/03.2-corpus-ground-truth-correctness/ground-truth-audit-report.md` | Human-readable audit with flagged cases and suggested corrections | VERIFIED | 54206 bytes (197 flags documented); enumerates Pass 1 and Pass 2 issues per benchmark with nearest-neighbour suggestions. |
| `.planning/phases/03.2-corpus-ground-truth-correctness/ground-truth-audit-diff.json` | Machine-readable audit diff | VERIFIED | 98084 bytes; machine-readable flag diff. |
| `.planning/phases/03.2-corpus-ground-truth-correctness/audit-remap-proposal.md` | Per-cluster remap design with PDF evidence | VERIFIED | 436 lines; cluster-by-cluster evidence-backed design. |
| `src/scripts/patch_ground_truth_excel.py` | Idempotent authoritative-Excel patcher | VERIFIED | 915 lines; per-cluster rules with ClusterMatcher + RuleFn pattern; ER_FOOTER cluster added in Plan 07. |
| `src/scripts/regenerate_test_suite_from_excel.py` | Excel→JSONL deterministic regen | VERIFIED | 297 lines; propagates metadata + status + deprecated_reason fields. |
| `ground-truth/schema/validate.py` | Hard-fail validator with Pass 1+2 (ID existence) + Pass 3 (semantic) | VERIFIED | 439 lines; `validate_clause_references()` at line 96 enforces Pass 1 + Pass 2; `validate_semantic_mismatch()` at line 134 enforces Pass 3; `run_validation()` at line 319 programmatic entry; deprecated + audit_exempt skip logic at lines 113 and 276. |
| `src/presentation/cli/commands/validate_ground_truth.py` | Typer CLI wrapper | VERIFIED | 112 lines; `validate_app` with `invoke_without_command=True` callback; loads validator via `importlib.util.spec_from_file_location`. |
| `src/presentation/cli/main.py` (modified) | CLI subcommand registered | VERIFIED | `app.add_typer(validate_app, name="validate-ground-truth", ...)` at line 29. `poetry run ccop-eval validate-ground-truth --help` returns usage. |
| `src/tests/presentation/cli/test_validate_ground_truth.py` | 4 integration tests | VERIFIED | 159 lines, 4 tests, all PASSING (`pytest tests/presentation/cli/test_validate_ground_truth.py -v` → 4 passed in 10.29s). |
| `src/infrastructure/adapters/repositories/jsonl_test_case_repository.py` | Deprecated-skip at repo load | VERIFIED | Skip logic at lines 108-111 with counter and info log. |
| `ground-truth/test-suite/*.jsonl` | Regenerated from patched Excel | VERIFIED | 13 files present with timestamped `.bak` siblings (2026-04-22). Validator confirms 0 errors across all benchmarks. |

---

## Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `run_ingestion.py` | `clause_aware_chunker.py` | `chunk_all_documents_by_clauses` | WIRED | Imported and called during Step 2/5 of ingestion pipeline. |
| `run_ingestion.py` | TOC sanity gate | `_verify_toc_coverage(chunks)` | WIRED | Called pre-upload at line 312; raises RuntimeError on missing sections. |
| `clause_inventory.json` | `validate.py` | `load_clause_inventory()` | WIRED | Loaded via `load_inventory()` at validate.py:55; default path resolves to `src/rag/ingestion/fixtures/clause_inventory.json`. |
| `validate.py` | `audit_ground_truth_citations.py` | import of `SOURCE_CCOP`, `extract_intext_citations`, `load_inventory`, `parse_clause_reference` | WIRED | sys.path fixup at lines 28-31; imports at lines 33-38. |
| `validate_ground_truth.py` (CLI) | `validate.py` | `importlib.util.spec_from_file_location` → `run_validation()` | WIRED | Loaded via `_load_validator_module()`; `run_validation()` invoked with typed kwargs at line 103. |
| `main.py` | `validate_ground_truth.py` | `app.add_typer(validate_app, name="validate-ground-truth", ...)` | WIRED | Confirmed by `poetry run ccop-eval validate-ground-truth --help` returning correct usage. |
| `jsonl_test_case_repository.py` | Eval pipeline | `load_by_benchmark()` skip filter | WIRED | `if data.get("status") == "deprecated": skipped_deprecated += 1; continue` at lines 108-111. |
| Patched Excel | Ground-truth JSONL | `regenerate_test_suite_from_excel.py` | WIRED | Excel is canonical; JSONL regenerated with metadata + status propagation. 196 rows regenerated across 13 files (Plan 06 + 07). |
| `ground-truth-audit-diff.json` | Plan 06 patcher | `patch_ground_truth_excel.py` | WIRED | Audit flags drove Phase C patcher rules; 176 cells patched + 17 ER_FOOTER cells (Plan 07). |

---

## Success Criteria Coverage (18 Must-Haves)

### Sub-goal A — Corpus/Ingestion Correctness

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | Docling parser output for CCoP 2.0 PDF verified to contain 5.3/5.4 text | VERIFIED | `docling-section-5-audit.md`: "## 5.3 Privileged Access Management" at pos 80,418; "## 5.4 Domain Controller" at pos 81,883. 151,269 chars parsed. |
| 2 | CLAUSE_PATTERN extended to capture item-letter boundaries | VERIFIED | `clause_aware_chunker.py:42-45` — regex includes `(?:\([a-z]\))?` group. |
| 3 | Chunker no longer merge-collapses across clause boundaries | VERIFIED | Merge rule removed (comment line 110-111). Code path `i += 3` at line 135 advances through clause matches with no buffering logic. |
| 4 | Leaf-per-chunk granularity (per CONTEXT.md decision) | VERIFIED | Every clause match emits its own chunk (line 122-125 in chunker). Live Qdrant scroll confirms clause-level IDs like `CCoP 2.0::5.3.1`. |
| 5 | Ingestion sanity test asserts TOC section count equals indexed section count | VERIFIED | `_verify_toc_coverage()` raises RuntimeError pre-upload; 9 passing tests in `test_run_ingestion_sanity.py`. |
| 6 | ccop_clauses_hybrid collection dropped and re-ingested with updated chunker | VERIFIED | `reingestion-verification.md` documents drop + re-ingestion (477 → 495 points). Live Qdrant count: 495 points. |
| 7 | Sections 5.1-5.12 all present as discrete points in Qdrant | VERIFIED | Live Qdrant scroll (2026-04-22T07:57Z): all 12 sections PRESENT. |
| 8 | Full-text scan for "individual accountability"/"individual authentication" returns ≥1 hit | N/A (terminal, human-approved) | `reingestion-verification.md`: phrases do not exist in source PDF (0 hits in 151,269 chars of parsed markdown). Human approved N/A on 2026-04-21 per SC #8 Resolution section. Real fix verified via SC #7. |
| 9 | B3-001 hybrid re-evaluation produces `context_recall > 0` | N/A (terminal, human-approved) | `b3-001-rerun-evidence.md`: corpus fix confirmed (5.3.1 at rank 5/20 post-rerank, just below top-3 cutoff). Human approved SC #9 as N/A on 2026-04-21 — remaining gap is retrieval-ranking scope (Phase 4), not corpus scope. |
| 10 | Addresses bugs #9 and #10 | VERIFIED | Bug #9 (merge rule) fixed at chunker level; Bug #10 (sections 5.3/5.4 missing) verified in Qdrant and by B3-001 retrievability proof. |

### Sub-goal B — Ground Truth Clause Reference Audit

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 11 | Authoritative CCoP 2.0 clause inventory built from the PDF | VERIFIED | `clause_inventory.json`: 691 entries across 7 source PDFs. 415 entries for CCoP 2.0 including all sections 5.1-5.12 plus item-letter sub-entries. |
| 12 | Every `clause_reference` field validated against inventory | VERIFIED | Plan 06 summary: 624 checked, 7 flagged — all either deprecated or audit_exempt. Validator live run confirms 0 Pass-1 errors. |
| 13 | Every clause citation inside `expected_response` validated | VERIFIED | Plan 06 + 07: 450 extracted, 90 HUMAN_REVIEW (context-anchored regex filters out non-CCoP-attributed dotted numbers). Validator live run confirms 0 Pass-2 errors. |
| 14 | Audit report enumerates every invalid reference with proposed correction | VERIFIED | `ground-truth-audit-report.md` (54,206 bytes) + `ground-truth-audit-diff.json` (98,084 bytes) enumerate all flags with nearest-neighbour suggestions and confidence scores. HUMAN_REVIEW terminal per CONTEXT.md locked decision. |
| 15 | Corrections applied to all affected v2 test cases | VERIFIED | Plan 06: 176 Excel cells patched across 13 clusters, 196 JSONL rows regenerated. Plan 07: +17 ER_FOOTER corrections. Timestamped `.bak` files confirm authoritative edits. |
| 16 | Zero unverifiable clause numbers remain in v2 ground truth after the audit (excluding deprecated + audit_exempt) | VERIFIED | Live validator run: `434 valid, 628 warnings, 0 errors`. 1 deprecated (B05-018) + 11 audit_exempt (B21) explicitly bypass the gate per CONTEXT.md contract. |
| 17 | Ground-truth JSONL validator extended to enforce clause_reference correctness at load time | VERIFIED | `validate_clause_references()` at `validate.py:96` hard-fails on missing IDs (Pass 1 + Pass 2). `run_validation()` returns non-zero exit code. CLI subcommand `ccop-eval validate-ground-truth` wired. 4 passing CLI tests. |
| 18 | Addresses bug #8 | VERIFIED | B3-001 hallucinated `5.1.5` corrected to `5.3.1` family (Plan 06); end-to-end validator gate prevents future regression (Plan 07). |

---

## Requirements Coverage

No `.planning/REQUIREMENTS.md` requirements explicitly mapped to Phase 3.2 — the phase is a data-integrity remediation phase whose contract is the 18 success criteria enumerated in PLAN frontmatter. All 18 satisfied (16 VERIFIED + 2 N/A human-approved).

---

## Anti-Patterns Found

No blocker anti-patterns. Notable observations:

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `validate.py` | 276 | `continue` on deprecated status | Info | Intentional skip, matches CONTEXT.md locked decision. |
| `validate.py` | 113 | `return` on audit_exempt | Info | Intentional skip for B21 hallucination benchmark. |
| `jsonl_test_case_repository.py` | 108-111 | `continue` on deprecated status | Info | Intentional — log at info level + counter. |
| `run_ingestion.py` comment | 259 | Comment says "8 CCoP documents" but reality is 7 source docs | Info | Documentation drift only; inventory correctly shows 7 unique source docs (ccop-official/supplementary/ has a duplicate `Guidelines_for_Auditing_Critical_Information_Infrastructure.pdf` versus references/, which Docling/ingestion dedupes). No functional impact. |

No TODO/FIXME/stubbed handlers in phase-touched files. No placeholder renders. No empty-return patterns in business logic.

---

## Human Verification Required

None. All 18 success criteria have automated evidence or human-approved terminal state (SC #8, SC #9). The 90 Pass-2 HUMAN_REVIEW audit flags are an accepted terminal state per CONTEXT.md locked decision ("flag-for-human-review only — no silent correction"); they are not CCoP-attributed citations and therefore do not pollute the hard-fail validator gate.

---

## Gaps Summary

None. Phase 03.2 delivered end-to-end:

- **Sub-goal A (corpus correctness):** Chunker regex extended, merge rule removed, TOC sanity gate enforced pre-upload, collection dropped and re-ingested with 495 points. All 12 CCoP 2.0 sections (5.1-5.12) present in live Qdrant index. SC #8 and SC #9 terminally resolved as N/A with human approval and Phase 4 handoff documentation.
- **Sub-goal B (ground truth audit):** 691-entry clause inventory built; authoritative Excel patcher + deterministic JSONL regen pattern established; 434 ground-truth cases validate cleanly; 1 deprecated + 11 audit_exempt cases bypass the gate by design; hard-fail validator wired as `ccop-eval validate-ground-truth` with 4 passing integration tests.
- **Runtime gate:** JSONL repository filters deprecated at load time; eval pipeline respects `status="deprecated"` per CONTEXT.md contract.

The evaluation substrate is correct. The main baseline is unblocked.

---

_Verified: 2026-04-22T07:57:23Z_
_Verifier: Claude (gsd-verifier)_
