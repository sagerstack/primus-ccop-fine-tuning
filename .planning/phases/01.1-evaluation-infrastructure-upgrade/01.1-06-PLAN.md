---
phase: 01.1-evaluation-infrastructure-upgrade
plan: 06
type: execute
wave: 4
depends_on: ["01.1-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "LLM-only evaluation run completed on all 118 test cases and recorded in MLflow"
    - "Hybrid (RAG) evaluation run completed on all 118 test cases and recorded in MLflow"
    - "mlflow ui shows side-by-side comparison of both runs with per-benchmark metrics"
    - "Per-benchmark scores visible for all 21 benchmarks in both runs"
    - "No NotImplementedError during evaluation (all benchmarks scoreable)"
  artifacts: []
  key_links:
    - from: "mlflow ui"
      to: "mlruns/"
      via: "Local MLflow experiment data"
      pattern: "mlruns|CCoP Evaluation"
---

<objective>
Execute two full evaluation runs (LLM-only baseline and hybrid RAG) with MLflow tracking, then verify results are comparable in the MLflow UI.

Purpose: This is the culmination of Phase 1.1 — recording the first tracked evaluation runs. The LLM-only run establishes a restated baseline under the new LLM-as-Judge methodology (replacing the old 49.2% semantic similarity baseline). The hybrid run measures RAG augmentation improvement. Both runs in MLflow enable side-by-side comparison.

Output: Two MLflow experiment runs with per-benchmark metrics, comparison viewable in mlflow ui.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.1-evaluation-infrastructure-upgrade/01.1-CONTEXT.md

@src/presentation/cli/commands/evaluate.py

# Need all prior summaries for full context
@.planning/phases/01.1-evaluation-infrastructure-upgrade/01.1-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute LLM-only baseline evaluation with tracking</name>
  <files></files>
  <action>
Run the full LLM-only evaluation across all 118 test cases with MLflow tracking:

1. **Pre-flight checks**:
   - Verify Ollama is running and primus-reasoning model is available: `ollama list | grep primus`
   - Verify the evaluate command works with --mode and --track flags: `ccop-eval evaluate run --help`
   - Verify ground truth files exist: `ls ground-truth/phase-2/test-suite/`

2. **Execute LLM-only evaluation**:
   ```bash
   cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src
   python -m presentation.cli.main evaluate run \
     --model primus-reasoning \
     --mode llm-only \
     --track \
     --phase baseline \
     --save
   ```
   This should:
   - Route all queries through LangGraph fallback path (no RAG retrieval)
   - Score all 21 benchmarks (6 rule-based + 15 LLM-as-Judge)
   - Log parameters, per-benchmark metrics, and results JSON to MLflow
   - Display summary table with overall score and per-benchmark breakdown

3. **If the evaluation fails**:
   - Check error logs for which benchmark or test case failed
   - If LLM judge errors occur on specific benchmarks, verify evaluation-rubrics.md has the rubric for that benchmark
   - If pipeline errors occur, check Ollama connectivity
   - Fix issues and re-run

4. **Verify MLflow logged the run**:
   - Check `mlruns/` directory was created
   - Verify run exists with expected params and metrics

IMPORTANT: This evaluation may take significant time (118 test cases * LLM inference + LLM judge calls for 15 benchmarks). Expect 30-60+ minutes depending on hardware. The Claude subprocess calls for LLM judging add overhead per test case.

NOTE: If the full 118-case run is too slow to complete in one execution, run a subset first (e.g., `--benchmarks B1 --benchmarks B8 --benchmarks B12`) to verify the pipeline works end-to-end, then run the full suite.
  </action>
  <verify>
`ls mlruns/` shows experiment data.
MLflow run exists with run_name containing "primus-reasoning_llm-only".
Overall score metric is logged.
Per-benchmark metrics (benchmark_B1, benchmark_B2, ..., benchmark_B21) are logged.
  </verify>
  <done>
LLM-only baseline evaluation completed on 118 test cases. Results recorded in MLflow as first experiment run. Per-benchmark scores available for all 21 benchmarks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute hybrid RAG evaluation with tracking</name>
  <files></files>
  <action>
Run the full hybrid (RAG-augmented) evaluation across all 118 test cases with MLflow tracking:

1. **Pre-flight checks**:
   - Verify Qdrant is running: `curl http://localhost:6333/collections`
   - Verify Qdrant collection has data: check the ccop_clauses_hybrid collection has 303 points
   - Verify .env.local has CCOP_QDRANT_URL, CCOP_QDRANT_COLLECTION_NAME, CCOP_QDRANT_EMBEDDING_MODEL set

2. **Execute hybrid evaluation**:
   ```bash
   cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src
   python -m presentation.cli.main evaluate run \
     --model primus-reasoning \
     --mode hybrid \
     --track \
     --phase baseline \
     --save
   ```
   This should:
   - Route all queries through LangGraph retrieval path (Qdrant retrieval + reranking + generation)
   - Score all 21 benchmarks
   - Log to MLflow as second run in the "CCoP Evaluation" experiment
   - Include RAG metadata (is_rag_augmented, retrieval_attempts, reranker_scores) in results

3. **If hybrid mode fails with RAG unavailability**:
   - Start Qdrant: `docker compose up -d qdrant`
   - Verify collection: `curl http://localhost:6333/collections/ccop_clauses_hybrid`
   - If collection missing, re-ingest: `python -m rag.ingestion.run_ingestion`

4. **Verify MLflow has both runs**:
   - Two runs in the "CCoP Evaluation" experiment
   - One with mode=llm-only, one with mode=hybrid

IMPORTANT: Same timing considerations as Task 1. Hybrid mode adds retrieval + reranking latency per query.
  </action>
  <verify>
MLflow has two runs: one llm-only, one hybrid.
Both runs have per-benchmark metrics logged.
Hybrid run has mode=hybrid in params and is_rag_augmented should be True for most test cases.
  </verify>
  <done>
Hybrid RAG evaluation completed on 118 test cases. Results recorded in MLflow as second experiment run. Both runs available for comparison.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete evaluation infrastructure: LLM-as-Judge rubrics for 15 benchmarks, ModelGateway routing through LangGraph, MLflow tracking with two evaluation runs (LLM-only baseline vs hybrid RAG).
  </what-built>
  <how-to-verify>
1. Start MLflow UI: `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && mlflow ui`
2. Open http://localhost:5000 in browser
3. Verify "CCoP Evaluation" experiment exists with 2 runs
4. Compare the two runs side-by-side:
   - Click both runs, use "Compare" view
   - Check per-benchmark metrics (benchmark_B1 through benchmark_B21)
   - Verify LLM-only vs hybrid scores make sense (hybrid should show improvement on factual benchmarks)
5. Check that parameters are logged (model, mode, test_case_count)
6. Check that artifacts are logged (results JSON file)
7. Verify no benchmarks show 0.0 score due to judge errors (check for judge_error in results)
  </how-to-verify>
  <resume-signal>Type "approved" if MLflow comparison looks correct, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. Two MLflow runs exist in "CCoP Evaluation" experiment
2. LLM-only run has mode=llm-only in params
3. Hybrid run has mode=hybrid in params
4. Both runs have overall_accuracy metric
5. Both runs have per-benchmark metrics (benchmark_B1 through benchmark_B21)
6. mlflow ui accessible at localhost:5000
7. Side-by-side comparison shows score differences between modes
</verification>

<success_criteria>
- LLM-only baseline evaluation completed and tracked in MLflow
- Hybrid RAG evaluation completed and tracked in MLflow
- Both runs comparable in MLflow UI with per-benchmark metrics
- All 21 benchmarks scored successfully (no NotImplementedError, minimal judge errors)
- User has verified MLflow comparison view shows meaningful results
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-evaluation-infrastructure-upgrade/01.1-06-SUMMARY.md`
</output>
