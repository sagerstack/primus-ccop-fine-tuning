---
phase: 10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2
plan: 05
type: execute
wave: 3
depends_on: ["10-04"]
files_modified:
  - src/rag/graph/ontology/clause_seeder.py
  - src/rag/graph/cli/graph.py
  - tests/rag/graph/ontology/test_clause_seeding.py
autonomous: true
requirements: [RAG-01, RAG-02, D-10, D-09]
must_haves:
  truths:
    - "Exactly 691 :Clause nodes are MERGE-seeded from clause_inventory.json with parent-child edges (D-10)"
    - "Seeding is deterministic and idempotent — no LLM, no hallucinated/unnamed clauses, re-runnable without duplicates (D-10)"
    - "Each seeded clause carries its function_type tag (ScopeClause/ControlClause/DefinitionClause) from the locked ontology (D-09), enabling function-type routing (D-12)"
  artifacts:
    - path: "src/rag/graph/ontology/clause_seeder.py"
      provides: "Deterministic Cypher MERGE seeding of the 691-clause backbone"
      contains: "MERGE (c:Clause"
    - path: "src/rag/graph/cli/graph.py"
      provides: "ccop-eval graph seed-clauses subcommand"
      contains: "seed"
  key_links:
    - from: "clause_seeder"
      to: "10-07 entity→clause linking + 10-09 clause-anchored retrieval"
      via: ":Clause nodes with real clause_id + function_type are the retrieval unit (D-11)"
      pattern: "clause_id"
---

<objective>
Seed the **clause backbone** deterministically (D-10): MERGE 691 `:Clause` nodes from `clause_inventory.json` with Title→Chapter→Article→Item parent-child edges and function_type tags from the locked ontology (D-09). No LLM → no hallucinated or unnamed clauses. These real-ID clause nodes become the fine-grained RETRIEVAL unit (D-11), decoupled from the coarse extraction chunk.

Purpose: A reliable, re-runnable regulatory skeleton that extracted entities link to and that clause-anchored retrieval targets.
Output: `clause_seeder.py` + `graph seed-clauses` CLI + integration test proving 691 nodes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md
@src/rag/graph/ontology/ontology_config.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic clause seeder (691 nodes + hierarchy + function_type) — D-10/D-09</name>
  <files>src/rag/graph/ontology/clause_seeder.py, tests/rag/graph/ontology/test_clause_seeding.py</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (Q4 — MERGE seeding + parent inference from clause_id dot-hierarchy)
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md ("clause_seeder.py" section — three combined analogs; the parameterized UNWIND/MERGE pattern; T-09-12 static Cypher)
    - src/rag/ingestion/chunkers/clause_aware_chunker.py (lines 298-322 `_build_parent_path` / `_extract_section` — REUSE for parent-child derivation, do not re-derive hierarchy rules)
    - src/rag/ingestion/fixtures/clause_inventory.json (the 691-entry source: flat `{clause_id, source_doc}`, NO titles — verify the top-level `entries` shape)
    - src/rag/graph/build/kg_builder.py (lines 109-148 idempotent index/`already exists` pattern; lines 170-199 direct `session.run` Cypher style)
    - src/rag/graph/ontology/ontology_config.json (function_type assignment source — which clause-function tag applies)
  </read_first>
  <behavior>
    - Test (integration, real Neo4j): after seeding, `MATCH (c:Clause) RETURN count(c)` == 691.
    - Test: a known child clause (e.g. "6.1.2") has a `parent` edge to "6.1", which has a parent edge to "6".
    - Test: re-running the seeder does NOT create duplicates (idempotent MERGE) — count stays 691.
    - Test: each :Clause carries a `function_type` property in {ScopeClause, ControlClause, DefinitionClause}.
  </behavior>
  <action>
    Implement `clause_seeder.py` reading `clause_inventory.json` `entries`, seeding via a STATIC parameterized Cypher `UNWIND $entries AS entry MERGE (c:Clause {clause_id: entry.clause_id, source_doc: entry.source_doc})` (T-09-12 — never string-interpolate). Derive `chapter`/`parent_id` from the clause_id dot-structure by REUSING `_build_parent_path`/`_extract_section` logic, then MERGE parent-child edges (e.g. `(:Clause{clause_id:"6.1"})-[:HAS_CHILD]->(:Clause{clause_id:"6.1.2"})`). Set each clause's `function_type` from the locked `ontology_config.json` mapping (D-09; default to a documented fallback if a clause is unmapped). Since `clause_inventory.json` has no titles, seed `clause_id`+`source_doc`+derived `chapter`/`parent_id`+`function_type` only (titles back-filled later if needed). Follow the idempotent `already exists`-swallow pattern for any constraint/index creation.
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/rag/graph/ontology/test_clause_seeding.py -m integration -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Post-seed `count(:Clause)` == 691; re-run keeps it 691 (idempotent).
    - Parent-child edges reflect the dot-hierarchy for a sampled clause.
    - Every :Clause has a `function_type` in the three D-09 tags.
    - All Cypher is static + parameterized (no f-string/`.format` into query bodies).
  </acceptance_criteria>
  <done>691 deterministic clause nodes with hierarchy + function_type exist, idempotently re-seedable, all via parameterized Cypher.</done>
</task>

<task type="auto">
  <name>Task 2: `ccop-eval graph seed-clauses` CLI subcommand</name>
  <files>src/rag/graph/cli/graph.py</files>
  <read_first>
    - src/rag/graph/cli/graph.py (`graph_app` Typer structure + existing `build_command`/`inspect`/`stats` subcommands to mirror)
    - src/rag/graph/ontology/clause_seeder.py (the seeder from Task 1)
    - CLAUDE.md (Build/Run/Test → existing `ccop-eval graph build/inspect/stats` surface)
  </read_first>
  <action>
    Add a `seed-clauses` subcommand to `graph_app` mirroring the existing `build`/`inspect`/`stats` Typer command shape: it constructs the seeder (driver/settings from the DI container/settings), runs seeding, and prints a summary (nodes seeded, edges created, function_type distribution). Wire it so `cd src/ && poetry run ccop-eval graph seed-clauses` works. Report failures via the existing stats/failure surface (T-09-08: never silently swallow).
  </action>
  <verify>
    <automated>cd src/ && poetry run ccop-eval graph seed-clauses --help >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ccop-eval graph seed-clauses --help` exits 0 and documents the subcommand.
    - Running it against live Neo4j prints a seeded-node summary and leaves exactly 691 :Clause nodes.
  </acceptance_criteria>
  <done>Operator-facing CLI exists to seed/re-seed the clause backbone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clause_inventory.json → Cypher MERGE | committed fixture data written to Neo4j |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Tampering | Cypher injection via seeding values | mitigate | Static class/module-level Cypher; all 691 entries passed as bound `$entries` parameter, never interpolated (T-09-12, RESEARCH V5 control) |
| T-10-05-02 | Denial of Service | accidental duplicate/runaway node creation | mitigate | MERGE (not CREATE) makes seeding idempotent; integration test asserts count stays 691 on re-run |
</threat_model>

<verification>
- `cd src/ && poetry run pytest ../tests/rag/graph/ontology/test_clause_seeding.py -m integration -q` green (691 nodes).
- `graph seed-clauses` CLI functional.
</verification>

<success_criteria>
Deterministic 691-node clause backbone with hierarchy + function_type tags seeded via parameterized Cypher, idempotent, CLI-exposed.
</success_criteria>

<output>
Create `.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-05-SUMMARY.md` when done.
</output>
