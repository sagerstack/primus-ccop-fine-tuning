---
phase: 10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2
plan: 08
type: execute
wave: 5
depends_on: ["10-07"]
files_modified:
  - src/rag/graph/ontology/shapes.ttl
  - src/rag/graph/ontology/shacl_validator.py
  - src/rag/graph/cli/graph.py
  - tests/rag/graph/ontology/test_shacl_validation.py
  - src/pyproject.toml
autonomous: false
requirements: [RAG-06, D-13]
must_haves:
  truths:
    - "SHACL validation rejects non-conforming facts (e.g. a node missing a canonical name, or a junk name like 'N.A.') and conforms on a valid graph (D-13)"
    - "Non-conforming facts are quarantined + logged separately, NOT silently deleted (D-13)"
  artifacts:
    - path: "src/rag/graph/ontology/shapes.ttl"
      provides: "SHACL NodeShapes encoding D-07 canonical-name + junk-rejection constraints"
      contains: "sh:NodeShape"
    - path: "src/rag/graph/ontology/shacl_validator.py"
      provides: "LPG→RDF export + pyshacl.validate + quarantine of violations"
      contains: "validate("
    - path: "src/rag/graph/cli/graph.py"
      provides: "ccop-eval graph validate subcommand"
      contains: "validate"
  key_links:
    - from: "shacl_validator"
      to: "built ontology graph"
      via: "LPG→RDF export query → pyshacl.validate against shapes.ttl"
      pattern: "shacl_graph"
---

<objective>
Add **SHACL validation** (D-13) as the structural backstop between "toy and production": export the built graph LPG→RDF, validate against committed `shapes.ttl`, and quarantine non-conforming facts (reject + log separately, never silent-delete). Shapes encode the D-07 anti-pattern fixes (canonical name required, junk names rejected) as machine-checked constraints. Use rdflib/pyshacl (pure Python) — NOT n10s (unverified Neo4j 5.26 compatibility, Pitfall 5).

Purpose: Whatever slips past the schema-constrained prompt is caught structurally.
Output: `shapes.ttl` + `shacl_validator.py` + `graph validate` CLI + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md
@src/rag/graph/ontology/ontology_config.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — rdflib + pyshacl ([ASSUMED]) before install</name>
  <action>Pause before installing rdflib + pyshacl. Surface the pypi.org legitimacy evidence for both packages and wait for explicit approval before any install runs. Never auto-approve.</action>
  <what-built>Nothing yet — this checkpoint gates `poetry add rdflib pyshacl`. RESEARCH marked both `[ASSUMED]` (slopcheck not run); protocol requires human verification before install.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/rdflib/ — confirm canonical RDFLib (repo github.com/RDFLib/rdflib, ~20 years, millions/month).
    2. Open https://pypi.org/project/pyshacl/ — confirm canonical pySHACL (repo github.com/RDFLib/pySHACL, current v0.31.0), which itself depends on rdflib.
    3. Confirm resolved versions are current, non-yanked.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `cd src/ && poetry add rdflib pyshacl`.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SHACL shapes + validator (LPG→RDF export + quarantine) — D-13</name>
  <files>src/pyproject.toml, src/rag/graph/ontology/shapes.ttl, src/rag/graph/ontology/shacl_validator.py, tests/rag/graph/ontology/test_shacl_validation.py</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (Q5 — pyshacl.validate signature; LPG→RDF export mapping; the `sh:NodeShape` canonical-name/junk-pattern example; reject+log-separately)
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md ("shacl_validator.py" section — KGInspector session-query style for the export; quarantine like BuildStats.failures, no delete)
    - src/rag/graph/inspect/metrics.py (`KGInspector` `self._session()` + `session.run(...)` → aggregate-into-dict pattern to mirror for the export query)
    - src/rag/graph/ontology/ontology_config.json (locked node types to author shapes for)
  </read_first>
  <behavior>
    - Test (pure, in-memory rdflib — no Neo4j): a data graph with a node missing `name` → `conforms=False` with a violation on that node.
    - Test: a node with junk name "N.A." → `conforms=False` (rejected by `sh:pattern`).
    - Test: a fully valid fixture graph → `conforms=True`.
    - Test: violations are collected into a quarantine report (focusNode/resultPath/severity), NOT deleted.
  </behavior>
  <action>
    After the approved install, `cd src/ && poetry add rdflib pyshacl`. Author `shapes.ttl`: an `sh:NodeShape` per locked node type requiring `sh:minCount 1` + `sh:datatype xsd:string` on the canonical `name` property and an `sh:pattern` rejecting junk values (`N.A.`, `A`, empty). Implement `shacl_validator.py`: export the built graph LPG→RDF by querying Neo4j (`MATCH (n) RETURN elementId(n), labels(n), properties(n)` + relationships) into an in-memory `rdflib.Graph` using the KGInspector session-query style; run `pyshacl.validate(data_graph, shacl_graph=..., inference="none", abort_on_error=False)`; iterate the SHACL Validation Report (`sh:conforms`, `sh:result`) and write violating node/edge IDs to a `validation_report.json` (or a `:ValidationFailure` label) — QUARANTINE, do not delete (D-13). All export Cypher static + parameterized (T-09-12).
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/rag/graph/ontology/test_shacl_validation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Missing-name and junk-name fixtures → `conforms=False`; valid fixture → `conforms=True`.
    - Violations quarantined to a report, not deleted (asserted).
    - Export queries are static + parameterized.
  </acceptance_criteria>
  <done>SHACL validation rejects+logs non-conforming facts via pure-Python pyshacl, encoding D-07 constraints as machine-checked shapes.</done>
</task>

<task type="auto">
  <name>Task 3: `ccop-eval graph validate` CLI subcommand</name>
  <files>src/rag/graph/cli/graph.py</files>
  <read_first>
    - src/rag/graph/cli/graph.py (Typer command shape; `inspect`/`stats` subcommands to mirror)
    - src/rag/graph/ontology/shacl_validator.py (Task 2 validator)
  </read_first>
  <action>
    Add a `validate` subcommand to `graph_app` mirroring `inspect`/`stats`: it runs `shacl_validator` against the live graph and prints conformance + a violations summary (count by shape/severity), pointing at the quarantine report. Non-zero exit if HIGH-severity violations exist (so it can gate a build).
  </action>
  <verify>
    <automated>cd src/ && poetry run ccop-eval graph validate --help >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ccop-eval graph validate --help` exits 0.
    - Against a graph with violations, prints a summary + references the quarantine report.
  </acceptance_criteria>
  <done>Operator-facing SHACL validation CLI exists and can gate on conformance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shapes.ttl (committed) → pyshacl | trusted repo config; parsed as SHACL |
| Neo4j graph → LPG→RDF export queries | internal graph read into rdflib |
| PyPI → poetry add | third-party install (rdflib, pyshacl) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08-SC | Tampering | `poetry add rdflib pyshacl` (supply chain) | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies pypi.org before install; never auto-approvable |
| T-10-08-01 | Tampering | shapes.ttl edited to silently weaken constraints | mitigate | Code review on `shapes.ttl` changes (committed config, single-developer repo); no runtime control needed |
| T-10-08-02 | Tampering | Cypher injection via LPG→RDF export queries | mitigate | Static parameterized export Cypher only (T-09-12) |
| T-10-08-03 | Tampering | untrusted/malformed TTL parsing | mitigate | shapes.ttl is a committed repo artifact, not user-supplied; pyshacl parses trusted local file only |
</threat_model>

<verification>
- `cd src/ && poetry run pytest ../tests/rag/graph/ontology/test_shacl_validation.py -q` green (pure, no Neo4j).
- `graph validate` CLI functional against the live graph.
</verification>

<success_criteria>
SHACL validation (D-13) rejects+quarantines non-conforming facts via pyshacl, CLI-exposed, encoding D-07 canonical-name/junk constraints.
</success_criteria>

<output>
Create `.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-08-SUMMARY.md` when done.
</output>
