---
phase: 10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2
plan: 09
type: execute
wave: 5
depends_on: ["10-05", "10-07"]
files_modified:
  - src/rag/graph/retrieval/neo4j_ontology_graph_retrieval_adapter.py
  - src/rag/retrieval/nodes/function_type_routing.py
  - src/rag/retrieval/edges/routing.py
  - tests/rag/graph/retrieval/test_ontology_graph_retrieval_adapter.py
  - tests/rag/retrieval/nodes/test_function_type_routing.py
autonomous: true
requirements: [RAG-02, D-12, D-11, D-15]
must_haves:
  truths:
    - "Retrieval targets seeded :Clause nodes (real clause_id in citation_id), not the coarse extraction chunk (D-11 retrieval unit)"
    - "Function-type routing classifies question intent and boosts clauses tagged with the matching function-type (D-12) — surfacing §1.2.1/§1.4.1 over §5.6 distractors for a scope question"
    - "Retrieval is deterministic: exact-search-or-stable-tie-break per the 10-01 spike decision (D-15 prerequisite)"
    - "The graph path flows through the SAME shared cross-encoder reranker as hybrid (Wave-6 parity — one-variable ablation)"
  artifacts:
    - path: "src/rag/graph/retrieval/neo4j_ontology_graph_retrieval_adapter.py"
      provides: "Clause-anchored retrieval with function-type boost + stable tie-break (fills the 10-02 skeleton)"
      contains: "boosted_score"
    - path: "src/rag/retrieval/nodes/function_type_routing.py"
      provides: "LangGraph node classifying Scope/Control/Definition intent"
      contains: "function_type"
  key_links:
    - from: "function_type_routing node"
      to: "ontology adapter RETRIEVAL_QUERY"
      via: "state['function_type'] passed as bound $function_type Cypher param"
      pattern: "\\$function_type"
    - from: "ontology adapter"
      to: "seeded :Clause + shared reranker"
      via: "LINKED_TO clause lookup + dense_rank attach"
      pattern: "LINKED_TO|dense_rank"
---

<objective>
Fill the 10-02 skeleton adapter with **real clause-anchored retrieval + function-type routing** (D-12), the lever that fixes RANKING (grounding + clause nodes fix citation correctness; routing fixes which clause out-ranks). Classify question intent (Scope/Control/Definition), boost clauses tagged with the matching function-type (D-09), retrieve at seeded-:Clause granularity with the real clause_id as citation_id, apply the 10-01 determinism decision (stable tie-break for D-15), and preserve the shared-reranker funnel (one-variable ablation).

Purpose: Make §1.2.1/§1.4.1 out-rank the §5.6 distractor for the B01-001 worked example.
Output: Real ontology retrieval adapter + function-type routing node + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md
@docs/project_notes/research/2026-07-02-neo4j-exact-vector-search-spike.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clause-anchored ontology retrieval adapter — function-type boost + stable tie-break (D-12/D-11/D-15)</name>
  <files>src/rag/graph/retrieval/neo4j_ontology_graph_retrieval_adapter.py, tests/rag/graph/retrieval/test_ontology_graph_retrieval_adapter.py</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md ("neo4j_ontology_graph_retrieval_adapter.py" section — EXACT structural match to Phase 9 adapter; the rewritten RETRIEVAL_QUERY with LINKED_TO clause lookup + `CASE WHEN c.function_type = $function_type THEN score * $boost` + `ORDER BY boosted_score DESC, chunk.clause_id ASC`; real clause_id in citation_id fixes the Phase-9 "Honesty note")
    - src/rag/graph/retrieval/neo4j_graph_retrieval_adapter.py (Phase 9 adapter — RETRIEVAL_QUERY 55-72, constructor 76-101, _format_record 103-131, retrieve 133-151; T-09-12 parameterization)
    - docs/project_notes/research/2026-07-02-neo4j-exact-vector-search-spike.md (the LOCKED determinism decision from 10-01 — apply it here)
    - src/rag/graph/retrieval/neo4j_ontology_graph_retrieval_adapter.py (the 10-02 skeleton to fill — keep the `metadata["provider"]="graphrag-ontology"` marker)
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (Q7 boost mechanism; Q8 stable tie-break; D-12 escalation path)
  </read_first>
  <behavior>
    - Test: RETRIEVAL_QUERY boosts a clause whose `function_type == $function_type` above an equal-base-score clause that doesn't match (asserted on mocked retriever records).
    - Test: results are ordered by `boosted_score DESC, clause_id ASC` (stable tie-break — identical ordering across two runs on tied scores).
    - Test: `citation_id`/`section` carry the REAL seeded clause_id (not `elementId(chunk)`).
    - Test: `$function_type` and `$boost` are passed as bound parameters (never interpolated).
  </behavior>
  <action>
    Replace the 10-02 skeleton `RETRIEVAL_QUERY` with the clause-anchored boosted query: `WITH node AS chunk, score OPTIONAL MATCH (chunk)-[:LINKED_TO]->(c:Clause) WITH chunk, score, c, CASE WHEN c.function_type = $function_type THEN score * $boost ELSE score END AS boosted_score RETURN chunk.text AS original_text, c.clause_id AS citation_id, ... boosted_score AS score ORDER BY boosted_score DESC, c.clause_id ASC` — keep it a STATIC class-level string; pass `$function_type` (from GraphState) and `$boost` (settings `function_type_boost`, default 1.5) as bound params via the HybridCypherRetriever `retrieval_query` mechanism (verify exact param-passing API at build time, Q7). Populate `citation_id`/`section` with the real seeded clause_id (fixes the Phase-9 honesty note). Apply the 10-01 determinism decision (exact-search API if it exists, else the stable Cypher tie-break already in the ORDER BY + frozen-index note). Attach `dense_rank` and populate `documents` (NOT `filtered_documents`) so the shared cross-encoder reranker owns the final funnel (Wave-6 parity — one-variable ablation). Keep the `metadata["provider"]` marker.
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/rag/graph/retrieval/test_ontology_graph_retrieval_adapter.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Function-type match boosts score (asserted); ordering stable on ties (`clause_id ASC`).
    - citation_id == real seeded clause_id.
    - `$function_type`/`$boost` bound params; RETRIEVAL_QUERY is a static string.
    - `documents` populated with `dense_rank`; `filtered_documents` NOT pre-set (reranker parity).
  </acceptance_criteria>
  <done>Clause-anchored retrieval with function-type boost + deterministic ordering + real clause_id citations, flowing through the shared reranker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Function-type routing node (D-12) + graph wiring</name>
  <files>src/rag/retrieval/nodes/function_type_routing.py, src/rag/retrieval/edges/routing.py, tests/rag/retrieval/nodes/test_function_type_routing.py</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md ("function_type_routing.py" section — EXACT analog to query_analysis.py HyDE call; gate on mode=="graphrag-ontology"; set state["function_type"]; the No-Analog note on the classification PROMPT content)
    - src/rag/retrieval/nodes/query_analysis.py (lines 27-76 — the OpenRouter gpt-4o-mini classification-call pattern + graceful-degradation + mode-gating to mirror)
    - src/rag/retrieval/state/graph_state.py (the `function_type` field added in 10-02)
    - src/rag/retrieval/edges/routing.py (route_by_mode — the graphrag-ontology branch from 10-02; wire the function_type node into the ontology retrieval subgraph)
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (Q7 FUNCTION_TYPE_PROMPT draft; D-12 escalation to "Both, layered" if hit@3 gate not cleared)
  </read_first>
  <behavior>
    - Test (mocked LLM): a scope question → `state["function_type"]=="ScopeClause"`; a "what must be done" question → "ControlClause"; a "what does X mean" question → "DefinitionClause".
    - Test: node is a no-op (does not fail the request) when the LLM call errors (graceful degradation, mirrors HyDE).
    - Test: node only runs for mode=="graphrag-ontology" (gated).
  </behavior>
  <action>
    Implement `function_type_routing.py` mirroring `query_analysis.py::_generate_hyde`: a gpt-4o-mini classification call (settings model/base_url/api_key) with the FUNCTION_TYPE_PROMPT classifying `state["query"]` into exactly one of `ScopeClause`/`ControlClause`/`DefinitionClause`, stored in `state["function_type"]`. Gate on `state.get("mode")=="graphrag-ontology"` (like HyDE's mode-gating). Reuse the try/except-log-return pattern (never fail the whole request). Wire the node into the ontology retrieval path in `routing.py` so it runs BEFORE the ontology retrieval node (so `$function_type` is set before the boosted query). Leave a documented hook for the D-12 escalation ("Both, layered": add entity-anchored traversal) if the clause-hit@3 gate (10-10) isn't cleared — do NOT implement escalation now.
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/rag/retrieval/nodes/test_function_type_routing.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Correct classification for scope/control/definition fixtures.
    - Graceful no-op on LLM error; runs only for graphrag-ontology mode.
    - Wired to run before the ontology retrieval node (state["function_type"] set first).
  </acceptance_criteria>
  <done>Question intent is classified and threaded to the boosted retrieval query, enabling function-type ranking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query → function-type classification LLM | untrusted query reaches gpt-4o-mini |
| GraphState function_type/query → Cypher retrieval | classification + query reach the boosted Cypher query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09-01 | Tampering | Cypher injection via `$function_type`/query into the boosted RETRIEVAL_QUERY | mitigate | RETRIEVAL_QUERY is a static class-level string; `$function_type`/`$boost`/query_vector passed as bound params only (T-09-12); classifier output constrained to a 3-value enum before binding |
| T-10-09-02 | Information Disclosure | classification LLM (OpenRouter) API key | mitigate | Env-sourced via settings, never logged; reuse HyDE degradation pattern |
| T-10-09-03 | Tampering | prompt injection steering misclassification | accept | Low impact — worst case is a wrong function-type boost (ranking only, not correctness); shared reranker + clause-hit@3 gate (10-10) surface regressions |
</threat_model>

<verification>
- `cd src/ && poetry run pytest ../tests/rag/graph/retrieval/test_ontology_graph_retrieval_adapter.py ../tests/rag/retrieval/nodes/test_function_type_routing.py -q` green.
- Integration: a live graphrag-ontology query returns clause-id-cited results with function-type boost applied.
</verification>

<success_criteria>
Clause-anchored, function-type-routed, deterministic retrieval flowing through the shared reranker — the ranking lever (D-12) that surfaces the correct clauses in top-3.
</success_criteria>

<output>
Create `.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-09-SUMMARY.md` when done.
</output>
