---
phase: 10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2
plan: 10
type: execute
wave: 6
depends_on: ["10-01", "10-09"]
files_modified:
  - src/domain/services/clause_hit_scoring_service.py
  - src/application/use_cases/clause_hit_harness.py
  - src/rag/graph/cli/graph.py
  - tests/domain/services/test_clause_hit_scoring_service.py
  - tests/application/use_cases/test_clause_hit_harness.py
autonomous: true
requirements: [EVAL-03, D-15]
must_haves:
  truths:
    - "For each GT case, the harness asserts the expected clause SET (not a single ID) against the top-3 that reaches the LLM (D-15)"
    - "Metrics computed: clause hit@3, recall@3, recall@pool(50) (D-15)"
    - "Retrieval feeding the harness is deterministic (exact search or stable tie-break per 10-01) — reproducible pass/fail across runs (D-15)"
    - "The 18-case gold clause sets are cross-checked against the D-17 xlsx bracketed citations, not clause_reference alone (Pitfall 4)"
  artifacts:
    - path: "src/domain/services/clause_hit_scoring_service.py"
      provides: "Pure scoring: hit@3 / recall@3 / recall@pool(50) against a gold clause SET"
      contains: "def "
    - path: "src/application/use_cases/clause_hit_harness.py"
      provides: "Harness use case running deterministic retrieval → clause-hit scoring over the 18-case GT"
  key_links:
    - from: "clause_hit_harness"
      to: "ontology retrieval adapter (10-09)"
      via: "runs graphrag-ontology retrieval, scores top-3 clause_ids vs gold set"
      pattern: "hit@3|recall"
---

<objective>
Build the **deterministic clause-hit@3 harness** (D-15) — the acceptance gate for Phase 10. For each of the 18 fixed-GT cases, assert the expected clause SET appears in the top-3 reaching the LLM, computing hit@3 / recall@3 / recall@pool(50). Determinism comes from the 10-01 spike decision (exact search or stable tie-break + frozen index). Gold clause sets are cross-checked against the D-17 xlsx bracketed citations (clause_reference alone under-represents — Pitfall 4).

Purpose: A reproducible, set-valued retrieval-quality gate that isolates whether the graph CONTAINS the right clauses (pool) and RANKS them (hit@3).
Output: `clause_hit_scoring_service.py` + harness use case + CLI + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md
@docs/project_notes/research/2026-07-02-neo4j-exact-vector-search-spike.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClauseHitScoringService — pure set-valued scoring (D-15)</name>
  <files>src/domain/services/clause_hit_scoring_service.py, tests/domain/services/test_clause_hit_scoring_service.py</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (Q8 — the three metric formulas; clause SET not single ID; Pitfall 4 clause_reference under-representation)
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md ("clause_hit_scoring_service.py" section — ScoringService stateless @staticmethod domain-service pattern, no external deps)
    - src/domain/services/scoring_service.py (the stateless static-method domain-service analog to mirror)
    - ground-truth/test-suite/b01_ccop_applicability_scope.jsonl (`metadata.clause_reference` is a LIST; B01-001 shows the under-representation issue)
  </read_first>
  <behavior>
    - Test: `hit@3` = 1 when gold_set ∩ top3_clause_ids ≠ ∅, else 0 (fixture with known sets).
    - Test: `recall@3` = |gold ∩ top3| / |gold| (partial-credit fraction).
    - Test: `recall@pool(50)` = |gold ∩ top50| / |gold| (containment independent of ranking).
    - Test: clause-id normalization handles `§`/whitespace/`(c)` variants consistently between gold and retrieved.
  </behavior>
  <action>
    Implement `ClauseHitScoringService` as a stateless `@staticmethod` domain service (mirror `ScoringService`), pure functions, NO external deps: `hit_at_3(gold_set, retrieved_top3)`, `recall_at_3(gold_set, retrieved_top3)`, `recall_at_pool(gold_set, retrieved_pool, pool_size=50)`. Normalize clause ids (strip `§`, normalize whitespace, keep `(c)` sub-item suffixes) on both sides before set ops. Gold is a SET (D-15), never a single id. Keep it dependency-free so it runs in the fast unit slice.
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/domain/services/test_clause_hit_scoring_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - hit@3 / recall@3 / recall@pool computed correctly on fixtures with known sets.
    - Clause-id normalization consistent across gold/retrieved.
    - No external dependency (runs in `-m "not integration"`).
  </acceptance_criteria>
  <done>Pure, set-valued clause-hit scoring service matching the D-15 metric definitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clause-hit harness use case (deterministic retrieval → scoring) + CLI + gold cross-check</name>
  <files>src/application/use_cases/clause_hit_harness.py, src/rag/graph/cli/graph.py, tests/application/use_cases/test_clause_hit_harness.py</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (Q8 harness design; Open Question 3 — cross-check ONLY the 18 bdc4927d cases against the D-17 xlsx, bounded task)
    - docs/project_notes/research/2026-07-02-neo4j-exact-vector-search-spike.md (determinism decision to apply)
    - src/rag/graph/ontology/gold_relation_parser.py (10-03 — reuse to pull bracketed clause citations from the xlsx for the cross-check)
    - src/domain/services/clause_hit_scoring_service.py (Task 1)
    - src/application/use_cases/evaluate_model.py (use-case structure/DI to mirror for the harness)
  </read_first>
  <behavior>
    - Test (mocked retrieval): harness runs the 18 cases, computes per-case hit@3/recall@3/recall@pool and an aggregate, deterministically (same inputs → same output across two invocations).
    - Test: gold clause SET per case = union of `metadata.clause_reference` AND the D-17 xlsx bracketed citations (cross-check), flagging disagreements (Pitfall 4).
    - Test (integration): a live graphrag-ontology run on B01-001 surfaces §1.2.1/§1.4.1 in the retrieved pool (the worked-example containment check).
  </behavior>
  <action>
    Implement `clause_hit_harness.py`: for each of the 18 bdc4927d GT cases, run graphrag-ontology retrieval (deterministic per 10-01), extract the top-3 and top-50 clause_ids, and score via `ClauseHitScoringService`. Build the gold clause SET per case by UNIONing `metadata.clause_reference` with the D-17 xlsx bracketed citations (reuse `gold_relation_parser`), flagging any case where the two disagree for human review (do NOT silently trust clause_reference — Pitfall 4). Add a `clause-hit` subcommand to `graph_app` printing per-case + aggregate hit@3/recall@3/recall@pool. Ensure the run is reproducible (assert stable output across two runs on the same frozen index).
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/application/use_cases/test_clause_hit_harness.py -x -q && poetry run ccop-eval graph clause-hit --help >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Harness output is deterministic (identical across two runs on a frozen index).
    - Gold set = clause_reference ∪ xlsx citations, with disagreements flagged.
    - `graph clause-hit --help` exits 0; live B01-001 containment check surfaces §1.2.1/§1.4.1 in the pool.
  </acceptance_criteria>
  <done>A reproducible clause-hit@3 harness scores graphrag-ontology retrieval against cross-checked gold clause sets over the 18-case GT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GT JSONL + eval-report xlsx → harness | committed fixtures read for gold sets |
| retrieval → scoring | internal graph retrieval results scored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10-01 | Tampering | non-deterministic retrieval producing flaky pass/fail (Pitfall 2) | mitigate | Apply the 10-01 determinism decision (exact search or stable tie-break + frozen index); test asserts identical output across two runs |
| T-10-10-02 | Tampering | trusting clause_reference alone → misleading gate (Pitfall 4) | mitigate | Gold set cross-checked against D-17 xlsx citations; disagreements flagged, not silently resolved |
</threat_model>

<verification>
- `cd src/ && poetry run pytest ../tests/domain/services/test_clause_hit_scoring_service.py ../tests/application/use_cases/test_clause_hit_harness.py -q` green.
- Integration containment check for B01-001 green.
</verification>

<success_criteria>
Deterministic, set-valued clause-hit@3 harness (D-15) is the reproducible acceptance gate, scoring against cross-checked gold clause sets.
</success_criteria>

<output>
Create `.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-10-SUMMARY.md` when done.
</output>
