---
phase: 10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2
plan: 06
type: execute
wave: 3
depends_on: ["10-04"]
files_modified:
  - src/rag/graph/build/section_aligned_splitter.py
  - src/rag/graph/build/gleaning_extractor.py
  - tests/rag/graph/build/test_section_aligned_splitter.py
  - tests/rag/graph/build/test_gleaning_extractor.py
autonomous: true
requirements: [RAG-02, D-11]
must_haves:
  truths:
    - "The extraction unit is decoupled from the retrieval unit: a section-aligned splitter groups multiple clauses under one top-level section boundary (D-11 extraction chunk)"
    - "A gleaning (multi-pass) extractor recovers additional entities that a single pass misses (D-11 gleaning)"
  artifacts:
    - path: "src/rag/graph/build/section_aligned_splitter.py"
      provides: "Coarse section-aligned TextSplitter component (neo4j-graphrag text_splitter= contract)"
      contains: "class SectionAlignedSplitter"
    - path: "src/rag/graph/build/gleaning_extractor.py"
      provides: "GleaningEntityRelationExtractor subclass overriding extract_for_chunk"
      contains: "extract_for_chunk"
  key_links:
    - from: "section_aligned_splitter + gleaning_extractor"
      to: "10-07 ontology_kg_builder pipeline"
      via: "injected as text_splitter= and the extractor in the hand-built Pipeline"
      pattern: "SectionAlignedSplitter|GleaningEntityRelationExtractor"
---

<objective>
Build the two **extraction-unit** components that implement D-11's decouple: (1) a coarse **section-aligned splitter** (multiple clauses under one top-level section boundary — NOT clause-granularity, which starves extraction) and (2) a **gleaning** (multi-pass) extractor that recovers entity recall lost to larger chunks. neo4j-graphrag ships neither — both are user-added override points.

Purpose: Extract at section scale + glean for recall, while retrieval stays fine-grained at the seeded clause node (10-05). These plug into the ontology KG builder (10-07).
Output: `section_aligned_splitter.py` + `gleaning_extractor.py` with mocked unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Section-aligned extraction splitter (D-11 extraction unit)</name>
  <files>src/rag/graph/build/section_aligned_splitter.py, tests/rag/graph/build/test_section_aligned_splitter.py</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (Q3 — section-aligned chunking; the extract-large/retrieve-fine decouple; the "no source ran this exact ablation" caveat)
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md ("section_aligned_splitter.py" section — reuse CLAUSE_PATTERN regex + hierarchy, but split only at TOP-LEVEL section boundaries; implements neo4j-graphrag Component `text_splitter=` contract)
    - src/rag/ingestion/chunkers/clause_aware_chunker.py (lines 42-74 CLAUSE_PATTERN; `_extract_section` — the boundary-detection source; do NOT reuse `chunk_by_clauses` output which is clause-granularity)
  </read_first>
  <behavior>
    - Test: given markdown with `5.3`, `5.3.1`, `5.3.2`, `5.4` headings, the splitter produces ONE chunk containing all of 5.3.* under `5.3` and a separate chunk for `5.4.*` (splits at top-level section, not sub-clause).
    - Test: output conforms to the neo4j-graphrag TextChunks shape (a `.run(text)`-style Component returning chunks).
    - Test: no sub-clause boundary (e.g. `5.3.1(a)`) creates a new chunk.
  </behavior>
  <action>
    Implement `SectionAlignedSplitter` as a custom neo4j-graphrag `Component` satisfying the `text_splitter=` contract (a `.run(text) -> TextChunks` async component per Context7 "Customize Text Splitter"). Reuse the SAME `## X.Y heading` boundary regex as `clause_aware_chunker.CLAUSE_PATTERN`, but split ONLY at top-level section numbers (e.g. `5.3`, not `5.3.1(a)`), merging all sub-clauses under one section into a single chunk. Parse boundaries fresh from the Docling markdown (Qdrant `ChunkMetadata` is NOT available on the raw corpus). This is the "extract-large" half of D-11 — coarser than Phase 9's arbitrary 4000-char FixedSizeSplitter but structure-aware; do NOT go clause-granularity (D-05/D-20 anti-pattern).
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/rag/graph/build/test_section_aligned_splitter.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Sub-clauses group under their top-level section (asserted on the 5.3/5.4 fixture).
    - Splitter satisfies the neo4j-graphrag `text_splitter=` component contract (importable + `.run` returns chunks).
  </acceptance_criteria>
  <done>A section-aligned splitter groups multi-clause sections into single extraction chunks, reusing proven regex, ready to inject as `text_splitter=`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gleaning multi-pass extractor (D-11 gleaning)</name>
  <files>src/rag/graph/build/gleaning_extractor.py, tests/rag/graph/build/test_gleaning_extractor.py</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (Q2 — `extract_for_chunk` override point verified against installed source; the glean-again prompt; the recall trade-off; A5 on hand-built Pipeline)
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md ("gleaning_extractor.py" section — factory-injection discipline; reuse base-class JSON-repair/OnError, don't hand-roll)
    - src/rag/graph/build/kg_builder.py (lines 37-77 factory-injection style — mirror `max_gleanings` as an explicit testable constructor param)
  </read_first>
  <behavior>
    - Test (mocked LLM scripted for 2 calls): with `max_gleanings=1`, the extractor makes a first extraction call then a follow-up "what was missed" call, and the returned graph contains entities from BOTH passes (union, no drop).
    - Test: with `max_gleanings=0`, behaviour equals a single-pass `super().extract_for_chunk`.
    - Test: malformed follow-up JSON is handled via the base class's repair/OnError path (no crash), not a new parser.
  </behavior>
  <action>
    Implement `GleaningEntityRelationExtractor(LLMEntityRelationExtractor)` with an explicit `max_gleanings: int` constructor param (default from `settings.gleaning_max_gleanings`). Override `async def extract_for_chunk(self, schema, examples, chunk)` to call `super().extract_for_chunk` once, then loop `max_gleanings` times issuing a follow-up prompt that lists already-found entities and asks for ADDITIONAL ones, extending `graph.nodes`/`graph.relationships`. REUSE the base class's `fix_invalid_json` + `Neo4jGraph.model_validate` for parsing follow-up results (Don't-Hand-Roll). This is designed to be injected into a hand-built `Pipeline` (SimpleKGPipeline hardcodes its own extractor — verify the constructor signature at build time per A5). Errors per pass reported, never swallowed (T-09-08).
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/rag/graph/build/test_gleaning_extractor.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `max_gleanings=1` yields a strict superset of single-pass entities (asserted with a scripted 2-call mock).
    - `max_gleanings=0` equals single-pass behaviour.
    - Follow-up JSON errors handled by the inherited repair path, not new code.
  </acceptance_criteria>
  <done>A gleaning extractor recovers additional entities via multi-pass extraction, injectable into the ontology build pipeline, reusing library JSON handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus prose → extraction LLM (gleaning) | untrusted regulatory + example prose reaches gpt-4o-mini across multiple passes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering (prompt injection) | gleaning follow-up prompt over corpus prose | mitigate | Extraction is schema-constrained downstream (10-07 `additional_node_types=false`) + the custom prompt instructs to ignore illustrative/example passages (D-07); gleaning inherits the same schema; SHACL (10-08) is the structural backstop |
| T-10-06-02 | Tampering | malformed/adversarial LLM JSON | mitigate | Reuse base-class `fix_invalid_json`/OnError; no hand-rolled parser; failures reported (T-09-08) |
</threat_model>

<verification>
- `cd src/ && poetry run pytest ../tests/rag/graph/build/test_section_aligned_splitter.py ../tests/rag/graph/build/test_gleaning_extractor.py -q` green.
</verification>

<success_criteria>
Extraction-unit decoupled from retrieval-unit (D-11): section-aligned splitter + gleaning extractor built and unit-tested, ready for the ontology KG builder.
</success_criteria>

<output>
Create `.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-06-SUMMARY.md` when done.
</output>
