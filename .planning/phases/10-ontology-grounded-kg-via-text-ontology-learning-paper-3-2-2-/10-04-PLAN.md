---
phase: 10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2
plan: 04
type: execute
wave: 2
depends_on: ["10-03"]
files_modified:
  - src/rag/graph/ontology/discovery/method_b_clustering.py
  - src/rag/graph/ontology/ontology_config.json
  - tests/rag/graph/ontology/test_method_b_clustering.py
  - src/pyproject.toml
autonomous: false
requirements: [RAG-06, D-01, D-05, D-14, D-17]
must_haves:
  truths:
    - "Method B (clustering) runs as an independent coverage cross-check against Method C (D-05)"
    - "B-only clusters are surfaced to the user as candidate missing types for keep/drop (D-05)"
    - "The reconciled ontology is LOCKED as a committed config after a second human curation gate (b) that passes the D-14 benchmark-coverage AND D-17 gold-relation coverage checks (D-14/D-17)"
  artifacts:
    - path: "src/rag/graph/ontology/ontology_config.json"
      provides: "The LOCKED ontology (node_types/relationship_types/patterns) consumed by all mechanical build waves"
      contains: "node_types"
    - path: "src/rag/graph/ontology/discovery/method_b_clustering.py"
      provides: "AffinityPropagation term-clustering cross-check (D-05)"
  key_links:
    - from: "method_b_clustering"
      to: "curation gate (b) reconcile"
      via: "B-only clusters presented as candidate missing types"
      pattern: "b_only"
    - from: "ontology_config.json (locked)"
      to: "10-05/10-06/10-07 build waves"
      via: "schema= kwarg + function_type tags consumed at build time"
      pattern: "additional_node_types"
---

<objective>
Complete D-01's `C → curate → B → reconcile → lock` sequence. Run **Method B (clustering)** as an independent coverage cross-check against the approved Method-C draft, surface B-only clusters as candidate missing types, reconcile at a second human curation gate (b), and LOCK the reconciled ontology as a committed config that every mechanical build wave consumes.

Purpose: A second, structurally-different discovery lens catches types C missed; the lock is the gate that all build waves depend on.
Output: Locked `ontology_config.json` + Method-B clustering cross-check + approved curation gate (b).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-CONTEXT.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md
@src/rag/graph/ontology/ontology_draft.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — scikit-learn ([ASSUMED]) before install</name>
  <action>Pause before installing scikit-learn. Surface the pypi.org legitimacy evidence and wait for explicit approval (or the scipy-fallback instruction) before any install runs. Never auto-approve.</action>
  <what-built>Nothing yet — this checkpoint gates the `poetry add scikit-learn` install required for Method B's AffinityPropagation clustering. RESEARCH marked scikit-learn `[ASSUMED]` (slopcheck not run); protocol requires human verification before install.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/scikit-learn/ — confirm it is the canonical scikit-learn project (maintainer: scikit-learn dev team; repo github.com/scikit-learn/scikit-learn; ~15+ years, very high downloads).
    2. Confirm the version Poetry will resolve is a current, non-yanked release.
    3. Approve the `poetry add scikit-learn` install (from `src/`).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `cd src/ && poetry add scikit-learn`, or "use scipy fallback" to instruct hand-rolled clustering via the already-transitive scipy instead (RESEARCH Environment Availability fallback).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Method B clustering cross-check (D-05)</name>
  <files>src/pyproject.toml, src/rag/graph/ontology/discovery/method_b_clustering.py, tests/rag/graph/ontology/test_method_b_clustering.py</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (Q6 Method B pipeline: extract terms → embed with bge-large-en-v1.5 → AffinityPropagation → LLM-name clusters → cross-check vs C; Assumption A3 on term-extraction-for-B and D-02 compliance)
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md ("method_b_clustering.py" section — script shape; sklearn has no project analog)
    - src/rag/graph/ontology/ontology_draft.json (the approved Method-C draft to cross-check against)
    - src/rag/retrieval/nodes/query_analysis.py (the OpenRouter LLM-call pattern for cluster naming)
  </read_first>
  <behavior>
    - Test (mocked embedder + fixed term list): clustering groups synonymous terms and `b_only_clusters` correctly excludes clusters already covered by the Method-C draft.
    - Test: cluster-naming LLM call is mocked; output candidate type names are returned with cluster membership.
  </behavior>
  <action>
    After the approved install, run `cd src/ && poetry add scikit-learn` (or wire the scipy fallback if the checkpoint chose it). Implement `method_b_clustering.py` as a one-shot CLI script: extract candidate domain terms from corpus prose via a lightweight "list noun phrases/domain terms" gpt-4o-mini prompt over section-level chunks (fresh corpus extraction — do NOT ingest the Phase 9 emergent GRAPH, per D-02; A3), embed each term with the SAME `SentenceTransformerEmbeddings(model=settings.graph_embedding_model)` already used for chunk embeddings (reuse, do not add a second embedding model), cluster with `sklearn.cluster.AffinityPropagation` (no pre-set k), and LLM-name each cluster. Compute `b_only_clusters` = clusters whose named type is absent from the Method-C draft. Output a reconcile report: `{c_types, b_types, b_only, overlap}` for the human gate. This is a COVERAGE cross-check, not a replacement for C.
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/rag/graph/ontology/test_method_b_clustering.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `b_only_clusters` excludes any cluster already covered by the Method-C draft (asserted on a fixture where one cluster overlaps C and one does not).
    - scikit-learn (or scipy fallback) dependency present in `pyproject.toml`.
    - Term extraction reads corpus prose, not the Phase 9 emergent graph (D-02).
  </acceptance_criteria>
  <done>Method B produces a reconcile report listing B-only candidate types for the human gate, using the reused embedding model + AffinityPropagation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Curation gate (b) — reconcile B vs C, pass coverage checks, LOCK the ontology (D-14/D-17)</name>
  <action>Pause for the second human curation gate. Present the reconcile report (C vs B-only clusters) + re-run D-14/D-17 coverage against the reconciled types; apply the user's keep/drop decisions and only then lock ontology_config.json with additional_node_types=false.</action>
  <what-built>A reconcile report (Method-C draft vs Method-B B-only clusters), re-run of the D-14 benchmark-coverage check and the D-17 gold-relation coverage check against the reconciled type set, and a proposed LOCKED `ontology_config.json`.</what-built>
  <how-to-verify>
    1. Review each B-only cluster — decide keep (add to ontology) or drop, per D-05.
    2. Confirm the reconciled ontology passes the D-14 check: all 18 benchmarks map to a type (no `unmapped`).
    3. Confirm the reconciled ontology passes the D-17 check: `missing_relations` empty (every gold relation family from the xlsx col-22 triples is covered), OR you explicitly waive specific ones with rationale.
    4. Confirm the final config sets `additional_node_types: false` and `additional_relationship_types: false` (locks the vocabulary — D-06/D-07 anti-pattern fix) ONLY after the coverage checks pass (Pitfall 1: locking too early silently drops entities).
  </how-to-verify>
  <resume-signal>Type "locked" to commit `ontology_config.json` as the authoritative build-time schema, or list the keep/drop/amend decisions to apply first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus prose → term-extraction + cluster-naming LLM | untrusted regulatory prose reaches gpt-4o-mini |
| PyPI → poetry add | third-party package install (scikit-learn) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-SC | Tampering | `poetry add scikit-learn` (supply chain) | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies pypi.org/project/scikit-learn before install; never auto-approvable |
| T-10-04-01 | Tampering (prompt injection) | cluster-naming / term-extraction LLM over corpus prose | mitigate | Curation gate (b) reviews every B-only candidate before it can enter the locked ontology |
| T-10-04-02 | Tampering | premature vocabulary lock silently dropping entities | mitigate | `additional_node_types=false` set ONLY after D-14 + D-17 coverage checks pass (Pitfall 1) |
</threat_model>

<verification>
- `cd src/ && poetry run pytest ../tests/rag/graph/ontology/ -m "not integration" -q` green.
- Curation gate (b) approved; `ontology_config.json` committed and locked.
</verification>

<success_criteria>
Reconciled ontology locked as a committed config with vocabulary closed only after passing D-14 + D-17 coverage; the C→curate→B→reconcile→lock sequence (D-01) is complete.
</success_criteria>

<output>
Create `.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-04-SUMMARY.md` when done.
</output>
