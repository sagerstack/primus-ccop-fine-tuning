---
phase: 10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2
plan: 11
type: execute
wave: 7
depends_on: ["10-01", "10-09", "10-10"]
files_modified:
  - src/results/evaluations/
  - report/term3/phase10-ontology-ab-report.md
  - tests/application/use_cases/test_ab_report.py
autonomous: false
requirements: [EVAL-02, EVAL-03, D-16]
must_haves:
  truths:
    - "A three-way A/B report compares graphrag-ontology vs basic graphrag (10-01 baseline) vs hybrid (canonical bdc4927d) on the identical 18-case GT (D-16)"
    - "The comparison isolates ONE variable — extraction governance — on the identical engine/input/harness (D-16)"
    - "Deciding signals reported: clause-hit@3 (10-10) + LLM-judge citation/grounding dims + RAGAs context metrics (D-16)"
    - "No 'ontology improved X' claim is made without the Phase 9 18-case baseline present (D-16 hard dependency honored)"
  artifacts:
    - path: "src/results/evaluations/2026-07/eval-run-graphrag-ontology-tests-18-bdc4927d-*.json"
      provides: "graphrag-ontology 18-case eval (A/B leg 3)"
      contains: "test_results"
    - path: "report/term3/phase10-ontology-ab-report.md"
      provides: "Three-way A/B comparison + per-benchmark gap analysis (EVAL-02/EVAL-03)"
      contains: "clause-hit@3"
  key_links:
    - from: "A/B report"
      to: "10-01 graphrag baseline + canonical hybrid + 10-10 harness"
      via: "three legs scored on the same 18-case GT with the same rubric judge"
      pattern: "graphrag-ontology|graphrag|hybrid"
---

<objective>
Produce the **A/B report** (D-16): graphrag-ontology vs basic graphrag (Phase 9 baseline from 10-01) vs hybrid (canonical `bdc4927d`), on the identical 18-case GT, isolating the single variable — ontology extraction governance — on the same engine/input/harness. Report clause-hit@3 (10-10) + LLM-judge citation/grounding dims + RAGAs context metrics, with a per-benchmark gap analysis. This is the phase's headline deliverable and the reason the Phase 9 baseline was a hard dependency.

Purpose: A trustworthy, one-variable measurement of whether ontology grounding helps.
Output: graphrag-ontology 18-case eval + a three-way comparison report + human review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-CONTEXT.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the graphrag-ontology 18-case evaluation (A/B leg 3)</name>
  <files>src/results/evaluations/</files>
  <read_first>
    - CLAUDE.md (Build/Run/Test → `evaluate run --mode`, `--test-ids`, `--judge-mode rubric`; canonical hybrid baseline note in Project Memory — only the rubric bdc4927d run is the parity mode)
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-01-SUMMARY.md (the graphrag baseline run id + the 18 test_ids used)
    - src/results/evaluations/2026-04/eval-run-hybrid-tests-18-bdc4927d-20260430-0232-primus-reasoning.json (the canonical hybrid leg — same 18 ids, rubric judge)
    - ~/.claude/rules/e2e-testing.md (this IS the full real E2E of the phase)
  </read_first>
  <action>
    With the ontology graph built (10-07), SHACL-validated (10-08), and seeded (10-05), run `cd src/ && poetry run ccop-eval evaluate run --model primus-reasoning --mode graphrag-ontology --judge-mode rubric` scoped to the SAME 18 `--test-ids` as the bdc4927d set (from 10-01). Use RUBRIC judge for parity with the canonical hybrid + graphrag baselines (mode-mismatched scoring would confound the one-variable ablation). Do NOT change generator (primus held constant), embedder, or reranker — only extraction governance differs from the graphrag baseline. This is leg 3 of the three-way A/B.
  </action>
  <verify>
    <automated>cd src/ && poetry run python -c "import glob,json; f=sorted(glob.glob('results/evaluations/2026-07/eval-run-graphrag-ontology-tests-18-bdc4927d-*-primus-reasoning.json'))[-1]; d=json.load(open(f)); assert len(d['test_results'])==18; print('OK',f)"</automated>
  </verify>
  <acceptance_criteria>
    - graphrag-ontology run JSON exists with 18 rubric-judged results over the bdc4927d ids.
    - Generator/embedder/reranker unchanged vs the graphrag baseline (only extraction governance differs).
  </acceptance_criteria>
  <done>A/B leg 3 (graphrag-ontology, 18 cases, rubric) exists alongside legs 1 (graphrag) and 2 (hybrid).</done>
</task>

<task type="auto">
  <name>Task 2: Three-way A/B comparison report + per-benchmark gap analysis (EVAL-02/EVAL-03/D-16)</name>
  <files>report/term3/phase10-ontology-ab-report.md, tests/application/use_cases/test_ab_report.py</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (D-16 deciding signals: clause-hit@3 + judge citation/grounding + RAGAs context metrics)
    - src/application/use_cases/clause_hit_harness.py (10-10 — run clause-hit@3 for all three legs)
    - src/results/evaluations/2026-07/ (graphrag + graphrag-ontology legs) and 2026-04/ (hybrid canonical leg)
    - CLAUDE.md (Reporting → `report generate --format markdown`; the specific B01/B03/B04 worked examples)
  </read_first>
  <action>
    Generate `report/term3/phase10-ontology-ab-report.md`: a three-way table (graphrag-ontology vs graphrag vs hybrid) on the 18-case GT for clause-hit@3 / recall@3 / recall@pool(50) (via the 10-10 harness run for all three legs), LLM-judge citation + grounding dimensions, and RAGAs context_recall/context_precision/context_faithfulness. Add a per-benchmark gap analysis (EVAL-03: which benchmarks improved, which didn't) and a B01-001 deep-dive confirming whether §1.2.1/§1.4.1 now out-rank the §5.6 distractor in top-3. State explicitly that the Phase 9 baseline (10-01) exists so the comparison is trustworthy (D-16). Add a small test asserting the report aggregation logic (reads three run JSONs, computes deltas) is correct on fixtures. Do NOT overclaim: report deltas with the within-noise caveat where n=18.
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/application/use_cases/test_ab_report.py -x -q && test -f ../report/term3/phase10-ontology-ab-report.md && grep -qi "clause-hit@3" ../report/term3/phase10-ontology-ab-report.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Report contains the three-way comparison across clause-hit@3 + judge citation/grounding + RAGAs context metrics.
    - Per-benchmark gap analysis present (EVAL-03); B01-001 deep-dive present.
    - Report aggregation test green; no claim made absent the Phase 9 baseline.
  </acceptance_criteria>
  <done>A trustworthy three-way A/B report isolating ontology-governance effect, with per-benchmark gap analysis and the B01-001 worked example.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review A/B conclusions</name>
  <action>Pause for human review of the A/B conclusions. Present the three-way report and confirm parity + appropriate hedging; capture the user's decision to close Phase 10 or trigger follow-ups (D-12 escalation, GT review).</action>
  <what-built>`report/term3/phase10-ontology-ab-report.md` — a three-way comparison (graphrag-ontology vs basic graphrag vs hybrid) on the 18-case GT, with clause-hit@3, judge citation/grounding, RAGAs context metrics, per-benchmark gap analysis, and a B01-001 deep-dive.</what-built>
  <how-to-verify>
    1. Confirm all three legs used the SAME 18 ids + rubric judge (parity — one variable).
    2. Confirm the deciding signals (clause-hit@3, judge citation/grounding, RAGAs context) are reported for all three legs.
    3. Confirm claims are appropriately hedged for n=18 (within-noise where applicable) and that no "ontology improved X" claim exists without the Phase 9 baseline present.
    4. Decide whether the D-12 escalation ("Both, layered" routing) is warranted if the clause-hit@3 gate is not cleared by function-type routing alone.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 10, or list the follow-ups (e.g. trigger the D-12 "Both, layered" escalation, or a GT over-strictness review).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval run → OpenRouter rubric judge | judge API key env-sourced during the 18-case run |
| three run JSONs → report aggregation | committed result files read for comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11-01 | Information Disclosure | OpenRouter judge API key during the graphrag-ontology run | mitigate | Key env-sourced (`CCOP_OPENROUTER_API_KEY`), never logged/printed in JSON |
| T-10-11-02 | Tampering | mismatched judge mode / changed generator confounding the A/B | mitigate | Task 1 pins rubric judge + holds generator/embedder/reranker constant; done-criteria + human gate assert parity |
| T-10-11-03 | Repudiation | overclaiming improvement absent the Phase 9 baseline | mitigate | Report explicitly cites the 10-01 baseline as present; human gate confirms hedging for n=18 |
</threat_model>

<verification>
- graphrag-ontology 18-case run present; report aggregation test green.
- Human review approved.
</verification>

<success_criteria>
A trustworthy, one-variable three-way A/B (D-16) with clause-hit@3 + judge + RAGAs signals, per-benchmark gap analysis, and human-reviewed conclusions.
</success_criteria>

<output>
Create `.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-11-SUMMARY.md` when done.
</output>
