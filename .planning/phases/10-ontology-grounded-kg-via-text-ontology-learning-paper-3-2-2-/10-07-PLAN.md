---
phase: 10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2
plan: 07
type: execute
wave: 4
depends_on: ["10-04", "10-05", "10-06"]
files_modified:
  - src/rag/graph/build/ontology_kg_builder.py
  - src/rag/graph/ontology/clause_linker.py
  - src/rag/graph/cli/graph.py
  - tests/rag/graph/build/test_ontology_kg_builder.py
  - tests/rag/graph/ontology/test_clause_linker.py
autonomous: true
requirements: [RAG-02, RAG-06, D-06, D-07, D-11]
must_haves:
  truths:
    - "Schema-constrained extraction produces ONLY the locked ontology's node/relationship types (D-06/D-07 anti-pattern fix)"
    - "Extraction ignores illustrative/example passages and enforces a canonical name (D-07)"
    - "Entity resolution dedups to canonical nodes (D-07); junk placeholders are filtered"
    - "Extracted entities LINK to the seeded :Clause nodes (D-10/D-11 retrieval-unit decouple)"
  artifacts:
    - path: "src/rag/graph/build/ontology_kg_builder.py"
      provides: "OntologyKGBuilder — schema-guided SimpleKGPipeline/hand-built Pipeline with gleaning + section splitter + resolver"
      contains: "class OntologyKGBuilder"
    - path: "src/rag/graph/ontology/clause_linker.py"
      provides: "Post-hoc entity→:Clause LINKED_TO linking pass (deterministic, reuses _clause_id_appears)"
      contains: "LINKED_TO"
    - path: "src/rag/graph/cli/graph.py"
      provides: "ccop-eval graph build-ontology subcommand"
      contains: "build-ontology"
  key_links:
    - from: "ontology_kg_builder"
      to: "locked ontology_config.json"
      via: "schema= kwarg with additional_node_types=false"
      pattern: "additional_node_types"
    - from: "clause_linker"
      to: "seeded :Clause nodes"
      via: "LINKED_TO edges via boundary-aware clause-id match"
      pattern: "_clause_id_appears|LINKED_TO"
---

<objective>
Build the **schema-constrained KG builder** (D-06/D-07 fix): wire the locked ontology `schema=`, the section-aligned splitter (10-06), the gleaning extractor (10-06), a custom extraction prompt (canonical-name + ignore-illustrative), and `SinglePropertyExactMatchResolver` into an `OntologyKGBuilder`; then LINK extracted entities to the seeded :Clause backbone (10-05). This is where the Phase 9 anti-patterns (fragmented duplicates, junk instances, scenario-not-regulation modelling) are structurally eliminated.

Purpose: A governed extraction that produces only locked types, deduped, junk-filtered, and clause-anchored.
Output: `ontology_kg_builder.py` + `clause_linker.py` + `graph build-ontology` CLI + smallest-slice E2E build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md
@src/rag/graph/ontology/ontology_config.json
@src/rag/graph/build/section_aligned_splitter.py
@src/rag/graph/build/gleaning_extractor.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OntologyKGBuilder — schema-constrained extraction + gleaning + splitter + resolver (D-06/D-07/D-11)</name>
  <files>src/rag/graph/build/ontology_kg_builder.py, tests/rag/graph/build/test_ontology_kg_builder.py</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (Q1 schema= dict + custom prompt for canonical-name/ignore-illustrative; Q2 hand-built Pipeline for gleaning; Q9 SinglePropertyExactMatchResolver; Pitfall 1 `additional_node_types=false` timing)
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md ("ontology_kg_builder.py" section — copy EmergentKGBuilder factories/index-bootstrap/build-loop; the ONE delta is schema= + custom prompt + splitter + resolver; PRESERVE the `file_path=doc_name` provenance fix)
    - src/rag/graph/build/kg_builder.py (EmergentKGBuilder — factories lines 37-77, index bootstrap 109-148, build loop + provenance 150-181, BuildStats 42-50)
    - src/rag/graph/build/section_aligned_splitter.py, src/rag/graph/build/gleaning_extractor.py (the components to inject)
    - src/rag/graph/ontology/ontology_config.json (the locked schema: node_types/relationship_types/patterns/additional_node_types)
    - docs/project_notes/bugs.md (2026-07-02 provenance-collapse bug — the `file_path=doc_name` fix MUST be preserved verbatim)
  </read_first>
  <behavior>
    - Test (mocked LLM/pipeline): builder passes the locked `schema=` (node_types/relationship_types/patterns, `additional_node_types=false`) into pipeline construction.
    - Test: the custom `prompt_template` contains the canonical-name + ignore-illustrative instructions (asserted by substring).
    - Test: the gleaning extractor + section-aligned splitter are the injected components (not the defaults).
    - Test: build loop calls `run_async(text=..., file_path=doc_name)` preserving provenance (no `document.txt` collapse).
    - Test: a `--permissive` toggle flips to `additional_node_types=true` for iteration (Pitfall 1).
  </behavior>
  <action>
    Implement `OntologyKGBuilder` by copying `EmergentKGBuilder`'s factory/index-bootstrap/build-loop/BuildStats structure VERBATIM, diverging only to: load the locked `ontology_config.json` and pass it as `schema={node_types, relationship_types, patterns, additional_node_types: false, additional_relationship_types: false}`; set a custom `prompt_template` (subclass/raw string) adding "this is a REGULATORY CODE — do NOT extract from illustrative examples/hypotheticals/placeholders (John Doe, Company X, N.A.); every node MUST carry a canonical `name` = exact source term" (D-07); inject `SectionAlignedSplitter()` as `text_splitter=` and the `GleaningEntityRelationExtractor` via a hand-built `Pipeline` (SimpleKGPipeline hardcodes its extractor — verify signature per A5); run `SinglePropertyExactMatchResolver(driver, resolve_property="name")` as a post-build step (D-07 dedup). Preserve `file_path=doc_name` (provenance-collapse fix). Expose a `permissive: bool` build flag (default False) that flips `additional_node_types` for iteration. Failures recorded in `BuildStats.failures` (T-09-08).
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/rag/graph/build/test_ontology_kg_builder.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Locked `schema=` passed with `additional_node_types=false` in strict mode (asserted).
    - Custom prompt contains canonical-name + ignore-illustrative language.
    - Section splitter + gleaning extractor + exact-match resolver all wired (not defaults).
    - `file_path=doc_name` provenance preserved.
  </acceptance_criteria>
  <done>A schema-constrained, gleaning-enabled, deduped ontology KG builder produces only locked types with preserved provenance and a permissive iteration escape hatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Post-hoc entity→clause linker (D-10/D-11) + `graph build-ontology` CLI + E2E slice</name>
  <files>src/rag/graph/ontology/clause_linker.py, src/rag/graph/cli/graph.py, tests/rag/graph/ontology/test_clause_linker.py</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (Q4 — post-hoc linking RECOMMENDED over extraction-time; reuse KGInspector clause-id matching)
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md ("clause_seeder.py" section — `_clause_id_appears` boundary-aware match at metrics.py:183-193 to reuse)
    - src/rag/graph/inspect/metrics.py (`KGInspector._clause_id_appears` — the boundary-aware clause-id text match; reuse, don't reimplement)
    - src/rag/graph/cli/graph.py (Typer command shape; the `seed-clauses` subcommand added in 10-05 for the ordering: seed → build → link)
    - ~/.claude/rules/e2e-testing.md (smallest real slice: build on ONE small text, then verify links)
  </read_first>
  <behavior>
    - Test (integration): after building on a fixture text mentioning "5.3.1", a deterministic linking pass creates `(entity)-[:LINKED_TO]->(:Clause {clause_id:"5.3.1"})`.
    - Test: linking uses boundary-aware matching (does NOT match "5.3.10" for "5.3.1") via reused `_clause_id_appears`.
    - Test (E2E slice): `graph seed-clauses` → `graph build-ontology --sample` → `clause_linker` runs end-to-end against live Neo4j and produces ≥1 LINKED_TO edge.
  </behavior>
  <action>
    Implement `clause_linker.py`: a deterministic post-extraction Cypher pass that, for each extracted entity/chunk, matches text against seeded :Clause `clause_id`s using the REUSED boundary-aware `_clause_id_appears` helper, and MERGEs `(:Entity|:Chunk)-[:LINKED_TO]->(:Clause)` edges (static parameterized Cypher, T-09-12). Do NOT ask the extraction LLM to emit clause_id strings (hallucination risk — Q4). Add a `build-ontology` subcommand to `graph_app` (mirror the existing `build` command) that runs: OntologyKGBuilder.build (with a `--sample`/`--permissive` flag) then the clause_linker pass, printing BuildStats + link counts. Add a smallest-slice E2E (integration-marked) proving the seed→build→link chain works on one small real document + confirms extracted entities carry canonical names (no "N.A."/"John Doe").
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/rag/graph/ontology/test_clause_linker.py -m integration -x -q && poetry run ccop-eval graph build-ontology --help >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - LINKED_TO edges created via boundary-aware matching (no "5.3.10"↔"5.3.1" false match).
    - `graph build-ontology --help` exits 0.
    - E2E slice: seed→build→link produces ≥1 LINKED_TO edge and entities carry canonical `name` (no junk placeholders).
  </acceptance_criteria>
  <done>Extracted entities are deterministically anchored to seeded clause nodes; the full seed→build→link chain runs via CLI and is proven by a real E2E slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus prose → schema-constrained extraction LLM | untrusted regulatory + example prose reaches gpt-4o-mini |
| extracted entity text → Cypher MERGE (linking) | LLM-derived values written to Neo4j |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | Tampering (prompt injection / poisoned corpus) | extraction LLM emitting spurious/adversarial entities | mitigate | `additional_node_types=false` (locked vocabulary) + custom prompt ignoring illustrative/example passages (D-07) + `SinglePropertyExactMatchResolver` dedup + SHACL backstop (10-08); junk placeholders rejected at prompt + validation layers |
| T-10-07-02 | Tampering | Cypher injection via extracted entity strings in the linker | mitigate | Static parameterized Cypher only; entity/clause values passed as bound params, never interpolated (T-09-12); driver parameterization neutralizes any Cypher-shaped property content at the write layer |
| T-10-07-03 | Information Disclosure | extraction LLM (OpenRouter) API key | mitigate | Key env-sourced via settings, never logged |
</threat_model>

<verification>
- `cd src/ && poetry run pytest ../tests/rag/graph/build/test_ontology_kg_builder.py -q` green (unit).
- Integration: `../tests/rag/graph/ontology/test_clause_linker.py -m integration` green; E2E seed→build→link slice green.
</verification>

<success_criteria>
Governed extraction (locked types, canonical names, junk-filtered, deduped) with entities clause-anchored — the D-06 anti-patterns structurally eliminated.
</success_criteria>

<output>
Create `.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-07-SUMMARY.md` when done.
</output>
