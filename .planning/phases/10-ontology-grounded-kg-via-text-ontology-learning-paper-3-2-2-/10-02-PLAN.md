---
phase: 10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infrastructure/config/settings.py
  - src/infrastructure/config/container.py
  - src/domain/value_objects/run_id.py
  - src/presentation/cli/commands/evaluate.py
  - src/rag/presentation/cli/query.py
  - src/rag/retrieval/edges/routing.py
  - src/rag/retrieval/state/graph_state.py
  - src/rag/graph/retrieval/graph_retrieval_node.py
  - src/rag/graph/retrieval/neo4j_ontology_graph_retrieval_adapter.py
  - tests/infrastructure/config/test_graph_provider_selection.py
  - tests/rag/graph/retrieval/test_graphrag_ontology_routing.py
autonomous: true
requirements: [RAG-06, D-16]
must_haves:
  truths:
    - "`--mode graphrag-ontology` is accepted by every mode allowlist (all four) and routes to the NEW Phase 10 provider, not Phase 9's"
    - "Phase 9's `--mode graphrag` provider/path is completely untouched (D-16 additivity)"
    - "An E2E smallest slice proves the routing distinction end-to-end"
  artifacts:
    - path: "src/rag/graph/retrieval/neo4j_ontology_graph_retrieval_adapter.py"
      provides: "Skeleton Neo4jOntologyGraphRetrievalAdapter implementing IGraphRetrievalProvider (contract; real query lands in 10-09)"
      contains: "class Neo4jOntologyGraphRetrievalAdapter"
    - path: "src/infrastructure/config/container.py"
      provides: "Mode-aware second provider singleton graph_retrieval_provider_ontology"
      contains: "graph_retrieval_provider_ontology"
    - path: "src/infrastructure/config/settings.py"
      provides: "All Phase 10 ontology/SHACL/routing settings fields (front-loaded so no other plan modifies settings.py)"
      contains: "ontology_config_path"
  key_links:
    - from: "route_by_mode"
      to: "ontology retrieval node → graph_retrieval_provider_ontology"
      via: "state['mode'] == 'graphrag-ontology'"
      pattern: "graphrag-ontology"
    - from: "graph_retrieve_documents (mode-aware)"
      to: "container.graph_retrieval_provider_ontology()"
      via: "provider selection by mode"
      pattern: "graph_retrieval_provider_ontology"
---

<objective>
Close RESEARCH **Pitfall 3** (provider selection is not mode-aware today) and the **multi-allowlist mode-wiring** gap, then prove the routing with an E2E smallest slice. This is BLOCKING: without it, `--mode graphrag-ontology` silently returns Phase 9's emergent-graph results for both modes, invalidating any A/B claim.

Interface-first: this plan lands the CONTRACT (a skeleton `Neo4jOntologyGraphRetrievalAdapter`, the second DI singleton, mode-aware node selection, the new `function_type` GraphState field, and ALL Phase 10 settings fields). The real clause-anchored retrieval query + function-type routing fill this skeleton in plan 10-09.

Purpose: Give every downstream plan a stable, mode-routed seam and a single owner of `settings.py`, avoiding same-wave config conflicts.
Output: Mode-aware wiring + skeleton adapter + E2E routing proof.
</objective>

<scope_rationale>
DELIBERATE ACCEPT: this plan lists 11 `files_modified`, above the 5-8 target. The overage is intentional and on-record. This is the "mode plumbing" plan, and 7 of the 11 files are single-line/small edits (the four `--mode` allowlists, the `route_by_mode` branch, the `graph_state` field, the mode-aware node select). Critically, `settings.py` is front-loaded here as the SINGLE Phase-10 owner: splitting settings ownership across same-wave plans would create `settings.py` write-conflicts within Waves 3 and 5 (10-08 and 10-09 are both Wave 5 and both read settings), forcing artificial wave serialization or merge races. One owner of `settings.py` + the allowlist set is safer than a smaller-but-conflicting split. Not splitting.
</scope_rationale>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-CONTEXT.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md

<interfaces>
<!-- Contracts the executor implements against. Extracted from PATTERNS.md live reads. -->
IGraphRetrievalProvider (src/rag/graph/ports/i_graph_retrieval_provider.py) — implement identically to Phase 9's Neo4jGraphRetrievalAdapter. Method: retrieve(query: str, top_k: int) -> list[Document].

Four allowlists that MUST all contain "graphrag-ontology" (state confirmed live 2026-07-02):
- src/domain/value_objects/run_id.py:24  _VALID_MODES               → MISSING, add
- src/application/use_cases/evaluate_model.py:40  _RETRIEVAL_EVAL_MODES → PRESENT, verify only
- src/presentation/cli/commands/evaluate.py:30  VALID_EVAL_MODES      → MISSING, add
- src/rag/presentation/cli/query.py:35  VALID_MODES                  → MISSING, add (+ spinner_label ~106-112, error-help branch ~136)

DI (src/infrastructure/config/container.py:258-280) _create_graph_retrieval_provider is a single unconditional Singleton. Add a sibling _create_ontology_graph_retrieval_provider + graph_retrieval_provider_ontology Singleton, mirroring the if/elif-on-settings shape of _create_vector_store_adapter (lines 177-215).

GraphState (src/rag/retrieval/state/graph_state.py) — add `function_type` TypedDict field the same way `rewritten_query`/`hyde_query` were added.

route_by_mode (src/rag/retrieval/edges/routing.py:14-43) — add branch `graphrag-ontology` → ontology retrieval node.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Front-load Phase 10 settings + mode-aware DI provider + skeleton ontology adapter + GraphState field</name>
  <files>src/infrastructure/config/settings.py, src/infrastructure/config/container.py, src/rag/graph/retrieval/neo4j_ontology_graph_retrieval_adapter.py, src/rag/retrieval/state/graph_state.py, tests/infrastructure/config/test_graph_provider_selection.py</files>
  <read_first>
    - src/infrastructure/config/settings.py (lines 410-473 "Neo4j GraphRAG Configuration" block — copy the `Field(default=..., description=..., alias="CCOP_...")` style with inline D-ref comments)
    - src/infrastructure/config/container.py (lines 177-215 `_create_vector_store_adapter` if/elif precedent; lines 258-280 `_create_graph_retrieval_provider` Singleton to mirror)
    - src/rag/graph/retrieval/neo4j_graph_retrieval_adapter.py (EXACT structural template: imports 27-38, constructor 76-101, retrieve() 133-151, _format_record 103-131)
    - src/rag/graph/ports/i_graph_retrieval_provider.py (the port to implement)
    - src/rag/retrieval/state/graph_state.py (TypedDict; where `rewritten_query`/`hyde_query` are declared)
  </read_first>
  <behavior>
    - Test: container returns a Neo4jOntologyGraphRetrievalAdapter instance from graph_retrieval_provider_ontology() when neo4j_uri is set, and None when unset (mirrors _create_vector_store_adapter branching test shape).
    - Test: the Phase 9 graph_retrieval_provider() still returns Neo4jGraphRetrievalAdapter (additivity — P9 provider unchanged).
    - Test: new settings fields load with documented defaults and CCOP_-prefixed aliases.
  </behavior>
  <action>
    Add ALL Phase 10 settings fields to `settings.py` in a new "Neo4j GraphRAG Ontology Configuration (Phase 10)" block so NO other Phase 10 plan needs to touch settings.py: `ontology_config_path` (default `src/rag/graph/ontology/ontology_config.json`), `shacl_shapes_path` (default `src/rag/graph/ontology/shapes.ttl`), `ontology_discovery_model` (default `openai/gpt-4o-mini`), `function_type_boost` (float default 1.5, D-12), `gleaning_max_gleanings` (int default 1, D-11), `graphrag_ontology_enabled` (bool default True). Use the same `Field(...)` + inline `# D-##` comment style as the existing block. Create `neo4j_ontology_graph_retrieval_adapter.py` as a SKELETON implementing `IGraphRetrievalProvider` — copy the Phase 9 adapter structure verbatim, but the `RETRIEVAL_QUERY` is a minimal clause-node placeholder returning documents tagged with `metadata["provider"]="graphrag-ontology"` so routing is provably distinct (the real clause-anchored boosted query lands in 10-09). Add `_create_ontology_graph_retrieval_provider` static method + `graph_retrieval_provider_ontology = providers.Singleton(...)` to the container, mirroring `_create_vector_store_adapter`'s if/elif-on-settings + log-which-chosen shape. Add the `function_type` field to `GraphState` TypedDict. Do NOT modify Phase 9's `_create_graph_retrieval_provider` or `graph_retrieval_provider` (D-16 additivity).
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/infrastructure/config/test_graph_provider_selection.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "graph_retrieval_provider_ontology" src/infrastructure/config/container.py` ≥ 2
    - `python -c "from rag.graph.ports.i_graph_retrieval_provider import IGraphRetrievalProvider; from rag.graph.retrieval.neo4j_ontology_graph_retrieval_adapter import Neo4jOntologyGraphRetrievalAdapter; assert issubclass(Neo4jOntologyGraphRetrievalAdapter, IGraphRetrievalProvider)"` (run from src/ via poetry) exits 0
    - Phase 9 `_create_graph_retrieval_provider` unchanged: `git diff` shows no edits to that method body
    - `grep -q "ontology_config_path" src/infrastructure/config/settings.py`
  </acceptance_criteria>
  <done>Second mode-aware provider singleton exists, skeleton adapter implements the port and is distinguishable via `metadata["provider"]`, all Phase 10 settings fields present, `function_type` on GraphState, P9 provider byte-for-byte unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-allowlist mode wiring + mode-aware retrieval node + routing branch</name>
  <files>src/domain/value_objects/run_id.py, src/presentation/cli/commands/evaluate.py, src/rag/presentation/cli/query.py, src/rag/retrieval/edges/routing.py, src/rag/graph/retrieval/graph_retrieval_node.py, tests/rag/graph/retrieval/test_graphrag_ontology_routing.py</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md ("Multi-Allowlist Mode Wiring" section — the four allowlists table + the P3-bug lesson)
    - ~/.claude/rules/e2e-testing.md (the studio-ssdlc Phase-9 multi-allowlist near-miss is this rule's motivating example)
    - src/rag/retrieval/edges/routing.py (lines 14-43 `route_by_mode`, 46-74 `decide_after_grading`)
    - src/rag/graph/retrieval/graph_retrieval_node.py (full file — provider lookup + error handling + `dense_rank` reranker-parity pattern, lines 74-76)
    - tests/rag/graph/retrieval/test_graphrag_routing.py (copy this test file's structure exactly)
  </read_first>
  <behavior>
    - Test: route_by_mode({"mode":"graphrag-ontology"}) returns the ontology retrieval route (distinct from "graphrag").
    - Test: RunId accepts "graphrag-ontology"; rejects a bogus mode.
    - Test: the compiled build_rag_graph contains the ontology retrieval node name.
    - Test: the mode-aware node selects graph_retrieval_provider_ontology() for graphrag-ontology and graph_retrieval_provider() for graphrag.
  </behavior>
  <action>
    Add `"graphrag-ontology"` to `run_id.py::_VALID_MODES` (line 24), `evaluate.py::VALID_EVAL_MODES` (line 30, + help text), and `query.py::VALID_MODES` (line 35) plus its `spinner_label` dict ("Querying ontology-grounded graph pipeline...") and extend the `elif mode in ("graphrag", "graphrag-retrieval")` error-help branch (~line 136) to include it. VERIFY `evaluate_model.py::_RETRIEVAL_EVAL_MODES` still contains it (already patched, commit 7658505) — do not remove. In `routing.py::route_by_mode`, add a `graphrag-ontology` branch routing to the ontology retrieval path. Make `graph_retrieve_documents` mode-aware: pick `container.graph_retrieval_provider_ontology()` when `state["mode"]=="graphrag-ontology"` else `container.graph_retrieval_provider()` — preserve the existing error handling and `dense_rank` attach (Wave-6 reranker parity; MUST NOT pre-set `filtered_documents`). Before declaring done, run `grep -rn "graphrag" src/` and confirm no additional un-patched allowlist has appeared since 2026-07-02.
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/rag/graph/retrieval/test_graphrag_ontology_routing.py -x -q && grep -v '^#' ../src/domain/value_objects/run_id.py ../src/presentation/cli/commands/evaluate.py ../src/rag/presentation/cli/query.py | grep -c "graphrag-ontology" | awk '{if($1<3){print "MISSING allowlist"; exit 1}else print "OK "$1}'</automated>
  </verify>
  <acceptance_criteria>
    - All four allowlists contain `graphrag-ontology` (three added + one verified).
    - `route_by_mode({"mode":"graphrag-ontology"})` returns a route distinct from the `graphrag` route.
    - Mode-aware node picks the ontology provider only for the ontology mode.
  </acceptance_criteria>
  <done>All four mode allowlists accept `graphrag-ontology`; router + node select the Phase 10 provider for that mode and the Phase 9 provider for `graphrag`.</done>
</task>

<task type="auto">
  <name>Task 3: E2E smallest-slice — prove graphrag-ontology routes to the NEW provider and P9 is untouched</name>
  <files>tests/rag/graph/retrieval/test_graphrag_ontology_routing.py</files>
  <read_first>
    - ~/.claude/rules/e2e-testing.md (smallest real vertical slice before claiming done; no mocks at the seam under test)
    - src/rag/presentation/cli/query.py (the `query ask --mode ...` entry point)
    - src/rag/graph/retrieval/neo4j_ontology_graph_retrieval_adapter.py (the `metadata["provider"]="graphrag-ontology"` marker from Task 1)
  </read_first>
  <action>
    Add a smallest-slice E2E that exercises the real CLI seam (no boundary mocks): with Neo4j up, run `cd src/ && poetry run ccop-eval query ask "test scope question" --mode graphrag-ontology --mode-help-only`-style minimal invocation OR a direct `build_rag_graph` compile + single-node invocation that routes a `{"mode":"graphrag-ontology"}` state and asserts the returned documents carry `metadata["provider"]=="graphrag-ontology"` (the Phase 10 skeleton marker), while a `{"mode":"graphrag"}` state returns documents WITHOUT that marker (Phase 9 path). This proves the two providers coexist live and route correctly — the exact failure the Phase-9 multi-allowlist near-miss (and RESEARCH Pitfall 3) warns about. Mark the live-Neo4j portion `@pytest.mark.integration`.
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/rag/graph/retrieval/test_graphrag_ontology_routing.py -k "e2e or provider_distinct" -q</automated>
  </verify>
  <acceptance_criteria>
    - The E2E asserts graphrag-ontology → provider marker present; graphrag → marker absent (routing is provably distinct, not just labelled).
    - Integration-marked so it runs in the full suite but not the fast slice.
  </acceptance_criteria>
  <done>A real vertical slice proves `--mode graphrag-ontology` reaches the Phase 10 provider and Phase 9's `graphrag` path is unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI `--mode` string → provider selection | untrusted flag value crosses into DI/router |
| GraphState `query` → (future) Cypher retrieval | user query reaches the retrieval adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Spoofing/Tampering | An unlisted `--mode` value silently falling through to the wrong provider | mitigate | Four allowlists enforce membership; RunId rejects unknown modes; router has explicit branches only |
| T-10-02-02 | Tampering | Skeleton adapter Cypher | mitigate | Placeholder `RETRIEVAL_QUERY` is a static class-level string; any query param passed as a bound parameter only (T-09-12), never interpolated |
| T-10-02-03 | Information Disclosure | neo4j_password / OpenRouter key in new settings block | mitigate | New settings fields carry no secrets; secret fields remain the existing env-sourced ones, never literals |
</threat_model>

<verification>
- Fast slice: `cd src/ && poetry run pytest -m "not integration" -q` green.
- Full suite incl. integration routing E2E green.
</verification>

<success_criteria>
Mode-aware provider selection is live (Pitfall 3 closed), all four allowlists patched, skeleton adapter + settings front-loaded, E2E proves routing distinction and P9 additivity.
</success_criteria>

<output>
Create `.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-02-SUMMARY.md` when done.
</output>
