---
phase: 10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rag/graph/ontology/__init__.py
  - src/rag/graph/ontology/gold_relation_parser.py
  - src/rag/graph/ontology/discovery/__init__.py
  - src/rag/graph/ontology/discovery/method_c_synthesis.py
  - src/rag/graph/ontology/discovery/coverage_check.py
  - src/rag/graph/ontology/ontology_draft.json
  - tests/rag/graph/ontology/test_gold_relation_parser.py
  - tests/rag/graph/ontology/test_coverage_check.py
autonomous: false
requirements: [RAG-06, D-01, D-02, D-03, D-04, D-08, D-09, D-17, D-18]
must_haves:
  truths:
    - "A Method-C ontology draft exists, discovered (not hand-authored) from structured sources with provenance + flagged ambiguities (D-01/D-04)"
    - "The draft includes the regulatory-structure layer (Clause/Control/Obligation/Definition + GOVERNS/REQUIRES/APPLIES_TO/RESPONSIBLE_FOR/MITIGATES) and clause-function tags (ScopeClause/ControlClause/DefinitionClause) (D-08/D-09)"
    - "The draft covers the D-18 negation/modal relation families surfaced by the gold-relation xlsx (D-17/D-18)"
    - "A human curation gate (a) approves/amends the Method-C draft before Method B runs (D-14)"
  artifacts:
    - path: "src/rag/graph/ontology/gold_relation_parser.py"
      provides: "Parser for eval-report xlsx col-22 graph_relation gold triples (D-17)"
      contains: "TRIPLE_RE"
    - path: "src/rag/graph/ontology/discovery/method_c_synthesis.py"
      provides: "Grounded-synthesis ontology discovery (Method C, D-04)"
    - path: "src/rag/graph/ontology/ontology_draft.json"
      provides: "Method-C draft ontology (node_types/relationship_types/patterns + provenance)"
      contains: "node_types"
  key_links:
    - from: "gold_relation_parser"
      to: "coverage_check (D-17 gap report)"
      via: "relation types diffed against ontology draft"
      pattern: "missing_relations"
    - from: "method_c_synthesis"
      to: "curation gate (a)"
      via: "markup table + coverage report presented for approval"
      pattern: "provenance"
---

<objective>
Discover the CCoP ontology via **Method C (grounded synthesis)** — the first leg of D-01's `C → curate → B → reconcile → lock` sequence. Anchor discovery on STRUCTURED sources (D-04), NOT open per-chunk NER (the Phase 9 failure). Build the D-17 gold-relation parser and coverage checker so the human curation gate (a) sees a concrete, per-case coverage report, not just a type list.

Purpose: Produce a governed, provenance-carrying ontology draft that fixes the D-06 anti-patterns at the schema layer and is approved before mechanical build begins.
Output: `ontology_draft.json` + gold-relation parser + coverage checker + an approved curation gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-CONTEXT.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gold-relation parser (D-17) + benchmark/gold coverage checker (D-14/D-17)</name>
  <files>src/rag/graph/ontology/__init__.py, src/rag/graph/ontology/gold_relation_parser.py, src/rag/graph/ontology/discovery/__init__.py, src/rag/graph/ontology/discovery/coverage_check.py, tests/rag/graph/ontology/test_gold_relation_parser.py, tests/rag/graph/ontology/test_coverage_check.py</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md ("gold_relation_parser.py" section — TRIPLE_RE / CLAUSE_BRACKET_RE + the `NOT DESIGNATED_AS` whitespace-normalization pitfall)
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (Q10 — verified xlsx cell structure; the col-22 `graph_relation` format)
    - src/rag/ingestion/scripts/audit_ground_truth_citations.py (analog: openpyxl read + regex extraction + report-artifact shape)
    - src/results/evaluations/eval-report-hybrid-suite-20260630-0907.xlsx (input; sheet `eval-18`, column 22 `graph_relation` — hand-authored gold triples with `[1.2.1, 1.4.1]`-style clause citations)
    - ground-truth/test-suite/*.jsonl (the 18 benchmark definitions for the D-14 benchmark-coverage side)
  </read_first>
  <behavior>
    - Test: parser extracts `(subject)-[REL]->(object)` triples from a fixture cell and normalizes `NOT DESIGNATED_AS` (space) → `NOT_DESIGNATED_AS` (underscore).
    - Test: parser extracts bracketed clause citations `[1.2.1, 1.4.1]` and `[5.3.1(c)]`.
    - Test: coverage_check reports `missing_relations = gold_relation_types - ontology_relationship_types` for a fixture ontology missing `CANNOT_SATISFY`.
    - Test: coverage_check maps each of the 18 benchmarks to at least one node type (D-14) and lists any unmapped benchmark.
  </behavior>
  <action>
    Create the `src/rag/graph/ontology/` module (mirrors the RAG-slice CA layering; pure parsing/logic, import-free of Neo4j/network). Implement `gold_relation_parser.py` with `TRIPLE_RE = re.compile(r"\(([^)]+)\)\s*-\[([^\]]+)\]->\s*\(([^)]+)\)")` and `CLAUSE_BRACKET_RE = re.compile(r"\[([\w.\s,()§]+)\]")`, reading col-22 `graph_relation` from the eval-report xlsx via `openpyxl`, normalizing whitespace→underscore on relation names before returning `{relation_types, clause_citations}` per case. Implement `discovery/coverage_check.py` with two functions: `gold_relation_coverage(ontology, xlsx_path)` → D-17 gap list (relation + entity types in gold but missing from ontology), and `benchmark_coverage(ontology, benchmark_dir)` → D-14 map of each of the 18 benchmarks to a covering type + list of unmapped. Emit a machine-readable report dict (READ-ONLY inputs; report artifact for the human gate).
  </action>
  <verify>
    <automated>cd src/ && poetry run pytest ../tests/rag/graph/ontology/test_gold_relation_parser.py ../tests/rag/graph/ontology/test_coverage_check.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Parser normalizes `NOT DESIGNATED_AS` → `NOT_DESIGNATED_AS` (asserted on a fixture cell).
    - `gold_relation_coverage` returns a non-empty `missing_relations` when the ontology lacks a gold relation.
    - `benchmark_coverage` returns all-18 mapping with an explicit `unmapped: []` (or the list of gaps).
  </acceptance_criteria>
  <done>Deterministic tooling exists to diff any ontology draft against BOTH the 18 benchmark definitions (D-14) and the per-case gold relations (D-17/D-18), producing the report the curation gate reviews.</done>
</task>

<task type="auto">
  <name>Task 2: Method C grounded-synthesis ontology discovery (D-01/D-02/D-04/D-08/D-09/D-18)</name>
  <files>src/rag/graph/ontology/discovery/method_c_synthesis.py, src/rag/graph/ontology/ontology_draft.json</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (Q1 schema dict shape; Q6 Method C pipeline — anchor sources + one-LLM-call-per-source-category)
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-PATTERNS.md ("method_c_synthesis.py" section — script CLI shape + `_generate_hyde` OpenRouter call pattern)
    - src/rag/retrieval/nodes/query_analysis.py (lines 27-48 `_generate_hyde` — the OpenRouter gpt-4o-mini call + graceful-degradation error handling to mirror)
    - src/rag/ingestion/chunkers/clause_aware_chunker.py (CLAUSE_PATTERN — the `## X.Y heading` regex to reuse for extracting CCoP section/clause HEADERS as the control-taxonomy source)
    - ground-truth/test-suite/*.jsonl (the 18 benchmark `input.question`/`ground_truth`/`metadata.clause_reference` — the reasoning/relation vocabulary source, D-04)
    - src/rag/ingestion/fixtures/clause_inventory.json (NOTE: IDs + source_doc only, NO titles — headers must come from Docling markdown, not this fixture)
  </read_first>
  <action>
    Implement `method_c_synthesis.py` as a one-shot curation-time CLI script (argparse, `if __name__=="__main__"`, NOT a runtime service). Three anchor sources per D-04: (a) CCoP section/clause HEADERS parsed from the Docling markdown corpus via the reused `## X.Y heading` regex (control taxonomy); (b) the 18 benchmark JSONL definitions (reasoning/relation concepts); (c) a stratified prose sample across the 7 `source_docs` (domain entities). Do NOT use Phase 9's emergent graph as discovery input (D-02). Make ONE `gpt-4o-mini` synthesis call per source category (settings `ontology_discovery_model`, base_url/api_key from settings; reuse the `_generate_hyde` try/except-log pattern) producing candidate types with `provenance` + `flagged_ambiguities`. HAND-ADD the D-08 regulatory-structure layer (node_types `Clause`,`Control`,`Obligation`,`Definition`; relations `GOVERNS`,`REQUIRES`,`APPLIES_TO`,`RESPONSIBLE_FOR`,`MITIGATES`) and D-09 clause-function tags (`ScopeClause`,`ControlClause`,`DefinitionClause`) — discovery can only cluster scenario-centric prose, so these are seeded. Pre-populate the D-18 negation/modal relation families (`NOT_DESIGNATED_AS`,`CANNOT_SATISFY`,`DOES_NOT_WAIVE`,`DEFINES_NO`,`DOES_NOT_SPECIFY`,`TECHNOLOGY_NEUTRAL_ON`,`RECOMMENDS_AGAINST`,`DEFERS_TO`,`IS_A`,`DEFINED_AS`,`CLASSIFIED_AS`,`DESIGNATES`,`DETERMINED_BY`). Write `ontology_draft.json` as a `node_types`/`relationship_types`/`patterns` dict matching `neo4j_graphrag.experimental.components.schema.GraphSchema` shape, each type carrying `provenance` + `flagged_ambiguities`. Run the Task-1 coverage checker and embed its D-14 + D-17 report alongside the draft.
  </action>
  <verify>
    <automated>cd src/ && poetry run python -c "import json; d=json.load(open('rag/graph/ontology/ontology_draft.json')); nt={n if isinstance(n,str) else n['label'] for n in d['node_types']}; rt=set(d['relationship_types']); assert {'Clause','Control','Obligation','Definition','ScopeClause','ControlClause','DefinitionClause'}<=nt, nt; assert {'GOVERNS','REQUIRES','APPLIES_TO','NOT_DESIGNATED_AS','CANNOT_SATISFY'}<=rt, rt; print('OK', len(nt),'types', len(rt),'relations')"</automated>
  </verify>
  <acceptance_criteria>
    - `ontology_draft.json` contains the full D-08 regulatory layer + D-09 function tags + D-18 modal/negation relations (asserted by the verify command).
    - Discovered types carry `provenance` and `flagged_ambiguities` keys.
    - No Phase 9 emergent-graph file is read by the script (D-02) — `grep -L "emergent" src/rag/graph/ontology/discovery/method_c_synthesis.py` confirms absence, or the only reference is an explicit "do NOT read" comment.
  </acceptance_criteria>
  <done>A provenance-carrying Method-C ontology draft exists with the regulatory layer, function tags, and gold-derived modal relations, plus an embedded D-14/D-17 coverage report.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Curation gate (a) — user approves/amends the Method-C draft (D-14)</name>
  <action>Pause for human curation. Present the Method-C draft as the markup table + D-14 benchmark-coverage report + D-17 gold-relation coverage report; capture the user's keep/drop/amend decisions and apply them before proceeding. No automation beyond rendering the review artifacts.</action>
  <what-built>A Method-C ontology draft (`ontology_draft.json`) with a markup table (type | definition | example terms | provenance | flagged ambiguities), a D-14 benchmark-coverage check (all 18 benchmarks → a type), and a D-17 gold-relation coverage report (missing relations vs the eval-report xlsx col-22 gold triples).</what-built>
  <how-to-verify>
    1. Review the markup table rendered from `ontology_draft.json` — confirm the regulatory-structure layer (Clause/Control/Obligation/Definition), function tags (ScopeClause/ControlClause/DefinitionClause), and modal/negation relations are present and correctly defined.
    2. Confirm the D-14 report maps every one of the 18 benchmarks to at least one type (no `unmapped`).
    3. Confirm the D-17 report `missing_relations` list is empty OR the user explicitly decides to add/skip each missing gold relation.
    4. Amend/keep/drop any flagged-ambiguity type. Your role is oversight/curation (verify, amend, advise) — not authoring (D-01, Specifics).
  </how-to-verify>
  <resume-signal>Type "approved" to lock the Method-C draft as the input to Method B (plan 10-04), or list the amendments to apply first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus prose / benchmark JSONL → discovery LLM | untrusted regulatory text + example scenarios reach gpt-4o-mini |
| eval-report xlsx → parser | committed gold-standard file, read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering (prompt injection) | Method-C synthesis LLM calls over corpus/scenario prose | mitigate | Synthesis prompts operate on SOURCE-CATEGORY summaries, not raw per-chunk NER; the human curation gate (a) reviews every proposed type before it can enter the locked ontology — no unreviewed type reaches the build |
| T-10-03-02 | Information Disclosure | ontology_discovery_model API key | mitigate | Key sourced via settings/env only (OpenRouter), never logged; reuse `_generate_hyde` degradation pattern |
| T-10-03-03 | Tampering | gold_relation xlsx parsing | accept | Committed repo artifact; code review covers changes; parser is read-only regex extraction, no eval |
</threat_model>

<verification>
- `cd src/ && poetry run pytest ../tests/rag/graph/ontology/ -m "not integration" -q` green.
- Curation gate (a) explicitly approved.
</verification>

<success_criteria>
Method-C draft discovered from structured sources with full regulatory layer + function tags + gold modal relations, D-14/D-17 coverage reported, and human-approved before Method B.
</success_criteria>

<output>
Create `.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-03-SUMMARY.md` when done.
</output>
