---
phase: 10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/project_notes/research/2026-07-02-neo4j-exact-vector-search-spike.md
  - src/results/evaluations/
autonomous: true
requirements: [EVAL-02, D-16, D-15]
must_haves:
  truths:
    - "A basic-GraphRAG (--mode graphrag) 18-case baseline run exists for the bdc4927d fixed GT (D-16 hard dependency satisfied)"
    - "The exact-vs-approximate vector-search question (RESEARCH Open Question 1) is resolved in writing before the D-15 harness is designed"
  artifacts:
    - path: "src/results/evaluations/2026-07/eval-run-graphrag-tests-18-bdc4927d-*.json"
      provides: "Phase 9 basic-GraphRAG 18-case baseline (D-16 A/B leg 1)"
      contains: "test_results"
    - path: "docs/project_notes/research/2026-07-02-neo4j-exact-vector-search-spike.md"
      provides: "Determinism strategy decision for the clause-hit@3 harness (D-15)"
      min_lines: 20
  key_links:
    - from: "10-01 baseline run"
      to: "10-11 A/B report"
      via: "graphrag baseline JSON consumed as A/B leg 1"
      pattern: "eval-run-graphrag-tests-18-bdc4927d"
    - from: "10-01 spike finding"
      to: "10-09 retrieval adapter + 10-10 harness"
      via: "exact-search decision determines tie-break/index strategy"
      pattern: "exact.*vector"
---

<objective>
Discharge the two Phase 10 prerequisites that do NOT depend on ontology work and can run in parallel with ontology discovery:

1. Execute the **deferred Phase 9 Wave-6 18-case basic-GraphRAG baseline** (D-16 hard dependency). Without this, no "ontology improved X" claim is trustworthy. Pure execution — the Phase 9 emergent graph and `--mode graphrag` already exist; this only runs the eval across the 18-case fixed GT (`bdc4927d`).
2. Resolve **RESEARCH Open Question 1 / Pitfall 2**: does Neo4j / neo4j-graphrag expose an exact (non-ANN) vector-search mode, or must determinism come from a stable Cypher tie-break + frozen index state? This gates the D-15 clause-hit@3 harness design (plans 10-09/10-10) and must be answered before those lock an implementation.

Purpose: Remove the two moving dependencies from the critical path early.
Output: A `graphrag` 18-case baseline eval JSON + a committed spike-finding note with a determinism decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-CONTEXT.md
@.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the deferred Phase 9 basic-GraphRAG 18-case baseline (D-16)</name>
  <files>src/results/evaluations/</files>
  <read_first>
    - CLAUDE.md (Build/Run/Test → `evaluate run` flags: `--mode graphrag`, `--test-ids`, `--resume`)
    - src/results/evaluations/2026-04/eval-run-hybrid-tests-18-bdc4927d-20260430-0232-primus-reasoning.json (the canonical hybrid baseline — its `test_results[].test_id` list IS the 18-case `bdc4927d` set to reproduce)
    - src/application/use_cases/evaluate_model.py (`_RETRIEVAL_EVAL_MODES` already includes graphrag; confirms graphrag routes through the graph provider + judge + RAGAs)
    - .planning/phases/09-basic-graphrag-reference-baseline-microsoft-graphrag-package/09-CONTEXT.md (D-16 deferral; the parity harness uses the RUBRIC judge, NOT universal)
  </read_first>
  <action>
    Confirm the Phase 9 emergent graph is built and Neo4j is up (`docker compose up -d neo4j`/`qdrant`; if the graph is empty, run the Phase 9 `ccop-eval graph build` path first — do NOT rebuild if node/relationship counts are already non-zero via `ccop-eval graph stats`). Extract the exact 18 `test_id` values from the canonical hybrid baseline JSON (`eval-run-hybrid-tests-18-bdc4927d-20260430-0232-primus-reasoning.json`, one case per active benchmark). Run `cd src/ && poetry run ccop-eval evaluate run --model primus-reasoning --mode graphrag --judge-mode rubric` scoping to those 18 IDs via repeated `--test-ids B01-001 --test-ids B02-...` (the RUBRIC judge is the parity mode per P9 D-16 — the canonical hybrid uses rubric, NOT universal; mode-mismatched scoring would confound the A/B). Use `--resume` if interrupted. Do NOT change generator, embedder, or retrieval config — this is the identical Phase 9 stack, one label. This is the A/B "basic GraphRAG" leg for plan 10-11.
  </action>
  <verify>
    <automated>cd src/ && ls results/evaluations/2026-07/eval-run-graphrag-tests-18-bdc4927d-*-primus-reasoning.json >/dev/null 2>&1 && poetry run python -c "import json,glob; f=sorted(glob.glob('results/evaluations/2026-07/eval-run-graphrag-tests-18-bdc4927d-*-primus-reasoning.json'))[-1]; d=json.load(open(f)); assert len(d['test_results'])==18, f'{len(d[\"test_results\"])} != 18'; print('OK', f)"</automated>
  </verify>
  <done>A `graphrag` run JSON exists under `results/evaluations/2026-07/` with exactly 18 `test_results`, rubric-judged, over the bdc4927d id set. The run is committed/preserved as A/B leg 1.</done>
</task>

<task type="auto">
  <name>Task 2: Exact vector-search / determinism spike (RESEARCH Open Question 1)</name>
  <files>docs/project_notes/research/2026-07-02-neo4j-exact-vector-search-spike.md</files>
  <read_first>
    - .planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-RESEARCH.md (Q8, Pitfall 2, Open Question 1, Assumptions Log A4)
    - src/rag/graph/retrieval/neo4j_graph_retrieval_adapter.py (Phase 9 `HybridCypherRetriever` usage + static `RETRIEVAL_QUERY` — the thing whose determinism is in question)
  </read_first>
  <action>
    Resolve whether the installed `neo4j-graphrag 1.18.0` + `neo4j:5.26-community` vector index exposes an exact/brute-force (non-ANN) search mode reachable through `HybridCypherRetriever` (or `create_vector_index`). Use the `ctx7` CLI on the `neo4j-graphrag-python` library for the retriever/index API, and empirically probe the live index: run the SAME query vector against the Phase 9 vector index N=5 times and record whether the top-K ordering is bit-stable. Write `docs/project_notes/research/2026-07-02-neo4j-exact-vector-search-spike.md` capturing: (a) whether an exact mode exists (yes/no + the API surface if yes), (b) observed run-to-run stability of the ANN path, (c) the LOCKED determinism strategy for the D-15 harness — either "use exact-search API X" OR "ANN + deterministic secondary Cypher sort key (`ORDER BY score DESC, clause_id ASC`) + frozen index (no concurrent writes during eval)". This decision is consumed by plans 10-09 (adapter tie-break) and 10-10 (harness).
  </action>
  <verify>
    <automated>test -f docs/project_notes/research/2026-07-02-neo4j-exact-vector-search-spike.md && grep -qiE "determinism (strategy|decision)|LOCKED" docs/project_notes/research/2026-07-02-neo4j-exact-vector-search-spike.md && echo OK</automated>
  </verify>
  <done>Spike note committed with an unambiguous determinism strategy decision (exact-mode vs stable-tie-break-plus-frozen-index) that 10-09/10-10 can implement without re-litigating the open question.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus prose → extraction LLM | Phase 9 graph already built from CCoP prose; this plan only READS it, no new extraction |
| eval harness → OpenRouter judge | judge/API keys sourced from `config/.env.local`, never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Information Disclosure | OpenRouter judge API key during the 18-case run | mitigate | Key sourced via `CCOP_OPENROUTER_API_KEY` env only (existing pattern); no key printed in logs/JSON output |
| T-10-01-02 | Tampering | Reusing wrong judge mode (universal vs rubric) silently confounds the A/B | accept→mitigate | Task 1 pins `--judge-mode rubric` to match the canonical hybrid baseline; done-criteria asserts parity |
</threat_model>

<verification>
- `graphrag` 18-case baseline JSON present with 18 rubric-judged results.
- Spike note present with a locked determinism decision.
</verification>

<success_criteria>
D-16 A/B leg 1 (basic GraphRAG baseline) exists; D-15 determinism open question resolved before harness design.
</success_criteria>

<output>
Create `.planning/phases/10-ontology-grounded-kg-via-text-ontology-learning-paper-3-2-2-/10-01-SUMMARY.md` when done.
</output>
