---
phase: 02.4-llm-judge-redesign-and-metric-simplification
plan: 05
type: execute
wave: 4
depends_on: ["02.4-01", "02.4-02", "02.4-03", "02.4-04"]
files_modified:
  - src/tests/domain/services/test_llm_judge_service.py
  - src/tests/domain/services/test_scoring_service_option_a.py
  - src/tests/domain/services/test_response_extractor.py
  - src/tests/domain/services/test_ragas_evaluation_service.py
  - src/tests/domain/value_objects/test_quality_group.py
  - src/tests/domain/entities/test_evaluation_result.py
  - src/tests/application/use_cases/test_evaluate_model_metadata.py
autonomous: true

must_haves:
  truths:
    - "All existing tests pass with updated code (backward compatibility verified)"
    - "New tests cover JudgeEvaluation model with hallucination fields"
    - "New tests cover extract_final_answer utility"
    - "New tests cover ScoringService universal judge routing"
    - "Updated tests verify ragas_composite_score uses new formula without factual_precision"
    - "Updated tests verify QualityGroup has 7 metrics without factual_precision"
    - "pytest runs clean with no failures"
  artifacts:
    - path: "src/tests/domain/services/test_llm_judge_service.py"
      provides: "Tests for universal judge model, prompt, parsing"
    - path: "src/tests/domain/services/test_response_extractor.py"
      provides: "Tests for final answer extraction"
    - path: "src/tests/domain/services/test_scoring_service_option_a.py"
      provides: "Updated tests for judge_mode toggle routing"
    - path: "src/tests/domain/services/test_ragas_evaluation_service.py"
      provides: "Updated tests without factual_precision"
    - path: "src/tests/domain/value_objects/test_quality_group.py"
      provides: "Updated tests for 7-metric quality groups"
    - path: "src/tests/domain/entities/test_evaluation_result.py"
      provides: "Updated tests for new ragas_composite_score formula"
    - path: "src/tests/application/use_cases/test_evaluate_model_metadata.py"
      provides: "Updated tests for judge_mode in metadata and DTOs"
  key_links: []
---

<objective>
Update existing tests and add new tests for all Phase 2.4 changes: universal judge model, response extractor, scoring toggle, RAGAs formula, quality groups, and application layer wiring.

Purpose: Ensure all new code has test coverage and all existing tests pass with the updated code. This verifies backward compatibility (rubric mode unchanged) and correctness of the new universal mode.

Output: Updated and new test files covering the full Phase 2.4 scope.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.4-llm-judge-redesign-and-metric-simplification/02.4-CONTEXT.md
@.planning/phases/02.4-llm-judge-redesign-and-metric-simplification/02.4-01-SUMMARY.md
@.planning/phases/02.4-llm-judge-redesign-and-metric-simplification/02.4-03-SUMMARY.md
@.planning/phases/02.4-llm-judge-redesign-and-metric-simplification/02.4-04-SUMMARY.md
@src/tests/domain/services/test_scoring_service_option_a.py
@src/tests/domain/services/test_ragas_evaluation_service.py
@src/tests/domain/value_objects/test_quality_group.py
@src/tests/domain/entities/test_evaluation_result.py
@src/tests/application/use_cases/test_evaluate_model_metadata.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tests for response extractor and JudgeEvaluation model extensions</name>
  <files>
    src/tests/domain/services/test_response_extractor.py
    src/tests/domain/services/test_llm_judge_service.py
  </files>
  <action>
    **1. Create test_response_extractor.py:**

    New test file for extract_final_answer function:
    - `test_extracts_final_answer_basic` — "thinking...\nFinal Answer: The answer" -> "The answer"
    - `test_extracts_final_answer_case_insensitive` — "FINAL ANSWER: The answer" -> "The answer"
    - `test_extracts_final_answer_mixed_case` — "final Answer: The answer" -> "The answer"
    - `test_returns_full_text_when_no_marker` — "no marker here" -> "no marker here"
    - `test_extracts_multiline_final_answer` — "think...\nFinal Answer: Line1\nLine2\nLine3" -> "Line1\nLine2\nLine3"
    - `test_handles_empty_string` — "" -> ""
    - `test_extracts_after_last_final_answer` — "Final Answer: first\nFinal Answer: second" -> "second"

    **2. Add tests for JudgeEvaluation new fields in test_llm_judge_service.py:**

    Read existing test file. Add new tests (don't modify existing ones):
    - `test_judge_evaluation_default_fields` — new fields have correct defaults (hallucination_detected=False, claims=[], etc.)
    - `test_from_universal_judge_no_hallucination` — with reasoning_criteria_met={"clause_citations": True, "conditional_analysis": True, "actionable_steps": None}, hallucination_detected=False: overall_score should be 2/3 (~0.667)
    - `test_from_universal_judge_all_criteria_met` — all 3 criteria True, no hallucination: overall_score = 1.0
    - `test_from_universal_judge_hallucination_detected` — hallucination_detected=True: overall_score = 0.0 regardless of reasoning depth
    - `test_from_universal_judge_no_applicable_criteria` — all criteria None: overall_score = 0.0 (no applicable criteria)
    - `test_from_universal_judge_one_criterion_met` — one True, two None: overall_score = 1/1 = 1.0 (normalized by applicable count)
    - `test_from_dimensions_backward_compatible` — existing factory still works, new fields default correctly
    - `test_parse_universal_judge_response` — mock a valid JSON response string and verify parsing produces correct JudgeEvaluation

    Note: For test_parse_universal_judge_response, you'll need to instantiate LLMJudgeService. Since it loads rubrics from a file in __init__, either:
    - Pass a rubric_path that doesn't need to exist (for universal method testing)
    - Or mock the file existence
    The universal_evaluate_response method doesn't use rubrics, so the rubric file missing is acceptable for testing the parsing method directly.
  </action>
  <verify>
    - `cd src && poetry run pytest tests/domain/services/test_response_extractor.py -v` — all pass
    - `cd src && poetry run pytest tests/domain/services/test_llm_judge_service.py -v` — all pass (existing + new)
  </verify>
  <done>
    - extract_final_answer fully tested (7 cases including edge cases)
    - JudgeEvaluation model extensions tested (6+ cases covering all factory paths)
    - Backward compatibility of from_dimensions verified
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update scoring service, RAGAs, quality group, and evaluation result tests</name>
  <files>
    src/tests/domain/services/test_scoring_service_option_a.py
    src/tests/domain/services/test_ragas_evaluation_service.py
    src/tests/domain/value_objects/test_quality_group.py
    src/tests/domain/entities/test_evaluation_result.py
  </files>
  <action>
    **1. Update test_scoring_service_option_a.py:**

    Read the existing tests. Add new tests:
    - `test_score_response_default_judge_mode_is_rubric` — verify default judge_mode="rubric" produces same results as before
    - `test_score_response_universal_routes_llm_benchmarks` — with judge_mode="universal", mock LLMJudgeService.universal_evaluate_response to verify B3 routes through universal path
    - `test_score_response_universal_keeps_rule_based` — with judge_mode="universal", B1 still uses rule-based scoring
    - `test_score_response_universal_migrates_b21` — with judge_mode="universal", B21 routes through universal judge instead of regex
    - `test_score_response_passes_retrieved_contexts` — verify retrieved_contexts parameter is passed to universal judge

    Update any existing tests that call score_response with only 2 args to still work (since new params have defaults).

    **2. Update test_ragas_evaluation_service.py:**

    Read existing tests. Update:
    - Remove any tests asserting factual_precision metric is present
    - Update metric count assertions (was 7 metrics total for RAG, now 6; was 4 base metrics, now 3)
    - Add `test_no_factual_precision_metric` — verify factual_precision is NOT in returned metrics
    - Update any mock data that includes factual_precision

    **3. Update test_quality_group.py:**

    Read existing tests. Update:
    - Update metric count in Model Response Quality group: was 5 (factual_precision, factual_recall, answer_relevancy, semantic_similarity, llm_judge), now 4
    - Remove any assertions about factual_precision being in the group
    - Add `test_factual_precision_not_in_any_group` — verify no group contains factual_precision
    - Update total metric count: was 8, now 7
    - Update display name test: factual_precision should fall through to default (metric_name itself)

    **4. Update test_evaluation_result.py:**

    Read existing tests. Update:
    - Update ragas_composite_score test: formula is now (factual_recall + answer_relevancy + semantic_similarity) / 3
    - Remove factual_precision from mock RagasEvaluation metrics in composite score tests
    - Add `test_ragas_composite_score_new_formula` — with known values (recall=0.8, relevancy=0.7, similarity=0.9), expect (0.8+0.7+0.9)/3 = 0.8
    - Add `test_ragas_composite_score_missing_semantic_similarity_returns_none` — verify None returned if semantic_similarity is missing
    - Update existing test assertions that checked old formula (base * factual_precision)
  </action>
  <verify>
    - `cd src && poetry run pytest tests/domain/services/test_scoring_service_option_a.py -v` — all pass
    - `cd src && poetry run pytest tests/domain/services/test_ragas_evaluation_service.py -v` — all pass
    - `cd src && poetry run pytest tests/domain/value_objects/test_quality_group.py -v` — all pass
    - `cd src && poetry run pytest tests/domain/entities/test_evaluation_result.py -v` — all pass
  </verify>
  <done>
    - Scoring service tests verify both judge modes
    - RAGAs tests reflect removal of factual_precision
    - Quality group tests verify 7 metrics, 4 in Model Response Quality
    - Evaluation result tests verify new ragas_composite_score formula
    - All existing tests updated and still passing
  </done>
</task>

<task type="auto">
  <name>Task 3: Update application layer tests and run full test suite</name>
  <files>
    src/tests/application/use_cases/test_evaluate_model_metadata.py
  </files>
  <action>
    **1. Update test_evaluate_model_metadata.py:**

    Read existing tests. Update:
    - Add `test_metadata_includes_judge_mode` — verify judge_mode appears in evaluation metadata
    - Add `test_dto_includes_judge_evaluation_fields` — verify EvaluationResultDTO has hallucination_detected, reasoning_criteria_met when judge_mode="universal"
    - Update any assertions about quality_categories that reference factual_precision
    - Update any ragas_overall_score assertions that used the old formula

    Note: These tests mock the full pipeline, so they may need mock updates for the new ScoringService.score_response signature (judge_mode, retrieved_contexts params).

    **2. Run the full test suite:**

    ```bash
    cd src && poetry run pytest tests/ -v --tb=short
    ```

    If any test fails:
    - Read the failure message carefully
    - Fix the specific assertion or mock that's broken
    - Re-run until all pass

    **3. Check for any tests referencing factual_precision that were missed:**

    ```bash
    grep -r 'factual_precision' src/tests/
    ```

    If any remain, update them to either remove factual_precision or update the assertion.
  </action>
  <verify>
    - `cd src && poetry run pytest tests/ -v --tb=short` — ALL tests pass (0 failures)
    - `grep -r 'factual_precision' src/tests/` — returns 0 results (or only in comments explaining removal)
    - `cd src && poetry run pytest tests/ --co -q | wc -l` — shows test count (should be >= existing count + new tests)
  </verify>
  <done>
    - All existing tests pass with Phase 2.4 changes
    - New tests cover universal judge model, routing, RAGAs formula, quality groups
    - No test references factual_precision as an expected metric
    - Full test suite green
  </done>
</task>

</tasks>

<verification>
1. Full test suite: `cd src && poetry run pytest tests/ -v --tb=short` — 0 failures
2. Coverage spot check: `cd src && poetry run pytest tests/domain/ -v --tb=short` — domain tests green
3. No factual_precision in tests: `grep -r 'factual_precision' src/tests/` — empty or comment-only
4. New test files exist: `ls src/tests/domain/services/test_response_extractor.py` — exists
</verification>

<success_criteria>
- Full test suite passes with zero failures
- All Phase 2.4 new code has test coverage
- Backward compatibility verified (rubric mode tests unchanged and passing)
- factual_precision removed from all test assertions
- New test count: at least 15 new test functions across all files
</success_criteria>

<output>
After completion, create `.planning/phases/02.4-llm-judge-redesign-and-metric-simplification/02.4-05-SUMMARY.md`
</output>
