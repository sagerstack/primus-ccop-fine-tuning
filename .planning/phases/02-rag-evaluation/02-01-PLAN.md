---
phase: 02-rag-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/dtos/evaluation_request_dto.py
  - src/application/dtos/evaluation_result_dto.py
  - src/domain/entities/evaluation_result.py
  - src/presentation/cli/commands/evaluate.py
autonomous: true

must_haves:
  truths:
    - "EvaluationRequestDTO accepts evaluation_mode field with values 'hybrid' or 'llm-only'"
    - "EvaluationResultDTO carries retrieved_chunk_ids, chunk_count, and evaluation_mode per test case"
    - "evaluate run CLI accepts --mode parameter with validation (hybrid or llm-only)"
    - "Default mode is hybrid when --mode is not specified"
  artifacts:
    - path: "src/application/dtos/evaluation_request_dto.py"
      provides: "evaluation_mode field on request DTO"
      contains: "evaluation_mode"
    - path: "src/application/dtos/evaluation_result_dto.py"
      provides: "RAG metadata fields on result DTO"
      contains: "retrieved_chunk_ids"
    - path: "src/domain/entities/evaluation_result.py"
      provides: "RAG context storage on entity"
      contains: "retrieved_chunk_ids"
    - path: "src/presentation/cli/commands/evaluate.py"
      provides: "--mode CLI parameter"
      contains: "evaluation_mode"
  key_links:
    - from: "src/presentation/cli/commands/evaluate.py"
      to: "src/application/dtos/evaluation_request_dto.py"
      via: "evaluation_mode field passed from CLI to DTO"
      pattern: "evaluation_mode.*request"
---

<objective>
Add evaluation mode data structures and CLI parameter for RAG-eval integration.

Purpose: Foundation data layer -- add `evaluation_mode` (hybrid/llm-only) to request DTO, add RAG metadata fields (retrieved_chunk_ids, chunk_count, evaluation_mode) to result DTO and entity, add `--mode` parameter to `evaluate run` CLI. No behavior changes yet -- just data plumbing so Plan 02 can wire the graph.

Output: Updated DTOs, entity, and CLI command accepting `--mode` parameter.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rag-evaluation/02-CONTEXT.md

Key files:
@src/application/dtos/evaluation_request_dto.py
@src/application/dtos/evaluation_result_dto.py
@src/domain/entities/evaluation_result.py
@src/presentation/cli/commands/evaluate.py
@src/rag/presentation/cli/query.py (reference for --mode pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add evaluation_mode to DTOs and entity</name>
  <files>
    src/application/dtos/evaluation_request_dto.py
    src/application/dtos/evaluation_result_dto.py
    src/domain/entities/evaluation_result.py
  </files>
  <action>
    1. In EvaluationRequestDTO (evaluation_request_dto.py):
       - Add `evaluation_mode: str = Field(default="hybrid", description="Evaluation mode: hybrid (RAG-augmented) or llm-only")` field
       - Valid values: "hybrid", "llm-only"

    2. In EvaluationResultDTO (evaluation_result_dto.py):
       - Add `evaluation_mode: Optional[str] = Field(None, description="Evaluation mode used: hybrid or llm-only")`
       - Add `retrieved_chunk_ids: Optional[List[str]] = Field(None, description="IDs of chunks retrieved by RAG pipeline")`
       - Add `chunk_count: Optional[int] = Field(None, ge=0, description="Number of RAG chunks retrieved")`

    3. In EvaluationResult entity (evaluation_result.py):
       - Add `retrieved_chunk_ids` parameter to __init__ (Optional[List[str]], default None)
       - Add `chunk_count` parameter to __init__ (Optional[int], default None)
       - Add `evaluation_mode` parameter to __init__ (Optional[str], default None)
       - Store as private attributes: `self._retrieved_chunk_ids`, `self._chunk_count`, `self._evaluation_mode`
       - Add read-only properties for each
       - No validation changes needed -- these are metadata fields
  </action>
  <verify>
    Run: `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -c "from application.dtos.evaluation_request_dto import EvaluationRequestDTO; r = EvaluationRequestDTO(model_name='test', evaluation_mode='hybrid'); print(r.evaluation_mode)"`
    Run: `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -c "from application.dtos.evaluation_result_dto import EvaluationResultDTO; print('retrieved_chunk_ids' in EvaluationResultDTO.model_fields)"`
    Run: `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -c "from domain.entities.evaluation_result import EvaluationResult; print('retrieved_chunk_ids' in EvaluationResult.__init__.__code__.co_varnames)"`
  </verify>
  <done>
    EvaluationRequestDTO has evaluation_mode field defaulting to "hybrid". EvaluationResultDTO has retrieved_chunk_ids, chunk_count, evaluation_mode fields. EvaluationResult entity stores and exposes RAG metadata via properties.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add --mode parameter to evaluate run CLI</name>
  <files>
    src/presentation/cli/commands/evaluate.py
  </files>
  <action>
    1. Add VALID_EVAL_MODES constant at module level: `VALID_EVAL_MODES = ["hybrid", "llm-only"]`
       (Note: "rag-only" excluded -- not meaningful for benchmark scoring per CONTEXT.md)

    2. Add `mode` parameter to the `run` command function signature:
       ```
       mode: str = typer.Option("hybrid", "--mode", "-m", help="Evaluation mode: hybrid (RAG-augmented) or llm-only")
       ```

    3. Add mode validation after parameter parsing (before request creation):
       ```
       if mode not in VALID_EVAL_MODES:
           console.print(f"[red]Invalid mode: {mode}. Must be one of: {', '.join(VALID_EVAL_MODES)}[/red]")
           raise typer.Exit(1)
       ```

    4. Display mode in the console output before running:
       ```
       console.print(f"[bold]Evaluation Mode:[/bold] {mode}")
       ```

    5. Pass evaluation_mode to the EvaluationRequestDTO:
       ```
       request = EvaluationRequestDTO(
           ...existing fields...,
           evaluation_mode=mode,
       )
       ```

    6. Do NOT change any panel output or result display yet -- that's Plan 03.
  </action>
  <verify>
    Run: `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -c "from presentation.cli.commands.evaluate import VALID_EVAL_MODES; print(VALID_EVAL_MODES)"`
    Verify output: `['hybrid', 'llm-only']`
  </verify>
  <done>
    `evaluate run` CLI accepts `--mode` parameter with "hybrid" (default) and "llm-only" as valid values. Mode is displayed before evaluation starts. evaluation_mode is passed to the request DTO.
  </done>
</task>

</tasks>

<verification>
1. All imports resolve without errors
2. EvaluationRequestDTO accepts evaluation_mode field
3. EvaluationResultDTO has retrieved_chunk_ids, chunk_count, evaluation_mode
4. EvaluationResult entity has RAG metadata properties
5. CLI --mode parameter validates against hybrid/llm-only
6. Existing tests still pass: `cd /Users/sagarpratapsingh/dev/sagerstack/studio-ssdlc/src && python -m pytest tests/application/use_cases/test_evaluate_model_metadata.py -v`
</verification>

<success_criteria>
Data layer foundation complete: DTOs carry evaluation_mode and RAG metadata, entity stores chunk info, CLI accepts --mode parameter. No runtime behavior changes -- evaluation still uses model_gateway directly (Plan 02 changes this).
</success_criteria>

<output>
After completion, create `.planning/phases/02-rag-evaluation/02-01-SUMMARY.md`
</output>
